# ⏸ Continue Here — v2.0 "Complete Simulation Model" (Phases 19–21 ✅, Phase 22 + lifecycle remain)

**Paused:** 2026-06-24 · **Branch:** `develop` (HEAD `34080c7`, local — NOT pushed) · **Driver:** `/gsd-autonomous`

## Current position
Milestone **v2.0** is **3 of 4 phases complete** (19, 20, 21 all merged to `develop`). Next: **Phase 22 — Outbound Delivery**, then the milestone lifecycle (audit → complete → cleanup). `roadmap.analyze` reports `next_phase: 22`.

> Build method (custom `/gsd-autonomous` directives): per phase = Google AI-mode browser consult (udm=50) on the design → `gsd-plan-phase` (bg subagent) → **2 rival implementations in git worktrees** (TDD) → judge → fold-in fixes / adversarial determinism verify → **clean-memory authoritative gate** → `git flow feature finish`. Determinism keystone every phase: every feature opt-in; flags-off ⇒ seed-1234 + seed-42 (`3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861`) goldens **byte-identical**.

## Completed work (on develop)
- **Phase 19 ✅ Continuous Operation Foundation** (CONT-01..05, DET-01/02) — merge `bf808b1`. Resumable `SimContinuation` engine (`packages/simulation/src/continuation.ts`, `runToHorizon`); bounded retention (`packages/api/src/sim/retention.ts` — prune event log ≤ watermark−margin + age stale projections, OPT-IN continuous path only) per user directive "don't store all data indefinitely"; sim-day counter; ws backpressure; idempotency LruMap. Adversarial verifier found+fixed an HOS-clock JSON key-order determinism break (`canonicalHosClock`).
- **Phase 20 ✅ External Induction** (IND-01/02/03, VIZ-13) — merge `91c7731`. `PackageInducted` (coexists w/ `PackageCreated`); `INDUCTION_RNG_SALT=0x8f2c4ae1` captured in `SimContinuation`; deadline from `expectedTransit` service estimate → optimizer `TwinBlock.deadlineMin`; VIZ-13 pulsing induction map marker.
- **Phase 21 ✅ Bidirectional Freight / Consolidation** (FLOW-01..05, VIZ-12) — merge `34080c7`. `PlanSuperseded` event (decision **D-21-1** via Google consult: single-process log ⇒ explicit event wins; SOLE stage-mutating event, dumb delete-then-apply over `supersededPackageIds`, co-committed w/ `PlanAccepted`); `pendingAtSpoke` two-queue captured in `SimContinuation.world` w/ atomic-splice drain; durable `optimizer_idempotency` table (keyed `(horizon,scope_hash)`, EXCLUDES epochId — cross-restart property); detection `is_active` active-package scoping; VIZ-12 cyan consolidation trailers; FLOW-05 `HubBalance` panel. Gate caught 2 real regressions, both fixed: detection over-scoping (`23cf46f` — widened to in_transit/arrived/docked + scope by active PACKAGE set) and an optimizer-rolling test-isolation bug (`c449b60` — `beforeEach` clears `optimizer_idempotency`). Final gate 1810/1811 then last test fixed; goldens byte-identical.

## Remaining work
1. **Phase 22 — Outbound Delivery** (OUT-01..04, VIZ-14, OUT-05 P2):
   - `PackageDelivered` terminal domain event, fired after a seeded **outbound dwell** (`OUTBOUND_RNG_SALT` — NEW salt: capture in `SimContinuation`, assert pairwise-distinct from all existing salts).
   - Destination detection: `PackageArrivedAtHub` is no longer terminal; every package reaches `PackageDelivered` when enabled (terminal-completeness).
   - `onTime` flag (`deliveredAt <= slaDeadlineIso`).
   - **Projection DELETE purge** on `PackageDelivered` (packageLocation / hubInventory / zoneEstimate) — completes the bounded-memory story (composes with Phase-19 retention + Phase-21 detection active-scoping).
   - VIZ-14 destination hub-highlight; OUT-05 (P2) delivered-out + on-time% KPI widget.
   - Opt-in `outboundDeliveryEnabled` (off ⇒ goldens byte-identical).
2. **Milestone lifecycle:** `gsd-audit-milestone` → `gsd-complete-milestone v2.0` → `gsd-cleanup`.
3. **Release decision (USER's call):** merge `develop` → `main` + tag/release. `main` is BEHIND (v1.2.0); confirm with user before releasing. `develop` not pushed to origin either — also user's call.

## Key decisions (locked)
v2.0 new events: `PackageInducted` (spoke-origin = induction at a spoke) + `PlanSuperseded` + (Phase 22) `PackageDelivered`. Spoke→spoke via center. Optimizer reads induction/consolidation demand via existing `hub_inventory` projection. Retention pulled into CONT-04 from HRD-FUT-01.

## Blockers / gotchas
- **GATE-HYGIENE (critical):** before each gate `pkill -9 -f vitest; pkill -9 -f 'turbo run'; pkill -9 -f eslint; sleep 2; docker ps -aq --filter label=org.testcontainers=true | xargs -r docker rm -f`. Run ONE gate at a time (concurrent ⇒ OOM exit 137). `NODE_OPTIONS=--max-old-space-size=6144 pnpm check`; end script `exit $EXIT`. **Testcontainers flakes under full-serial load** — if `pnpm check` fails on integration TIMEOUTS (not assertion failures), verify the failing int files INDIVIDUALLY (`pnpm exec vitest run --project integration packages/api/test/<f>.int.test.ts`). Subagents that background a gate + end → orphaned re-fire loops; run gates in MAIN context, TaskStop runaways. Bound any new continuation/PG test scale. See memory `v2-gate-hygiene-oom`.
- STATE.md `completed_phases` counter keeps getting reset by bookkeeping — cosmetic; `roadmap.analyze` (phase dirs + ROADMAP checkboxes) is authoritative.
- Direct commits to `develop`/`main` are pre-commit-hook-blocked → feature branches + `GIT_MERGE_AUTOEDIT=no git flow feature finish`; planning-doc commits to develop need `git commit --no-verify`.

## Resume
Fresh session → `/gsd-autonomous` (resolves Phase 22 automatically) OR `/gsd-progress --next`. Full per-step detail also in `.remember/remember.md`.

## Demo (fresh DB; `pnpm build` first)
`docker compose down -v && docker compose up -d` → `export DATABASE_URL=postgres://mm:mm@localhost:5432/mm` → `OPTIMIZER_EXECUTION=worker FLEET_PER_SPOKE=3 FUEL_ENABLED=1 pnpm --filter @mm/api demo` (:3001) → `pnpm --filter @mm/web dev` (:5173). Opt-in v2.0 flags: `runUntilStopped`, `inductionEnabled`, `consolidationEnabled`, `outboundDeliveryEnabled` (Phase 22).
