# Continue Here — Session Handoff

**Paused:** 2026-06-21 · **State:** v1.1 **fully scoped**; execution **blocked by an Anthropic 529 overload** (6/6 subagents died). On branch **`feature/v1.1-realistic-time-model`**. **Next: resume `/gsd-autonomous` once the overload clears** (it resumes at Phase 6 planning).

## Immediate next action
When `https://status.claude.com` shows the API healthy again, re-run **`/gsd-autonomous`** (or just message me). It will: discover Phases 6–8 incomplete → Phase 6 `has_context=true` so it SKIPS discuss → resume at **plan-phase 6** (exactly where the planner 529'd). No rework — all scoping is committed.

Alternative if you want momentum without subagents: ask me to **proceed INLINE on Phase 6** (I plan + TDD-implement on the main loop, which stayed healthy throughout the overload).

## v1.1 milestone — scoped & committed (branch `feature/v1.1-realistic-time-model`)
- `b8222f7` docs: start milestone v1.1 (PROJECT.md Current Milestone + STATE.md switched to v1.1/planning)
- `d236558` docs: v1.1 research — `.planning/research/{STACK,FEATURES,ARCHITECTURE,PITFALLS,SUMMARY}.md` (done **inline** after the 529 wave killed the 4 research subagents; main loop was healthy)
- `dfe9ebd` docs: REQUIREMENTS.md — 7 reqs: OPT-09/10, TIME-01/02, VIZ-06, HRD-01, QA-01
- `e95bd0f` docs: ROADMAP.md — 3 phases (generated **inline** after roadmapper 529'd):
  - **Phase 6 Realistic Geography & Time Model** — VIZ-06, TIME-01, TIME-02
  - **Phase 7 Time-Aware Optimizer** — OPT-09, OPT-10 (design-heavy core; depends on 6)
  - **Phase 8 Client Hardening & Coverage** — HRD-01, QA-01
- `ae2b79e` docs(06): Phase 6 CONTEXT.md (smart-discuss, 4 grey areas resolved)

## Key v1.1 design decisions (locked in research/CONTEXT — for whoever resumes)
1. **Shared timing source of truth → `@mm/domain`**: move `TimingConfig`/`DEFAULT_TIMING_CONFIG` + a pure `expectedMinutes(p)=clamp(p.median*exp(p.sigma**2/2),p.min,p.max)` (the log-normal **MEAN**) into domain (the leaf both `@mm/simulation` and `@mm/optimizer` import — no circular dep). `sampleLogNormal` stays in simulation.
2. **Stochastic→deterministic estimate = MEAN** (not median = optimistic, not percentile = over-conservative). This is the OPT-10 pivot.
3. **VIZ-06**: ORS `driving-hgv` precomputed **offline** → committed static GeoJSON (`packages/simulation/src/network/road-geometry.generated.json`); per-leg great-circle fallback + hub-coord checksum guard; geometry stays `[lon,lat][]` so ws/animation untouched. NO runtime routing, NO new runtime deps.
4. **TIME-01**: each leg's transit median = ORS `summary.duration` (s→min); σ from config; per-leg clamp scaled off median.
5. **TIME-02**: center = `hubs[0]` (Memphis); `dwellCenter` applied exactly once at re-dispatch boundary; no double-count.
6. **Top pitfalls**: keystone-regression masking (explain every fixture delta, never blind `-u`), mean-vs-median bias, double-counted dwell, determinism breakage. See `.planning/research/PITFALLS.md`.

## Build order (the optimizer phase depends on the sim-side time model)
Phase 6: (a) `expectedMinutes` + config move to domain [no behavior change → keystone stays green] → (b) ORS precompute + `routes.ts` swap (VIZ-06) → (c) distance-derived transit (TIME-01) + center dwell (TIME-02). Then Phase 7 (optimizer consumes it), then Phase 8 (hardening + coverage).

## Notes / environment
- Baseline gate (pre-v1.1): lint 0 · typecheck 0 · **1149 tests / 117 files** · 95.2% lines. v1.0 shipped (main @ v1.0.0).
- Pre-commit hook blocks direct commits to `develop`/`main`; work on `feature/*`, merge via git-flow. This branch was created with `git flow feature start v1.1-realistic-time-model`.
- `.planning/phases/` had been empty (v1.0 phases archived to `milestones/v1.0-phases/`); now contains `06-realistic-geography-time-model/` with CONTEXT.md only (no PLAN.md — planner 529'd before writing).
- Overload timeline: 4 research subagents + roadmapper + Phase-6 planner all returned `API Error: 529 Overloaded`; the last two died with 0 tool uses. Research + roadmap were salvaged by doing them inline on the (healthy) main loop.
