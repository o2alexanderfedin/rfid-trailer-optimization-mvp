# ⏸ Continue Here — v2.0 SHIPPED; v2.1 demo fixes on develop (unreleased/unpushed); v3.0 designed

**Paused:** 2026-06-26 · **Branch:** `develop` (HEAD `fe03b19`, clean tree) · **Mode:** rapid iterative (custom `/gsd-autonomous` directives: rival subagents, Google AI-mode consults, worktrees, determinism keystone)

## Current position
- **v2.0 "Complete Simulation Model" (Phases 19–22): COMPLETE + RELEASED.** `main` @ `9e21469`, tag **`v2.0.0`** pushed to origin. Milestone audited→completed→cleaned-up; phases archived to `.planning/milestones/v2.0-phases/`.
- **v2.1 hardening: on `develop`, UNRELEASED + NOT pushed** (`develop` is **7 commits ahead** of both `main` and `origin/develop`). Two fixes (below).
- **v3.0: DESIGNED, not started.** Design seed committed: `.planning/v3.0-DESIGN-NOTES.md`.
- **Live demo is RUNNING** (background): API :3001 (pid was 72189) + web :5173, on the fixed build at ~53 sim-min/sec.

## v2.1 work done this session (on develop)
1. **Perf — projection fold O(n²)→O(affected-keys)** (merge `0d80979`). Root cause of the live-demo FREEZE: inline appliers in `packages/projections/src/runner/inline.ts` did load-entire-table → fold → re-upsert-every-row PER EVENT (O(events²)); fixed to key-scoped load/write (same pure reducers). Was 807ms+/event climbing → flat ~40ms; sim now sustains past the old ~2000-event wall. TDD test `packages/api/test/projection-fold-bounded.int.test.ts` (xmin-probed). Found by a headless debugger that DISPROVED the optimizer/queue-drain hypotheses. See memory [[sim-freeze-was-on2-projection-fold]].
2. **Viz — snapshot clock anchor** (merge `fe03b19`, fix `9318ccc`). Fixed BOTH user-reported demo bugs: (1) refresh restarted all trucks east→west, and (2) trucks "parked forever". Root cause: `packages/api/src/ws/snapshots.ts` hardcoded `simMs:0` on connect/resync → client computed `(0−departMs)/span`→clamped 0 → trailers snapped to leg origin + parked trucks' future departures unreachable. Fix: stamp snapshot with `speedController.getLastSimMs()` + `deriveSimDay`. Test guards in `packages/api/src/ws/snapshots.test.ts`. Bug-2 also has a LEGIT component: HOS 10h rests + fuel stops genuinely park trucks (p90≈10h, max≈34h) — surfaced via stop markers; at ~53 sim-min/sec a 10h rest clears in ~11 wall-sec.

Both verified: seed-42 golden `3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861` BYTE-IDENTICAL; typecheck+lint clean. Projection/snapshot-layer only (`simulate()` untouched).

## Remaining / next options (user's call)
1. **Last perf debt — `twin-snapshot.ts`**: still does two full event-log scans (`readAll(es,0n)`) per optimizer epoch (O(log)/O(log²) over a run). Make the snapshot builder fold incrementally from a cursor → optimizer can run at full frequency on very long runs without slowing. (The demo currently runs `OPTIMIZER_EVERY_TICKS=30` to keep it lively as a workaround.)
2. **Release v2.1**: `develop`→`main` + tag `v2.1.0` (git-flow), like v2.0. AND/OR push `develop` to origin (7 ahead, unpushed). Outward-facing — user's call.
3. **v3.0 milestone**: `/gsd-new-milestone` (reads `.planning/v3.0-DESIGN-NOTES.md`) — big-city hubs (1–3/state) + regional centers + OODA step-agents + coordination-center entities. Best from a FRESH session. See memory [[v3.0-continental-ooda-direction]].
4. **v2.0 audit debt**: materialize the delivery-KPI projection before enabling event-log retention in a long run (see `.planning/milestones/v2.0-MILESTONE-AUDIT.md`).

## v3.0 locked design decisions (from this session's discussion)
- Topology: **regional centers** (multi-center backbone, each big-city hub spoked to nearest center; engine today assumes ONE center=Memphis → needs multi-center). Geometry: **great-circle arcs** (not per-leg ORS).
- **OODA as event-emitting decision pattern** (each truck/hub `step()`=Observe→Orient→Decide(seeded,pure)→Act(emit events)); keep event-sourcing, NOT an ABM rewrite. New model ⇒ NEW goldens, flag-gated.
- **Coordination-center entities** = event-sourcing process-managers consuming truck/hub events, emitting suggestion/command events (decentralized, replaces the global optimizer).

## Demo (currently running; to restart cleanly)
`docker compose up -d` (Postgres; compose service name is **`postgres`**, not `db`) → build (`pnpm build`) → 
`DATABASE_URL=postgres://mm:mm@localhost:5432/mm RUN_UNTIL_STOPPED=1 FLEET_PER_SPOKE=3 FUEL_ENABLED=1 INDUCTION_ENABLED=1 CONSOLIDATION_ENABLED=1 OUTBOUND_DELIVERY_ENABLED=1 SIM_MAX_TICKS_PER_FRAME=12 OPTIMIZER_EVERY_TICKS=30 OPTIMIZER_EXECUTION=worker pnpm --filter @mm/api demo` (:3001) → `pnpm --filter @mm/web dev` (:5173) → POST `/sim/speed {"multiplier":64}` for visible motion.

## Gotchas (this session, important)
- **Orphaned demo procs hold :3001** — `pnpm→tsx→node` leaves a node child after killing the parent → next launch crashes `EADDRINUSE` and you unknowingly watch a stale instance. ALWAYS kill by PORT: `lsof -t -iTCP:3001 -sTCP:LISTEN | xargs -r kill -9`.
- **Headless browser throttles rAF + WS** → can't screenshot live truck motion; verify movement via DB sim-time advancing, not screenshots. See memory [[external-drive-skews-db-test-timeouts]].
- Default speed is **1×** on a fresh API (looks frozen on a continental map) → POST `/sim/speed` to bump. Finite paced mode pre-bakes the whole stream + ends; use `RUN_UNTIL_STOPPED=1` (continuous) for a forever demo.
- Gate-hygiene + external-drive caveats: memories [[v2-gate-hygiene-oom]], [[gitflow-hook-allows-merges]] (planning-doc commits to develop need `git commit --no-verify`; code via feature branch + `git flow feature finish`).

## Resume
Fresh session → pick from "Remaining / next options" above. To continue v3.0: `/gsd-new-milestone`. To finish perf: dispatch a debugger on the `twin-snapshot.ts` incremental fix. Memories index: `~/.claude/projects/-Volumes-Unitek-B-Projects-jobs-intelliswift/memory/MEMORY.md`.
