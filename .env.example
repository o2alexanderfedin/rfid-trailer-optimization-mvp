# Postgres connection for the API server and (optionally) integration tests.
# When DATABASE_URL is set, integration tests use it instead of spinning a
# Testcontainers Postgres. Matches docker-compose.yml (Postgres on OrbStack).
DATABASE_URL=postgres://mm:mm@localhost:5432/mm

# API server port (default 3001).
PORT=3001
