/**
 * `@mm/optimizer` — the rolling-horizon barrel (OPT-05/06).
 *
 * Placeholder for the Wave-2 rolling-epoch scoping + freeze-window +
 * `(epoch, scopeHash)` idempotency (anti-P7 thrash). The root `src/index.ts`
 * re-exports this barrel; the owning plan FILLS this file and never touches the
 * root or another plan's barrel.
 */
export {};
