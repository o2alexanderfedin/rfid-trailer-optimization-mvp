/**
 * `@mm/optimizer` — the VRPTW heuristic barrel (OPT-03).
 *
 * Placeholder for the Wave-2 cheapest-insertion + 2-opt/or-opt trailer router.
 * The root `src/index.ts` re-exports this barrel; the owning plan FILLS this
 * file and never touches the root or another plan's barrel.
 */
export {};
