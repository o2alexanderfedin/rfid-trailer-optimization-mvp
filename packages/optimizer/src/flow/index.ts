/**
 * `@mm/optimizer` — the min-cost-flow (SSP) barrel (OPT-02).
 *
 * Placeholder for the Wave-2 successive-shortest-path solver. The root
 * `src/index.ts` re-exports this barrel; the Plan-03 implementer FILLS this file
 * and never touches the root or another plan's barrel (the no-merge-conflict
 * barrel convention).
 */
export {};
