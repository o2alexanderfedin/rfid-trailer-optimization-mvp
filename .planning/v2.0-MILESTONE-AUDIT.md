---
milestone: v2.0
audited: 2026-06-25
status: tech_debt
scores:
  requirements: 23/23
  phases: 4/4
  integration: 5/5 wired (2 demo-wiring gaps fixed during audit; 1 latent conflict documented)
  flows: 1/1 (full freight lifecycle connected end-to-end; now demo-wired)
gaps: {}  # No unsatisfied requirements — all v2.0 requirements met at capability level + verified.
tech_debt:
  - phase: v2.0 (cross-phase)
    items:
      - "OUT-05 KPI vs CONT-04 retention: the GET /api/delivery-kpi route COUNT(*)s PackageDelivered over the events log, which pruneEventLog would shrink. Latent — retention is a deeper opt-in (RETENTION_ENABLED), OFF by default, so the live KPI is exact in every default config. RESOLUTION before enabling retention in a long-running deployment: materialize the delivery-KPI as a persisted projection (the deliveryKpiReducer already exists — fold it incrementally with the projection watermark and have the route read the materialized row instead of re-counting the prunable log)."
      - "No integration test exercises induction + consolidation + outbound ON together. The full lifecycle (induction → consolidation → center re-sort → distribution → delivery) is verified by composition of per-feature tests + engine code path + the now-wired live demo, but not by a single all-flags-on integration run."
      - "Phases 20/21/22 record verification in VERIFICATION.md (not SUMMARY.md) — an artifact of the rival-worktree build method. Each VERIFICATION carries the per-requirement evidence table; no information lost."
fixed_during_audit:
  - "Finding 1 (consolidation not live): main.ts now wires consolidationEnabled (CONSOLIDATION_ENABLED env, default on) → the FLOW-01/02 spoke→center consolidation leg runs in the live demo."
  - "Finding 2 (continuous operation not live): main.ts now offers an opt-in RUN_UNTIL_STOPPED=1 path that drives driveSimulationOpenEnded (CONT-01/02) — the full lifecycle runs indefinitely on the live map. Default off keeps the finite demo unchanged."
nyquist:
  note: "Per-phase *-VALIDATION.md present (19,22 authored; 20/21 covered via VERIFICATION evidence tables). Discovery-only."
---

# Milestone v2.0 "Complete Simulation Model" — Audit

**Status: tech_debt** — all 23 requirements met and verified; cross-phase contracts coherent;
the end-to-end freight lifecycle is connected and (after two audit fixes) demo-wired. One latent
cross-phase design refinement + one test-coverage gap remain as documented debt.

## Requirements coverage — 23/23 satisfied

| Group | Reqs | Phase | Status |
|-------|------|-------|--------|
| Continuous operation | CONT-01..05 | 19 | ✅ satisfied (open-ended driver + retention verified; CONT-01/02 now demo-wired opt-in) |
| Determinism | DET-01/02 | 19 | ✅ satisfied (seed-42 golden `3920accc…` byte-identical across all 4 phases) |
| External induction | IND-01/02/03, VIZ-13 | 20 | ✅ satisfied |
| Bidirectional freight | FLOW-01..05, VIZ-12 | 21 | ✅ satisfied (consolidation now demo-wired) |
| Outbound delivery | OUT-01..05, VIZ-14 | 22 | ✅ satisfied |

3-source cross-reference: every REQ-ID is `[x]` Complete in REQUIREMENTS.md, carries a passing
evidence row in its phase VERIFICATION.md, and participates in a verified cross-phase flow. No orphans,
no unsatisfied requirements → no `gaps_found` blocker gate triggered.

## Phases — 4/4 passed
19 ✅ · 20 ✅ · 21 ✅ · 22 ✅ — each VERIFICATION.md status `passed`, determinism keystone held every phase.

## Cross-phase integration (read-only checker + remediation)
- **Closed `DomainEvent` union (25 members):** coherent — `PackageInducted`/`PlanSuperseded`/`PackageDelivered`
  added via the same ceremony; every exhaustive switch (14 reducers, optimizer scope, contract.assert)
  handles all 25 with a `never` guard; type-equality proof fails the build on drift. WIRED.
- **Opt-in flag composition + determinism keystone:** all flags OFF ⇒ seed-42 golden byte-identical (solid).
  All flags compose ON. **Fixed during audit:** consolidation (Finding 1) + opt-in continuous run (Finding 2)
  are now wired into the live demo (`main.ts`).
- **WS tick (VIZ-13 + VIZ-14):** induction + delivery events are tick-only (never on snapshot → no reconnect
  re-flash, Pitfall-7); both map layers wired in MapView. WIRED.
- **Bounded memory:** P22 projection DELETE-purge + P21 detection active-scoping + P19 retention compose.
  WIRED, with the latent OUT-05↔retention KPI refinement noted below (tech-debt).
- **`SimContinuation`:** captures all 8 RNG substreams + every world queue/counter (induction, consolidation
  `pendingAtSpoke`, outbound dwell + `slaDeadlineByPackage`) → chunked continuous run byte-identical. WIRED.

## End-to-end lifecycle flow
`induction (P20) → transit → consolidation spoke→center (P21) → center re-sort → distribution center→spoke →
destination arrival → delivery PackageDelivered (P22)` — engine chain fully connected; now runs live with
all flags wired (Findings 1+2 fixed). Continuous operation (P19) available via `RUN_UNTIL_STOPPED=1`.

## Tech debt (non-blocking)
See frontmatter `tech_debt`. Headline: (1) materialize the delivery-KPI projection before enabling
event-log retention in a long-running deployment (latent; retention off by default); (2) add one
all-flags-on integration test for the complete lifecycle; (3) SUMMARY.md absence for P20–22 (no info lost).

## Verdict
All requirements satisfied, no blockers. Two demo-wiring gaps fixed during the audit; remaining items are
documented tech-debt. **Cleared to complete the milestone** (accept + backlog the debt).
