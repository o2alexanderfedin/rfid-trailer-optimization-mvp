---
context: milestone-planning
milestone: v2.0
milestone_name: Complete Simulation Model
status: paused
last_updated: 2026-06-24T04:37:12Z
---

# Resume v2.0 milestone PLANNING (do NOT implement yet)

<current_state>
Milestone **v2.0 "Complete Simulation Model"** is INITIALIZED. `PROJECT.md` (v2.0 Current Milestone + Active + Out-of-Scope reframe) and `STATE.md` (switched to `v2.0 / planning`) are committed (`2638290`) and pushed; `develop` is in sync with origin. **Planning is unfinished** — research → requirements → roadmap remain. Paused for context budget; no implementation started. No active phase yet (0 phases).
</current_state>

<completed_work>
- v2.0 scope confirmed: all 4 audited gaps; version **v2.0 (major)**; **research-first** chosen.
- PROJECT.md updated → v2.0; Out-of-Scope reframed (outbound *handoff* in scope, door-level last-mile *routing* still out).
- STATE.md `milestone-switch` → v2.0/planning; committed (`2638290`) + pushed.
</completed_work>

<remaining_work>
Re-run `/gsd-new-milestone` (it continues from `v2.0/planning`):
- **Step 8 Research** (research-first): 4 `gsd-project-researcher` (stack/features/architecture/pitfalls) on real middle-mile modeling of external induction, outbound/last-mile handoff, spoke→center consolidation, continuous/open-ended DES → `gsd-research-synthesizer` → `.planning/research/SUMMARY.md`.
- **Step 9 Requirements** → `.planning/REQUIREMENTS.md` (REQ-IDs `CONT-*`, `IND-*`, `OUT-*`, `FLOW-*` + projections/viz/optimizer-awareness). User confirm gate.
- **Step 10 Roadmap** → `gsd-roadmapper` → `.planning/ROADMAP.md`. User approval gate. Then Step 10.5 (link todos) + Step 11.
- **THEN STOP** — implementation needs a separate go-ahead.
</remaining_work>

<decisions_made>
- v2.0 (major) over v1.3 — playback→continuous-sim is a v2-level upgrade.
- Scope = all 4 gaps (continuous, external induction, outbound delivery, bidirectional freight).
- Outbound HANDOFF in scope; door-level last-mile ROUTING out of scope.
- Reconcile SP1 (paced-loop) + SP2 (rest/fuel) into v2.0 history (already on develop).
</decisions_made>

<blockers>
- None. (Optional human action: decide develop→main release — main is behind by SP1+SP2; user's call.)
</blockers>

## Required Reading (in order)
1. `.planning/PROJECT.md` — v2.0 Current Milestone scope + Out-of-Scope reframe.
2. `.remember/remember.md` — full continuation detail + gotchas.
3. `~/.claude/get-shit-done/workflows/new-milestone.md` — resume from Step 8.

## Critical Anti-Patterns (do NOT repeat)
- [ANTI-PATTERN]: rival/judge gate ran build+lint+unit but MISSED test-file TS errors → always include `pnpm typecheck` (tsc -p tsconfig.eslint.json) in any gate. (severity: advisory)
- [ANTI-PATTERN]: api (tsx) loads `@mm/simulation` from compiled `dist` → run `pnpm build` after any engine edit or the live path runs stale code. (severity: advisory)
- [CONSTRAINT]: determinism keystone — every new sim behavior must be seeded + golden-safe; feature-OFF byte-identical to current golden. (severity: blocking)

## Infrastructure State
- Branches: `develop` (= origin, has SP1 `34f02cc` + SP2 `eb65a75` + v2.0-init `2638290`), `main` (behind — no v2.0/SP1/SP2 release). Worktrees clean.
- Demo: `docker compose down -v && up -d` → `DATABASE_URL=postgres://mm:mm@localhost:5432/mm OPTIMIZER_EXECUTION=worker FLEET_PER_SPOKE=3 FUEL_ENABLED=1 pnpm --filter @mm/api demo` (:3001) + `pnpm --filter @mm/web dev` (:5173). Fresh DB each run.

<context>
We turned the demo audit ("it's a finite center→spoke playback, not a real sim") into the v2.0 milestone. Front half of /gsd-new-milestone is done + committed; the research/requirements/roadmap half is the next session's job, then pause again before building.
</context>

<next_action>
Start with: re-run `/gsd-new-milestone` (STATE is at v2.0/planning) and proceed to Step 8 (spawn the 4 research agents). Do NOT start implementation.
</next_action>
