---
phase: 20-external-induction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/events/schemas.ts
  - packages/domain/src/events/domain-event.ts
  - packages/domain/src/events/contract.assert.ts
  - packages/domain/src/events/index.ts
  - packages/domain/src/index.ts
autonomous: true
requirements:
  - IND-01

must_haves:
  truths:
    - "PackageInducted exists in the DomainEvent closed union and the build gate (contract.assert.ts) enforces exhaustiveness at compile time"
    - "A well-formed PackageInducted event passes the validate() round-trip test without error"
    - "pnpm build && pnpm typecheck exit 0 after the 5-file ceremony lands atomically"
  artifacts:
    - path: packages/domain/src/events/schemas.ts
      provides: "packageInductedSchema via eventSchema factory; added to domainEventSchema discriminated union"
      contains: "packageInductedSchema"
    - path: packages/domain/src/events/domain-event.ts
      provides: "PackageInducted inferred type; member of DomainEvent union"
      contains: "PackageInducted"
    - path: packages/domain/src/events/contract.assert.ts
      provides: "case PackageInducted in assertExhaustive; type-equality proof covers the new member"
      contains: "case \"PackageInducted\""
    - path: packages/domain/src/events/index.ts
      provides: "PackageInducted type + packageInductedSchema re-exported"
    - path: packages/domain/src/index.ts
      provides: "barrel re-exports for PackageInducted type + schema"
  key_links:
    - from: packages/domain/src/events/schemas.ts
      to: packages/domain/src/events/domain-event.ts
      via: "z.infer<typeof packageInductedSchema>"
      pattern: "PackageInducted"
    - from: packages/domain/src/events/domain-event.ts
      to: packages/domain/src/events/contract.assert.ts
      via: "assertExhaustive exhaustive switch + Exact<> proof"
      pattern: "case \"PackageInducted\""
---

<objective>
Add `PackageInducted` to the domain closed-union event system — the 5-file atomic ceremony that all downstream plans (engine, reducers, optimizer, API) depend on.

Purpose: Every reducer, contract assert, and WS envelope update in Wave 2 depends on `PackageInducted` being a valid `DomainEvent` member. This plan must land and pass `pnpm build && pnpm typecheck` before any Wave 2 plan starts.

Output: 5 modified domain files; `PackageInducted` exported and build-gated; `validate()` round-trip test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-external-induction/20-CONTEXT.md
@.planning/phases/20-external-induction/20-RESEARCH.md
@.planning/phases/20-external-induction/20-PATTERNS.md

<interfaces>
<!-- All line numbers verified against live codebase in research session. -->

From packages/domain/src/events/schemas.ts (lines 34-43, 376-419):
- `eventSchema(type, payload)` factory — wraps with `{ type: z.literal(type), schemaVersion, payload: payload.strict() }`. Call it; do NOT call `.strict()` again on the result.
- `truckRefueledSchema` (lines 376-386) is the closest payload analog — copy its shape.
- `domainEventSchema` discriminated union (lines 393-419) — add `packageInductedSchema` to the array.
- `id` is already imported (line 24); `occurredAt` constant already defined (line 201).
- `slaClassSchema` is NOT currently imported in schemas.ts — import from `"../planning/index.js"`.

From packages/domain/src/events/domain-event.ts (lines 20-165):
- Import block at lines 1-26: add `packageInductedSchema` to imports from `"./schemas.js"`.
- `TruckRefueled` pattern at lines 20-26 (import) + line 132 (type) + line 164 (union member).
- `DomainEvent` union at lines 140-165 — add `PackageInducted` after `TruckRefueled`.

From packages/domain/src/events/contract.assert.ts (lines 25-68):
- `assertExhaustive` switch at lines 25-53: add `case "PackageInducted": return;` before `default`.
- Type-equality proof at line 68: `const _zodMatchesHandWrittenUnion: Exact<Inferred, DomainEvent> = true` — NO edit; it auto-covers new member if domainEventSchema is updated.
- Warning: if domainEventSchema and DomainEvent are not updated atomically, line 68 produces `Type 'false' is not assignable to type 'true'`.

From packages/domain/src/events/index.ts (lines 1-56):
- `TruckRefueled` type re-export + `truckRefueledSchema` schema re-export as the pattern.
- Add `PackageInducted` to type re-exports and `packageInductedSchema` to schema re-exports.

From packages/domain/src/index.ts (lines 79-134):
- Barrel at lines 79-107: add `PackageInducted` type re-export.
- Barrel at lines 107-134: add `packageInductedSchema` re-export.
- `validateEvent` exported at line 170 — used in the round-trip test.
- `slaClassSchema` re-exported from `packages/domain/src/index.ts:134`.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: PackageInducted 5-file ceremony + validate round-trip test (IND-01)</name>
  <files>
    packages/domain/src/events/schemas.ts,
    packages/domain/src/events/domain-event.ts,
    packages/domain/src/events/contract.assert.ts,
    packages/domain/src/events/index.ts,
    packages/domain/src/index.ts
  </files>

  <read_first>
    1. packages/domain/src/events/schemas.ts — read fully; note eventSchema factory (lines 34-43), truckRefueledSchema (lines 376-386), domainEventSchema union (lines 393-419), id import (line 24), occurredAt (line 201).
    2. packages/domain/src/events/domain-event.ts — read fully; note DomainEvent union (lines 140-165), TruckRefueled pattern (lines 20-26, 132, 164).
    3. packages/domain/src/events/contract.assert.ts — read fully; note assertExhaustive switch (lines 25-53), type-equality proof (line 68).
    4. packages/domain/src/events/index.ts — read fully; note re-export patterns for TruckRefueled.
    5. packages/domain/src/index.ts — read fully; note barrel patterns at lines 79-134, validateEvent at line 170.
    6. packages/domain/src/planning/index.ts — note slaClassSchema export location.
    7. .planning/phases/20-external-induction/20-PATTERNS.md patterns 1-5 — the exact add sequences for each file.
    8. .planning/phases/20-external-induction/20-RESEARCH.md VQ#1 — the 5-file protocol.
  </read_first>

  <behavior>
    TDD RED phase: Write the round-trip test first in the existing domain test suite (or a new file `packages/domain/test/package-inducted.unit.test.ts` if no suitable file exists). The test calls `validateEvent(buildWellFormedPackageInducted())` and asserts no error — it will fail (type does not exist yet). Then perform the GREEN phase: the 5-file ceremony makes the test pass.

    - packageInductedSchema: uses eventSchema factory with payload containing packageId (id), inductionHubId (id), destHubId (id), slaClass (slaClassSchema imported from "../planning/index.js"), slaDeadlineIso (z.string().min(1)), externalOriginRef (id), occurredAt. The `id` and `occurredAt` are already in scope.
    - PackageInducted type: z.infer<typeof packageInductedSchema>.
    - DomainEvent union: append `| PackageInducted` after `| TruckRefueled`.
    - domainEventSchema discriminated union: add `packageInductedSchema` to the array.
    - assertExhaustive: add `case "PackageInducted": return;` before `default`.
    - Barrel re-exports: both type and schema exported from events/index.ts and src/index.ts.

    ALL 5 domain files + the test land in ONE commit. After commit: run `pnpm build && pnpm typecheck` immediately — any missing step in the ceremony is caught here before Wave 2 begins.

    Per PATTERNS.md: slaClassSchema is NOT currently imported in schemas.ts; it must be added as an import from "../planning/index.js". Do not add it to planning/index.ts itself (it already exists there).

    Per RESEARCH.md Pitfall 1: missing domainEventSchema update (schemas.ts) while updating DomainEvent (domain-event.ts) causes `Type 'false' is not assignable to type 'true'` at contract.assert.ts:68. Both must be updated atomically.
  </behavior>

  <action>
    RED phase: Create `packages/domain/test/package-inducted.unit.test.ts` importing `validateEvent` and `packageInductedSchema` from `@mm/domain`. Build a well-formed `PackageInducted` event literal matching the expected schema shape. Call `validateEvent(event)` and assert `result.success === true` (or no error thrown — check the validateEvent return contract in the source). Run `vitest run packages/domain/test/package-inducted.unit.test.ts` — expect FAILURE (type does not exist).

    GREEN phase: Perform the 5-file ceremony:
    1. schemas.ts — add `import { slaClassSchema } from "../planning/index.js"` to the import block; define `packageInductedSchema` using `eventSchema("PackageInducted", z.object({...}))` after `truckRefueledSchema` (line 386+); add `packageInductedSchema` to `domainEventSchema` discriminated union array (per D-01/IND-01).
    2. domain-event.ts — add `packageInductedSchema` to the import from `"./schemas.js"`; add `export type PackageInducted = z.infer<typeof packageInductedSchema>;` after `TruckRefueled`; append `| PackageInducted` to `DomainEvent`.
    3. contract.assert.ts — add `case "PackageInducted": return;` to assertExhaustive before `default`. Do NOT touch the type-equality proof at line 68.
    4. events/index.ts — add `PackageInducted` to the type re-export block and `packageInductedSchema` to the schema re-export block.
    5. src/index.ts — add `PackageInducted` to the barrel type re-export and `packageInductedSchema` to the barrel schema re-export.

    All 5 files + test file are committed in a single commit: `feat(20-01): IND-01 PackageInducted closed-union ceremony`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm build && pnpm typecheck && pnpm vitest run packages/domain/test/package-inducted.unit.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm build` exits 0 — Turbo build succeeds with the 5 ceremony files updated atomically.
    - `pnpm typecheck` exits 0 — contract.assert.ts:68 type-equality proof passes; assertExhaustive switch covers PackageInducted.
    - `vitest run packages/domain/test/package-inducted.unit.test.ts` exits 0 — validate() round-trip test green (IND-01).
    - `grep -c "PackageInducted" packages/domain/src/events/schemas.ts` returns ≥ 1 (schema defined).
    - `grep -c "PackageInducted" packages/domain/src/events/domain-event.ts` returns ≥ 2 (type + union member).
    - `grep -c '"PackageInducted"' packages/domain/src/events/contract.assert.ts` returns ≥ 1 (exhaustive switch case present).
    - `grep -c "packageInductedSchema" packages/domain/src/events/index.ts` returns ≥ 1 (schema re-exported).
    - `grep -c "PackageInducted" packages/domain/src/index.ts` returns ≥ 1 (barrel re-export present).
  </acceptance_criteria>

  <done>
    PackageInducted is a full member of the DomainEvent closed union; build and typecheck are green; the validate() round-trip test passes. All Wave 2 plans may begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain types → consumers | All consumers import from @mm/domain; no user-supplied input crosses this boundary in Phase 20 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | PackageInducted payload | accept | All fields (packageId, inductionHubId, destHubId, slaDeadlineIso, externalOriginRef) are deterministically simulation-generated, not user-supplied. No PII. No ASVS category implicated. |

Phase 20 makes no changes to authentication, session management, input validation at API boundaries, or cryptography. No high-severity threats. ASVS L1 has no applicable controls for simulation-internal domain events.
</threat_model>

<verification>
After this plan:
- `pnpm build && pnpm typecheck` green
- `vitest run packages/domain` green (all domain tests including new round-trip)
- `PackageInducted` importable from `@mm/domain` by Wave 2 plans
</verification>

<success_criteria>
1. All 5 domain files updated atomically in one commit
2. `pnpm build && pnpm typecheck` exit 0
3. `validate()` round-trip test for PackageInducted green
4. contract.assert.ts:68 type-equality proof satisfied (Exact<Inferred, DomainEvent> = true)
5. PackageInducted and packageInductedSchema importable from @mm/domain barrel
</success_criteria>

<output>
After completion, create `.planning/phases/20-external-induction/20-01-SUMMARY.md`
</output>
