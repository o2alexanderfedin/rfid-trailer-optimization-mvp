---
phase: 24-ooda-step-agents
plan: 04
type: tdd
wave: 4
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - packages/simulation/src/continuation.ts
  - packages/simulation/src/engine.ts
  - packages/simulation/src/ooda/canonical.ts
  - eslint.config.ts
  - packages/simulation/test/ooda-determinism.unit.test.ts
  - packages/simulation/test/ooda-continuation.unit.test.ts
  - packages/simulation/test/determinism.unit.test.ts
autonomous: true
requirements: [OODA-04, OODA-05, DET-03]

must_haves:
  truths:
    - "Agent state serializes into SerializedWorldState so a chunked/continued OODA-on run is byte-identical to an uninterrupted run"
    - "Shuffling the per-tick agent set produces a byte-identical event batch (agent-order independence golden)"
    - "N agents yield N decorrelated streams — no two share their first K draws (decorrelation golden)"
    - "oodaAgentsEnabled:false is byte-identical to absent, AND absent keeps the seed-42 10k golden = 3920accc..."
    - "A new OODA-on golden is captured reproducibility-first and committed"
    - "No Date.now/Math.random/async-queue import appears in packages/simulation/src/ooda/ (ESLint static guard fails on a violation); hashed payloads go through canonicalize"
  artifacts:
    - path: "packages/simulation/test/ooda-determinism.unit.test.ts"
      provides: "agent-order-shuffle + N-agent decorrelation + OODA-on golden + salt-collision tests"
      contains: "byte-identical"
    - path: "eslint.config.ts"
      provides: "no-restricted-imports/syntax guard scoped to ooda/ (DET-03)"
      contains: "no-restricted"
    - path: "packages/simulation/src/continuation.ts"
      provides: "agent state in SerializedWorldState"
      contains: "Agent"
  key_links:
    - from: "packages/simulation/src/engine.ts captureContinuation"
      to: "SerializedWorldState agent fields"
      via: "agent state captured + restored on resume"
      pattern: "Agent"
    - from: "eslint.config.ts"
      to: "packages/simulation/src/ooda/**"
      via: "no-restricted-imports bans async-queue/kysely; no-restricted-syntax bans Date.now/Math.random"
      pattern: "packages/simulation/src/ooda"
---

<objective>
Close the determinism + continuation contract for the OODA agents: (1) serialize any per-agent mutable state into `SerializedWorldState` so a chunked/continued OODA-on run is byte-identical to an uninterrupted run (OODA-05 continuation-equivalence); (2) capture the determinism witnesses as first-class GOLDENS — the agent-order-shuffle byte-identical batch + the N-agent decorrelation test (OODA-04); (3) land the DET-03 static guard — an ESLint `no-restricted-imports`/`no-restricted-syntax` rule scoped to `packages/simulation/src/ooda/**` that FAILS on `Date.now`/`Math.random`/`async-queue`/`kysely`, plus a `canonicalize` pass over any hashed OODA payload; (4) re-assert the two-part flags-off gate (`oodaAgentsEnabled:false === absent` AND `absent ⇒ 3920accc…`) and capture a NEW committed OODA-on golden reproducibility-first.

Purpose: OODA-04 (the remaining determinism goldens), OODA-05 (continuation-equivalence), DET-03 (the static guard + canonicalize). This is the phase's keystone gate — without it the agent layer cannot be trusted to replay byte-identically. It is sequenced LAST because it asserts properties of the fully-wired agent system (24-01..03).

Output: agent state in the continuation, the OODA determinism + continuation test suites, the ESLint DET-03 guard, the re-asserted flags-off gate, and a committed OODA-on golden SHA-256.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ooda-step-agents/24-CONTEXT.md
@.planning/phases/24-ooda-step-agents/24-01-SUMMARY.md
@.planning/phases/24-ooda-step-agents/24-02-SUMMARY.md
@.planning/phases/24-ooda-step-agents/24-03-SUMMARY.md
@./CLAUDE.md

<read_first>
- packages/simulation/src/continuation.ts:112-155 (`SerializedWorldState`) — where agent state must be added (mirror `pendingBySpoke`/`odometerByTrailer`: ordered `[key, value]` tuple arrays, deterministic field order, present-only-when-on shape so the off path stays byte-identical).
- packages/simulation/src/continuation.ts:157-170 (`SerializedRngStates`) — if agents carry a serialized per-agent RNG position, add an `ooda?: ...` field present ONLY when on (mirror `fuel?`/`induction?`/`outbound?`). NOTE per ARCHITECTURE §3: agent substreams are derived per-pass from the stable id via `deriveAgentRng(seed, id)`, so they may be STATELESS (re-derived each pass from `seed`+`tick`) — if so, NO new rng state field is needed and the off path is trivially clean. Decide and document which (stateless re-derive is simpler + safer for byte-identity).
- packages/simulation/src/engine.ts:2248-2311 (`captureContinuation`) — add the agent-state capture here (mirror the existing `[...map.entries()].map(...)` captures) and the restore in the bootstrap (mirror :1166-1172 `pendingBySpoke` restore).
- packages/simulation/test/determinism.unit.test.ts:194-293 — the EXACT two-part flags-off gate pattern to copy for `oodaAgentsEnabled` (the `explicit X:false === absent` short-run test + the `absent ⇒ 3920accc 10k golden` test + the `X:false === absent over the 10k run` test). The committed golden constant is `3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861`.
- packages/simulation/test/fuel-determinism.unit.test.ts:42-57 — the salt-collision test pattern; add `OODA_RNG_SALT` to the pairwise-distinct set (now 9 salts).
- packages/simulation/test/continuation.unit.test.ts (or the equivalent continuation-equivalence test, find it) — the chunked==all-at-once harness to extend for an OODA-on run.
- eslint.config.ts — the flat-config structure (the comment at :14-19 ALREADY anticipates a `no-restricted-imports` rule barring async-queue from the deterministic core "added when it is wired in Phase 27"; add the OODA-scoped guard NOW per DET-03, scoped to `packages/simulation/src/ooda/**` + `packages/simulation/src/engine.ts`'s stepAgents region as practical).
- packages/optimizer/src/rolling/freeze-idempotency.ts:25 (`canonicalize`) — the canonicalization reference; OODA hashed payloads (if any are hashed for a scopeHash-like digest) must pass through an equivalent stable key-order canonicalizer (mirror `canonicalHosClock` in engine.ts:2332 for fixed key order).
- .planning/research/PITFALLS.md Pitfalls 1, 3, 4, 5, 8 — the determinism guards these goldens witness.
</read_first>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Agent state in SerializedWorldState + continuation-equivalence (OODA-05)</name>
  <files>packages/simulation/src/continuation.ts, packages/simulation/src/engine.ts, packages/simulation/test/ooda-continuation.unit.test.ts</files>
  <action>Determine whether the OODA agents hold ANY per-agent mutable state across ticks beyond what the existing world maps already carry (the truck/hub observations are read from `pendingBySpoke`/`odometerByTrailer`/`clockByDriver` which are ALREADY serialized; any NEW agent memory — e.g. a divert-cooldown counter or a last-decision tick — must be captured). If new state exists: add deterministic-field-order tuple-array fields to `SerializedWorldState` (mirror `pendingBySpoke`), populate them in `captureContinuation`, and restore them in the bootstrap (present-only-when-on so the off path is byte-identical). If agent substreams are stateless re-derives (per 24-01's `deriveAgentRng`), document that NO rng-state field is needed. Add `packages/simulation/test/ooda-continuation.unit.test.ts`: an OODA-on run executed all-at-once vs in two chunks via `runToHorizon` must produce a byte-identical event stream (hash equality) — the continuation-equivalence keystone for the agent layer. If there is genuinely zero new cross-tick agent state, the test still proves the existing capture covers OODA (assert chunked==all-at-once for an OODA-on seed).</action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- test/ooda-continuation.unit.test.ts && pnpm typecheck</automated>
  </verify>
  <done>OODA-on chunked run is byte-identical to all-at-once; any new agent state round-trips through SerializedWorldState with deterministic field order; off path byte-identical.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: OODA determinism goldens — order-shuffle, N-agent decorrelation, salt-collision, OODA-on golden</name>
  <files>packages/simulation/test/ooda-determinism.unit.test.ts, packages/simulation/src/ooda/canonical.ts</files>
  <behavior>
    - AGENT-ORDER-SHUFFLE (the single strongest witness, PITFALLS Pitfall 1+4): running an OODA-on simulation with the per-tick agent set shuffled before the pass yields a byte-identical event batch to the unshuffled run (`JSON.stringify` equality over a fixed seed+ticks). The shuffle must perturb the INPUT iteration order, proving the `sortAgentsByStableId` + frozen-observation rules make the pass order-independent.
    - N-AGENT DECORRELATION (Pitfall 3): for N=64+ agent ids, no two per-agent substreams share their first K=8 draws (extend the 24-01 unit test into a committed golden assertion over the real agent id set, e.g. all trailer ids + hub ids).
    - SALT-COLLISION: `OODA_RNG_SALT` is pairwise-distinct from the 8 existing salts (Set size 9) — mirror fuel-determinism.unit.test.ts.
    - OODA-ON GOLDEN: `simulate({ seed: 42, durationTicks: 10000, oodaAgentsEnabled: true })` hashes to a NEW committed SHA-256 (captured reproducibility-first: run it twice in-process, assert equal, then commit the hash) — and it MUST differ from `3920accc…` (the model changed).
  </behavior>
  <action>Create `packages/simulation/test/ooda-determinism.unit.test.ts` with the four test groups above, mirroring the structure of `determinism.unit.test.ts` (the 10k-golden + reproducibility-within-process pattern at :128-150). For the OODA-on golden: FIRST run the engine to obtain the hash, assert in-process reproducibility (two runs equal), commit the literal as `OODA_ON_GOLDEN_SHA256`, and assert it `!==` the flags-off `3920accc…`. If any OODA payload is hashed for a digest (e.g. a future scopeHash-like value), create `packages/simulation/src/ooda/canonical.ts` exporting a fixed-key-order `canonicalizeOodaPayload` (mirror `canonicalHosClock`) and route those payloads through it; if no OODA payload is hashed beyond the event stream itself, document that the event stream's existing `JSON.stringify` ordering is the canonical surface and skip the helper. Round any geometry-derived observation field to an integer at the engine boundary (re-assert Pitfall 2 with a test if practical).</action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- test/ooda-determinism.unit.test.ts</automated>
  </verify>
  <done>RED→GREEN: agent-order-shuffle byte-identical; N=64 K=8 decorrelation; salt-collision (Set size 9); OODA-on golden committed, reproducible in-process, and != 3920accc.</done>
</task>

<task type="execute">
  <name>Task 3: DET-03 static guard (ESLint) + two-part flags-off gate</name>
  <files>eslint.config.ts, packages/simulation/test/determinism.unit.test.ts</files>
  <action>(1) Add a flat-config block to eslint.config.ts scoped to `files: ["packages/simulation/src/ooda/**/*.ts"]` (excluding the `*.test.ts` siblings as needed) with `no-restricted-imports` banning `@alexanderfedin/async-queue` + `kysely` (+ any async-queue re-export) and `no-restricted-syntax` banning `Date.now` (`CallExpression[callee.object.name='Date'][callee.property.name='now']`) and `Math.random` (`CallExpression[callee.object.name='Math'][callee.property.name='random']`) — the DET-03 CI/ESLint static guard that FAILS the lint on a violation. Mirror the intent documented at eslint.config.ts:14-19. (2) Extend `packages/simulation/test/determinism.unit.test.ts`'s `DET-01 flags-off gate` describe block with the THREE OODA assertions copied from the outbound/continental precedent: (a) `oodaAgentsEnabled:false` byte-identical to absent over the short FLAGS_OFF_OPTS run; (b) `oodaAgentsEnabled` ABSENT ⇒ the seed-42 10k golden is still `3920accc…`; (c) `oodaAgentsEnabled:false` byte-identical to absent over the 10k run. These prove the entire OODA layer is fully inert when off (the determinism keystone).</action>
  <verify>
    <automated>pnpm lint && pnpm --filter @mm/simulation test -- test/determinism.unit.test.ts</automated>
  </verify>
  <done>The ESLint guard fails on a planted `Date.now()`/`Math.random()`/async-queue import in ooda/ (verify by a temporary local edit, then revert) and passes on the clean tree; the two-part flags-off gate is green — oodaAgentsEnabled:false === absent AND absent keeps 3920accc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OODA core source → CI lint | A wall-clock/random/async-queue call silently entering the decision core breaks reproducibility; the static guard is the gate |
| all-at-once run ↔ chunked/continued run | Unserialized agent state makes a resumed run diverge (phantom state) |
| flags-off path ↔ committed golden | Any non-inert OODA addition shifts the seed-42 stream away from 3920accc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11 | Tampering | Date.now/Math.random/async-queue in the OODA core | mitigate | ESLint `no-restricted-imports`/`no-restricted-syntax` scoped to `ooda/**` fails the lint on a violation (DET-03); verified by a planted-violation check |
| T-24-12 | Tampering | unserialized agent state across a continuation boundary | mitigate | Capture/restore agent state in `SerializedWorldState` (or prove stateless re-derive); continuation-equivalence test (chunked==all-at-once) is the witness (OODA-05) |
| T-24-13 | Repudiation | OODA-on model with no committed golden | mitigate | Capture a NEW `OODA_ON_GOLDEN_SHA256` reproducibility-first, asserted in-process-reproducible and `!= 3920accc` |
| T-24-14 | Denial of service | a flags-off regression shifting the seed-42 golden | mitigate | The two-part flags-off gate (`:false === absent` AND `absent ⇒ 3920accc`) re-asserted over short + 10k runs |
</threat_model>

<verification>
- `pnpm --filter @mm/simulation test -- test/ooda-determinism.unit.test.ts test/ooda-continuation.unit.test.ts test/determinism.unit.test.ts` (all OODA determinism + continuation + flags-off gates green)
- `pnpm lint` (the DET-03 ooda/ guard active; planted-violation check confirms it fires)
- `pnpm typecheck`
- `pnpm --filter @mm/simulation test` (full sim suite green; mind the v2-gate OOM note in MEMORY — run one gate at a time, clean up workers)
- The committed `OODA_ON_GOLDEN_SHA256` is reproducible in-process and `!= 3920accc…`.
</verification>

<success_criteria>
- OODA-05: agent state serializes into SerializedWorldState; a chunked/continued OODA-on run is byte-identical to an uninterrupted run.
- OODA-04 (goldens): agent-order-shuffle byte-identical batch + N-agent decorrelation + salt-collision are committed first-class tests.
- DET-03: no Date.now/Math.random/async-queue in the OODA core (ESLint static guard fails on a violation); hashed payloads canonicalized.
- The two-part flags-off gate holds (`oodaAgentsEnabled:false === absent` AND `absent ⇒ 3920accc…`); a NEW OODA-on golden is captured reproducibility-first and committed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-ooda-step-agents/24-04-SUMMARY.md` recording: whether agents carry new cross-tick state (and how it serializes) or are stateless re-derives, the committed `OODA_ON_GOLDEN_SHA256` value, the ESLint guard selectors used, and the canonicalization decision.
</output>
