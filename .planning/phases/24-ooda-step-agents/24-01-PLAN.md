---
phase: 24-ooda-step-agents
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/simulation/src/ooda/agent.ts
  - packages/simulation/src/ooda/rng.ts
  - packages/simulation/src/ooda/observe.ts
  - packages/simulation/src/ooda/truck.ts
  - packages/simulation/src/ooda/index.ts
  - packages/simulation/src/ooda/agent.unit.test.ts
  - packages/simulation/src/ooda/rng.unit.test.ts
  - packages/simulation/src/ooda/truck.unit.test.ts
  - packages/domain/src/events/schemas.ts
  - packages/domain/src/events/domain-event.ts
  - packages/domain/src/events/index.ts
  - packages/domain/src/events/contract.assert.ts
  - packages/simulation/src/index.ts
autonomous: true
requirements: [OODA-01, OODA-04, DET-03]

must_haves:
  truths:
    - "A pure truck Decide(observation, rng) returns one of proceed/divert/rest/refuel/hold from a frozen observation"
    - "Two agents with different stable ids derive decorrelated substreams (no shared first-K draws)"
    - "Agents iterate a sorted-by-stable-id array (renaming/reordering does not change which stream an id gets)"
    - "The new TrailerDiverted domain event validates through the closed union + zod + every exhaustive switch"
  artifacts:
    - path: "packages/simulation/src/ooda/rng.ts"
      provides: "stableAgentHash (FNV-1a) + lazy per-agent substream deriver + OODA_RNG_SALT"
      contains: "OODA_RNG_SALT"
    - path: "packages/simulation/src/ooda/truck.ts"
      provides: "pure truck Observe->Orient->Decide->Act decision function"
      exports: ["decideTruck"]
    - path: "packages/simulation/src/ooda/agent.ts"
      provides: "Agent/AgentObservation types + sortAgentsByStableId"
      exports: ["sortAgentsByStableId"]
    - path: "packages/domain/src/events/schemas.ts"
      provides: "trailerDivertedSchema in domainEventSchema discriminated union"
      contains: "trailerDivertedSchema"
  key_links:
    - from: "packages/simulation/src/ooda/truck.ts"
      to: "packages/simulation/src/ooda/rng.ts"
      via: "decideTruck draws from the per-agent substream"
      pattern: "deriveAgentRng|stableAgentHash"
    - from: "packages/domain/src/events/domain-event.ts"
      to: "packages/domain/src/events/schemas.ts"
      via: "TrailerDiverted inferred from trailerDivertedSchema"
      pattern: "trailerDivertedSchema"
---

<objective>
Build the OODA agent scaffolding (`packages/simulation/src/ooda/`) — the deterministic, PURE decision-core primitives every agent type reuses — and the truck agent's `Observe→Orient→Decide→Act` decision function. This is "disciplined reuse," not novel machinery: the per-agent seeded substream mirrors the repo's existing salt/`mixSeed` discipline, the sorted-by-stable-id ordering mirrors `trailerRoster`, and the truck emits the SAME existing domain events the centralized engine emits today (proceed→no-op / rest→TruckRested / refuel→TruckRefueled / hold→no-op) plus ONE genuinely-new decision with no current analog: a `TrailerDiverted` re-route event threaded through the closed domain union.

Purpose: Establish OODA-04's three determinism primitives (sorted-by-stable-id iteration, stable-id-derived seeded substream, frozen observation surface) and OODA-01's truck Decide BEFORE wiring them into the engine (plan 24-02 wires `stepAgents`). Interface-first: this plan defines the contracts the engine integration and the hub agent (24-02, 24-03) implement against. The `ooda/` package is a pure, synchronous, import-restricted leaf — no `Date.now`/`Math.random`/`async-queue`/`kysely` (DET-03, enforced by the static guard in 24-04).

Output: `packages/simulation/src/ooda/` (agent types + rng + observe + truck + index), the new `TrailerDiverted` event threaded through `@mm/domain`, and TDD unit tests proving determinism + decorrelation + event validity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ooda-step-agents/24-CONTEXT.md
@./CLAUDE.md

<read_first>
- .planning/research/ARCHITECTURE.md §3 "OODA step() inside the deterministic engine" — the per-agent substream formula `makeRngFromState(mixSeed(mixSeed(seed) ^ stableAgentHash(agentId)))`, the four OODA phases, the sorted-id ordering rule, the frozen-observation rule.
- .planning/research/PITFALLS.md Pitfalls 1, 2, 3, 4, 5 — agent step order, transcendental-out-of-payload, substream decorrelation, frozen observation surface, no wall-clock/random.
- packages/simulation/src/rng.ts — `makeRng`, `makeRngFromState`, the private `mixSeed` (splitmix32 finaliser). NOTE: `mixSeed` is NOT currently exported; export it (additive, no behavior change) so `ooda/rng.ts` can reuse it.
- packages/simulation/src/network/centers.ts:297-316 — the EXACT FNV-1a pattern to mirror in `stableAgentHash`: init `0x811c9dc5`, per-char `hash ^= c; hash = Math.imul(hash, 0x01000193)`, finalize `(hash >>> 0)`.
- packages/simulation/src/engine.ts:86-135 — the 8 existing substream salts (RFID_RNG_SALT … OUTBOUND_RNG_SALT) the new `OODA_RNG_SALT` must be pairwise-distinct from.
- packages/domain/src/events/schemas.ts:379-428 — `truckRestedSchema`/`truckRefueledSchema`/`packageDeliveredSchema` as the analog for the new `trailerDivertedSchema`; `domainEventSchema` discriminatedUnion at :474; the `eventSchema(type, shape)` helper at :35.
- packages/domain/src/events/domain-event.ts — the closed `DomainEvent` union (:178), `z.infer` type exports, `assertNever`.
- packages/domain/src/events/index.ts + contract.assert.ts — the type-equality proof that fails the BUILD if the hand-written union and zod union diverge.
- packages/simulation/src/engine.ts:1056-1095 (`emitTruckRested`/`emitTruckRefueled` payload shape) — so the truck Decide's "rest"/"refuel" outcomes carry exactly the fields those existing events need (reason, durationMin / gallons, odometerMiles, durationMin).
</read_first>

<interfaces>
<!-- Extracted contracts the executor builds against. No codebase exploration needed. -->

From packages/simulation/src/rng.ts (export `mixSeed` additively):
```typescript
export function makeRngFromState(rawState: number): Rng;
export interface Rng { next(): number; int(maxExclusive: number): number; pick<T>(items: readonly T[]): T; getState(): number; }
// ADD: export function mixSeed(seed: number): number; // currently private splitmix32 finaliser
```

From packages/simulation/src/network/centers.ts (FNV-1a pattern to mirror):
```typescript
// init 0x811c9dc5; for each charCode c: hash ^= c; hash = Math.imul(hash, 0x01000193); return (hash >>> 0)
```

From packages/domain/src/events/schemas.ts:
```typescript
function eventSchema<TType extends string, TShape extends z.ZodRawShape>(type: TType, shape: TShape | z.ZodObject<TShape>): ...;
export const domainEventSchema = z.discriminatedUnion("type", [ /* all member schemas */ ]);
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: OODA RNG primitives — stableAgentHash + lazy per-agent substream + OODA_RNG_SALT</name>
  <files>packages/simulation/src/ooda/rng.ts, packages/simulation/src/ooda/rng.unit.test.ts, packages/simulation/src/rng.ts</files>
  <behavior>
    - `stableAgentHash("T001")` is a pure 32-bit FNV-1a digest (init 0x811c9dc5, multiplier 0x01000193, finalize `>>> 0`); identical input ⇒ identical output; different ids ⇒ (overwhelmingly) different digests.
    - `OODA_RNG_SALT` is a uint32 pairwise-distinct from all 8 existing engine salts (assert with `new Set([...salts, OODA_RNG_SALT]).size === 9`).
    - `deriveAgentRng(seed, agentId)` returns an `Rng` positioned at `makeRngFromState(mixSeed((mixSeed(seed) ^ OODA_RNG_SALT ^ stableAgentHash(agentId)) >>> 0))` — DECORRELATION test: for N=64 distinct agent ids, the first K=8 draws of each agent's stream are pairwise-distinct sequences (no two agents share their first-K draws).
    - STABLE-ID test: `deriveAgentRng(seed, "T002")` is independent of any array position — derived purely from the id string.
  </behavior>
  <action>Export `mixSeed` from `packages/simulation/src/rng.ts` (change `function mixSeed` to `export function mixSeed`; additive, zero behavior change — the goldens never call it directly). Create `packages/simulation/src/ooda/rng.ts` exporting: (a) `stableAgentHash(agentId: string): number` mirroring `centers.ts`'s FNV-1a EXACTLY; (b) `OODA_RNG_SALT` as a uint32 hex literal pairwise-distinct from RFID/OVER_CARRY/TIMING/HOS/FUEL/INDUCTION/OUTBOUND (and the over-carry/rfid set) — pick a fresh constant and document the choice; (c) `deriveAgentRng(seed: number, agentId: string): Rng` using the two-stage `mixSeed(mixSeed(seed) ^ OODA_RNG_SALT ^ stableAgentHash(agentId))` derivation per ARCHITECTURE §3 and PITFALLS Pitfall 3, importing `makeRngFromState`+`mixSeed` from `../rng.js`. Construction is LAZY by contract: callers (the engine, plan 24-02) only invoke `deriveAgentRng` when `oodaAgentsEnabled` is true, so an off run constructs ZERO agent streams. Pure, synchronous, no wall-clock/random. Import the 8 existing salts from `../engine.js` in the TEST only for the pairwise-distinct assertion (mirror packages/simulation/test/fuel-determinism.unit.test.ts:45-57).</action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- src/ooda/rng.unit.test.ts</automated>
  </verify>
  <done>RED→GREEN: stableAgentHash purity, OODA_RNG_SALT pairwise-distinct (Set size 9), N=64 K=8 decorrelation, and stable-id-not-index tests all pass. `mixSeed` exported from rng.ts.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Agent + frozen observation types + sorted-by-stable-id iteration</name>
  <files>packages/simulation/src/ooda/agent.ts, packages/simulation/src/ooda/observe.ts, packages/simulation/src/ooda/agent.unit.test.ts</files>
  <behavior>
    - `sortAgentsByStableId(agents)` returns a NEW array sorted ascending by `stableId` string (never input/Map/Set order); a SHUFFLED input yields a byte-identical sorted output (the order-independence witness primitive).
    - The `AgentObservation` (truck) type is a READONLY plain-data snapshot — fuel odometer miles, HOS clock + remaining-legal-drive minutes, position/leg, next-hub queue depth + dock availability, assigned center id, current tick. No methods, no live references; a frozen value the Decide consumes (PITFALLS Pitfall 4: Observe is a pure read at pass entry, never re-read mid-pass).
    - `TruckDecision` is a closed discriminated union: `{ kind: "proceed" } | { kind: "divert"; toHubId; reason } | { kind: "rest"; reason; durationMin } | { kind: "refuel"; gallons; odometerMiles; durationMin } | { kind: "hold"; reason }`.
  </behavior>
  <action>Create `packages/simulation/src/ooda/agent.ts` with: the `AgentKind = "truck" | "hub"` literal; a base `Agent` with a `readonly stableId: string`; `sortAgentsByStableId<T extends { readonly stableId: string }>(agents: readonly T[]): T[]` doing a pure localeless string compare (`a.stableId < b.stableId ? -1 : ...`, mirroring `partitionChecksum`'s sort) on a copied array. Create `packages/simulation/src/ooda/observe.ts` with the `AgentObservation` readonly type (truck fields above; mirror the exact in-engine fold-map shapes — `odometerByTrailer` number, the `SerializedHosClock`-equivalent, `remainingLegalDriveMinutes` integer) and the `TruckDecision` union. Keep ALL hashed/decision-relevant fields INTEGER or string (PITFALLS Pitfall 2: no transcendental geometry in decision payloads; round at the boundary in the engine before building the observation). No `any`, no assertions, `readonly` throughout.</action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- src/ooda/agent.unit.test.ts</automated>
  </verify>
  <done>RED→GREEN: shuffle-then-sort yields byte-identical order; sorted output is a new array; types compile under strict TS with no `any`/assertions.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: New TrailerDiverted domain event + pure truck Decide (decideTruck)</name>
  <files>packages/domain/src/events/schemas.ts, packages/domain/src/events/domain-event.ts, packages/domain/src/events/index.ts, packages/domain/src/events/contract.assert.ts, packages/simulation/src/ooda/truck.ts, packages/simulation/src/ooda/truck.unit.test.ts, packages/simulation/src/ooda/index.ts, packages/simulation/src/index.ts</files>
  <behavior>
    - `validateEvent` ACCEPTS a well-formed `TrailerDiverted` (`{ type:"TrailerDiverted", schemaVersion:1, payload:{ trailerId, tripId, fromHubId, toHubId, reason, occurredAt } }`) and REJECTS a malformed one — proving the closed union + zod + exhaustive switches were all threaded (the contract.assert type-equality proof must still compile).
    - `decideTruck(observation, rng)` is PURE and DETERMINISTIC: identical `(observation, rng-state)` ⇒ identical `TruckDecision`. It applies a clear, documented priority order over the FROZEN observation (e.g. binding-feasibility branches first — out-of-legal-hours ⇒ rest, odometer over threshold ⇒ refuel; then divert/hold/proceed). Any stochastic tie-break draws from the passed `rng` ONLY (the per-agent substream), never `Math.random`.
    - decideTruck NEVER mutates the observation and NEVER reads wall-clock.
  </behavior>
  <action>Thread the new event end-to-end through `@mm/domain` (mirror `truckRefueled` exactly): add `trailerDivertedSchema = eventSchema("TrailerDiverted", { trailerId, tripId, fromHubId, toHubId, reason: z.enum([...]), occurredAt })` to schemas.ts and into the `domainEventSchema` discriminatedUnion array; add `export type TrailerDiverted = z.infer<typeof trailerDivertedSchema>` to domain-event.ts and add `| TrailerDiverted` to the `DomainEvent` union; re-export from index.ts; ensure contract.assert.ts's type-equality proof still compiles (it auto-covers the new member). Then create `packages/simulation/src/ooda/truck.ts` exporting `decideTruck(obs: AgentObservation, rng: Rng): TruckDecision` — a pure Observe→Orient→Decide function with a documented, deterministic priority ladder over the frozen `obs` (Claude's discretion on the exact predicate per CONTEXT). Create `packages/simulation/src/ooda/index.ts` re-exporting the public OODA surface (`deriveAgentRng`, `stableAgentHash`, `OODA_RNG_SALT`, `sortAgentsByStableId`, `decideTruck`, the types). Re-export the OODA surface + `TrailerDiverted` types from `packages/simulation/src/index.ts` only as needed by 24-02. Do NOT touch the engine in this plan — wiring is 24-02. Projection reducers that exhaustively switch over `DomainEvent` will FAIL TO COMPILE until they add a (no-op or audit) case for `TrailerDiverted`; add the minimal case to each exhaustive switch flagged by typecheck (audit-timeline + geo-track at minimum) so the build is green — keep them additive/no-behavior beyond an audit entry.</action>
  <verify>
    <automated>pnpm --filter @mm/domain test && pnpm --filter @mm/simulation test -- src/ooda/truck.unit.test.ts && pnpm typecheck</automated>
  </verify>
  <done>RED→GREEN: `validateEvent` accepts/rejects TrailerDiverted correctly; `decideTruck` determinism + no-mutation + priority-ladder tests pass; `pnpm typecheck` green across the workspace (every exhaustive switch threads the new member).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sim core → emitted event log | Decision outputs become persisted/hashed events; a non-deterministic or wall-clock-derived field corrupts the golden |
| agent id → seeded substream | The id is the only entropy source for per-agent randomness; a weak/colliding hash correlates agents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | per-agent substream derivation | mitigate | Two-stage `mixSeed` over FNV-1a `stableAgentHash` + a pairwise-distinct `OODA_RNG_SALT`, asserted by the decorrelation + salt-collision tests (PITFALLS Pitfall 3) |
| T-24-02 | Repudiation | TrailerDiverted payload | mitigate | Carry `reason` + `tripId` + from/to hub ids so every divert is replayable/auditable; validated by zod at the boundary |
| T-24-03 | Tampering | decision payload contains float geometry | mitigate | Keep transcendental/geometry OUT of decision-relevant fields; observation fields are integer/string only (PITFALLS Pitfall 2) |
| T-24-04 | Denial of service | `Math.random`/`Date.now` in decision core | mitigate | Pure synchronous functions drawing only from the passed seeded `Rng`; static guard enforced in 24-04 (DET-03) |
</threat_model>

<verification>
- `pnpm --filter @mm/simulation test -- src/ooda` (all OODA unit tests green)
- `pnpm --filter @mm/domain test` (TrailerDiverted validates)
- `pnpm typecheck` (no `any`, every exhaustive switch threads TrailerDiverted)
- `pnpm lint` (no new violations)
- The `ooda/` package imports ONLY from `../rng.js` and `@mm/domain` types — no engine state, no async, no wall-clock (DET-03 surface established; static guard lands in 24-04).
</verification>

<success_criteria>
- OODA-04 primitives exist and are proven: sorted-by-stable-id iteration, stable-id-derived decorrelated seeded substream, frozen-observation readonly type.
- OODA-01 truck Decide is a pure, deterministic, seeded function emitting decisions mappable to existing events + the new TrailerDiverted.
- DET-03 foundation: the OODA core is synchronous/pure with no wall-clock/random; the new event flows through canonicalizable integer/string payloads.
- The new TrailerDiverted event is threaded through the closed domain union + zod + every exhaustive switch; the build is green.
</success_criteria>

<output>
After completion, create `.planning/phases/24-ooda-step-agents/24-01-SUMMARY.md` recording: the chosen `OODA_RNG_SALT` value, the `TrailerDiverted` payload shape, the truck Decide priority ladder, and the list of exhaustive switches threaded.
</output>
