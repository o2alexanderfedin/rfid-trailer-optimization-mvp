---
phase: 24-ooda-step-agents
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - packages/simulation/src/continuation.ts
  - packages/simulation/src/engine.ts
  - packages/simulation/src/ooda/hub.ts
  - packages/simulation/src/ooda/hub.unit.test.ts
  - packages/simulation/src/ooda/index.ts
  - packages/simulation/test/ooda-engine.unit.test.ts
autonomous: true
requirements: [OODA-01, OODA-02, OODA-04]

must_haves:
  truths:
    - "With oodaAgentsEnabled on, a stepAgents SimTask fires on the (fireTick,seq) queue at a fixed OODA_INTERVAL_TICKS cadence and self-reschedules"
    - "Under the flag, trucks and hubs make their own dispatch/hold/consolidate/rest/refuel decisions; the centralized decision code is bypassed"
    - "The stepAgents pass iterates a sorted-by-stable-id agent array and skips agents with nothing-to-decide (the anything-to-decide guard)"
    - "Each hub runs a deterministic Decide (dispatch/hold/consolidate) emitting existing events"
  artifacts:
    - path: "packages/simulation/src/continuation.ts"
      provides: "stepAgents SimTask variant"
      contains: "stepAgents"
    - path: "packages/simulation/src/ooda/hub.ts"
      provides: "pure hub Observe->Decide->Act decision function"
      exports: ["decideHub"]
    - path: "packages/simulation/src/engine.ts"
      provides: "oodaAgentsEnabled flag + stepAgents dispatch case + bootstrap self-reschedule + decision bypass"
      contains: "oodaAgentsEnabled"
  key_links:
    - from: "packages/simulation/src/engine.ts"
      to: "packages/simulation/src/ooda/index.ts"
      via: "stepAgents calls decideTruck/decideHub over the frozen observation surface"
      pattern: "decideTruck|decideHub|sortAgentsByStableId"
    - from: "packages/simulation/src/engine.ts dispatch"
      to: "stepAgents SimTask"
      via: "dispatch case + self-rescheduling scheduleNext(+OODA_INTERVAL_TICKS)"
      pattern: "case \"stepAgents\""
---

<objective>
Wire the OODA agents (24-01) INTO the deterministic engine. Add the `stepAgents` SimTask variant, the `dispatch` case, the bootstrap self-reschedule, and the `oodaAgentsEnabled` flag — mirroring the existing `inductPackage`/`createPackageBatch` self-rescheduling pattern EXACTLY. Under the flag the engine's per-tick `stepAgents` pass builds a FROZEN observation per agent at pass entry, iterates agents in sorted-by-stable-id order, applies the "anything-to-decide?" guard (an agent runs Decide/Act ONLY when it has a pending decision — never a blanket per-tick sweep), and routes each decision's Act through the existing `emit`. Also add the hub agent's pure `decideHub` (dispatch/hold/consolidate) so BOTH agent types run this phase (P25 coordinators need both).

Purpose: OODA-01 (truck step inside the engine) + OODA-02 (hub step) + OODA-04 (sorted iteration + frozen surface, now exercised against real engine state). The agents OWN the decision points under the flag; the centralized decision code is bypassed (CONTEXT decision: "agents are NOT advisory here"). Flag OFF = the existing centralized path, byte-identical — the determinism gate proving that is plan 24-04.

Output: the `stepAgents` machinery in the engine, the hub Decide, and an OODA-on integration test exercising the cadence + guard + sorted pass (the full flags-off golden gate is 24-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ooda-step-agents/24-CONTEXT.md
@.planning/phases/24-ooda-step-agents/24-01-SUMMARY.md
@./CLAUDE.md

<read_first>
- packages/simulation/src/engine.ts:1280-1373 (`inductPackage`) — the SELF-RESCHEDULING task to MIRROR: the off-guard (`if (!inductionOn ...) return`), the `scheduleNext(nextTick, { kind, tick: nextTick })` tail.
- packages/simulation/src/engine.ts:2141-2218 — the `dispatch` switch (add the `case "stepAgents"`) + the `!resuming` bootstrap block (seed the FIRST stepAgents, gated on the flag, exactly as `inductPackage` is seeded at INDUCTION_START_TICK).
- packages/simulation/src/engine.ts:593-735 — the flag-construction block (`inductionOn`/`outboundOn`/`continentalOn` STRICT `=== true`; the lazy `inductionRng`/`outboundRng` construction). Add `oodaAgentsEnabled` here the SAME way (STRICT `=== true`, never `??`/`||`).
- packages/simulation/src/engine.ts:1580-1690 (`departTrailer`) — the CENTRALIZED truck decisions the flag bypasses: the HOS `accrueDrivingLeg`/`selectDriverForLeg` rest decision and the fuel odometer-threshold refuel decision. Under the flag these outcomes come from `decideTruck` instead (the binding-feasibility REUSE contract is formalized in 24-03; here, route the Act through the existing `emitTruckRested`/`emitTruckRefueled`/`emit` so the SAME events are produced).
- packages/simulation/src/engine.ts:1166-1265 + 1356-1366 — the hub-side centralized decisions (`pendingBySpoke`/`pendingAtSpoke` manifests, consolidation staging/cadence) the hub agent owns under the flag.
- packages/simulation/src/continuation.ts:27-87 — the `SimTask` union (add `| { readonly kind: "stepAgents"; readonly tick: number }` mirroring `inductPackage`).
- packages/simulation/src/ooda/index.ts (from 24-01) — `decideTruck`, `deriveAgentRng`, `sortAgentsByStableId`, `AgentObservation`, `TruckDecision`.
- packages/simulation/src/engine.ts:830-840 (`legMilesFor`), :659-661 (`hosRng`), the `centerOf` helper at :811 — the engine state the observation builder reads (the in-engine fold maps, NOT Postgres projections — PITFALLS Pitfall 4).
- .planning/research/ARCHITECTURE.md §3 "Agent step ordering" + "Read-projections mid-tick vs end-of-tick" + "Cadence" — the frozen-surface rule and `OODA_INTERVAL_TICKS` as a fixed modular constant like `PACKAGE_INTERVAL_TICKS`.
</read_first>
</context>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: Hub agent decideHub (pure Observe->Decide->Act)</name>
  <files>packages/simulation/src/ooda/hub.ts, packages/simulation/src/ooda/hub.unit.test.ts, packages/simulation/src/ooda/index.ts</files>
  <behavior>
    - `decideHub(obs, rng)` is PURE/DETERMINISTIC: identical `(obs, rng-state)` ⇒ identical `HubDecision`.
    - `HubDecision` is a closed union: `{ kind: "dispatch"; trailerId } | { kind: "hold"; reason } | { kind: "consolidate"; spokeHubId; packageIds }`.
    - The hub observation is a FROZEN readonly snapshot: inbound/outbound queue depths, dock availability (integer), trailer fill, pending-consolidation manifest sizes, assigned center id, tick — integer/string only (PITFALLS Pitfall 2).
    - "anything-to-decide?" is expressible from the observation alone (e.g. empty queues + no pending consolidation ⇒ the engine skips this hub — the guard lives in the engine pass, but decideHub returns a `hold` no-op default so a tick always closes).
  </behavior>
  <action>Create `packages/simulation/src/ooda/hub.ts` exporting `decideHub(obs: HubObservation, rng: Rng): HubDecision` plus the `HubObservation` readonly type and `HubDecision` union. Mirror the truck Decide's structure from 24-01 (pure priority ladder over the frozen obs, any tie-break from the passed `rng` only). Add `HubObservation`/`HubDecision`/`decideHub` to `ooda/index.ts`. Keep it a pure leaf — no engine import, no wall-clock, no `Math.random`. The default branch returns `{ kind: "hold" }` so every agent always has a feasible no-op (the foundation for P25's no-livelock contract).</action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- src/ooda/hub.unit.test.ts</automated>
  </verify>
  <done>decideHub determinism + closed-union + no-op-default tests pass; pure leaf (no engine/wall-clock import).</done>
</task>

<task type="execute">
  <name>Task 2: stepAgents SimTask — variant, dispatch case, bootstrap self-reschedule, oodaAgentsEnabled flag</name>
  <files>packages/simulation/src/continuation.ts, packages/simulation/src/engine.ts</files>
  <action>(1) Add `| { readonly kind: "stepAgents"; readonly tick: number }` to the `SimTask` union in continuation.ts, with a doc comment mirroring `inductPackage`'s. (2) In engine.ts add `const oodaAgentsEnabled = opts.oodaAgentsEnabled === true;` in the flag block (STRICT `=== true`) and document the new `oodaAgentsEnabled?: boolean` option in `SimulateOptions` (mirror the `inductionEnabled`/`continentalTopology` doc: DEFAULT FALSE, two-part flags-off gate, lazy substream, byte-identical to 3920accc when absent). (3) Add `OODA_INTERVAL_TICKS` as a fixed module constant (Claude's discretion: 1 or 5; bake it into the new golden) alongside `PACKAGE_INTERVAL_TICKS`. (4) Add a `stepAgents(tick)` function that returns early when `!oodaAgentsEnabled`, then self-reschedules its successor via `scheduleNext(tick + OODA_INTERVAL_TICKS, { kind: "stepAgents", tick: tick + OODA_INTERVAL_TICKS })` — EXACTLY the `inductPackage` tail. (5) Add `case "stepAgents": stepAgents(task.tick); return;` to the `dispatch` switch. (6) In the `!resuming` bootstrap block, seed the first stepAgents at a fixed start tick gated on `if (oodaAgentsEnabled)` (mirror the `if (inductionOn) schedule(INDUCTION_START_TICK, ...)` block); on resume the pending task is restored from the captured queue (skip the seed). Leave the Decide/Act body to Task 3 — this task lands the empty-but-self-rescheduling skeleton so the off path constructs no agent stream and seeds no task (byte-identical foundation).</action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- src/engine && pnpm typecheck</automated>
  </verify>
  <done>The stepAgents task variant + dispatch case + bootstrap seed compile; with the flag absent NO stepAgents task is scheduled and NO agent substream is constructed; existing engine tests stay green.</done>
</task>

<task type="execute">
  <name>Task 3: stepAgents pass — frozen observation, sorted iteration, anything-to-decide guard, Act bypass</name>
  <files>packages/simulation/src/engine.ts, packages/simulation/test/ooda-engine.unit.test.ts</files>
  <action>Implement the `stepAgents(tick)` body. (1) BUILD a frozen observation array at pass ENTRY: for each truck (from `trailerRoster`, stable trailer ids) and each hub (sorted hub ids) read the in-engine fold maps ONLY (`odometerByTrailer`, `clockByDriver`/`driverByTrailer` + `remainingLegalDriveMinutes`, `pendingBySpoke`/`pendingAtSpoke`, dock state, `centerOf`) — NEVER re-read them during the pass (PITFALLS Pitfall 4). ROUND any geometry-derived field to an integer at this boundary (Pitfall 2). (2) Build the combined agent list and pass it through `sortAgentsByStableId` (trucks then hubs by stable id, or one unified stable-id sort — document the chosen total order). (3) For each agent in sorted order: apply the "anything-to-decide?" guard (skip agents whose frozen observation shows no pending decision — empty manifests / no HOS-or-fuel trigger / mid-leg with nothing to choose); for agents WITH a decision, derive the lazy per-agent substream via `deriveAgentRng(seed, stableId)`, call `decideTruck`/`decideHub`, and ACT by routing the outcome through the EXISTING emit helpers (`emitTruckRested`, `emitTruckRefueled`, the dispatch/consolidation `emit` paths) plus `emit(...)` for the new `TrailerDiverted`. (4) Under `oodaAgentsEnabled`, BYPASS the centralized truck/hub decision code so it is not double-applied (guard the relevant `departTrailer` rest/refuel block + the consolidation-cadence block with `if (!oodaAgentsEnabled)`, so the agent pass is the sole decider when on). (5) Add `packages/simulation/test/ooda-engine.unit.test.ts`: with `oodaAgentsEnabled:true` assert (a) stepAgents fires at the OODA cadence and self-reschedules across a multi-thousand-tick run, (b) the emitted stream contains agent-driven events including at least one `TrailerDiverted` or a rest/refuel decided by an agent, (c) a SHUFFLED trailerRoster (reorder the agent set before the pass) yields a byte-identical event batch over a fixed run — the single strongest determinism witness (the full golden + N-agent decorrelation goldens are 24-04). Keep all decision-relevant draws on the per-agent substream; no `Math.random`/`Date.now` in the pass.</action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- test/ooda-engine.unit.test.ts && pnpm typecheck</automated>
  </verify>
  <done>OODA-on run: stepAgents cadence + self-reschedule proven; agents emit real decisions (existing events + TrailerDiverted); the agent-order-shuffle batch is byte-identical; the centralized decision code is bypassed under the flag (no double-apply). typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stepAgents pass → fold maps | The pass reads in-engine state; reading a peer's same-tick write would couple decisions to agent order |
| flag-on path → centralized decision code | If both run, decisions double-apply and the stream is wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | mid-tick read-your-writes in stepAgents | mitigate | Build the frozen observation array at pass ENTRY; never re-read fold maps during the pass (PITFALLS Pitfall 4); the agent-order-shuffle test is the witness |
| T-24-06 | Tampering | double-applied decisions (centralized + agent) | mitigate | Guard the centralized rest/refuel/consolidation blocks with `if (!oodaAgentsEnabled)` so the agent pass is the sole decider when on |
| T-24-07 | Elevation of privilege | unsorted Map/Set agent iteration | mitigate | `sortAgentsByStableId` over a stable-id total order; emitted `seq` via the existing `(fireTick, claimSeq())` queue (PITFALLS Pitfall 1) |
</threat_model>

<verification>
- `pnpm --filter @mm/simulation test -- src/ooda test/ooda-engine.unit.test.ts` (hub Decide + engine integration green)
- `pnpm typecheck` (SimTask union threaded; no `any`)
- `pnpm lint`
- OODA OFF: no stepAgents task seeded, no agent substream constructed (the flags-off golden gate is 24-04, but verify here that the off path constructs nothing).
</verification>

<success_criteria>
- OODA-01: trucks make their own proceed/divert/rest/refuel/hold decisions via the in-engine stepAgents pass under the flag.
- OODA-02: hubs make their own dispatch/hold/consolidate decisions via the same pass.
- OODA-04 (exercised): sorted-by-stable-id iteration + frozen-observation surface proven against real engine state; the agent-order-shuffle batch is byte-identical.
- The cadence task self-reschedules like inductPackage; the centralized decision code is bypassed under the flag.
</success_criteria>

<output>
After completion, create `.planning/phases/24-ooda-step-agents/24-02-SUMMARY.md` recording: the chosen `OODA_INTERVAL_TICKS`, the total stable-id ordering (trucks-then-hubs vs unified), the "anything-to-decide?" predicate, which centralized blocks are bypassed under the flag, and the hub Decide ladder.
</output>
