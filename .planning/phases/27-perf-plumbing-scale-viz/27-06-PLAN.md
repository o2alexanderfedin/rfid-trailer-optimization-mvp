---
phase: 27-perf-plumbing-scale-viz
plan: 06
type: execute
wave: 3
depends_on: [27-03, 27-05]
files_modified:
  - packages/api/src/main.ts
  - packages/api/src/ws/envelope.ts
  - packages/api/src/ws/snapshots.ts
  - packages/web/src/map/suggestionColoring.ts
  - packages/web/src/map/layers.ts
  - packages/web/src/panels/useSuggestions.ts
  - packages/web/src/panels/SuggestionFeed.tsx
  - packages/web/src/panels/RightRail.tsx
  - packages/web/src/App.tsx
  - packages/web/src/map/MapView.tsx
  - packages/web/test/useSuggestions.unit.test.ts
autonomous: true
requirements: [VIZ-17]
must_haves:
  truths:
    - "the continental demo config deterministically fires a live 'won't divert: HOS/fuel' reject (COORD-03 becomes Complete)"
    - "an opt-in Suggestions toggle (default OFF) governs both a map overlay layer and an Advisory Suggestions rail feed"
    - "accepted suggestions render green, rejected render red; reject copy reuses COORDINATION_REJECT_LABELS verbatim"
    - "suggestions are transient tick-only (TickPayload.suggestions, never on SnapshotPayload); map markers flash ~2500ms then remove"
    - "the continental short-run determinism test (seed-42/300, difference-not-hash) stays green; baked goldens 3920accc/edfa5a6d unchanged"
  artifacts:
    - path: "packages/api/src/main.ts"
      provides: "continental demo fuel/HOS config override (enable fuel + lower refuelThresholdMiles / tighten maxDriveMin) firing the live reject"
      contains: "refuelThresholdMiles"
    - path: "packages/api/src/ws/envelope.ts"
      provides: "transient TickPayload.suggestions field (mirrors inductionEvents; NOT on SnapshotPayload)"
      contains: "suggestions?"
    - path: "packages/web/src/panels/useSuggestions.ts"
      provides: "suggestion feed hook (MAX_FEED_ENTRIES cap, newest-first, dedup-by-id) cloned from useAlertFeed"
      contains: "MAX_FEED_ENTRIES"
    - path: "packages/web/src/map/suggestionColoring.ts"
      provides: "two pre-allocated accept(green)/reject(red) cached styles"
      contains: "suggestionStyle"
  key_links:
    - from: "packages/api/src/main.ts"
      to: "DEFAULT_FUEL_CONFIG / hosConfig"
      via: "continental demo-config override (deterministic, no clock/RNG)"
      pattern: "refuelThresholdMiles|maxDriveMin"
    - from: "packages/web/src/App.tsx"
      to: "useSuggestions"
      via: "onAlertEnvelope dispatch of tick-only suggestions"
      pattern: "onSuggestions|useSuggestions"
---

<objective>
P27-B (live reject-with-reason, COORD-03) + VIZ-17 (advisory-suggestion overlay):

- **P27-B (demo-config-only, NO baked golden change):** tune the CONTINENTAL DEMO config (option 1, chosen) —
  enable fuel + lower `refuelThresholdMiles` (and/or tighten `maxDriveMin` via hosConfig) at the `main.ts` demo
  site — so a long backbone leg deterministically pushes a mid-trip truck past the refuel/HOS limit exactly when
  it is behind a congested hub the reroute rule already targets → the headline "won't divert: HOS/fuel" reject
  fires LIVE. The reject is ALREADY fully rendered server-side (COORDINATION_REJECT_LABELS → coordination-rejected
  → blockedFreight → AlertFeed) — no reject-rendering work. Do NOT touch the targeting heuristic or any default
  config (that would move `edfa5a6d`/`3920accc`).
- **VIZ-17:** an opt-in Suggestions toggle (default OFF) that governs BOTH a decluttered map overlay (accept-green
  ✓ / reject-red ✕, ~2500ms flash) AND an "Advisory Suggestions" rail feed (clone of the AlertFeed pipeline).
  Suggestions ride a NEW transient tick-only `TickPayload.suggestions` field (mirrors `inductionEvents`).

Purpose: this is the "smart and honest" demo moment made visible — the network suggests reroutes and an agent
visibly, honestly declines on its binding HOS/fuel feasibility. COORD-03 flips to Complete when this fires live.

Output: the demo-config reject + the suggestion DTO field + overlay layer + feed pipeline; baked goldens intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-perf-plumbing-scale-viz/27-CONTEXT.md
@.planning/phases/27-perf-plumbing-scale-viz/27-PATTERNS.md
@.planning/phases/27-perf-plumbing-scale-viz/27-UI-SPEC.md

<interfaces>
<!-- P27-B demo config — main.ts:63-64: fuelConfig = {...DEFAULT_FUEL_CONFIG, enabled} ← override refuelThresholdMiles
     (+/- tighten maxDriveMin via hosConfig main.ts:178); DEFAULT_FUEL_CONFIG (domain/src/fuel.ts:47-53,
     refuelThresholdMiles 1200) — do NOT edit the default; override only at the demo site. Deterministic, no clock/RNG. -->
<!-- Reject already rendered (NO work): COORDINATION_REJECT_LABELS exceptions.ts:54-61 (hos/fuel/dock/infeasible);
     coordinationRejectId exceptions.ts:134; coordination-rejected → exceptionKindToWire → "blockedFreight" snapshots.ts:416-427;
     AlertFeed renders reason/action -->

<!-- VIZ-17 DTO — envelope.ts: TickPayload :222-258 (mirror inductionEvents :250); SnapshotPayload :197-216 FORBIDS suggestions (Pitfall 7);
     suggestions attach onto the tick delta in snapshots.ts (mirror inductionEvents attach ~:885-892) -->
<!-- VIZ-17 overlay — inductionColoring.ts (whole file, single cached Style); layers.ts createInductionLayer :249-253 + flashInduction :261-279
     (add-then-setTimeout-remove; Date.now()/Math.random() for marker-id uniqueness ONLY = sanctioned); createDeliveryLayer/flashDelivery :288-301 -->
<!-- VIZ-17 feed — AlertFeed.tsx: MAX_FEED_ENTRIES=200 :40, applyExceptionsNew :51, sortFeed :88, kindLabel :107, AlertFeed component :172-208 (React escaping, NO dangerouslySetInnerHTML);
     useAlertFeed :124-152; App.tsx onAlertEnvelope :56-72; RightRail Live Exceptions section :80-94, SpeedControl :77 -->

<!-- UI-SPEC concrete values: toggle "Suggestions" default OFF, ON blue #1d4ed8; section "Advisory Suggestions";
     accept marker #16a34a / feed #4ade80 / glyph ✓; reject marker #dc2626 / feed #f87171 / glyph ✕; radius 13 white 2px;
     flash ~2500ms; declutter:true on the suggestion layer; reuse .alert-feed__* CSS; reject copy verbatim from COORDINATION_REJECT_LABELS -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: P27-B continental demo-config reject + transient suggestions DTO field</name>
  <read_first>
    - packages/api/src/main.ts:63-64 (fuelConfig literal — the ONLY P27-B change site), :99,:178,:181 (hosConfig/fuel wiring into the run)
    - packages/domain/src/fuel.ts:47-53 (DEFAULT_FUEL_CONFIG — do NOT edit; override at the demo site)
    - packages/api/src/ws/envelope.ts:197-216 (SnapshotPayload — must NOT gain suggestions, Pitfall 7), :222-258 (TickPayload; inductionEvents :250), :135-142 (InductionEvent shape to mirror for SuggestionEvent)
    - packages/api/src/ws/snapshots.ts:416-427 (exceptionKindToWire — reject already wired), the inductionEvents attach point (~:885-892)
    - packages/projections/src/reducers/exceptions.ts:54-61 (COORDINATION_REJECT_LABELS), :134 (coordinationRejectId)
    - packages/simulation/test/continental-determinism.unit.test.ts (seed-42/300 difference-not-hash test — must stay green)
  </read_first>
  <action>
    P27-B: at the `main.ts` continental demo site (`:63-64`) override `refuelThresholdMiles` lower (and/or tighten
    `maxDriveMin` via the demo `hosConfig`, `:178`) on top of `{...DEFAULT_FUEL_CONFIG, enabled: true}` so a long
    backbone leg deterministically pushes a mid-trip truck past the refuel/HOS limit exactly when it is behind a
    congested hub the reroute rule already targets → the truck declines the reroute on its own feasibility → a live
    "won't divert: HOS/fuel" `SuggestionRejected`. Pick the exact numbers (your discretion, CONTEXT Area 4 — MUST be
    deterministic, no clock/RNG). Do NOT edit `DEFAULT_FUEL_CONFIG` or the targeting heuristic (option 2 is forbidden —
    it moves `edfa5a6d`/`3920accc`).

    VIZ-17 DTO: add a NEW transient OPTIONAL `suggestions?: readonly SuggestionEvent[]` field to `TickPayload`
    (`envelope.ts:222-258`), mirroring `inductionEvents` (`:250`) — define `SuggestionEvent` (suggestionId, kind ∈
    reroute|hold|consolidate|dispatch, outcome ∈ accepted|rejected, target entity id, toHubId, reasonCode? for
    rejects, lon/lat for the overlay marker). Do NOT add it to `SnapshotPayload` (`:197-216` — Pitfall 7). Attach
    `suggestions` onto the tick delta in `snapshots.ts` the same way `inductionEvents` is attached (~:885-892),
    sourcing accepted/rejected suggestion outcomes from the tick's events (SuggestionAccepted/SuggestionRejected,
    reusing `coordinationRejectId` + `COORDINATION_REJECT_LABELS` for reject copy).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/api typecheck && pnpm --filter @mm/simulation vitest run test/continental-determinism.unit.test.ts && pnpm --filter @mm/api test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The continental short-run determinism test (seed-42/300, difference-not-hash) stays green; the flags-off `3920accc` + rule-based `edfa5a6d` goldens are byte-identical (grep confirms no change to those literals; `DEFAULT_FUEL_CONFIG` unchanged).
    - `TickPayload` gains optional `suggestions?`; `SnapshotPayload` does NOT (grep -v '^#' packages/api/src/ws/envelope.ts | grep -c 'suggestions' on the SnapshotPayload interface is 0).
    - A test/manual witness shows the continental demo config produces at least one `SuggestionRejected` with reasonCode hos|fuel (the live reject fires).
  </acceptance_criteria>
  <done>Continental demo config deterministically fires a live HOS/fuel reject; transient suggestions DTO field added (tick-only); goldens intact.</done>
</task>

<task type="auto">
  <name>Task 2: suggestion overlay layer + cached accept/reject styles (map surface)</name>
  <read_first>
    - packages/web/src/map/inductionColoring.ts (whole file — single pre-allocated Style, zero-per-frame StyleFunction)
    - packages/web/src/map/layers.ts:249-253 (createInductionLayer), :261-279 (flashInduction add-then-setTimeout-remove; Date.now()/Math.random() for marker-id uniqueness = sanctioned), :288-301 (createDeliveryLayer/flashDelivery)
    - packages/web/src/map/MapView.tsx (where transient layers are created + flashed on tick)
    - 27-UI-SPEC.md (VIZ-17 overlay: radius 13, accept #16a34a ✓ / reject #dc2626 ✕, white 2px, flash ~2500ms, declutter:true)
  </read_first>
  <action>
    Create `packages/web/src/map/suggestionColoring.ts` cloning `inductionColoring.ts` but with TWO pre-allocated
    cached `Style`s (zero per-frame alloc): accept (green `#16a34a` fill, glyph `✓`) and reject (red `#dc2626` fill,
    glyph `✕`), radius 13, white 2px stroke. Export `suggestionStyle(feature)` branching on an `outcome` feature prop
    (accepted/rejected). In `layers.ts` add `createSuggestionLayer` (clone `createInductionLayer` :249-253) on a
    layer with `declutter: true` so a burst never stacks, and `flashSuggestion(source, id, lon, lat, outcome,
    durationMs = 2500)` (clone `flashInduction` :261-279 — add-then-`setTimeout`-remove; `Date.now()`/`Math.random()`
    for marker-id uniqueness ONLY, the sanctioned use). Wire the layer into `MapView.tsx` and flash markers from the
    tick `suggestions` array (when the toggle is ON). The overlay layer is hidden when the toggle is OFF.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/web typecheck && pnpm --filter @mm/web build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `suggestionColoring.ts` has exactly two module-level pre-allocated `Style`s (accept/reject); `suggestionStyle` allocates nothing per call.
    - `createSuggestionLayer` uses `declutter: true`; `flashSuggestion` defaults to ~2500ms and removes the feature individually.
    - `pnpm --filter @mm/web typecheck && build` succeed.
  </acceptance_criteria>
  <done>Decluttered suggestion overlay with two cached accept/reject styles + ~2500ms flash, wired to the tick suggestions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useSuggestions hook + SuggestionFeed + App dispatch + RightRail toggle (feed surface)</name>
  <read_first>
    - packages/web/src/panels/AlertFeed.tsx:18-21 (exported pure helpers), :40 (MAX_FEED_ENTRIES=200), :51-93 (applyExceptionsNew/Resolved/sortFeed), :107-118 (kindLabel), :124-152 (useAlertFeed), :172-208 (AlertFeed component — React escaping, NO dangerouslySetInnerHTML)
    - packages/web/src/App.tsx:53-74 (useAlertFeed wiring + onAlertEnvelope :56-72 — dispatch site)
    - packages/web/src/panels/RightRail.tsx:77 (SpeedControl), :80-94 (Live Exceptions section to clone)
    - packages/projections/src/reducers/exceptions.ts:54-61 (COORDINATION_REJECT_LABELS — reuse verbatim for reject rows)
    - 27-UI-SPEC.md (Copywriting Contract + VIZ-17 feed styling: toggle "Suggestions" default OFF / ON blue; section "Advisory Suggestions"; accept green #4ade80 / reject red #f87171; reuse .alert-feed__* CSS; empty "No active suggestions")
  </read_first>
  <behavior>
    - useSuggestions: appends tick suggestions newest-first, dedups by suggestionId, caps at MAX_FEED_ENTRIES=200 (drop oldest), returns the sorted feed + an onSuggestions dispatcher.
    - Accepted rows render green ("Accepted — {Kind} {entityId} → {toHubId}"); rejected rows render the verbatim COORDINATION_REJECT_LABELS[reasonCode] in red.
    - Empty state reads "No active suggestions".
    - The toggle (default OFF) governs BOTH the rail feed visibility AND the map overlay visibility (same toggle).
  </behavior>
  <action>
    Create `packages/web/src/panels/useSuggestions.ts` cloning `useAlertFeed` (`AlertFeed.tsx:124-152`) + its pure
    helpers (`applyExceptionsNew`/`sortFeed` shape, `:51-93`) with the same `MAX_FEED_ENTRIES = 200` cap, newest-first
    sort, dedup-by-id; export the pure helpers for Node unit tests. Create `packages/web/src/panels/SuggestionFeed.tsx`
    cloning the `AlertFeed` component (`:172-208`) — empty state "No active suggestions", three-line row layout
    (kind label Title Case via a `kindLabel` clone / reason / action), accept rows green `#4ade80`, reject rows red
    `#f87171` reusing `COORDINATION_REJECT_LABELS` verbatim, all text via React default escaping (NO
    `dangerouslySetInnerHTML`, threat T-05-16), reusing the `.alert-feed__*` CSS 1:1 (visual sibling of Live
    Exceptions). In `App.tsx` add `useSuggestions` and dispatch in `onAlertEnvelope` (`:56-72`) in the TICK branch
    only (`if (envelope.payload.suggestions !== undefined) onSuggestions(...)`) — never on the snapshot branch
    (Pitfall 7). In `RightRail.tsx` add an "Advisory Suggestions" section cloning the Live Exceptions section
    (`:80-94`) with a count badge, plus a "Suggestions" toggle near `SpeedControl` (`:77`) — default OFF, blue accent
    `#1d4ed8` ON — that governs both the feed section AND (via shared state) the map overlay layer's visibility.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/web typecheck && pnpm --filter @mm/web vitest run test/useSuggestions.unit.test.ts && pnpm --filter @mm/web build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - useSuggestions unit test: newest-first, dedup-by-suggestionId, caps at 200 (oldest dropped), accept vs reject classified correctly with verbatim reject labels.
    - Suggestions dispatched only on the tick branch (grep -v '^#' packages/web/src/App.tsx shows the suggestions dispatch inside the tick/else branch, not the snapshot branch).
    - SuggestionFeed has NO `dangerouslySetInnerHTML`; the toggle defaults OFF and governs both feed + overlay.
    - `pnpm --filter @mm/web typecheck && build` succeed.
  </acceptance_criteria>
  <done>Opt-in Suggestions toggle governs the Advisory Suggestions feed + map overlay; accept-green/reject-red with verbatim reject copy; tick-only dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server suggestion events → client render | suggestion text rendered in the DOM; untrusted-shaped strings must not execute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-15 | T (Tampering) / XSS | SuggestionFeed rendering reason/action strings | mitigate | All text via React default escaping; NO `dangerouslySetInnerHTML` (threat T-05-16 precedent from AlertFeed); reject copy is from the closed `COORDINATION_REJECT_LABELS` constant, not free text. |
| T-27-16 | D (DoS) | unbounded suggestion feed growth in a long demo | mitigate | `MAX_FEED_ENTRIES = 200` cap (oldest dropped) cloned from AlertFeed; map markers are transient (~2500ms then removed). |
| T-27-17 | T (Tampering) | P27-B demo-config drifting a baked golden | mitigate | Demo config overrides ONLY at the `main.ts` site (not `DEFAULT_FUEL_CONFIG`, not the targeting heuristic); acceptance criterion asserts `3920accc`/`edfa5a6d` unchanged + the continental difference-not-hash test green. |
</threat_model>

<verification>
- `pnpm --filter @mm/api typecheck && test` green; `pnpm --filter @mm/web typecheck && test && build` green.
- `continental-determinism.unit.test.ts` (seed-42/300) green; `3920accc` + `edfa5a6d` byte-identical (P27-B is demo-config-only).
- The continental demo deterministically fires a live "won't divert: HOS/fuel" reject (COORD-03 demonstrable).
- Bookkeeping: this plan's completion flips COORD-03 to Complete in REQUIREMENTS.md (noted in SUMMARY).
</verification>

<success_criteria>
- COORD-03 fires LIVE: the continental demo deterministically surfaces a "won't divert: HOS/fuel" reject (demo-config-only, goldens intact).
- VIZ-17: an opt-in Suggestions toggle (default OFF) governs a decluttered accept-green/reject-red map overlay + an Advisory Suggestions feed; suggestions are transient tick-only.
- Reject copy reuses COORDINATION_REJECT_LABELS verbatim (single source of truth).
</success_criteria>

<output>
After completion, create `.planning/phases/27-perf-plumbing-scale-viz/27-06-SUMMARY.md`
</output>
