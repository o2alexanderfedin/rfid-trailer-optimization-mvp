---
phase: 27-perf-plumbing-scale-viz
plan: 04
type: execute
wave: 2
# depends_on: capture the NEW optimizer-on golden against the final post-perf/plumbing/viz tree
# so Phase 28's consolidated audit sees one coherent base — isolates the reproducibility-first
# golden recapture to its own wave (zero file overlap with same-wave 27-05).
depends_on: [27-01, 27-02, 27-03]
files_modified:
  - packages/simulation/src/engine.ts
  - packages/simulation/src/coordinator/optimize.ts
  - packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts
autonomous: true
requirements: [COORD-06]
must_haves:
  truths:
    - "the optimizer-backed reroute is genuinely route-aware-divergent from the rule-based path (COORD-06 criterion-1)"
    - "the optimizer can DECLINE an infeasible reroute (over-capacity / LIFO-blocked) and recommend a different/better one"
    - "the optimizer's CHOSEN next hub is read from the routed EpochResult, not the static twin route[0]"
    - "a NEW optimizer-on golden is captured reproducibility-first (in-process twice + 2 separate node processes) BEFORE baking the literal"
    - "the new optimizer-on golden DIFFERS from edfa5a6d AND 3920accc AND 94689f99; the three prior goldens stay byte-identical"
  artifacts:
    - path: "packages/simulation/src/engine.ts"
      provides: "optimizerRerouteFor with real destination choice + real freeze/feasibility + real blocks/capacity"
      contains: "optimizerRerouteFor"
    - path: "packages/simulation/src/coordinator/optimize.ts"
      provides: "epochResultToRerouteSuggestions reading the optimizer's actually-chosen next hub; buildCenterTwinFromFold wiring real fold-state legs/blocks"
      contains: "epochResultToRerouteSuggestions"
    - path: "packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts"
      provides: "NEW golden + flipped EQUALS→DIFFERS assertion + recaptured reroute counts"
      contains: "COORDINATOR_OPTIMIZER_ON_GOLDEN_SHA256"
  key_links:
    - from: "packages/simulation/src/engine.ts optimizerRerouteFor"
      to: "runEpoch (per-center twin)"
      via: "real candidate relief legs + real freeze + real blocks/capacity"
      pattern: "optimizerRerouteFor"
    - from: "packages/simulation/src/coordinator/optimize.ts epochResultToRerouteSuggestions"
      to: "EpochResult routed next hub"
      via: "read the routed choice (not static route[0])"
      pattern: "epochResultToRerouteSuggestions"
---

<objective>
P27-A (demo carry-over, COORD-06 criterion-1): make the optimizer-backed reroute GENUINELY route-aware-divergent
from the rule-based heuristic — and CHANGE the optimizer-on golden accordingly (this is expected and required).

Today the optimizer-on golden is byte-identical to the rule-based coordinator golden `edfa5a6d…` because three
structural pins in `optimizerRerouteFor` force the optimizer to only ever endorse the same reroute the rule makes:
(1) route head statically pinned to `obs.centerId`, (2) always-actionable `departureOffsetMin = FREEZE+1`,
(3) no real load/capacity (`blocks:[]`, constant capacity 50, center↔hub-only legs). Removing these pins gives the
optimizer a real destination choice, real freeze/feasibility, and real capacity — so it can decline an infeasible
reroute and recommend a different/better one, producing a divergent event stream.

Purpose: this is the headline "route-aware, not just rule-based" optimization moment AND it is the prerequisite
for the new optimizer-on golden that Phase 28's consolidated audit consumes. This plan is ISOLATED in its own wave
so the reproducibility-first golden recapture is auditable.

Output: a divergent optimizer reroute path + a NEW optimizer-on golden captured reproducibility-first, with the
documented-equality assertion flipped to documented-divergence and the three prior goldens left intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-perf-plumbing-scale-viz/27-CONTEXT.md
@.planning/phases/27-perf-plumbing-scale-viz/27-PATTERNS.md

<interfaces>
<!-- The 3 pins to remove — packages/simulation/src/engine.ts optimizerRerouteFor :2410-2531:
     PIN 2 departureOffsetMin = COORDINATOR_OPTIMIZER_FREEZE_WINDOW_MIN + 1  (:2476) → derive from real schedule
     PIN 1 routeStops head pinned to obs.centerId (:2481-2484) → give real destination choice (alternate cross-dock hubs)
     PIN 3 blocks:[] + constant capacity 50 + center↔hub-only legs (:2485, :2493-2509) → real blocks + per-leg capacity/travelMin/distanceMiles
     COORDINATOR_OPTIMIZER_FREEZE_WINDOW_MIN = 15 (engine.ts:581); runEpoch call :2522-2526 (UNCHANGED) -->

<!-- The next-hub-derivation bug — packages/simulation/src/coordinator/optimize.ts epochResultToRerouteSuggestions :186-223:
     :193-197 reads trailer.route[0] (the STATIC twin head = always obs.centerId) → read the optimizer's ROUTED choice from EpochResult
     buildCenterTwinFromFold :104-154 already supports distanceMiles on legs (:128) + carries blocks (:145-149);
     CenterFoldRouteLeg.optionalDistanceMiles + CenterFoldTrailer.blocks fields :50-54,66-67
     the 3-gate emit (feasible+!frozen / has-current-next / differs) :201-217 STAYS — with real feasibility it now genuinely DECLINES -->

<!-- Reproducibility-first protocol — coordinator-optimizer-determinism.unit.test.ts:
     sha helper :58-59; protocol :92-95 (run twice in-process ⇒ equal AND 2 separate node processes ⇒ equal BEFORE baking);
     COORDINATOR_OPTIMIZER_ON_GOLDEN_SHA256 literal :111-112; COORDINATOR_ON_GOLDEN_SHA256 (edfa5a6d) :117-118;
     FLAGS_OFF (3920accc) :120; OODA_ON (94689f99) :122;
     EQUALS test to FLIP :138-146; existing DIFFERS test (3920accc + 94689f99) to EXTEND :148-154;
     reroute-count assertions to RECAPTURE :176-181 (currently reroute === 9553) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: remove the 3 pins — engine.ts optimizerRerouteFor (real choice + real freeze + real capacity)</name>
  <read_first>
    - packages/simulation/src/engine.ts:2410-2531 (optimizerRerouteFor — the 3 pins), :581 (COORDINATOR_OPTIMIZER_FREEZE_WINDOW_MIN), :2522-2526 (runEpoch call, unchanged), :2493-2509 (center↔hub leg construction), :2450 (partitioned slice for this center)
    - packages/simulation/src/coordinator/optimize.ts:104-154 (buildCenterTwinFromFold + the CenterFold* slice fields it consumes)
  </read_first>
  <action>
    In `optimizerRerouteFor` (`engine.ts:2410-2531`) remove the three structural pins (CONTEXT Area 4):
    PIN 2 (`:2476`) — replace the always-actionable `departureOffsetMin = FREEZE+1` with the trailer's REAL scheduled
    departure derived from fold state, so a near-departure trailer is genuinely frozen (left untouched) and only
    actionable trailers are routed. PIN 1 (`:2481-2484`) — give the optimizer a REAL destination choice: build the
    per-center twin with MULTIPLE candidate relief legs (the center AND alternate uncongested cross-dock hubs from
    this center's partitioned slice, `:2450`), not just the static center head. PIN 3 (`:2485`, `:2493-2509`) —
    populate REAL `blocks` + per-leg `capacity`/`travelMin`/`distanceMiles` from fold state (replace `blocks:[]` and
    the constant capacity 50 / center↔hub-only legs) so the optimizer can DECLINE an over-capacity / LIFO-blocked
    reroute. Keep `runEpoch` itself unchanged (`:2522-2526`). The per-center candidate-relief-leg selection is at
    your discretion (CONTEXT "Claude's Discretion") provided it is deterministic (no clock/RNG; sorted-by-stable-id
    tie-breaks) and stays bounded to the partitioned slice (no full-region re-solve — COORD-06 criterion-2).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @mm/simulation typecheck` passes (no `any`; closed switches intact).
    - `optimizerRerouteFor` no longer hard-codes `blocks:[]` or `departureOffsetMin = FREEZE+1` (grep -v '^#' packages/simulation/src/engine.ts | grep -c 'blocks: \[\]' within optimizerRerouteFor is 0).
    - Candidate legs include alternate cross-dock hubs from the partitioned slice (not center↔hub only); per-leg capacity/travelMin/distanceMiles sourced from fold state.
  </acceptance_criteria>
  <done>The 3 pins are gone; the per-center twin offers real destination choice + real freeze + real capacity; runEpoch unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: read the optimizer's CHOSEN next hub — coordinator/optimize.ts</name>
  <read_first>
    - packages/simulation/src/coordinator/optimize.ts:186-223 (epochResultToRerouteSuggestions; :191-197 the route[0] bug; :201-217 the 3-gate emit), :104-154 (buildCenterTwinFromFold; distanceMiles leg field :128; blocks carry :145-149), :50-54,66-67 (CenterFoldRouteLeg.optionalDistanceMiles + CenterFoldTrailer.blocks)
  </read_first>
  <action>
    In `epochResultToRerouteSuggestions` (`optimize.ts:186-223`) change the next-hub derivation (`:191-197`): instead
    of reading `trailer.route[0]` (the STATIC twin head, always `obs.centerId` — the bug), read the optimizer's
    ACTUALLY-CHOSEN next hub from the routed `EpochResult` (the destination the optimizer routed the trailer to,
    given the real candidate legs from Task 1). Wire the real fold-state values into the `CenterFoldRouteLeg` /
    `CenterFoldTrailer` slices that `buildCenterTwinFromFold` (`:104-154`) consumes — the `optionalDistanceMiles`
    (`:50-54`, `:128`) + `blocks` (`:66-67`, `:145-149`) fields — so the twin the optimizer routes over carries real
    capacity/distance/load. Keep the 3-gate emit (feasible + !frozen / has-current-next / differs, `:201-217`) — with
    real feasibility it will now genuinely DECLINE infeasible reroutes (the divergence source). This is the
    read-side translator change that pairs with the engine twin change in Task 1.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation typecheck && pnpm --filter @mm/simulation vitest run test/coordinator-optimize.unit.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `epochResultToRerouteSuggestions` reads the routed next hub from `EpochResult`, not `trailer.route[0]` (grep -v '^#' packages/simulation/src/coordinator/optimize.ts | grep -c 'trailer.route\[0\]' is 0 in the next-hub derivation).
    - `pnpm --filter @mm/simulation typecheck` passes; any existing coordinator/optimize unit test compiles against the new slice fields.
    - The 3-gate emit is preserved (still gates feasible/!frozen/differs).
  </acceptance_criteria>
  <done>Translator reads the optimizer's chosen hub; twin slices carry real blocks/capacity/distance; optimizer can decline.</done>
</task>

<task type="auto">
  <name>Task 3: capture the NEW optimizer-on golden reproducibility-first + flip EQUALS→DIFFERS</name>
  <read_first>
    - packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts:58-59 (sha helper), :92-95 (reproducibility-first protocol), :111-118 (the two golden literals), :120-122 (3920accc + 94689f99), :138-146 (EQUALS test to FLIP), :148-154 (DIFFERS test to EXTEND), :176-181 (reroute-count assertions to RECAPTURE)
  </read_first>
  <action>
    Capture the NEW `COORDINATOR_OPTIMIZER_ON_GOLDEN_SHA256` (`:111-112`) REPRODUCIBILITY-FIRST per the documented
    protocol (`:92-95`): run `sha(simulate(COORDINATOR_OPTIMIZER_ON_OPTS))` twice IN-PROCESS (must be equal) AND across
    TWO SEPARATE `node` process invocations (must be equal) BEFORE baking the literal — never commit a non-reproducible
    golden (PITFALLS). Then update the test:
    - FLIP the EQUALS assertion (`:138-146`): change the "EQUALS edfa5a6d… (documented coincidence)" `it` to assert the
      optimizer-on golden DIFFERS from `COORDINATOR_ON_GOLDEN_SHA256` (edfa5a6d) — `expect(...).not.toBe(COORDINATOR_ON_GOLDEN_SHA256)`
      — and update the comment to documented-divergence (route-aware, no longer endorses the same reroute).
    - EXTEND the existing DIFFERS test (`:148-154`) so it also asserts `!= COORDINATOR_ON_GOLDEN_SHA256` (in addition to
      the existing `!= 3920accc` and `!= 94689f99`).
    - RECAPTURE the reroute-count assertions (`:176-181`): the hard-coded `reroute === 9553` (and `accepted + rejected ==
      suggested`) will change with the divergent path — run the stream, read the new post-guard counts, and update the exact
      literals (keep the `toBeGreaterThan(1000)` real-path witnesses).
    The three prior goldens `3920accc` / `edfa5a6d` / `94689f99` (`:120-122`, `:117-118`) MUST stay byte-identical
    (do NOT change those literals — only the optimizer-on one changes). Document the capture host/arch next to the new
    literal (mirror the existing arm64 capture note) for the Phase 28 audit.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation vitest run test/coordinator-optimizer-determinism.unit.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The new optimizer-on golden is reproducible: `simulate()` twice in-process equal AND two separate `node` processes equal (witnessed by the in-process-reproducible `it` and the captured-host note).
    - The optimizer-on golden DIFFERS from `edfa5a6d` (COORDINATOR_ON) AND `3920accc` (FLAGS_OFF) AND `94689f99` (OODA_ON) — all three DIFFERS assertions green.
    - `3920accc`, `edfa5a6d`, `94689f99` literals are unchanged (grep confirms the three prior constants are byte-identical to their current values).
    - The recaptured reroute/suggested/accepted/rejected counts assert exact new literals and pass.
  </acceptance_criteria>
  <done>NEW optimizer-on golden captured reproducibility-first; EQUALS flipped to DIFFERS; prior 3 goldens intact; counts recaptured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator → optimizer (in-fold) | pure `runEpoch` over fold-derived inputs; no external input; the determinism gate is the trust boundary (a non-reproducible golden would corrupt the audit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | T (Tampering) | baking a non-reproducible / wrong optimizer-on golden | mitigate | Reproducibility-first capture (in-process twice + 2 separate node processes) BEFORE baking; the in-process-reproducible `it` + the DIFFERS-from-3-priors assertions guard against a corrupted literal. |
| T-27-09 | D (DoS) | per-center optimizer epoch scope growing with network size | mitigate | The candidate-relief-leg selection stays bounded to this center's partitioned slice (COORD-06 criterion-2: scope ⊆ affected hubs, size independent of total network) — no full-region re-solve. |
| T-27-10 | T (Tampering) | unintended drift in flags-off / rule-based / OODA goldens | mitigate | Acceptance criterion asserts the three prior golden literals (`3920accc`/`edfa5a6d`/`94689f99`) are byte-identical; only the optimizer-on literal changes. |
</threat_model>

<verification>
- `pnpm --filter @mm/simulation typecheck` green.
- `pnpm --filter @mm/simulation test` green — INCLUDING the flags-off `3920accc`, rule-based `edfa5a6d`, and OODA-on `94689f99` goldens still byte-identical (only the optimizer-on golden changes).
- The new optimizer-on golden DIFFERS from all three priors; reproducibility-first witnesses green.
</verification>

<success_criteria>
- The optimizer-backed reroute is route-aware-divergent (declines infeasible reroutes, reads the routed choice) — COORD-06 criterion-1 satisfied.
- A NEW optimizer-on golden is captured reproducibility-first and DIFFERS from edfa5a6d / 3920accc / 94689f99.
- The three prior goldens stay byte-identical; the new golden is documented for the Phase 28 consolidated audit.
</success_criteria>

<output>
After completion, create `.planning/phases/27-perf-plumbing-scale-viz/27-04-SUMMARY.md`
</output>
