---
phase: 27-perf-plumbing-scale-viz
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/app.ts
  - packages/api/src/routes/queries.ts
  - packages/web/src/map/coloring.ts
  - packages/web/src/map/layers.ts
  - packages/web/src/map/MapView.tsx
  - packages/web/src/panels/Legend.tsx
  - packages/web/test/scale-viz.unit.test.ts
autonomous: true
requirements: [VIZ-15, VIZ-16]
must_haves:
  truths:
    - "100+ hubs render without clutter: spokes are clustered, centers stay individually visible"
    - "centers / spokes / backbone legs / spoke legs read as four distinct visual tiers (size + ring + weight, NOT new hue)"
    - "static topology (hub kind/tier + route isBackbone) is sent ONCE via REST on map init, not over ws"
    - "per-tick ws payload bytes stay bounded as hub count grows (deltas carry only id-keyed metric buckets)"
  artifacts:
    - path: "packages/api/src/app.ts"
      provides: "GET /hubs DTO with kind + tier sourced from network/centers isCenter"
      contains: "kind"
    - path: "packages/api/src/routes/queries.ts"
      provides: "RouteDto with isBackbone sourced from the backbone leg set"
      contains: "isBackbone"
    - path: "packages/web/src/map/layers.ts"
      provides: "clustered spoke layer (Cluster + VectorImageLayer declutter) + un-clustered center tier-1 layer"
      contains: "Cluster"
    - path: "packages/web/src/map/coloring.ts"
      provides: "tier-branched cached style fns (center/spoke radius+ring; backbone/spoke leg weight+opacity)"
      contains: "tier"
  key_links:
    - from: "packages/api/src/app.ts"
      to: "packages/simulation/src/network/centers.ts isCenter"
      via: "kind/tier derivation on the REST hub DTO"
      pattern: "isCenter|kind"
    - from: "packages/web/src/map/layers.ts"
      to: "ol/source/Cluster + VectorImageLayer"
      via: "createHubLayer swap (spokes clustered, centers separate)"
      pattern: "Cluster|VectorImageLayer"
---

<objective>
VIZ-15 (declutter) + VIZ-16 (tier hierarchy): render 100+ hubs cleanly on the existing OpenLayers map by
(a) clustering the spoke field via `ol/source/Cluster` + `declutter: true` on a `VectorImageLayer`, keeping
regional centers on their own un-clustered tier-1 layer; (b) propagating the Phase-23 topology distinction
to the client — `kind`/`tier` on the static REST hub DTO and `isBackbone` on the route DTO — and styling
four distinct tiers (centers / spokes / backbone legs / spoke legs) by SIZE + RING + WEIGHT, never by hue
(hue is owned by the existing volume/load/risk ramps).

Purpose: at the default continental zoom, 80–130 hubs + ~30 backbone legs + live trailers must read as a
clean operator view, not noise. Static topology stays sent-once via REST; per-tick deltas carry only
id-keyed metric buckets so payload bytes stay bounded as hub count grows.

Output: tier-aware DTOs, a clustered/decluttered hub layer with a separate center tier, tier-branched
cached styles, and an extended legend — all following the zero-per-frame cached-Style convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-perf-plumbing-scale-viz/27-CONTEXT.md
@.planning/phases/27-perf-plumbing-scale-viz/27-PATTERNS.md
@.planning/phases/27-perf-plumbing-scale-viz/27-UI-SPEC.md

<interfaces>
<!-- Tier source — packages/simulation/src/network/centers.ts: pickRegionalCenters :102, buildBackbone :206
     (directed "<from>-><to>" backbone leg ids), deriveCenterPartition :330, isCenter helper -->
<!-- REST topology DTO sites: GET /hubs map app.ts:26-34 (HubDto = Hub, app.ts:8); RouteDto queries.ts:64-69 -->
<!-- Web map: createHubLayer layers.ts:40-57 (swap target → Cluster + VectorImageLayer{declutter:true});
     createRouteLayer layers.ts:66-80; createTrailerLayer layers.ts:90-92 (STAYS plain VectorLayer — do NOT cluster trailers) -->
<!-- Cached-style precedence: hubStyle coloring.ts:119-133 (duty→volume→default), routeStyle coloring.ts (load→default);
     STYLE_CACHE pre-allocated coloring.ts:66-76; MARKER_RADIUS=16 coloring.ts:35;
     HUB_COLORS/HUB_BUCKET_LABELS single-source-of-truth coloring.ts:48-63 -->

<!-- UI-SPEC concrete values (decided):
     Cluster distance 40px / minDistance 20px; center radius 20px + amber #f59e0b 3px ring;
     spoke radius 12px + white 2px ring; backbone leg #cbd5e1 4px opacity 0.9; spoke leg 2px opacity 0.55;
     cluster bubble slate-700 #334155 disc, white count, radius 14→22 log-bucketed into ≤4 cached sizes;
     tier encoded by size+ring NOT hue; centers EXCLUDED from the clustered source -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tier DTO fields — kind/tier on hub DTO, isBackbone on route DTO (REST, sent once)</name>
  <read_first>
    - packages/api/src/app.ts:8 (HubDto = Hub), :26-34 (GET /hubs row map)
    - packages/api/src/routes/queries.ts:64-69 (RouteDto)
    - packages/simulation/src/network/centers.ts:102 (pickRegionalCenters), :206 (buildBackbone directed leg ids :288-289), :330 (deriveCenterPartition), the isCenter helper
  </read_first>
  <action>
    Extend the REST hub DTO (`app.ts:8` `HubDto`, surfaced by `GET /hubs` :26-34) with `kind: "center" | "spoke"`
    and an optional `tier?: number`, sourced from `network/centers.ts` (`isCenter` / the derived center partition —
    a hub id present in `pickRegionalCenters`/`deriveCenterPartition` centers is `"center"`, else `"spoke"`). Extend
    `RouteDto` (`queries.ts:64-69`) with `readonly isBackbone: boolean`, sourced from the `buildBackbone` directed
    `"<from>-><to>"` leg-id set (:288-289). Keep these on the STATIC REST topology DTOs ONLY — they are fetched once
    on map init (CONTEXT Area 3); do NOT add them to the ws `HubState`/`RouteState` tick payloads. If the
    continental flag is off (single-center legacy), default `kind:"spoke"` for non-`hubs[0]` and `isBackbone:false`
    so the legacy 10-hub map is unaffected.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/api typecheck && pnpm --filter @mm/api test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @mm/api typecheck` passes; `HubDto` has `kind` (+optional `tier`), `RouteDto` has `isBackbone`.
    - `GET /hubs` returns `kind:"center"` for regional-center hub ids and `kind:"spoke"` otherwise; route DTO `isBackbone` true exactly for backbone legs.
    - No new field appears on the ws `HubState`/`RouteState` (grep -v '^#' packages/api/src/ws/envelope.ts | grep -c 'isBackbone' on HubState/RouteState stays 0 — topology stays REST-only).
  </acceptance_criteria>
  <done>Hub DTO carries kind/tier, route DTO carries isBackbone, sourced from centers.ts; ws tick payloads unchanged; api tests green.</done>
</task>

<task type="auto">
  <name>Task 2: tier-branched cached styles + legend (VIZ-16)</name>
  <read_first>
    - packages/web/src/map/coloring.ts:35 (MARKER_RADIUS), :48-63 (HUB_COLORS/HUB_BUCKET_LABELS single source of truth), :66-76 (pre-allocated STYLE_CACHE), :119-133 (hubStyle precedence duty→volume→default), routeStyle block
    - packages/web/src/panels/Legend.tsx (derives labels from the coloring single-source arrays)
    - 27-UI-SPEC.md (Color → VIZ-16 tier table: concrete radius/ring/opacity values)
  </read_first>
  <action>
    In `coloring.ts`, add tier-aware cached style functions that branch on `feature.get("tier")` (or `"kind"`)
    FIRST, then fall through to the existing bucket logic (same precedence pattern as `hubStyle` duty→volume→default).
    Pre-allocate ONE `Style` per (tier × bucket) cell at module load — never allocate per frame (mutate
    `feature.set(...)`, never `feature.setStyle(new Style(...))`). Per UI-SPEC: center = radius 20 + amber
    `#f59e0b` 3px ring; spoke = radius 12 + white 2px ring; keep the `volumeBucket`/`dutyBucket` fill ramp (tier =
    size+ring, NOT hue). For routes: backbone leg = `#cbd5e1` 4px opacity 0.9; spoke leg = 2px opacity 0.55,
    branching on `feature.get("isBackbone")`. Extend the `HUB_COLORS`/`*_LABELS` single-source-of-truth arrays (or
    add a parallel tier-label array) so `Legend.tsx` can label the two hub tiers ("Regional center" large amber-ring
    / "Spoke hub" small) and the two leg weights ("Backbone" heavy / "Spoke leg" light) — the legend MUST derive
    from the same constants the styles use. Exact color/size scale within the UI-SPEC envelope is at your discretion.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/web typecheck && pnpm --filter @mm/web vitest run test/scale-viz.unit.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Cached styles are pre-allocated at module load (no `new Style(...)` inside a style function — grep -v '^#' packages/web/src/map/coloring.ts | grep -c 'new Style' counts only module-level allocations, not per-call).
    - A unit test asserts the style fn returns the center-tier style for `tier=center` and the spoke-tier style for `tier=spoke`, each preserving its volume-bucket fill; route style returns the heavy backbone style for `isBackbone=true`.
    - Legend renders the two hub tiers + two leg weights, derived from the coloring constants.
  </acceptance_criteria>
  <done>Tier-branched zero-per-frame styles + extended legend; tiers read by size+ring+weight, not hue.</done>
</task>

<task type="auto">
  <name>Task 3: clustered/decluttered hub layer + separate center tier (VIZ-15)</name>
  <read_first>
    - packages/web/src/map/layers.ts:40-57 (createHubLayer — swap target), :66-80 (createRouteLayer), :90-92 (createTrailerLayer — STAYS plain VectorLayer), :1 (VectorLayer import)
    - packages/web/src/map/MapView.tsx:267 (REST topology fetch on map init — where hubs/routes geometry loads once)
    - 27-UI-SPEC.md (VIZ-15 declutter table: cluster distance 40 / minDistance 20; cluster bubble slate-700 disc, white count, radius 14→22 log-bucketed ≤4 cached sizes; centers always individual)
  </read_first>
  <action>
    Rework `createHubLayer` (`layers.ts:40-57`): split hubs into spokes vs centers (by the `kind` DTO field from
    Task 1). Put SPOKES on an `ol/source/Cluster({ distance: 40, minDistance: 20, source: spokeVectorSource })`
    rendered on a `VectorImageLayer({ source: clusterSource, declutter: true })`. Keep CENTERS on their own
    un-clustered tier-1 `VectorLayer` (a center is NEVER absorbed into a spoke cluster — UI-SPEC). Add a
    cluster-bubble `StyleFunction`: when a cluster feature wraps >1 member, render a slate-700 `#334155` disc with
    a white count, radius 14→22 log-bucketed into ≤4 pre-allocated cached sizes (zero per-cluster alloc); when it
    wraps exactly 1, render that member's individual tier+bucket style (from Task 2). Do NOT cluster trailers —
    `createTrailerLayer` (:90-92) stays a plain `VectorLayer` (they animate via postrender; clustering fights the
    in-place tween). Wire the new layers in `MapView.tsx` where the REST topology loads (:267), passing the
    `kind`-tagged hubs and `isBackbone`-tagged routes. Cluster distance is tunable (UI-SPEC note 1).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/web typecheck && pnpm --filter @mm/web build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @mm/web typecheck && build` succeed.
    - Spokes are on a `Cluster` source + `VectorImageLayer({declutter:true})`; centers are on a separate un-clustered layer (grep -v '^#' packages/web/src/map/layers.ts | grep -c 'Cluster' >= 1; trailers remain on `VectorLayer`).
    - The cluster bubble style uses ≤4 pre-allocated cached sizes (no per-cluster `new Style`).
  </acceptance_criteria>
  <done>Spoke field clusters/declutters, centers stay individually visible, trailers uncluttered; map builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → ws client | per-tick payload crosses here; payload size must stay bounded as hub count grows (DoS surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | D (DoS) | per-tick ws payload growing with hub count (client render/parse pressure) | mitigate | Static topology (kind/tier/isBackbone geometry) is sent ONCE via REST on map init; the ws tick carries only id-keyed metric buckets — per-tick bytes are bounded by active trailers/metrics, not hub count. The new DTO fields are REST-only (acceptance criterion in Task 1). |
| T-27-07 | D (DoS) | 100+ feature render allocating per frame | mitigate | Cluster + VectorImageLayer renders the dense field as an image; styles are pre-allocated cached `Style`s (zero per-frame alloc), enforced by Task 2/3 acceptance criteria. |
</threat_model>

<verification>
- `pnpm --filter @mm/api typecheck && test` green (tier DTOs).
- `pnpm --filter @mm/web typecheck && build` green; scale-viz unit test green.
- No golden impact — this plan touches only DTOs + client rendering, never `simulate()`.
</verification>

<success_criteria>
- 100+ hubs render without clutter (spokes clustered/decluttered, centers individual).
- Four distinct visual tiers (centers/spokes/backbone/spoke legs) by size+ring+weight, not hue.
- Static topology REST-once; per-tick payload bounded as hub count grows.
</success_criteria>

<output>
After completion, create `.planning/phases/27-perf-plumbing-scale-viz/27-03-SUMMARY.md`
</output>
