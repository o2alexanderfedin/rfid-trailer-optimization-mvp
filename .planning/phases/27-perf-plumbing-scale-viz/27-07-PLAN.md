---
phase: 27-perf-plumbing-scale-viz
plan: 07
type: execute
wave: 4
depends_on: [27-01, 27-05, 27-04, 27-06, 27-03]
files_modified:
  - packages/api/test/sustained-continental-run.int.test.ts
  - packages/api/src/sim/driver.ts
autonomous: true
requirements: [PERF-04]
must_haves:
  truths:
    - "a sustained continental run at ~80-130 hubs holds a target sim-min/wall-sec without the freeze/stall failure mode"
    - "per-epoch optimizer/twin-snapshot cost does NOT grow with run length (PERF-02 incremental projections + 27-05 plumbing in effect)"
    - "the run is demonstrable live end-to-end (continental topology + OODA + coordinators + optimizer-backed reroute all on)"
  artifacts:
    - path: "packages/api/test/sustained-continental-run.int.test.ts"
      provides: "sustained-run validation: throughput holds + per-epoch cost flat over a long continental run"
      contains: "continental"
  key_links:
    - from: "packages/api/test/sustained-continental-run.int.test.ts"
      to: "driver loop (applyInline incremental fold + buildTwinSnapshot bounded reads + runEpoch)"
      via: "long-run throughput + flat per-epoch-cost assertion"
      pattern: "sim.?min|wall|throughput|elapsed"
---

<objective>
PERF-04: prove a SUSTAINED continental run at ~80–130 hubs holds a target sim-min/wall-sec WITHOUT the
freeze/stall failure mode — the milestone's demo-readiness gate, demonstrable live end-to-end. This is the LAST
wave: it depends on all the perf + plumbing + viz + golden work landing (PERF-02 incremental projections,
PERF-03 bounded plumbing seams, P27-A divergent optimizer reroute, P27-B live reject, VIZ scale rendering).

Today the failure mode is the per-epoch optimizer/twin-snapshot cost growing with run length (the two full-log
scans, now removed by PERF-02) plus unbounded in-flight epochs / ws buffers (now bounded by PERF-03). This plan
validates the fix holds over a long run: throughput stays above a target and per-epoch cost stays FLAT (does not
decay as the event log grows).

Purpose: this is the go/no-go for the live demo. If throughput degrades or per-epoch cost grows with run length,
the sustained run is not achieved and an earlier plan regressed.

Output: a sustained-continental-run integration test asserting bounded throughput + flat per-epoch cost, plus any
minimal driver-loop instrumentation needed to measure it (no behavior change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-perf-plumbing-scale-viz/27-CONTEXT.md
@.planning/phases/27-perf-plumbing-scale-viz/27-PATTERNS.md
@.planning/phases/27-perf-plumbing-scale-viz/27-01-PLAN.md
@.planning/phases/27-perf-plumbing-scale-viz/27-05-PLAN.md

<interfaces>
<!-- Established perf-test pattern — packages/api/test/projection-fold-bounded.int.test.ts:
     TEST 2 (corroborating wall-clock, generous threshold) :32-49; "per-event fold time at LARGE state ≈ SMALL state
     (no O(state) decay)" :124 — the FLAT-per-event-cost assertion style to mirror for per-EPOCH cost over RUN LENGTH -->
<!-- Driver loop — packages/api/src/sim/driver.ts: applyInline per-tick fold :499; broadcast per tick;
     buildTwinSnapshot (bounded reads after 27-01); runEpoch (optimizer). Add measurement only — NO behavior change. -->
<!-- Continental all-on config — coordinator-optimizer-determinism.unit.test.ts COORDINATOR_OPTIMIZER_ON_OPTS shape
     + continentalTopology:true for ~80-130 hubs (the live demo config); continental-determinism.unit.test.ts seed-42/300 -->
<!-- v2.1 freeze precedent: sim-freeze-was-on2-projection-fold (MEMORY) — inline appliers load-all/write-all per event
     was O(events²); PERF-01 key-scoped it; PERF-02 removes the twin-snapshot scans. This test guards the regression. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sustained-continental-run integration test — throughput holds + per-epoch cost flat</name>
  <read_first>
    - packages/api/test/projection-fold-bounded.int.test.ts:32-49 (corroborating wall-clock test pattern, generous threshold), :124 (the "no O(state) decay" flat-cost assertion to adapt for per-epoch-over-run-length)
    - packages/api/src/sim/driver.ts:499 (applyInline per-tick fold), the buildTwinSnapshot + runEpoch call sites in the loop
    - .planning/phases/27-perf-plumbing-scale-viz/27-01-PLAN.md (the PERF-02 bounded reads this validates)
  </read_first>
  <behavior>
    - Drive a continental all-on run (continentalTopology + OODA + coordinators + optimizer-backed reroute, ~80-130 hubs) for a long duration in two windows: an EARLY window (e.g. first N ticks) and a LATE window (e.g. ticks at a much higher run length).
    - The per-epoch optimizer/twin-snapshot cost in the LATE window is within a generous multiple of the EARLY window (FLAT — does NOT grow with run length / event-log size). This is the PERF-02/PERF-04 guarantee: no O(events) decay.
    - Sustained throughput (sim-ticks-or-sim-min processed per wall-second) in the LATE window stays at or above a target fraction of the EARLY window (no stall/freeze).
  </behavior>
  <action>
    Create `packages/api/test/sustained-continental-run.int.test.ts` modeling the corroborating wall-clock pattern in
    `projection-fold-bounded.int.test.ts:32-49,124`. Run the driver loop on a continental all-on config (~80-130 hubs)
    against a real Postgres (testcontainers, like the existing int tests) for a long run. Measure per-epoch cost
    (twin-snapshot + runEpoch wall time, or the bounded read-count) in an EARLY window vs a LATE window and assert the
    LATE window is within a GENEROUS multiple of the EARLY window (flat per-epoch cost — no O(events) decay; this is
    the freeze/stall guard). Also assert sustained throughput (sim-min or sim-ticks per wall-second) in the LATE window
    stays ≥ a target fraction of the EARLY window. Use generous thresholds (CI/host-variance tolerant, like the
    existing TEST 2) — the assertion is "flat, not degrading", not a hard absolute number. If a small, behavior-neutral
    measurement hook is needed in `driver.ts` (e.g. exposing per-epoch elapsed via an injected callback), add it WITHOUT
    changing the loop's behavior or any event output. Target sim-min/wall-sec is at your discretion within the
    "no-stall" envelope; document the chosen thresholds in the SUMMARY.

    NOTE on the external-drive caveat (MEMORY: external-drive-skews-db-test-timeouts): DB-bound int tests can be slow
    from the external drive. If the absolute timing is unreliable here, anchor the assertion on the RELATIVE
    early-vs-late RATIO (flat cost) + read-count invariance rather than absolute wall-clock, so the test is
    drive-agnostic; note the run-location caveat in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/api typecheck && pnpm --filter @mm/api vitest run test/sustained-continental-run.int.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The test runs a continental all-on run (~80-130 hubs) and passes: LATE-window per-epoch cost is within a generous multiple of EARLY-window cost (flat — no O(events) decay).
    - Sustained throughput in the LATE window stays ≥ the target fraction of the EARLY window (no stall/freeze).
    - The assertion is relative (early-vs-late ratio / read-count invariance), drive-agnostic; any driver hook is behavior-neutral (no event output change — a determinism unit test, if re-run, stays byte-identical).
    - `pnpm --filter @mm/api typecheck` green.
  </acceptance_criteria>
  <done>A sustained continental run holds throughput + flat per-epoch cost over a long run; the freeze/stall mode is gone; demonstrable live end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sustained run → server resources | over a long run, per-epoch cost and memory must stay bounded (the freeze/stall DoS-on-self failure mode). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-18 | D (DoS) | per-epoch optimizer/twin-snapshot cost growing with run length (the freeze) | mitigate | PERF-02 removed the two full-log scans (27-01); this test asserts flat per-epoch cost early-vs-late so a regression that reintroduces O(events) cost fails CI. |
| T-27-19 | D (DoS) | unbounded memory over a sustained run (in-flight epochs / ws buffers / projection growth) | mitigate | PERF-03 bounded the three plumbing seams (27-05); PERF-02 projections are entity-count-bounded (27-01); the sustained run is the integration witness that they hold together. |
</threat_model>

<verification>
- `pnpm --filter @mm/api typecheck` green.
- `pnpm --filter @mm/api vitest run test/sustained-continental-run.int.test.ts` green (flat per-epoch cost + sustained throughput).
- All upstream gates still green: flags-off `3920accc`, rule-based `edfa5a6d`, OODA-on `94689f99` byte-identical; new optimizer-on golden (27-04) stable; PERF-02 rebuild-equivalence + cost-invariance (27-01) green.
- No golden impact — any driver instrumentation is behavior-neutral.
</verification>

<success_criteria>
- A sustained continental run at ~80–130 hubs holds a target sim-min/wall-sec without the freeze/stall failure mode (PERF-04).
- Per-epoch cost is flat over run length (PERF-02/03 hold together); throughput does not degrade.
- Demonstrable live end-to-end with continental + OODA + coordinators + optimizer-backed reroute all on.
</success_criteria>

<output>
After completion, create `.planning/phases/27-perf-plumbing-scale-viz/27-07-SUMMARY.md`
</output>
