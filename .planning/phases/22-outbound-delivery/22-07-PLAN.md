---
phase: 22-outbound-delivery
plan: "07"
type: execute
wave: 5
depends_on:
  - "22-06"
files_modified:
  - packages/web/src/map/MapView.tsx
autonomous: false
requirements:
  - VIZ-14
must_haves:
  truths:
    - "MapView.tsx constructs a createDeliveryLayer() at layer-init time alongside the existing inductionLayer"
    - "MapView.tsx has a useEffect hook (or equivalent WS envelope handler) that reads deliveryEvents from each TickPayload and calls flashDelivery() for each event, looking up lon/lat from the existing hub registry"
    - "No deliveryEvents processing on SnapshotPayload (the Pitfall-7 guard holds end-to-end)"
    - "The delivery layer is added to the OL map instance and removed/cleaned up on unmount (mirrors induction layer lifecycle)"
    - "pnpm --filter @mm/web build passes with zero TS errors"
    - "A human observer watching the live map with outboundDeliveryEnabled: true sees destination hubs highlight (distinct color, not purple, not cyan) when PackageDelivered fires"
  artifacts:
    - path: packages/web/src/map/MapView.tsx
      provides: "createDeliveryLayer() construction at ~L163 + deliveryEvents WS handler at ~L388"
      contains: "createDeliveryLayer"
  key_links:
    - from: packages/web/src/map/MapView.tsx
      to: packages/web/src/map/layers.ts (createDeliveryLayer, flashDelivery)
      via: import and call in useEffect / map init
      pattern: "flashDelivery"
    - from: packages/web/src/map/MapView.tsx
      to: WS TickPayload.deliveryEvents
      via: envelope handler switch/if checking payload type === "tick" and reading deliveryEvents
      pattern: "deliveryEvents"
---

<objective>
Wire the VIZ-14 delivery layer end-to-end into `MapView.tsx`: construct `createDeliveryLayer()`
alongside the induction layer at map initialization (~L163), add it to the OL map, and add a
WS envelope handler that reads `deliveryEvents` from each `TickPayload` and calls
`flashDelivery(source, hubId, lon, lat)` for each event — looking up hub coordinates from the
existing hub geometry registry already used by the induction handler. End with a
human-verify checkpoint confirming the color is visually distinct from induction purple and
consolidation cyan on the live map.

Purpose: Completes the VIZ-14 end-to-end wiring so the human checkpoint can pass.
Without this plan, `flashDelivery()` exists in `layers.ts` but is never called; the delivery
layer exists but is never added to the map. The checkpoint lives here (not in Plan 06) because
the visual gate requires MapView.tsx to be wired first.
Output: Destination hubs briefly highlight (DELIVERY_COLOR #16a34a) when PackageDelivered fires,
visible on the live map. Human confirms visual correctness. pnpm build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-outbound-delivery/22-CONTEXT.md
@.planning/phases/22-outbound-delivery/22-RESEARCH.md
@.planning/phases/22-outbound-delivery/22-PATTERNS.md
@.planning/phases/22-outbound-delivery/22-CODE-ANCHORS.md
@.planning/phases/22-outbound-delivery/22-VALIDATION.md
@.planning/phases/22-outbound-delivery/22-06-SUMMARY.md

<interfaces>
<!-- Key sections in MapView.tsx to read before editing. All line numbers are from 22-CODE-ANCHORS.md §10. -->

packages/web/src/map/MapView.tsx:
  - ~L163: layer construction block where createInductionLayer() is called and stored in a ref — add createDeliveryLayer() immediately after, following the same ref pattern.
  - ~L388: WS envelope handler (useEffect or message handler switch) where inductionEvents is extracted from a TickPayload and flashInduction() is called for each event — add a parallel deliveryEvents handler here.
  - Read the full useEffect/WS message handler to understand: (a) how the envelope type is discriminated (tick vs snapshot), (b) how hub lon/lat is looked up for inductionHubId (extract the same registry for hubId on delivery events), (c) how the induction layer source ref is passed to flashInduction() — replicate for deliveryLayer.

packages/web/src/map/layers.ts (output of Plan 06):
  - createDeliveryLayer(): Layer — mirrors createInductionLayer; returns { layer, source }.
  - flashDelivery(source, deliveryHubId, lon, lat, durationMs?): void — mirrors flashInduction.
  Read the exact function signatures as implemented in Plan 06 before calling them.

packages/api/src/ws/envelope.ts:
  - DeliveryEvent interface (added in Plan 05): { packageId, hubId, deliveredAt, onTime }
  - TickPayload.deliveryEvents?: readonly DeliveryEvent[]
  Import DeliveryEvent type if MapView.tsx needs it for type annotation.

packages/web/src/map/inductionLayer.test.ts — read to understand the hub-lookup pattern and how flashInduction is invoked from MapView.tsx (serves as a cross-reference for how to write the flashDelivery call site).

ANALOG: the inductionEvents handler block inside MapView.tsx ~L388 is the EXACT template for the deliveryEvents handler. Identify the hub-lon/lat lookup mechanism used for inductionEvents (likely a hubsRef or hubsById map built from the SnapshotPayload.hubs array) and reuse it for deliveryEvents.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MapView.tsx — createDeliveryLayer construction + deliveryEvents WS handler</name>
  <files>packages/web/src/map/MapView.tsx</files>
  <read_first>
    packages/web/src/map/MapView.tsx (full file — read every layer construction site, the WS message handler useEffect, how inductionLayer source ref is stored and passed to flashInduction, how hub lon/lat is resolved, and the cleanup return in the useEffect);
    packages/web/src/map/layers.ts (read createDeliveryLayer and flashDelivery exact signatures as implemented in Plan 06);
    packages/api/src/ws/envelope.ts (read DeliveryEvent interface and TickPayload.deliveryEvents field as added in Plan 05);
    packages/web/src/map/inductionLayer.test.ts (cross-check how flashInduction is expected to be called from outside the layer file).
    ANALOG: the inductionLayer construction (~L163) and inductionEvents handler (~L388) inside MapView.tsx — this is the verbatim template for the delivery layer wiring. The only substitutions are: createInductionLayer → createDeliveryLayer, inductionLayer source ref → deliveryLayer source ref, inductionEvents → deliveryEvents, inductionHubId → hubId, flashInduction → flashDelivery.
  </read_first>
  <behavior>
    After this task, the following behaviors must hold:
    - MapView.tsx builds with zero TypeScript errors
    - The delivery layer ref is initialized (createDeliveryLayer()) and the layer is added to the OL map instance at the same lifecycle stage as the induction layer
    - When a TickPayload arrives over WS with deliveryEvents, flashDelivery() is called once per event
    - When a SnapshotPayload arrives, NO deliveryEvents processing occurs (the deliveryEvents field is not on SnapshotPayload — this is the Pitfall-7 structural guarantee; just do not access it on the snapshot path)
    - On component unmount, the delivery layer is removed from the map (matches induction layer cleanup pattern)
  </behavior>
  <action>
    Read MapView.tsx in full first. Then apply two additions:

    ADDITION 1 — Layer construction (~L163 region, alongside createInductionLayer):
    - Declare a deliveryLayerRef (useRef) for the VectorSource of the delivery layer — the same ref pattern used for the induction layer source.
    - Call createDeliveryLayer() at the same point where createInductionLayer() is called. Store the returned source in deliveryLayerRef.current (or use the same initialization pattern as inductionLayer).
    - Add the returned layer to the OL map instance via map.addLayer() at the same point where the induction layer is added.
    - In the cleanup / unmount return, remove the delivery layer from the map via map.removeLayer() following the same pattern as the induction layer cleanup.

    ADDITION 2 — WS envelope handler (~L388 region, inside the TickPayload branch):
    - In the WS message handler where the envelope type is discriminated (tick vs snapshot), in the tick branch where inductionEvents is processed, add a parallel block for deliveryEvents:
      `if (payload.deliveryEvents !== undefined && deliveryLayerRef.current !== null) { for (const ev of payload.deliveryEvents) { const hub = hubsById.get(ev.hubId); if (hub !== undefined) { flashDelivery(deliveryLayerRef.current, ev.hubId, hub.lon, hub.lat); } } }`
    - The hubsById lookup (or equivalent hub geometry registry) is already present for the induction handler — use the same reference. Executor: read what hub data structure is used in the induction handler (could be hubsRef.current, a Map built from the snapshot hubs array, or similar) and replicate the lookup for ev.hubId.
    - Do NOT access deliveryEvents on the snapshot branch — SnapshotPayload does not have this field structurally (Pitfall-7 guard is type-level, not runtime).
    - Import flashDelivery and createDeliveryLayer from ./layers.js (add to existing import from that file).
    - Import DeliveryEvent type from the api/ws/envelope.ts if needed for the type annotation of payload.deliveryEvents.

    MUST NOT modify the induction layer logic, comparator, or any existing WS handler. MUST NOT call flashDelivery on the snapshot path.
  </action>
  <verify>
    <automated>pnpm --filter @mm/web build 2>&1 | tail -20</automated>
  </verify>
  <done>
    packages/web builds with zero TS errors. MapView.tsx has createDeliveryLayer() construction and deliveryLayer added to the OL map. The WS tick handler calls flashDelivery() per deliveryEvent. The snapshot handler does not process deliveryEvents. Delivery layer cleanup (removeLayer) is present in the useEffect return. No TypeScript errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    VIZ-14 destination-hub delivery highlight end-to-end: createDeliveryLayer/flashDelivery wired
    into MapView.tsx in green #16a34a, distinct from induction purple #7c3aed and consolidation cyan.
    DeliveryKpi operator panel widget (from Plan 06) showing delivered-out count + on-time %
    from GET /api/delivery-kpi. This checkpoint runs AFTER MapView.tsx is wired (Task 1 above)
    so the delivery layer is actually live on the map.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev` (or `pnpm --filter @mm/api dev` + `pnpm --filter @mm/web dev`)
    2. Open the live map in a browser
    3. Enable outbound delivery: ensure the simulation is running with `outboundDeliveryEnabled: true` and `inductionEnabled: true` (check how to toggle in the operator panel or start script)
    4. Watch destination spoke hubs — within 1-20 tick-minutes of PackageArrivedAtHub events, destination hubs should flash with a GREEN circle marker (DELIVERY_COLOR #16a34a, confirmed green not purple not cyan)
    5. Confirm the delivery flash is visually distinct from:
       a. Induction events (purple #7c3aed circle at spoke hubs when PackageInducted fires)
       b. Consolidation trailer styling (cyan direction indicator)
    6. Open the operator panel / right rail — confirm the DeliveryKpi widget is visible showing "X delivered (Y% on time)"
    7. Watch for 2-3 delivery cycles — confirm the delivered count increments and on-time% updates
    8. Disconnect and reconnect to the WS (close and reopen the browser tab) — confirm NO historical delivery flashes re-fire on reconnect (Pitfall-7 visual check)
  </how-to-verify>
  <resume-signal>
    Type "approved" if the delivery highlight is visible, green/distinct, and the KPI widget shows live counters. Describe any issues otherwise (wrong color, no flash, KPI not updating, reconnect re-fires).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS tick → MapView.tsx | deliveryEvents consumed client-side; same trust level as inductionEvents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | Tampering | MapView.tsx deliveryEvents handler | accept | No new threats: simulation event + projection/UI extension; no user-facing auth, no external input, no secrets; PackageDelivered validated at the existing validateEvent() Zod .strict() ingestion boundary; deliveryEvents is a read-only transient tick field (never persisted). Same pattern as flashInduction (Phase 20). |
</threat_model>

<verification>
After all tasks (including checkpoint):
1. `pnpm --filter @mm/web build` — zero TS errors (MapView.tsx compiles with new layer + handler)
2. `pnpm --filter @mm/web test -- --run` — all existing web tests still pass (no regression in inductionLayer or other layer tests)
3. Human checkpoint: destination hubs flash green on PackageDelivered; color distinct from induction purple + consolidation cyan; KPI widget live; reconnect does not re-flash
</verification>

<success_criteria>
- MapView.tsx constructs createDeliveryLayer() and adds it to the OL map alongside the induction layer
- WS TickPayload.deliveryEvents handler calls flashDelivery(source, hubId, lon, lat) per event
- SnapshotPayload path does NOT access deliveryEvents
- Delivery layer is cleaned up on unmount
- pnpm --filter @mm/web build: zero errors
- End-to-end VIZ-14: destination hubs flash DELIVERY_COLOR (#16a34a) on PackageDelivered WS events
- Human confirms visual correctness: color distinct, flash visible, KPI updates, no reconnect re-flash
</success_criteria>

<output>
After completion, create `.planning/phases/22-outbound-delivery/22-07-SUMMARY.md`
</output>
