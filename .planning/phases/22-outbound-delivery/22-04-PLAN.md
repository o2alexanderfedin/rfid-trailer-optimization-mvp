---
phase: 22-outbound-delivery
plan: "04"
type: execute
wave: 2
depends_on:
  - "22-01"
  - "22-02"
files_modified:
  - packages/projections/src/reducers/package-location.ts
  - packages/projections/src/reducers/hub-inventory.ts
  - packages/projections/src/reducers/zone-estimate.ts
  - packages/projections/src/reducers/delivery-kpi.ts
  - packages/projections/src/reducers/index.ts
autonomous: true
requirements:
  - OUT-04
  - OUT-05
must_haves:
  truths:
    - "Applying PackageDelivered to packageLocationReducer removes the package row (Map.delete)"
    - "Applying PackageDelivered to hubInventoryReducer removes the package from placement (placePackage null)"
    - "Both purge reducers are no-ops on missing package IDs (never throw — D-22-1)"
    - "zoneEstimateReducer handles PackageDelivered as a no-op (exhaustive switch passes)"
    - "deliveryKpiReducer increments deliveredCount and (when onTime) onTimeCount on each PackageDelivered event"
    - "All four reducers compile with the assertNeverEvent default branch satisfied"
    - "hub-inventory.test.ts PackageDelivered purge + no-op tests GREEN"
  artifacts:
    - path: packages/projections/src/reducers/package-location.ts
      provides: PackageDelivered case => Map.delete idempotent purge
      contains: '"PackageDelivered"'
    - path: packages/projections/src/reducers/hub-inventory.ts
      provides: PackageDelivered case => placePackage(state, packageId, null) idempotent purge
      contains: '"PackageDelivered"'
    - path: packages/projections/src/reducers/zone-estimate.ts
      provides: PackageDelivered case => no-op (return state)
      contains: '"PackageDelivered"'
    - path: packages/projections/src/reducers/delivery-kpi.ts
      provides: new event-derived KPI reducer (deliveredCount + onTimeCount, D-22-3)
      min_lines: 40
  key_links:
    - from: packages/projections/src/reducers/hub-inventory.ts
      to: placePackage() helper (same file)
      via: placePackage(state, event.payload.packageId, null)
      pattern: "placePackage.*null"
    - from: packages/projections/src/reducers/delivery-kpi.ts
      to: packages/domain/src/events/index.ts
      via: PackageDelivered type import
      pattern: "PackageDelivered"
---

<objective>
Add `PackageDelivered` cases to all three existing projection reducers
(`packageLocation`, `hubInventory`, `zoneEstimate`) to implement OUT-04 hard-DELETE
purge and complete the `assertNeverEvent` exhaustive switch. Create the new
`delivery-kpi.ts` pure reducer (OUT-05 D-22-3) that accumulates event-derived
`deliveredCount` + `onTimeCount` counters. Turn the `hub-inventory.test.ts`
PackageDelivered tests GREEN.

Purpose: Bounded-memory proof — delivered package rows are truly deleted from all
projection tables. The KPI reducer provides a permanent event-derived aggregate that
survives the purge (D-22-3).
Output: Four reducers compile; projection build clean; purge + no-op tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-outbound-delivery/22-CONTEXT.md
@.planning/phases/22-outbound-delivery/22-RESEARCH.md
@.planning/phases/22-outbound-delivery/22-PATTERNS.md
@.planning/phases/22-outbound-delivery/22-VALIDATION.md
@.planning/phases/22-outbound-delivery/22-01-SUMMARY.md
@.planning/phases/22-outbound-delivery/22-02-SUMMARY.md

<interfaces>
<!-- Key sections verified in 22-RESEARCH.md and 22-PATTERNS.md. -->

packages/projections/src/reducers/package-location.ts — read full file. Key: upsert pattern at lines 67-75 (copy-and-mutate `new Map(state)` + `.set()`); no-op fall-through at lines 96-117; `default: return assertNeverEvent(event)` at line 119. PackageDelivered DELETE: `new Map(state); next.delete(event.payload.packageId); return next`.

packages/projections/src/reducers/hub-inventory.ts — read full file. Key: `placePackage(state, packageId, null)` idiom at lines 192-196 (TrailerDeparted DELETE pattern); `if (prior !== undefined)` guard inside placePackage at lines 103-132 (no-op on missing key — D-22-1 safe); `default: return assertNeverEvent(event)` at line 244.

packages/projections/src/reducers/zone-estimate.ts — read full file. Key: no-op fall-through for PackageInducted + PlanSuperseded at lines 179-204; `default: return assertNeverEvent(event)` pattern.

packages/projections/src/reducers/hub-inventory.ts (test) — read the test stubs added by Plan 02 (PackageDelivered purge + no-op test cases).

packages/projections/src/reducers/index.ts — read to understand how to export the new delivery-kpi reducer.

packages/projections/src/reducers/reducer.ts — read to understand OccurredEvent type and assertNeverEvent import pattern used in all reducers.

ANALOG for delivery-kpi.ts: hub-inventory.ts (role match — pure reducer shape per 22-PATTERNS.md § delivery-kpi.ts section). The delivery-kpi reducer is the first pure-counter event-derived reducer in this package.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Purge cases in package-location, hub-inventory, zone-estimate reducers + turn hub-inventory tests GREEN</name>
  <files>
    packages/projections/src/reducers/package-location.ts,
    packages/projections/src/reducers/hub-inventory.ts,
    packages/projections/src/reducers/zone-estimate.ts
  </files>
  <read_first>
    packages/projections/src/reducers/package-location.ts (full file);
    packages/projections/src/reducers/hub-inventory.ts (full file);
    packages/projections/src/reducers/zone-estimate.ts (full file);
    packages/projections/src/reducers/hub-inventory.test.ts (read the PackageDelivered test stubs added in Plan 02 to understand what the tests expect);
    packages/projections/src/reducers/reducer.ts (OccurredEvent type, assertNeverEvent function).
    ANALOG: hub-inventory.ts `case "TrailerDeparted": return placePackage(state, packageIds.reduce(...))` at lines 192-196 for the null-target DELETE pattern (22-PATTERNS.md § hub-inventory.ts section).
    ANALOG: package-location.ts copy-and-mutate at lines 67-75 for the MAP.DELETE pattern (22-PATTERNS.md § package-location.ts section).
    ANALOG: zone-estimate.ts no-op list at lines 179-204 for the no-op addition pattern (22-PATTERNS.md § zone-estimate.ts section).
  </read_first>
  <behavior>
    Tests that must go GREEN (from hub-inventory.test.ts Plan-02 stubs):
    - "PackageDelivered purges the package from hub inventory" — apply PackageDelivered event, packageId absent from state.placement
    - "PackageDelivered is a no-op when package is absent (idempotent)" — apply PackageDelivered for absent packageId, state unchanged, no throw
  </behavior>
  <action>
    package-location.ts: Insert a `case "PackageDelivered":` block BEFORE the long no-op fall-through. Implementation per 22-PATTERNS.md: `const next = new Map(state); next.delete(event.payload.packageId); return next;` — `Map.delete()` returns false on a missing key and never throws, so this is naturally idempotent (D-22-1). Add comment: `// OUT-04 / D-22-1: hard DELETE — Map.delete() is a no-op on missing key (crash-safe on re-apply)`.

    hub-inventory.ts: Insert a `case "PackageDelivered":` immediately before the no-op fall-through (before the existing `PackageInducted`/`PlanSuperseded` cases). Implementation per 22-PATTERNS.md: `return placePackage(state, event.payload.packageId, null);` — `placePackage(..., null)` with null target exits early if `prior === undefined` (the guard at line 115 in hub-inventory.ts), so this is naturally idempotent (D-22-1). Add comment: `// OUT-04 / D-22-1: placePackage(..., null) is idempotent — exits early if package absent (prior === undefined guard).`

    zone-estimate.ts: Add `case "PackageDelivered":` alongside `PackageInducted` and `PlanSuperseded` in the no-op fall-through. Implementation per 22-PATTERNS.md: share the `return state;` with the existing no-op cases. Add comment: `// Phase-22 OUT-04: zone estimates are RFID-only; Phase-21 is_active filter already excludes delivered packages from detection scope. No-op (no RFID data in PackageDelivered payload).`

    MUST NOT change any existing non-PackageDelivered reducer logic. MUST NOT use a read-modify-write pattern that assumes the row exists (never call .get() and dereference without null check). Each file's `default: return assertNeverEvent(event)` is the compile gate — the new case must compile without TS error.
  </action>
  <verify>
    <automated>pnpm --filter @mm/projections build && pnpm --filter @mm/projections test -- --run packages/projections/src/reducers/hub-inventory.test.ts</automated>
  </verify>
  <done>
    Projections package builds with zero TS errors. hub-inventory.test.ts: PackageDelivered purge test GREEN (package absent after delete); PackageDelivered no-op test GREEN (unknown packageId does not throw). All pre-existing hub-inventory tests still green. package-location.ts and zone-estimate.ts compile with new case present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: delivery-kpi.ts new reducer (D-22-3) + export + test</name>
  <files>
    packages/projections/src/reducers/delivery-kpi.ts,
    packages/projections/src/reducers/index.ts
  </files>
  <read_first>
    packages/projections/src/reducers/hub-inventory.ts (full file — role-match analog: pure reducer shape per 22-PATTERNS.md § delivery-kpi.ts section);
    packages/projections/src/reducers/reducer.ts (OccurredEvent, assertNeverEvent — imports needed in delivery-kpi.ts);
    packages/projections/src/reducers/index.ts (understand existing exports to add delivery-kpi exports correctly).
    ANALOG: hub-inventory.ts (pure reducer structure: state type, empty initial, reducer function with switch + default: assertNeverEvent per 22-PATTERNS.md).
  </read_first>
  <behavior>
    deliveryKpiReducer must pass these tests (write tests inline or in a new delivery-kpi.test.ts file):
    - Initial state: `emptyDeliveryKpiState` = `{ deliveredCount: 0, onTimeCount: 0 }`
    - Apply PackageDelivered with onTime: true => deliveredCount+1, onTimeCount+1
    - Apply PackageDelivered with onTime: false => deliveredCount+1, onTimeCount unchanged
    - Any other event type => state unchanged (return state, not undefined)
    - Two consecutive PackageDelivered events => deliveredCount: 2, onTimeCount accumulates correctly
  </behavior>
  <action>
    delivery-kpi.ts (NEW FILE per 22-PATTERNS.md § delivery-kpi.ts section):

    1. `DeliveryKpiState` interface: `{ readonly deliveredCount: number; readonly onTimeCount: number; }` — exported.

    2. `emptyDeliveryKpiState: DeliveryKpiState = { deliveredCount: 0, onTimeCount: 0 }` — exported.

    3. `deliveryKpiReducer(state: DeliveryKpiState, { event }: OccurredEvent): DeliveryKpiState` — exported. Switch on `event.type`:
       - `case "PackageDelivered":` return `{ deliveredCount: state.deliveredCount + 1, onTimeCount: state.onTimeCount + (event.payload.onTime ? 1 : 0) }` (D-22-3: event-derived, NOT COUNT(*) over purged tables).
       - ALL OTHER event type cases: `return state;` — must enumerate every other DomainEvent type in the switch (or use fall-through groups) to satisfy `default: return assertNeverEvent(event)` exhaustiveness. Follow the hub-inventory.ts pattern for handling the full DomainEvent union.

    NOTE: The delivery-kpi reducer handles ALL DomainEvent types because `OccurredEvent` wraps `DomainEvent` and the `default: assertNeverEvent` branch enforces exhaustiveness at compile time. All non-PackageDelivered events return state unchanged. Per D-22-3: the reducer is the sole source of truth for delivered counts; a `COUNT(*)` query over `packageLocation` would undercount (rows deleted by purge).

    Create a test file `delivery-kpi.test.ts` (or embed tests inline) covering the 5 behaviors above. Use the existing Vitest pattern from hub-inventory.test.ts.

    index.ts: Export `DeliveryKpiState`, `emptyDeliveryKpiState`, `deliveryKpiReducer` from `./delivery-kpi.js`.
  </action>
  <verify>
    <automated>pnpm --filter @mm/projections build && pnpm --filter @mm/projections test -- --run delivery-kpi</automated>
  </verify>
  <done>
    delivery-kpi.ts compiles clean. delivery-kpi tests: 5 behaviors all GREEN. `deliveryKpiReducer` increments deliveredCount on each PackageDelivered; increments onTimeCount only when onTime: true; all other events return state unchanged. Exported correctly from index.ts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Event stream → Projection reducers | PackageDelivered events fold into read models; pure functions, no side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | Projection reducer DELETE | accept | No new threats: simulation event + projection extension; DELETE keyed by packageId from event payload; payload validated at existing validateEvent() Zod .strict() ingestion boundary before reducers run. No user-facing auth, no external input, no secrets. |
| T-22-06 | Information Disclosure | delivery-kpi reducer | accept | KPI counters (deliveredCount, onTimeCount) are aggregate non-PII simulation metrics; no sensitive data exposed. |
</threat_model>

<verification>
After both tasks:
1. `pnpm --filter @mm/projections build` — zero TS errors (all four reducers satisfy assertNeverEvent exhaustiveness)
2. `pnpm --filter @mm/projections test -- --run packages/projections/src/reducers/hub-inventory.test.ts` — all tests green including PackageDelivered purge + no-op
3. `pnpm --filter @mm/projections test -- --run delivery-kpi` — 5 delivery-kpi tests green
4. `pnpm --filter @mm/projections test --run` — all projections tests green (no regressions)
</verification>

<success_criteria>
- All four reducers compile with `case "PackageDelivered":` satisfying `assertNeverEvent` exhaustiveness
- packageLocationReducer: Map.delete() on PackageDelivered; no-op on missing key (never throws)
- hubInventoryReducer: placePackage(state, id, null) on PackageDelivered; prior===undefined guard makes it crash-safe (D-22-1)
- zoneEstimateReducer: return state (no-op) on PackageDelivered
- deliveryKpiReducer: deliveredCount+1 every PackageDelivered; onTimeCount+1 only when onTime: true (D-22-3, event-derived not row-count)
</success_criteria>

<output>
After completion, create `.planning/phases/22-outbound-delivery/22-04-SUMMARY.md`
</output>
