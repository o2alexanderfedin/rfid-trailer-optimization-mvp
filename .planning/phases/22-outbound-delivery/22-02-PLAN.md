---
phase: 22-outbound-delivery
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/simulation/test/outbound-determinism.unit.test.ts
  - packages/api/test/ws-delivery.unit.test.ts
  - packages/web/src/map/deliveryLayer.test.ts
  - packages/projections/src/reducers/hub-inventory.test.ts
autonomous: true
requirements:
  - OUT-02
  - OUT-03
  - OUT-04
  - VIZ-14
must_haves:
  truths:
    - "Wave-0 RED test stubs exist for all four file gaps in 22-VALIDATION.md"
    - "outbound-determinism.unit.test.ts exists with failing tests covering flag-off, terminal-completeness, lifecycle-ordering, onTime, whole-minute ISO"
    - "ws-delivery.unit.test.ts exists with failing Pitfall-7 guard test"
    - "deliveryLayer.test.ts exists with failing flashDelivery self-removal test"
    - "hub-inventory.test.ts gains PackageDelivered no-op-on-missing-row test cases (failing until Plan 04)"
  artifacts:
    - path: packages/simulation/test/outbound-determinism.unit.test.ts
      provides: RED stub tests for OUT-02/OUT-03
      min_lines: 80
    - path: packages/api/test/ws-delivery.unit.test.ts
      provides: RED stub for Pitfall-7 (deliveryEvents absent from snapshot)
      min_lines: 50
    - path: packages/web/src/map/deliveryLayer.test.ts
      provides: RED stub for flashDelivery feature + self-removal
      min_lines: 30
  key_links:
    - from: packages/simulation/test/outbound-determinism.unit.test.ts
      to: packages/simulation/src/engine.ts
      via: simulate() call with outboundDeliveryEnabled options
      pattern: "outboundDeliveryEnabled"
    - from: packages/api/test/ws-delivery.unit.test.ts
      to: packages/api/src/ws/envelope.ts
      via: TickPayload.deliveryEvents + SnapshotPayload absence
      pattern: "deliveryEvents"
---

<objective>
Write all Wave-0 RED failing test stubs required by 22-VALIDATION.md before any
implementation begins. These are: `outbound-determinism.unit.test.ts` (OUT-02/03),
`ws-delivery.unit.test.ts` (VIZ-14 Pitfall-7), `deliveryLayer.test.ts` (VIZ-14 map),
and `PackageDelivered` no-op-on-missing-row cases in `hub-inventory.test.ts` (OUT-04).

Purpose: Establish the failing test harness (RED state) before implementation so every
Wave-2/3 plan flips tests GREEN — the TDD mandate from CLAUDE.md.
Output: Four test files (3 new, 1 extended) all in RED state; domain/engine not yet
implemented so failures are expected import/runtime failures, not wrong assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-outbound-delivery/22-CONTEXT.md
@.planning/phases/22-outbound-delivery/22-RESEARCH.md
@.planning/phases/22-outbound-delivery/22-PATTERNS.md
@.planning/phases/22-outbound-delivery/22-VALIDATION.md

<interfaces>
<!-- Analog files the executor must read before writing stubs. -->

packages/simulation/test/induction-determinism.unit.test.ts — verbatim copy target for outbound-determinism.unit.test.ts. Read the full file. Particular attention to: SEED/TICKS constants, SHORT_TIMING definition, `types()` helper, "flag ABSENT => zero events" test, "flag: false => byte-identical" test, lifecycle and terminal-completeness tests.

packages/api/test/ws-induction.unit.test.ts — verbatim copy target for ws-delivery.unit.test.ts. Read the full file. Particular attention to: FakeSocket, makeFakeApp, EMPTY_SNAPSHOT, Pitfall-7 test at lines 123-142 ("never places inductionEvents on the initial snapshot payload").

packages/web/src/map/inductionLayer.test.ts — verbatim copy target for deliveryLayer.test.ts. Read the full file. Particular attention to: vi.useFakeTimers(), feature add/remove pattern, setTimeout expiry simulation.

packages/projections/src/reducers/hub-inventory.test.ts — read to understand existing test structure before adding PackageDelivered no-op test cases.

packages/simulation/test/determinism.unit.test.ts — read DET-01 section (lines 190-220) to understand pattern for flag-off byte-identity tests. Phase 22 adds an `outboundDeliveryEnabled: false` case to this file's DET-01 block (in Plan 03 after engine implemented; do NOT touch determinism.unit.test.ts here).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: outbound-determinism.unit.test.ts RED stub</name>
  <files>packages/simulation/test/outbound-determinism.unit.test.ts</files>
  <read_first>
    packages/simulation/test/induction-determinism.unit.test.ts (full file — verbatim copy target per 22-PATTERNS.md);
    packages/simulation/test/determinism.unit.test.ts (lines 125-149 — golden SHA constant, understand existing DET-01 tests to mirror the flag-off test style).
    ANALOG: packages/simulation/test/induction-determinism.unit.test.ts (exact copy-and-adapt per 22-PATTERNS.md § outbound-determinism.unit.test.ts section).
  </read_first>
  <behavior>
    Tests to write (all initially RED because engine not yet implemented):
    - Test 1: "outboundDeliveryEnabled ABSENT => ZERO PackageDelivered events (DET-01)" — simulate() without flag produces no PackageDelivered events
    - Test 2: "outboundDeliveryEnabled: false => byte-identical to absent" — JSON.stringify of both runs equal
    - Test 3: "outboundDeliveryEnabled: true => at least one PackageDelivered fires within 500 ticks with induction" — use inductionEnabled: true also (inducted packages have deadlines; center-origin packages also deliver)
    - Test 4: "lifecycle-ordering: PackageDelivered always follows PackageArrivedAtHub for same packageId" — for every PackageDelivered event, there is a preceding PackageArrivedAtHub with same packageId in the stream
    - Test 5: "terminal-completeness: every package eventually reaches PackageDelivered within horizon (flag on)" — all packageIds that appear in PackageArrivedAtHub also appear in PackageDelivered
    - Test 6: "onTime flag is boolean on every PackageDelivered payload" — all onTime values are true or false, none are undefined
    - Test 7: "deliveredAt is whole-minute ISO (no sub-minute residue)" — deliveredAt string matches /T\d{2}:\d{2}:00\.000Z$/
  </behavior>
  <action>
    Create packages/simulation/test/outbound-determinism.unit.test.ts by adapting induction-determinism.unit.test.ts. Use `TICKS = 500` (gate-hygiene: durationTicks <= 800 per binding_invariants). Use SHORT_TIMING with same values as the induction test. Import `simulate` from `../src/engine.js` and type helpers from `@mm/domain`. Helper `types(s)` returns event type strings. Tests 1-2 use `outboundDeliveryEnabled` absent/false. Tests 3-7 use `{ outboundDeliveryEnabled: true, inductionEnabled: true, timing: SHORT_TIMING, durationTicks: TICKS }`. For terminal-completeness (Test 5): collect all packageIds that appear in any PackageArrivedAtHub, then assert every such id also appears in PackageDelivered. For lifecycle-ordering (Test 4): for each PackageDelivered event, find a PackageArrivedAtHub with matching packageId that occurred at an earlier tick (occurredAt < deliveredAt lexicographically, or use the stream index ordering). NOTE: Tests 3-7 will fail at import or runtime until Plan 03 implements the engine; the stubs must still be syntactically valid TS that compiles and then fails at the assertion level.
  </action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- --run packages/simulation/test/outbound-determinism.unit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    File exists, is syntactically valid TS, compiles without type errors. Tests 1-2 may pass (engine already returns no PackageDelivered events by default). Tests 3-7 fail with assertion errors (not syntax/import errors) indicating RED state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ws-delivery.unit.test.ts RED stub + deliveryLayer.test.ts RED stub + hub-inventory.test.ts extensions</name>
  <files>
    packages/api/test/ws-delivery.unit.test.ts,
    packages/web/src/map/deliveryLayer.test.ts,
    packages/projections/src/reducers/hub-inventory.test.ts
  </files>
  <read_first>
    packages/api/test/ws-induction.unit.test.ts (full file — verbatim copy target for ws-delivery.unit.test.ts; critical: FakeSocket interface, makeFakeApp, EMPTY_SNAPSHOT, Pitfall-7 test at end);
    packages/web/src/map/inductionLayer.test.ts (full file — copy target for deliveryLayer.test.ts; critical: vi.useFakeTimers() pattern, feature add/remove, advanceTimersByTime);
    packages/projections/src/reducers/hub-inventory.test.ts (full file — understand existing test structure to add new cases).
    ANALOG for ws-delivery: packages/api/test/ws-induction.unit.test.ts (exact copy-and-adapt per 22-PATTERNS.md § ws-delivery section).
    ANALOG for deliveryLayer: packages/web/src/map/inductionLayer.test.ts (exact copy-and-adapt per 22-PATTERNS.md § layers.ts section).
  </read_first>
  <behavior>
    ws-delivery.unit.test.ts tests (RED until Plan 05 implements API wiring):
    - Test 1: "includes deliveryEvents in TickPayload when delivery events present" — broadcast with deliveryEvents array produces tick with deliveryEvents field
    - Test 2: "omits deliveryEvents from TickPayload when none" — no deliveryEvents field when empty/undefined
    - Test 3 (Pitfall-7): "never places deliveryEvents on the initial snapshot payload" — initial snapshot envelope must NOT have deliveryEvents key

    deliveryLayer.test.ts tests (RED until Plan 06 implements the layer):
    - Test 1: "flashDelivery adds a Point feature to the source" — source has one feature immediately after call
    - Test 2: "flashDelivery self-removes the feature after durationMs" — feature removed after advancing fake timer

    hub-inventory.test.ts extensions (RED until Plan 04 adds PackageDelivered reducer case):
    - Test A: "PackageDelivered purges the package from hub inventory" — apply PackageDelivered event, package row gone
    - Test B: "PackageDelivered is a no-op when package is absent (idempotent)" — apply PackageDelivered for unknown packageId, state unchanged (D-22-1)
  </behavior>
  <action>
    ws-delivery.unit.test.ts (NEW): Mirror ws-induction.unit.test.ts structure. Replace `InductionEvent`/`inductionEvents` with `DeliveryEvent`/`deliveryEvents`. Import `DeliveryEvent` from `../src/ws/envelope.js`. Build a `DELIVERY_FIXTURE: DeliveryEvent[]` with `{ packageId: "PKG-001", hubId: "hub-spoke-b", deliveredAt: "2026-06-24T12:34:00.000Z", onTime: true }`. Three tests: (1) tick with deliveryEvents present, (2) tick without deliveryEvents absent, (3) snapshot NEVER has deliveryEvents. The Pitfall-7 test (3) asserts `(snapshotEnv.payload as Record<string, unknown>)["deliveryEvents"]` is `undefined`. Note: imports will fail until Plan 01 adds `DeliveryEvent` to envelope.ts — write with the correct import path and let it be RED.

    deliveryLayer.test.ts (NEW): Mirror inductionLayer.test.ts. Use `vi.useFakeTimers()` in `beforeEach` and `vi.useRealTimers()` in `afterEach`. Import `flashDelivery`, `createDeliveryLayer` from `./layers.js`. Test 1: call `flashDelivery(source, "hub-spoke-b", -87.63, 41.88)` and assert `source.getFeatures().length === 1`. Test 2: call then `vi.advanceTimersByTime(2001)` and assert `source.getFeatures().length === 0`. File will fail at import until Plan 06 adds the functions.

    hub-inventory.test.ts (EXTEND): Append a new describe block `"PackageDelivered (OUT-04 purge)"`. Import helpers from the existing test file pattern. Test A: start from a state with packageId "PKG-1" placed at "hub-spoke-a", apply a `PackageDelivered` event for "PKG-1", assert the resulting state has no entry for "PKG-1" in `state.placement`. Test B: apply `PackageDelivered` for packageId "UNKNOWN-99" (not in any projection state), assert the state is returned unchanged (not undefined, not throwing). Tests will fail once `case "PackageDelivered":` is absent from the switch (compile gate prevents missing case — so add a comment `// PHASE 22: PackageDelivered case pending Plan 04` and temporarily guard the tests with `it.todo()` if they would cause a compile error in the reducer; otherwise let them be runtime-RED).
  </action>
  <verify>
    <automated>pnpm --filter @mm/projections test -- --run packages/projections/src/reducers/hub-inventory.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    All three files exist and are syntactically valid TS. ws-delivery.unit.test.ts: 3 test cases present (may fail at import if DeliveryEvent not yet exported — acceptable RED). deliveryLayer.test.ts: 2 test cases present (fail at import — acceptable RED). hub-inventory.test.ts: 2 new test cases present in todo/failing state (PackageDelivered not yet handled by reducer — compile gate triggers compile error or tests are todo). No pre-existing hub-inventory tests broken.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | This plan creates test files only; no production code changes, no new API surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Tampering | Test stubs | accept | No new threats: simulation event + projection extension tests; no user-facing auth, no external input, no secrets; no production code changes in this plan. |
</threat_model>

<verification>
After both tasks:
1. All four test files exist in the repo
2. `pnpm --filter @mm/simulation build` still passes (outbound-determinism.unit.test.ts is a test file, not production code)
3. `pnpm --filter @mm/projections test -- --run packages/projections/src/reducers/hub-inventory.test.ts` runs without breaking existing tests
4. No existing passing tests are broken by the new stubs
</verification>

<success_criteria>
- outbound-determinism.unit.test.ts: 7 test cases exist; Tests 1-2 (flag-off) pass or are RED with assertion failures
- ws-delivery.unit.test.ts: 3 test cases exist; all RED (import failure or assertion failure — not syntax error)
- deliveryLayer.test.ts: 2 test cases exist; RED (import failure)
- hub-inventory.test.ts: 2 new PackageDelivered test cases added; existing tests unchanged and green
</success_criteria>

<output>
After completion, create `.planning/phases/22-outbound-delivery/22-02-SUMMARY.md`
</output>
