---
phase: 22-outbound-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/events/schemas.ts
  - packages/domain/src/events/domain-event.ts
  - packages/domain/src/events/contract.assert.ts
  - packages/domain/src/events/index.ts
  - packages/domain/test/package-delivered.unit.test.ts
  - packages/simulation/src/continuation.ts
  - packages/simulation/test/fuel-determinism.unit.test.ts
  - packages/simulation/test/continuation-equivalence.unit.test.ts
autonomous: true
requirements:
  - OUT-01
  - D-22-4
must_haves:
  truths:
    - "packages/domain builds with PackageDelivered in the DomainEvent closed union"
    - "validateEvent() round-trips a well-formed PackageDelivered and rejects an extra payload field"
    - "OUTBOUND_RNG_SALT is pairwise-distinct from all 7 prior salts (Set(8).size === 8)"
    - "SimTask union contains deliverPackage variant; SerializedRngStates.outbound field exists; SerializedWorldState contains deliveredCounter + slaDeadlineByPackage fields"
    - "continuation-equivalence test file has an outbound FEATURE_CASE placeholder (may be skipped until engine is implemented)"
  artifacts:
    - path: packages/domain/src/events/schemas.ts
      provides: packageDeliveredSchema Zod definition
      contains: "packageDeliveredSchema"
    - path: packages/domain/src/events/domain-event.ts
      provides: PackageDelivered type + union member
      contains: "PackageDelivered"
    - path: packages/domain/src/events/contract.assert.ts
      provides: exhaustive switch case for PackageDelivered
      contains: '"PackageDelivered"'
    - path: packages/domain/src/events/index.ts
      provides: exports of PackageDelivered type + schema
      contains: "packageDeliveredSchema"
    - path: packages/domain/test/package-delivered.unit.test.ts
      provides: round-trip + strict-reject tests
      min_lines: 40
    - path: packages/simulation/src/continuation.ts
      provides: SimTask.deliverPackage variant + SerializedRngStates.outbound + SerializedWorldState.deliveredCounter + slaDeadlineByPackage
      contains: "deliverPackage"
    - path: packages/simulation/test/fuel-determinism.unit.test.ts
      provides: extended pairwise-distinct salt assertion including OUTBOUND_RNG_SALT
      contains: "OUTBOUND_RNG_SALT"
  key_links:
    - from: packages/domain/src/events/schemas.ts
      to: packages/domain/src/events/domain-event.ts
      via: z.infer<typeof packageDeliveredSchema>
      pattern: "packageDeliveredSchema"
    - from: packages/domain/src/events/domain-event.ts
      to: packages/domain/src/events/contract.assert.ts
      via: exhaustive DomainEvent switch
      pattern: '"PackageDelivered"'
---

<objective>
Perform the `PackageDelivered` domain event 5-file union ceremony (OUT-01), add the
`OUTBOUND_RNG_SALT` constant and extend the pairwise-distinct salt test (D-22-4),
and extend `continuation.ts` with all new TS types (`SimTask.deliverPackage`,
`SerializedRngStates.outbound`, `SerializedWorldState.deliveredCounter +
slaDeadlineByPackage`) needed by the Wave-2 engine implementation.

Purpose: Establish the closed-union contract + TS type scaffolding so Wave-2 plans
(engine implementation, projection reducers) can be written against stable types.
Output: Compile-clean domain package + extended continuation types + RED→GREEN domain
test + extended salt test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-outbound-delivery/22-CONTEXT.md
@.planning/phases/22-outbound-delivery/22-RESEARCH.md
@.planning/phases/22-outbound-delivery/22-PATTERNS.md
@.planning/phases/22-outbound-delivery/22-VALIDATION.md

<interfaces>
<!-- Key source files the executor must read first. Line anchors from RESEARCH.md. -->

packages/domain/src/events/schemas.ts — `eventSchema` helper at lines 35-44; `packageInductedSchema` at line 430 (exact copy target); `domainEventSchema` union at lines 448-478 (add `packageDeliveredSchema` as last entry).

packages/domain/src/events/domain-event.ts — `PackageInducted` type at line 145; `PlanSuperseded` at line 157; `DomainEvent` union tail at lines 165-194 ending with `| PlanSuperseded`.

packages/domain/src/events/contract.assert.ts — `assertExhaustive()` switch at lines 25-55; `case "PackageInducted":` at line 49; `case "PlanSuperseded":` at line 50.

packages/domain/src/events/index.ts — `PackageInducted` type export at line 28; `packageInductedSchema` export at line 58.

packages/domain/test/package-inducted.unit.test.ts — verbatim copy target for the new test.

packages/simulation/src/continuation.ts — `SimTask` union at lines 27-65; `SerializedWorldState` at lines 90-123 (inductionCounter at line 121 is the template); `SerializedRngStates` at lines 125-136 (induction at last field).

packages/simulation/test/fuel-determinism.unit.test.ts — pairwise-distinct salt assertion at lines 43-75; INDUCTION_RNG_SALT test at lines 60-75 is the exact copy target for the OUTBOUND_RNG_SALT test.

packages/simulation/test/continuation-equivalence.unit.test.ts — FEATURE_CASES array at lines 124-179; "consolidation" case at line 176 is the copy target for "outbound" case; `FeatureOpts` type at lines 46-48.

packages/simulation/src/engine.ts — `INDUCTION_RNG_SALT = 0x8f_2c_4a_e1` at line 116 (add OUTBOUND_RNG_SALT = 0xc4_f8_32_b6 immediately after).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PackageDelivered 5-file domain union ceremony + domain test</name>
  <files>
    packages/domain/src/events/schemas.ts,
    packages/domain/src/events/domain-event.ts,
    packages/domain/src/events/contract.assert.ts,
    packages/domain/src/events/index.ts,
    packages/domain/test/package-delivered.unit.test.ts
  </files>
  <read_first>
    packages/domain/src/events/schemas.ts (full file — understand eventSchema helper + packageInductedSchema at line 430 + domainEventSchema union at line 448);
    packages/domain/src/events/domain-event.ts (full file — DomainEvent union tail at lines 165-194);
    packages/domain/src/events/contract.assert.ts (full file — exhaustive switch at lines 25-55);
    packages/domain/src/events/index.ts (full file — export lists);
    packages/domain/test/package-inducted.unit.test.ts (verbatim copy target for the new test).
    ANALOG: packages/domain/test/package-inducted.unit.test.ts (exact copy-and-adapt template per 22-PATTERNS.md).
  </read_first>
  <behavior>
    - Test 1: well-formed PackageDelivered round-trips validateEvent() (type preserved, no throw)
    - Test 2: packageDeliveredSchema.safeParse() returns success: true on the same fixture
    - Test 3: validateEvent() throws on extra payload field (Zod .strict() boundary)
  </behavior>
  <action>
    File 1 — schemas.ts: Add `packageDeliveredSchema` using `eventSchema("PackageDelivered", z.object({ packageId: id, hubId: id, deliveredAt: z.string().min(1), onTime: z.boolean(), occurredAt }))` inserted after `packageInductedSchema` (around line 441) and before `domainEventSchema`. Add `packageDeliveredSchema` as the final entry in the `domainEventSchema` discriminated union (after `planSupersededSchema`). Per OUT-01 + 22-PATTERNS.md.

    File 2 — domain-event.ts: Import `packageDeliveredSchema` from `./schemas.js`. Add `export type PackageDelivered = z.infer<typeof packageDeliveredSchema>;` after the `PackageInducted` type block. Add `| PackageDelivered` as the final member of the `DomainEvent` union with a comment: `// Phase-22 terminal delivery event (OUT-01).`

    File 3 — contract.assert.ts: Add `case "PackageDelivered":` inside `assertExhaustive()` alongside the existing `case "PackageInducted":` and `case "PlanSuperseded":` fall-through (all three share one `return`). This makes the exhaustive build gate pass for the new type.

    File 4 — index.ts: Add `PackageDelivered` to the type re-export block (after `PlanSuperseded`). Add `packageDeliveredSchema` to the value re-export block (after `planSupersededSchema`).

    File 5 — package-delivered.unit.test.ts (NEW): Mirror `package-inducted.unit.test.ts` verbatim. Replace `PackageInducted`/`packageInductedSchema`/`inductionHub`/`destHub`/`slaClass`/`slaDeadlineIso`/`externalOriginRef` with the `PackageDelivered` payload shape: `{ packageId: "EXT-P00001", hubId: "hub-spoke-b", deliveredAt: "2026-06-24T12:34:00.000Z", onTime: true, occurredAt: "2026-06-24T12:34:00.000Z" }`. Three tests: round-trip, safeParse success, strict reject.

    MUST NOT use `??` or any other truthiness shortcut for any flag check. MUST NOT add any field to SnapshotPayload in this task.
  </action>
  <verify>
    <automated>pnpm --filter @mm/domain build && pnpm --filter @mm/domain test -- --run packages/domain/test/package-delivered.unit.test.ts</automated>
  </verify>
  <done>
    Domain builds with zero errors. package-delivered.unit.test.ts: 3/3 tests green. `contract.assert.ts` exhaustive switch compiles without error (no "not all code paths return a value" TS error for PackageDelivered).
  </done>
</task>

<task type="auto">
  <name>Task 2: OUTBOUND_RNG_SALT constant + continuation.ts TS types + salt test + continuation-equivalence stub</name>
  <files>
    packages/simulation/src/engine.ts,
    packages/simulation/src/continuation.ts,
    packages/simulation/test/fuel-determinism.unit.test.ts,
    packages/simulation/test/continuation-equivalence.unit.test.ts
  </files>
  <read_first>
    packages/simulation/src/engine.ts (lines 85-120 — RNG salt constants block; line 116 = INDUCTION_RNG_SALT);
    packages/simulation/src/continuation.ts (full file — SimTask union lines 27-65; SerializedWorldState lines 90-123; SerializedRngStates lines 125-136);
    packages/simulation/test/fuel-determinism.unit.test.ts (lines 43-75 — pairwise-distinct salt test to extend);
    packages/simulation/test/continuation-equivalence.unit.test.ts (lines 46-48 FeatureOpts; lines 124-179 FEATURE_CASES array).
    ANALOG for salt test: fuel-determinism.unit.test.ts lines 60-75 (INDUCTION_RNG_SALT test = exact copy target per 22-PATTERNS.md).
    ANALOG for continuation types: continuation.ts itself with Phase-20 induction additions as template per 22-PATTERNS.md § continuation.ts section.
  </read_first>
  <action>
    engine.ts (CONSTANT ONLY — no implementation yet): Add `export const OUTBOUND_RNG_SALT = 0xc4_f8_32_b6;  // substream 7 (when outboundDeliveryEnabled)` immediately after `INDUCTION_RNG_SALT` at line 116. Also add `outboundDeliveryEnabled?: boolean` to `SimulateOptions` interface (after `consolidationEnabled`, with jsdoc per 22-PATTERNS.md § SimulateOptions section). Use STRICT `=== true` check language in the jsdoc comment. NO other engine.ts changes in this task.

    continuation.ts — extend three interfaces:
    (1) `SimTask` union: add `| { readonly kind: "deliverPackage"; readonly packageId: string; readonly hubId: string; readonly slaDeadlineIso: string | undefined; readonly fireTick: number; }` as the final union member. This is a DATA variant (no closures), required for chunk-boundary serialization (D-22-4, Pitfall-6).
    (2) `SerializedRngStates`: add `readonly outbound: number | undefined;  // present only when outboundDeliveryEnabled. OUT-01.` after the `induction` field.
    (3) `SerializedWorldState`: add `readonly deliveredCounter: number;` and `readonly slaDeadlineByPackage: readonly (readonly [string, string])[];` after `inductionCounter`.

    fuel-determinism.unit.test.ts: Import `OUTBOUND_RNG_SALT` from `../src/engine.js`. Append a new `it()` test following the INDUCTION_RNG_SALT pattern at lines 60-75: create a `salts` array of all 7 prior salts + `OUTBOUND_RNG_SALT` (8 total), assert `new Set(salts).size === salts.length` (which equals 8) AND `expect(salts.length).toBe(8)`. Per D-22-4 + 22-PATTERNS.md § fuel-determinism section.

    continuation-equivalence.unit.test.ts: Widen `FeatureOpts` type to include `readonly outboundDeliveryEnabled?: boolean`. Add to the `FEATURE_CASES` array: `{ name: "outbound", opts: { timing: SHORT_TIMING, inductionEnabled: true, outboundDeliveryEnabled: true }, skip: true }`. The `skip: true` field mirrors the guard pattern used in Phase-20/21 for the `"consolidation"` and `"induction"` cases before their engines were implemented (see existing `FEATURE_CASES` entries with `skip: true` for precedent). The test harness reads `skip` on each case object and wraps the `it()` call with `it.skip()` when true — this prevents a test failure from an unimplemented engine without a conditional runtime check. Update the existing `"all-on"` case to include `outboundDeliveryEnabled: true`. Plan 03 removes `skip: true` from the "outbound" case when the engine is wired.
  </action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- --run packages/simulation/test/fuel-determinism.unit.test.ts</automated>
  </verify>
  <done>
    fuel-determinism.unit.test.ts passes with new OUTBOUND_RNG_SALT test: `Set(8 salts).size === 8`. continuation.ts and engine.ts compile (pnpm --filter @mm/simulation build passes). continuation-equivalence.unit.test.ts passes (outbound case is guarded with `skip: true` — shown as skipped, not failed, in Vitest output).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | This plan adds domain types and test scaffolding only; no new user-facing input, no new API surface, no new auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | PackageDelivered Zod schema | accept | No new threats: payload validated at existing validateEvent() Zod .strict() ingestion boundary. No user-facing auth, no external input, no secrets. Same protection as PackageInducted added in Phase 20. |
</threat_model>

<verification>
After both tasks:
1. `pnpm --filter @mm/domain build` — zero errors (exhaustive switch + closed union compile)
2. `pnpm --filter @mm/domain test -- --run` — all existing tests green + 3 new package-delivered tests green
3. `pnpm --filter @mm/simulation test -- --run packages/simulation/test/fuel-determinism.unit.test.ts` — salt test passes, `Set(8 salts).size === 8`
4. `pnpm --filter @mm/simulation build` — continuation.ts + engine.ts TS additions compile cleanly
</verification>

<success_criteria>
- PackageDelivered is a full member of DomainEvent (type, schema, exhaustive switch case, export)
- `validate()` round-trip passes; `.strict()` rejects extras
- `OUTBOUND_RNG_SALT = 0xc4_f8_32_b6` exported; all 8 salts pairwise-distinct (`Set(8).size === 8`)
- `SimTask.deliverPackage` variant typed; `SerializedRngStates.outbound`; `SerializedWorldState.deliveredCounter + slaDeadlineByPackage` — all compile
- `SimulateOptions.outboundDeliveryEnabled?: boolean` field present with jsdoc warning about `=== true` check
</success_criteria>

<output>
After completion, create `.planning/phases/22-outbound-delivery/22-01-SUMMARY.md`
</output>
