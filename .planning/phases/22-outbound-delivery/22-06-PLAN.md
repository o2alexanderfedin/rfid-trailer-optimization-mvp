---
phase: 22-outbound-delivery
plan: "06"
type: execute
wave: 4
depends_on:
  - "22-03"
  - "22-04"
  - "22-05"
files_modified:
  - packages/web/src/map/deliveryColoring.ts
  - packages/web/src/map/layers.ts
  - packages/web/src/map/deliveryLayer.test.ts
  - packages/web/src/panels/DeliveryKpi.tsx
  - packages/web/src/panels/DeliveryKpi.test.tsx
autonomous: true
requirements:
  - VIZ-14
  - OUT-05
must_haves:
  truths:
    - "deliveryColoring.ts exports DELIVERY_COLOR distinct from #7c3aed (induction purple) and consolidation cyan"
    - "createDeliveryLayer() and flashDelivery() exist in layers.ts, mirroring createInductionLayer/flashInduction"
    - "deliveryLayer.test.ts: flashDelivery adds a feature and self-removes after durationMs (both tests GREEN)"
    - "DeliveryKpi.tsx renders delivered-out count and on-time % fetched from GET /api/delivery-kpi"
    - "DeliveryKpi.test.tsx: pure helper unit tests green (formatDeliveryKpi, onTimePercent)"
  artifacts:
    - path: packages/web/src/map/deliveryColoring.ts
      provides: deliveryStyle function + DELIVERY_COLOR constant (distinct from #7c3aed and cyan)
      contains: "DELIVERY_COLOR"
    - path: packages/web/src/map/layers.ts
      provides: createDeliveryLayer() + flashDelivery() functions
      contains: "flashDelivery"
    - path: packages/web/src/map/deliveryLayer.test.ts
      provides: 2 tests GREEN (add feature, self-remove)
      contains: "flashDelivery"
    - path: packages/web/src/panels/DeliveryKpi.tsx
      provides: React component fetching /api/delivery-kpi
      contains: "DeliveryKpi"
    - path: packages/web/src/panels/DeliveryKpi.test.tsx
      provides: pure helper tests
      min_lines: 30
  key_links:
    - from: packages/web/src/map/deliveryColoring.ts
      to: packages/web/src/map/layers.ts
      via: deliveryStyle import in createDeliveryLayer
      pattern: "deliveryStyle"
    - from: packages/web/src/panels/DeliveryKpi.tsx
      to: GET /api/delivery-kpi
      via: fetch in useEffect
      pattern: "delivery-kpi"
---

<objective>
Implement the VIZ-14 destination-hub highlight map layer (`deliveryColoring.ts`,
`createDeliveryLayer` / `flashDelivery` in `layers.ts`) and the OUT-05 (P2)
`DeliveryKpi.tsx` operator panel widget. Turn `deliveryLayer.test.ts` GREEN.

Purpose: Close the end-to-end visualization — a viewer watching the map sees destination
hubs briefly highlight green/orange when `PackageDelivered` fires, and the KPI panel
shows live delivered-out count + on-time %. The human-verify checkpoint lives in Plan 07
(which wires MapView.tsx), so the visual gate occurs after the layer is fully live.
Output: Map layer implemented and tested; KPI widget renders; both pass automated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-outbound-delivery/22-CONTEXT.md
@.planning/phases/22-outbound-delivery/22-RESEARCH.md
@.planning/phases/22-outbound-delivery/22-PATTERNS.md
@.planning/phases/22-outbound-delivery/22-VALIDATION.md
@.planning/phases/22-outbound-delivery/22-01-SUMMARY.md
@.planning/phases/22-outbound-delivery/22-03-SUMMARY.md
@.planning/phases/22-outbound-delivery/22-05-SUMMARY.md

<interfaces>
<!-- Verified code sections from 22-RESEARCH.md and 22-PATTERNS.md. -->

packages/web/src/map/inductionColoring.ts — read full file (exact copy target for deliveryColoring.ts; INDUCTION_COLOR = "#7c3aed"; zero-allocation style pattern with pre-allocated INDUCTION_STYLE_DEFAULT per 22-PATTERNS.md § deliveryColoring.ts).

packages/web/src/map/layers.ts (lines 247-278) — read the createInductionLayer + flashInduction functions (exact copy target for createDeliveryLayer/flashDelivery per 22-PATTERNS.md § layers.ts); also read existing imports for VectorSource, VectorLayer, Feature, Point, fromLonLat.

packages/web/src/map/inductionLayer.test.ts — read full file (exact copy target for deliveryLayer.test.ts; vi.useFakeTimers pattern, feature add/self-remove assertions per 22-PATTERNS.md § layers.ts test).

packages/web/src/panels/HubBalance.tsx — read full file (exact copy target for DeliveryKpi.tsx: useState, useEffect, fetch, loadState, pure helpers per 22-PATTERNS.md § DeliveryKpi.tsx).

packages/web/src/panels/HubBalance.test.tsx — read for pure-helper test pattern (DeliveryKpi.test.tsx mirrors this).

packages/web/src/map/deliveryLayer.test.ts — read the RED stub created in Plan 02 (turn these specific tests GREEN).

packages/web/src/map/consolidationLayer.test.ts (if exists) or inductionLayer.test.ts — check consolidation cyan color constant to ensure DELIVERY_COLOR is distinct.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deliveryColoring.ts (new) + createDeliveryLayer/flashDelivery in layers.ts + turn deliveryLayer.test.ts GREEN</name>
  <files>
    packages/web/src/map/deliveryColoring.ts,
    packages/web/src/map/layers.ts,
    packages/web/src/map/deliveryLayer.test.ts
  </files>
  <read_first>
    packages/web/src/map/inductionColoring.ts (full file — verbatim copy target; zero-allocation pattern: pre-allocate INDUCTION_STYLE_DEFAULT at module load, return cached reference from inductionStyle() — zero per-frame allocation);
    packages/web/src/map/layers.ts (full file — locate createInductionLayer lines 248-252 + flashInduction lines 260-278; also check imports: VectorSource, VectorLayer, Feature, Point, fromLonLat, Style imports);
    packages/web/src/map/inductionLayer.test.ts (full file — vi.useFakeTimers pattern to use in deliveryLayer.test.ts);
    packages/web/src/map/deliveryLayer.test.ts (read the RED stubs from Plan 02 — these are the test cases to turn GREEN);
    packages/web/src/map/consolidationLayer.test.ts OR packages/web/src/map/coloring.ts (check consolidation cyan color constant — DELIVERY_COLOR must differ from it).
    ANALOG: inductionColoring.ts (zero-allocation style, exact copy target) + layers.ts createInductionLayer/flashInduction (exact copy target) per 22-PATTERNS.md § deliveryColoring.ts and § layers.ts sections.
  </read_first>
  <behavior>
    deliveryLayer.test.ts tests to turn GREEN (from Plan 02 RED stubs):
    - "flashDelivery adds a Point feature to the VectorSource" — source.getFeatures().length === 1 immediately after call
    - "flashDelivery self-removes the feature after durationMs" — source.getFeatures().length === 0 after vi.advanceTimersByTime(2001)
  </behavior>
  <action>
    deliveryColoring.ts (NEW FILE — mirror inductionColoring.ts per 22-PATTERNS.md § deliveryColoring.ts):
    - Import: `Style, Fill, Stroke, Circle as CircleStyle` from `ol/style.js`
    - `DELIVERY_RADIUS = 14` (same as induction)
    - `export const DELIVERY_COLOR = "#16a34a"` — green (Claude's discretion per CONTEXT.md; distinct from induction purple `#7c3aed` and consolidation cyan; green signals delivery/success)
    - Pre-allocate `DELIVERY_STYLE_DEFAULT = new Style({ image: new CircleStyle({ radius: DELIVERY_RADIUS, fill: new Fill({ color: DELIVERY_COLOR }), stroke: new Stroke({ color: "#ffffff", width: 2 }) }), text: new Text({ text: "✓", font: EMOJI_FONT }) })` — ONE allocation at module load (zero-allocation pattern from inductionColoring.ts)
    - `export function deliveryStyle(): Style { return DELIVERY_STYLE_DEFAULT; }` — cached reference, zero per-frame allocation

    layers.ts (ADD functions at end, after flashInduction):
    - Import `deliveryStyle` from `./deliveryColoring.js` (add to imports block)
    - `createDeliveryLayer()` — mirrors createInductionLayer exactly; uses `style: deliveryStyle` instead of `inductionStyle`
    - `flashDelivery(source, deliveryHubId, lon, lat, durationMs = 2000)` — mirrors flashInduction exactly; feature property is `deliveryHubId` (not `inductionHubId`); featureId prefix is `delivery:` (not `induction:`)
    Both functions follow the exact createInductionLayer/flashInduction pattern from 22-PATTERNS.md § layers.ts section.

    deliveryLayer.test.ts (FILL in RED stubs — turn them GREEN):
    Read the stub file. Ensure imports point to the now-implemented `flashDelivery`, `createDeliveryLayer` from `./layers.js`. The test uses `vi.useFakeTimers()` and `vi.advanceTimersByTime(2001)` to verify self-removal. Follow the inductionLayer.test.ts pattern exactly. No changes needed to the test logic — just ensure imports resolve and the implementation makes assertions pass.
  </action>
  <verify>
    <automated>pnpm --filter @mm/web test -- --run packages/web/src/map/deliveryLayer.test.ts</automated>
  </verify>
  <done>
    deliveryLayer.test.ts: 2/2 tests GREEN. deliveryColoring.ts exports DELIVERY_COLOR = "#16a34a" (distinct from purple and cyan). layers.ts builds with createDeliveryLayer + flashDelivery. DELIVERY_COLOR is not "#7c3aed" (induction purple) and not consolidation cyan.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeliveryKpi.tsx panel widget + DeliveryKpi.test.tsx pure helper tests</name>
  <files>
    packages/web/src/panels/DeliveryKpi.tsx,
    packages/web/src/panels/DeliveryKpi.test.tsx
  </files>
  <read_first>
    packages/web/src/panels/HubBalance.tsx (full file — exact copy target: pure helper exports + React component + useEffect fetch pattern + loadState state + data-testid attributes per 22-PATTERNS.md § DeliveryKpi.tsx);
    packages/web/src/panels/HubBalance.test.tsx (test pattern for pure helpers — what to mirror in DeliveryKpi.test.tsx);
    packages/web/src/panels/KpiDashboard.tsx (read to understand how to add DeliveryKpi to the existing operator panel set — where to mount the new widget).
    ANALOG: HubBalance.tsx (exact copy target per 22-PATTERNS.md § DeliveryKpi.tsx).
  </read_first>
  <behavior>
    DeliveryKpi.test.tsx pure helper unit tests:
    - formatDeliveryKpi(0, 0) returns a string with "0 delivered"
    - formatDeliveryKpi(10, 8) returns a string containing "10"
    - onTimePercent(0, 0) returns 0 (no division by zero)
    - onTimePercent(10, 8) returns 80
    - onTimePercent(10, 10) returns 100
  </behavior>
  <action>
    DeliveryKpi.tsx (NEW FILE — per 22-PATTERNS.md § DeliveryKpi.tsx):
    1. Pure helpers (exported, unit-testable, no DOM):
       - `formatDeliveryKpi(delivered: number, onTime: number): string` — returns a human-readable string like `"${delivered} delivered (${onTimePercent(delivered, onTime)}% on time)"` or similar demo-friendly text
       - `onTimePercent(delivered: number, onTime: number): number` — returns `delivered === 0 ? 0 : Math.round((onTime / delivered) * 100)`
    2. Component `DeliveryKpi(): React.JSX.Element` following the HubBalance pattern:
       - `useState<{ deliveredCount: number; onTimeCount: number }>({ deliveredCount: 0, onTimeCount: 0 })`
       - `useState<"loading" | "loaded" | "error">("loading")`
       - `useEffect` that fetches `GET /api/delivery-kpi` via `AbortController` (matching HubBalance's `fetchHubDetail` pattern exactly); updates state on success/failure
       - Renders: loading/error/loaded states with `data-testid` attributes (mirror HubBalance's pattern for `data-testid="delivery-kpi-loaded"`, `data-testid="delivery-kpi-loading"`, etc.)
       - Displays: delivered count + on-time % via `formatDeliveryKpi` + `onTimePercent`
    3. Wire into `KpiDashboard.tsx` or `RightRail.tsx` (whichever holds the existing operator panel widgets) — add `<DeliveryKpi />` alongside the existing widgets.

    DeliveryKpi.test.tsx (NEW FILE — pure helper tests only, per HubBalance.test.tsx pattern):
    - Import `formatDeliveryKpi`, `onTimePercent` from `./DeliveryKpi.js`
    - 5 tests from the behavior block above
    - NO DOM rendering tests (those are checkpoint:human-verify territory); pure unit tests only for the helper functions
  </action>
  <verify>
    <automated>pnpm --filter @mm/web test -- --run packages/web/src/panels/DeliveryKpi.test.tsx</automated>
  </verify>
  <done>
    DeliveryKpi.test.tsx: 5/5 helper tests GREEN. DeliveryKpi.tsx compiles. Component is mounted in the operator panel (KpiDashboard or RightRail). Web package builds clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web client → GET /api/delivery-kpi | Read-only fetch; same trust level as existing hub-detail API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Information Disclosure | DeliveryKpi.tsx API fetch | accept | No new threats: simulation aggregate metrics (deliveredCount, onTimeCount); no PII; same internal API trust level as existing HubBalance + KpiDashboard. No auth change. |
| T-22-10 | Tampering | flashDelivery setTimeout | accept | Timer-based DOM mutation is client-side only; no server data modified; same pattern as flashInduction (verified safe in Phase 20). |
</threat_model>

<verification>
After both tasks:
1. `pnpm --filter @mm/web build` — zero TS errors
2. `pnpm --filter @mm/web test -- --run packages/web/src/map/deliveryLayer.test.ts` — 2/2 green
3. `pnpm --filter @mm/web test -- --run packages/web/src/panels/DeliveryKpi.test.tsx` — 5/5 green
4. `pnpm --filter @mm/web test -- --run` — all web tests green (no regressions)
</verification>

<success_criteria>
- DELIVERY_COLOR = "#16a34a" (or another distinct color, not purple, not cyan) in deliveryColoring.ts
- createDeliveryLayer() + flashDelivery() implemented and tested (2 tests GREEN)
- DeliveryKpi.tsx fetches /api/delivery-kpi; renders delivered count + on-time %
- DeliveryKpi.test.tsx: 5 helper tests GREEN
- (Visual verification of the delivery flash on the live map is in Plan 07's human checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/22-outbound-delivery/22-06-SUMMARY.md`
</output>
