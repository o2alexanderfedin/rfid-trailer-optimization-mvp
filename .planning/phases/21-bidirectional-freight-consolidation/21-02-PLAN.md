---
phase: 21-bidirectional-freight-consolidation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/simulation/test/consolidation-determinism.unit.test.ts
  - packages/simulation/test/determinism.unit.test.ts
  - packages/simulation/test/continuation-equivalence.unit.test.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03

must_haves:
  truths:
    - "A new consolidation-determinism.unit.test.ts encodes the FLOW-01/02/03 contract (RED until 21-04 implements consolidationEnabled): spoke-origin TrailerDeparted carries real freight from pendingAtSpoke; the center re-stages it into pendingBySpoke[destSpoke]; consolidationEnabled:false is byte-identical to absent; empty return is valid"
    - "determinism.unit.test.ts DET-01 flags-off gate gains a consolidationEnabled:false byte-identical assertion AND the seed-42@10000 golden 3920accc… stays green"
    - "continuation-equivalence.unit.test.ts gains a 'consolidation' FEATURE_CASE (inductionEnabled:true to source spoke freight + consolidationEnabled:true) that crosses a chunk boundary mid-consolidation, bounded seed 1234 @ 800"
    - "No new RNG salt is introduced — the salt-collision test (fuel-determinism.unit.test.ts) is NOT touched"
  artifacts:
    - path: packages/simulation/test/consolidation-determinism.unit.test.ts
      provides: "RED-first FLOW-01/02/03 contract tests mirroring over-carry.unit.test.ts + induction-determinism.unit.test.ts; BOUNDED (seed 1234 @ ≤6000, seed-42@10000 golden)"
      contains: "consolidationEnabled"
    - path: packages/simulation/test/determinism.unit.test.ts
      provides: "DET-01 flags-off gate extended with consolidationEnabled:false byte-identical assertion"
      contains: "consolidationEnabled"
    - path: packages/simulation/test/continuation-equivalence.unit.test.ts
      provides: "consolidation FEATURE_CASE crossing a chunk boundary mid-consolidation; all-on extended with consolidationEnabled"
      contains: "consolidation"
  key_links:
    - from: packages/simulation/test/consolidation-determinism.unit.test.ts
      to: packages/simulation/src/engine.ts
      via: "simulate({ consolidationEnabled: true, inductionEnabled: true }) — the symbol 21-04 implements"
      pattern: "consolidationEnabled"
    - from: packages/simulation/test/continuation-equivalence.unit.test.ts
      to: packages/simulation/src/continuation.ts
      via: "chunkedStream FEATURE_CASE proves pendingAtSpoke capture/restore across a chunk boundary"
      pattern: "consolidation"
---

<objective>
Wave-0 RED test stubs for the engine consolidation path (FLOW-01/02/03 + the determinism keystone). These tests encode the observable contract BEFORE 21-04 implements `consolidationEnabled` — they are RED until the engine lands, then 21-04 turns them green.

Purpose: The locked TDD cadence (ROADMAP 19-01/20-01 are "Wave 0 RED test stubs") requires the determinism contract be written first. This plan writes the three test surfaces VALIDATION.md maps FLOW-01/02/03 to, so 21-04 implements against a fixed signal. The off-path byte-identity and the continuation-equivalence case are the make-or-break gates.

Output: 1 new test file (consolidation-determinism) + 2 extended test files (determinism DET-01 gate, continuation-equivalence FEATURE_CASES). All RED until 21-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-bidirectional-freight-consolidation/21-CONTEXT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md
@.planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md
@.planning/phases/21-bidirectional-freight-consolidation/21-VALIDATION.md

<interfaces>
<!-- All line anchors verified against the live codebase this session. -->

From packages/simulation/test/over-carry.unit.test.ts (the FLOW-01/02 template):
- Spoke-origin assertions: filter the stream for `TrailerDeparted` where `payload.fromHubId !== CENTER`. Over-carry is the ONLY existing producer of a non-center fromHubId today.
- CENTER = MEMPHIS.hubId = hubs[0]. Import MEMPHIS from the simulation network/hubs barrel (the engine exports `center = hubs[0]`).

From packages/simulation/test/induction-determinism.unit.test.ts (the off-path template):
- "flag absent ⇒ ZERO new events" + "flag:false ⇒ byte-identical to absent" via `expect(JSON.stringify(b)).toBe(JSON.stringify(a))`.
- To SOURCE spoke freight for the flag-on consolidation tests, set inductionEnabled:true (spoke-origin freight comes from Phase-20 induction).

From packages/simulation/test/determinism.unit.test.ts:
- OPTS = { seed: 1234, durationTicks: 6000 }.
- The golden: simulate({ seed: 42, durationTicks: 10000 }) → "3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861".
- describe("DET-01 flags-off gate (v2.0 regression)") with FLAGS_OFF_OPTS = { seed: 42, durationTicks: 500 }; existing cases assert "no flags byte-identical" and "explicit runUntilStopped:false byte-identical to absent". ADD a consolidationEnabled:false case alongside.

From packages/simulation/test/continuation-equivalence.unit.test.ts:
- `chunkedStream(seed, horizonTick, chunkSize, opts)` helper drives via runToHorizon in chunks; `hashStream` is a stable pointer-free SHA-256.
- SHORT_TIMING constant (fast transit so trailers round-trip inside the horizon).
- FEATURE_CASES: { name; opts }[]. Existing: rfid, over-carry, hos, fuel+hos, all-on. all-on already includes inductionEnabled:true and runs at chunk [1,7]; every other case runs at chunk [7].
- Loop: `simulate({ seed: 1234, durationTicks: 800, ...opts })` vs `chunkedStream(1234, 800, chunk, opts)`, hashes must match.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: consolidation-determinism.unit.test.ts (RED) — FLOW-01/02 freight-flow + cross-dock contract</name>
  <files>packages/simulation/test/consolidation-determinism.unit.test.ts</files>

  <read_first>
    1. packages/simulation/test/over-carry.unit.test.ts — read fully; the spoke-origin TrailerDeparted (fromHubId !== center) assertion pattern is the FLOW-01 template.
    2. packages/simulation/test/induction-determinism.unit.test.ts — read fully; the off-path "zero events" + "byte-identical to absent" assertions, AND how it enables inductionEnabled to source freight.
    3. packages/simulation/src/network/hubs.ts — confirm the MEMPHIS export (center = hubs[0]).
    4. .planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md "Code Examples" (the consolidation off-path test snippet) + Pattern 3 (over-carry return leg precedent).
    5. .planning/phases/21-bidirectional-freight-consolidation/21-VALIDATION.md rows FLOW-01, FLOW-02, FLOW-03 — the observable signal + automated command for each.
  </read_first>

  <behavior>
    RED stubs (fail until 21-04 implements consolidationEnabled). Mirror over-carry.unit.test.ts + induction-determinism.unit.test.ts. Use inductionEnabled:true to source spoke freight. CENTER = MEMPHIS.hubId.

    - FLOW-01 (freight flow): with consolidationEnabled:true + inductionEnabled:true (seed 1234, durationTicks ≤6000, SHORT-style timing so round-trips land), assert ≥1 TrailerDeparted with payload.fromHubId !== CENTER carrying a NON-EMPTY payload.packageIds (real freight drained from pendingAtSpoke — NOT the single-package over-carry case).
    - FLOW-01 (atomic drain / no double-drain): assert no packageId appears in two distinct spoke-origin TrailerDeparted.packageIds arrays across the run (two consolidation trailers at one spoke never share a package).
    - FLOW-02 (cross-dock re-stage): pick a package that departs Spoke A → center (consolidation), then assert it later appears in a center→destSpoke distribution TrailerDeparted (fromHubId === CENTER, toHubId === its destHubId) — proving the center unloaded + re-staged into pendingBySpoke[destSpoke].
    - FLOW-03 (empty return valid): assert a spoke→center TrailerDeparted with an EMPTY packageIds array can occur without error (the run completes; no throw).

    Keep all horizons ≤6000 (the existing determinism bound). Do NOT add or import any new RNG salt. This file is RED now and turns GREEN when 21-04 lands.
  </behavior>

  <action>
    Create packages/simulation/test/consolidation-determinism.unit.test.ts. Import simulate + the TrailerDeparted type (from @mm/domain) + MEMPHIS (the center hub). Define CENTER = MEMPHIS.hubId and a SHORT-style timing config so consolidation round-trips land inside the horizon.

    Write the four contract tests above (FLOW-01 freight flow, FLOW-01 no-double-drain, FLOW-02 cross-dock re-stage, FLOW-03 empty-return valid). Each gates on consolidationEnabled:true + inductionEnabled:true. Use deterministic stream filters (no RNG). Bound seed 1234 @ ≤6000.

    Commit: `test(21-02): FLOW-01/02/03 consolidation contract (RED until 21-04)`. Expected: RED (consolidationEnabled not implemented yet).
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm vitest run packages/simulation/test/consolidation-determinism.unit.test.ts || echo "EXPECTED RED until 21-04"</automated>
  </verify>

  <acceptance_criteria>
    - The file exists and `vitest run packages/simulation/test/consolidation-determinism.unit.test.ts` parses + RUNS (no TS/import error) — it is RED on the consolidation assertions (consolidationEnabled not yet implemented), which is the intended Wave-0 state.
    - `grep -c "fromHubId !== CENTER\|fromHubId).not.toBe(CENTER\|!== MEMPHIS" packages/simulation/test/consolidation-determinism.unit.test.ts` returns ≥ 1 (spoke-origin assertion present — FLOW-01).
    - `grep -c "consolidationEnabled" packages/simulation/test/consolidation-determinism.unit.test.ts` returns ≥ 4 (one per contract test).
    - `grep -c "RNG_SALT\|RngSalt" packages/simulation/test/consolidation-determinism.unit.test.ts` returns 0 (no new salt introduced — Pitfall 1).
    - No horizon literal in the file exceeds 6000 (GATE-HYGIENE) — confirm by grep for `durationTicks`.
  </acceptance_criteria>

  <done>
    The FLOW-01/02/03 contract is encoded as RED tests bounded to ≤6000 ticks, with no new RNG salt. 21-04 implements against this fixed signal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend DET-01 flags-off gate + add 'consolidation' continuation-equivalence FEATURE_CASE (FLOW-03)</name>
  <files>
    packages/simulation/test/determinism.unit.test.ts,
    packages/simulation/test/continuation-equivalence.unit.test.ts
  </files>

  <read_first>
    1. packages/simulation/test/determinism.unit.test.ts — read the golden block (the 3920accc… hash) + the describe("DET-01 flags-off gate (v2.0 regression)") block with FLAGS_OFF_OPTS.
    2. packages/simulation/test/continuation-equivalence.unit.test.ts — read fully; note chunkedStream, hashStream, SHORT_TIMING, the FEATURE_CASES array (all-on includes inductionEnabled:true at chunk [1,7]), and the loop comparing simulate(...) vs chunkedStream(...) at seed 1234 @ 800.
    3. .planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md Pitfall 2 (pendingAtSpoke-not-captured ⇒ chunked diverges) + the "Extending the continuation-equivalence test" code example.
    4. .planning/phases/20-external-induction/20-06-PLAN.md — how Phase 20 added induction to all-on (the precedent; Phase 21 adds a DEDICATED consolidation case per the LOCKED constraint, not just an all-on extension).
  </read_first>

  <behavior>
    Two extensions (both RED until 21-04 captures pendingAtSpoke):

    (a) determinism.unit.test.ts DET-01 flags-off gate: add a case asserting `simulate({ seed:42, durationTicks:500, consolidationEnabled:false })` is byte-identical (JSON.stringify equal) to `simulate({ seed:42, durationTicks:500 })`. Leave the seed-42@10000 golden 3920accc… assertion UNCHANGED — it must stay green (the off path adds zero behavior).

    (b) continuation-equivalence.unit.test.ts: add a DEDICATED FEATURE_CASE named "consolidation" with opts `{ timing: SHORT_TIMING, inductionEnabled: true, consolidationEnabled: true }` (induction sources the spoke freight; consolidation drains it). It runs at chunk [7] like the other non-all-on cases — bounded seed 1234 @ 800. ALSO add `consolidationEnabled: true` to the existing "all-on" opts so the maximal-state surface exercises consolidation at chunk [1] (boundary every tick — the strictest mid-consolidation capture test). Per the LOCKED constraint, the dedicated case MUST cross a chunk boundary mid-consolidation: chunk-7 with consolidation departures on the existing re-dispatch cadence lands a boundary between a consolidation departure and its center arrival.
  </behavior>

  <action>
    determinism.unit.test.ts: inside the DET-01 flags-off describe block, add `it("explicit consolidationEnabled:false is byte-identical to the flag being absent", ...)` asserting JSON.stringify equality of the two runs at FLAGS_OFF_OPTS. Do not modify the golden hash test.

    continuation-equivalence.unit.test.ts: append `{ name: "consolidation", opts: { timing: SHORT_TIMING, inductionEnabled: true, consolidationEnabled: true } }` to FEATURE_CASES; add `consolidationEnabled: true` to the all-on opts object. No change to the chunk-selection logic (consolidation inherits chunk [7]; all-on keeps [1,7]).

    Commit: `test(21-02): DET-01 consolidationEnabled:false gate + consolidation continuation-equivalence case (RED until 21-04)`. Expected: the off-path determinism case PASSES already (consolidationEnabled:false is a no-op flag the engine ignores until 21-04 — verify it does not error on the unknown option; if SimulateOptions does not yet accept the field, this case is RED until 21-04 adds the field). The continuation-equivalence consolidation/all-on cases are RED until 21-04 captures pendingAtSpoke.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm vitest run packages/simulation/test/determinism.unit.test.ts packages/simulation/test/continuation-equivalence.unit.test.ts || echo "EXPECTED RED on consolidation cases until 21-04"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861" packages/simulation/test/determinism.unit.test.ts` returns ≥ 1 (golden assertion preserved unchanged — FLOW-03 regression).
    - `grep -c "consolidationEnabled" packages/simulation/test/determinism.unit.test.ts` returns ≥ 1 (DET-01 off-path case added).
    - `grep -c '"consolidation"' packages/simulation/test/continuation-equivalence.unit.test.ts` returns ≥ 1 (dedicated FEATURE_CASE present — the LOCKED requirement).
    - `grep -c "consolidationEnabled" packages/simulation/test/continuation-equivalence.unit.test.ts` returns ≥ 2 (dedicated case + all-on extension).
    - No horizon literal exceeds 800 in the continuation-equivalence additions and 500/10000 unchanged in determinism (GATE-HYGIENE) — confirm by reading the touched lines.
  </acceptance_criteria>

  <done>
    The DET-01 off-path byte-identity assertion and the dedicated 'consolidation' continuation-equivalence case (chunk boundary mid-consolidation) exist, bounded; the seed-42@10k golden is preserved. RED until 21-04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → engine | Tests assert observable determinism properties; no untrusted input crosses; the threat surface for this plan is integrity (a missing assertion silently passing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | Determinism golden integrity | mitigate | The seed-42@10000 golden 3920accc… assertion is preserved verbatim and re-asserted; the new DET-01 case asserts consolidationEnabled:false is byte-identical to absent — a regression in the off path fails the gate. |
| T-21-05 | Tampering | Double-drain integrity (two trailers share a package) | mitigate | A dedicated FLOW-01 test asserts no packageId appears in two spoke-origin TrailerDeparted.packageIds arrays — the atomic-splice double-drain guard is witnessed by test, not assumed. |
| T-21-06 | Tampering | Continuation capture integrity (chunked ≠ all-at-once) | mitigate | The dedicated consolidation FEATURE_CASE + all-on chunk-1 prove pendingAtSpoke is captured/restored byte-identically across a mid-consolidation boundary. |

No high-severity threats — this is a test-only plan. No auth/crypto/API-input surface.
</threat_model>

<verification>
After this plan:
- The three test surfaces exist and RUN (parse + import clean); they are RED on the consolidation assertions, which is the intended Wave-0 state.
- The seed-42@10000 golden assertion is preserved unchanged.
- No new RNG salt is imported anywhere.
</verification>

<success_criteria>
1. consolidation-determinism.unit.test.ts encodes FLOW-01 (freight flow + no double-drain), FLOW-02 (cross-dock re-stage), FLOW-03 (empty return valid), bounded ≤6000
2. determinism.unit.test.ts DET-01 gate has a consolidationEnabled:false byte-identical case; the 3920accc… golden is unchanged
3. continuation-equivalence.unit.test.ts has a dedicated "consolidation" FEATURE_CASE crossing a chunk boundary mid-consolidation, bounded seed 1234 @ 800
4. No new RNG salt introduced; salt-collision test untouched
</success_criteria>

<output>
After completion, create `.planning/phases/21-bidirectional-freight-consolidation/21-02-SUMMARY.md`
</output>
