---
phase: 21-bidirectional-freight-consolidation
plan: 04
type: tdd
wave: 2
depends_on:
  - 21-02-PLAN.md
files_modified:
  - packages/simulation/src/engine.ts
  - packages/simulation/src/continuation.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03

must_haves:
  truths:
    - "consolidationEnabled:false (default) emits ZERO new behavior — seed-1234@6000 and seed-42@10000 (3920accc…) goldens are byte-identical to pre-Phase-21; NO empty returns appear when off"
    - "consolidationEnabled:true drains real freight from pendingAtSpoke onto spoke→center trailers via an atomic splice (no double-drain); the center re-stages each package into pendingBySpoke[destSpoke] (cross-dock)"
    - "pendingAtSpoke is captured in SimContinuation.world and restored on resume exactly like pendingBySpoke — a chunked run is byte-identical to all-at-once (the consolidation FEATURE_CASE passes)"
    - "Empty pendingAtSpoke (empty return leg) departs/arrives without error; the empty-return guard is deterministic (no silent empty returns)"
    - "NO new RNG salt or substream is added — consolidation reuses existing freight; selection/cadence are deterministic (modular tick arithmetic + stable trailerId/freightId sort). The salt-collision test is untouched."
  artifacts:
    - path: packages/simulation/src/engine.ts
      provides: "consolidationEnabled flag; pendingAtSpoke Map (init/restore/atomic-drain/capture); arriveConsolidationAtCenter task; dispatch case; deterministic sort key + empty-return guard"
      contains: "consolidationEnabled"
    - path: packages/simulation/src/continuation.ts
      provides: "SimTask arriveConsolidationAtCenter variant (DATA, never a closure); SerializedWorldState.pendingAtSpoke field"
      contains: "pendingAtSpoke"
  key_links:
    - from: packages/simulation/src/engine.ts
      to: packages/simulation/src/continuation.ts
      via: "captureContinuation() serializes pendingAtSpoke into world; SimTask union holds arriveConsolidationAtCenter"
      pattern: "pendingAtSpoke"
    - from: packages/simulation/src/engine.ts
      to: packages/simulation/src/engine.ts
      via: "spoke→center TrailerDeparted (fromHubId=spoke) → arriveConsolidationAtCenter → re-stage into pendingBySpoke[destSpoke]"
      pattern: "pendingBySpoke"
---

<objective>
Implement the engine consolidation path: the `consolidationEnabled` opt-in flag, the `pendingAtSpoke` two-queue manifest (mirroring `pendingBySpoke`), the spoke→center consolidation departure, the `arriveConsolidationAtCenter` re-sort task (cross-dock re-stage), and continuation capture/restore of `pendingAtSpoke`. This turns 21-02's RED tests green.

Purpose: FLOW-01/02/03 — the core of the phase. The off path must be byte-identical to pre-Phase-21 (the determinism keystone); the on path drains real freight, re-stages at the center, and is fully resumable. NO new RNG (consolidation reuses existing freight; all discretionary choices are deterministic).

Output: engine.ts + continuation.ts updated; the consolidation-determinism, DET-01, and consolidation continuation-equivalence tests (from 21-02) go green; goldens preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-CONTEXT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md
@.planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md
@.planning/phases/21-bidirectional-freight-consolidation/21-VALIDATION.md
@.planning/phases/21-bidirectional-freight-consolidation/21-02-SUMMARY.md
@.planning/phases/19-continuous-operation-foundation/19-04-SUMMARY.md

<interfaces>
<!-- All line anchors verified against the live codebase this session. -->

From packages/simulation/src/engine.ts — the pendingBySpoke lifecycle to MIRROR for pendingAtSpoke:
- SimulateOptions interface (line 147). inductionEnabled?: boolean is at line 281 — add consolidationEnabled?: boolean right after, same JSDoc style ("DEFAULT FALSE — the determinism keystone").
- inductionOn gate: `const inductionOn = opts.inductionEnabled === true;` (line 492) — add `const consolidationOn = opts.consolidationEnabled === true;` alongside.
- center = hubs[0]! (line 625); spokes = hubs.slice(1) (line 626). center is MEMPHIS.
- pendingBySpoke init + restore (lines 966-972): `const pendingBySpoke = new Map<string,string[]>(); if (resuming) { for (...start.world.pendingBySpoke) ...} for (const s of spokes) if (!pendingBySpoke.has(s.hubId)) pendingBySpoke.set(s.hubId, []);`
- Center distribution re-stage precedent: pendingBySpoke.get(dest.hubId)!.push(packageId) (line 1026) — the EXACT re-stage call shape FLOW-02 uses.
- Atomic drain at departure (lines 1283-1284): `const manifest = pendingBySpoke.get(spoke.hubId)!; const loaded = manifest.splice(0, manifest.length);` — atomic splice = the double-drain guard.
- Over-carry return leg (lines 1503-1547) — the ONLY existing spoke-origin TrailerDeparted (fromHubId !== center) + a scheduled arriveOverCarriedAtCenter. THE precedent for FLOW-01/02.
- arriveOverCarriedAtCenter (lines 1581-1613): emits TrailerArrivedAtHub + TrailerDocked, then unload PackageScanned scanType=unload + PackageArrivedAtHub@center. arriveConsolidationAtCenter mirrors this, then re-stages.
- dispatch switch (lines 1635-1666): arriveOverCarriedAtCenter case at 1663-1665 — add an arriveConsolidationAtCenter case.
- Bootstrap induction seed (lines 1686-1691): induction seeds a task only when inductionOn. Consolidation does NOT need a fresh-run seed — departures ride the existing departTrailer re-dispatch cadence.
- captureContinuation (lines 1722-1741): pendingBySpoke captured at 1724-1726 — add pendingAtSpoke alongside.

From packages/simulation/src/continuation.ts:
- SimTask union (line 27); arriveOverCarriedAtCenter variant at line 51 ({ kind; trailerId; packageId; tripId }). Add a parallel arriveConsolidationAtCenter variant carrying `packageIds: readonly string[]` (the drained manifest) + trailerId + tripId.
- SerializedWorldState (line 80): pendingBySpoke field at line 82. Add `readonly pendingAtSpoke: readonly (readonly [string, readonly string[]])[];` mirroring it.
- SerializedRngStates (line 102): DO NOT add a consolidation field — no new RNG (Pitfall 1).

From RESEARCH RNG Decision: consolidation reuses induction-origin/center-distribution freight already drawn. Discretionary choices (which spoke consolidates, cadence, freight selection) MUST be deterministic — modular tick arithmetic (the sortWave precedent draws NO RNG) + stable trailerId/freightId sort. Apply the deterministic sort key [priority]+[tick]+[freightId] to pendingAtSpoke ONLY; never perturb pendingBySpoke's FIFO order.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: consolidationEnabled flag + pendingAtSpoke manifest (init/restore/atomic-drain/capture) + continuation serialization</name>
  <files>
    packages/simulation/src/engine.ts,
    packages/simulation/src/continuation.ts
  </files>

  <read_first>
    1. packages/simulation/src/engine.ts — read the pendingBySpoke lifecycle (966-972 init/restore, 1283-1284 atomic drain, 1724-1726 capture), the inductionEnabled flag (281) + inductionOn gate (492), center/spokes (625-626), the line-1026 pendingBySpoke re-stage push.
    2. packages/simulation/src/continuation.ts — read SimTask union (27-51), SerializedWorldState (80-99, pendingBySpoke at 82), SerializedRngStates (102-112).
    3. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 1 (the 4 touch-points) + Concern 3 (the flag) + the "Determinism note" (no new RNG).
    4. .planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md Pattern 1 + Pattern 2 + Pitfall 1 + Pitfall 2 + the RNG Decision.
  </read_first>

  <behavior>
    Add the flag + the manifest with the 4 touch-points, all gated behind consolidationOn (off path = zero new behavior):

    - SimulateOptions: `readonly consolidationEnabled?: boolean;` (DEFAULT FALSE; JSDoc per the inductionEnabled style).
    - Gate: `const consolidationOn = opts.consolidationEnabled === true;`.
    - pendingAtSpoke: `const pendingAtSpoke = new Map<string, string[]>();` with init/restore mirroring pendingBySpoke (restore from start.world.pendingAtSpoke when resuming; default-empty per spoke). Guard construction/use so the off path neither populates nor reads it (when consolidationOn is false, pendingAtSpoke stays empty and is captured as empty — but the off-path goldens must be byte-identical, so prefer to only POPULATE it when consolidationOn; an empty serialized field that matches the resuming shape is acceptable only if it does not change the off-path event stream — verify via the DET-01 byte-identical test).
    - Atomic drain: when a consolidation trailer departs spoke→center, `const loaded = manifest.splice(0, manifest.length)` (atomic; the double-drain guard). Apply the deterministic sort key ([priority]+[tick]+[freightId]) to pendingAtSpoke BEFORE the splice; sort idle trailers by trailerId; evaluate one-by-one so a second trailer at the same spoke sees [] (a valid empty return).
    - Capture: add `pendingAtSpoke: [...pendingAtSpoke.entries()].map(([k,v]) => [k,[...v]] as const)` to captureContinuation's world (mirror pendingBySpoke at 1724-1726).
    - continuation.ts: add `readonly pendingAtSpoke: readonly (readonly [string, readonly string[]])[];` to SerializedWorldState. DO NOT touch SerializedRngStates.

    NO new RNG. The off path makes zero new draws and emits zero new events.
  </behavior>

  <action>
    engine.ts: add consolidationEnabled to SimulateOptions (after inductionEnabled, line 281); add `const consolidationOn = opts.consolidationEnabled === true;` (near line 492); add pendingAtSpoke Map with init/restore mirroring pendingBySpoke (lines 966-972 pattern); add pendingAtSpoke to captureContinuation's world object (line 1724-1726 pattern). Gate population strictly behind consolidationOn.

    continuation.ts: add the pendingAtSpoke field to SerializedWorldState (mirror pendingBySpoke at line 82). Leave SerializedRngStates unchanged.

    Run the DET-01 byte-identical test from 21-02 to confirm the off path is unchanged before adding the departure/arrival logic in Task 2. Commit: `feat(21-04): FLOW-01 pendingAtSpoke manifest + consolidationEnabled flag + continuation capture`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm vitest run packages/simulation/test/determinism.unit.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm typecheck` exits 0 — pendingAtSpoke serialization types clean under strict TS.
    - `vitest run packages/simulation/test/determinism.unit.test.ts` exits 0 — the seed-42@10000 golden 3920accc… AND the new consolidationEnabled:false byte-identical case (from 21-02) are green (FLOW-03 regression / the determinism keystone).
    - `grep -c "consolidationEnabled" packages/simulation/src/engine.ts` returns ≥ 2 (flag + gate).
    - `grep -c "pendingAtSpoke" packages/simulation/src/engine.ts` returns ≥ 4 (init, restore, drain, capture).
    - `grep -c "pendingAtSpoke" packages/simulation/src/continuation.ts` returns ≥ 1 (SerializedWorldState field).
    - `grep -c "consolidation" packages/simulation/src/continuation.ts` and `grep -ci "RNG_SALT" packages/simulation/src/engine.ts` show NO new consolidation salt added (the salt count is unchanged from pre-Phase-21 — confirm against 7 existing salts).
  </acceptance_criteria>

  <done>
    consolidationEnabled + pendingAtSpoke (4 touch-points) land; the off path is byte-identical (golden + DET-01 green); no new RNG; continuation serializes pendingAtSpoke.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidation departure (FLOW-01) + arriveConsolidationAtCenter re-sort (FLOW-02) + empty-return guard (FLOW-03)</name>
  <files>
    packages/simulation/src/engine.ts,
    packages/simulation/src/continuation.ts
  </files>

  <read_first>
    1. packages/simulation/src/engine.ts — read the over-carry return leg (1503-1547) + arriveOverCarriedAtCenter (1581-1613) + the dispatch switch (1635-1666) + the departTrailer drain site (1283) + the center distribution re-stage push (1026).
    2. packages/simulation/src/continuation.ts — read the arriveOverCarriedAtCenter SimTask variant (line 51) to mirror.
    3. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 2 (the departure + center re-sort + dispatch + SimTask variant excerpts).
    4. .planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md Pattern 3 + Pitfall 3 (double-drain) + Open Question 3 (empty-return threshold — pick the simplest deterministic rule).
    5. packages/simulation/test/consolidation-determinism.unit.test.ts (from 21-02) — the FLOW-01/02/03 assertions this task must satisfy.
  </read_first>

  <behavior>
    GREEN the 21-02 consolidation tests:

    - FLOW-01 departure: when a consolidation trailer departs spoke→center (gated on consolidationOn; deterministic cadence via modular tick arithmetic on the existing departTrailer re-dispatch, NO RNG), drain pendingAtSpoke[spoke] via the atomic splice (Task 1), emit a TrailerDeparted with fromHubId=spoke, toHubId=center, packageIds=loaded (NON-EMPTY when freight is staged). Sort idle trailers by trailerId; one trailer empties the manifest; the next sees [] (empty return).
    - Schedule the center arrival: `schedule(arriveTick + drawTransitTicks(spoke, center), { kind: "arriveConsolidationAtCenter", trailerId, packageIds: loaded, tripId })` — mirror the over-carry return-arrival schedule (carry the packageIds ARRAY, not a single id).
    - FLOW-02 arriveConsolidationAtCenter (new function + dispatch case): emit TrailerArrivedAtHub + TrailerDocked (mirror arriveOverCarriedAtCenter), then for EACH package: unload PackageScanned scanType=unload + PackageArrivedAtHub@center, then RE-STAGE into pendingBySpoke[destSpokeOf(packageId)] via the line-1026 push so the existing center→spoke distribution picks it up (the cross-dock). Resolve destSpoke from the package's destHubId (induction-origin packages carry it; deterministic lookup).
    - FLOW-03 empty-return guard: pick the SIMPLEST deterministic rule from Open Q3 — a consolidation departure fires on the same trailer re-dispatch cadence; an empty manifest departs empty (no special threshold) BUT do not SILENTLY emit empty returns where it would perturb determinism — gate the empty departure on consolidationOn and the deterministic cadence so an empty return only occurs as a deterministic consequence of an empty pendingAtSpoke at a scheduled departure. Document the chosen rule in a code comment + the SUMMARY.
    - continuation.ts: add the arriveConsolidationAtCenter SimTask variant (DATA: { kind; trailerId; packageIds: readonly string[]; tripId }) — never a closure (or the run is not resumable).
  </behavior>

  <action>
    engine.ts: add the consolidation departure logic at the departTrailer site (gated on consolidationOn, deterministic cadence, atomic splice + deterministic sort key on pendingAtSpoke). Add `arriveConsolidationAtCenter(trailerId, packageIds, tripId)` mirroring arriveOverCarriedAtCenter, looping the packageIds: unload + PackageArrivedAtHub@center + re-stage push into pendingBySpoke[destSpoke]. Add the dispatch case. Implement the deterministic empty-return guard.

    continuation.ts: add the arriveConsolidationAtCenter DATA variant to the SimTask union.

    Commit: `feat(21-04): FLOW-01/02/03 consolidation departure + center re-sort + empty-return guard`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm vitest run packages/simulation/test/consolidation-determinism.unit.test.ts packages/simulation/test/continuation-equivalence.unit.test.ts packages/simulation/test/determinism.unit.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `vitest run packages/simulation/test/consolidation-determinism.unit.test.ts` exits 0 — FLOW-01 (non-empty spoke-origin manifest + no double-drain), FLOW-02 (cross-dock re-stage), FLOW-03 (empty return valid) all green.
    - `vitest run packages/simulation/test/continuation-equivalence.unit.test.ts` exits 0 — the dedicated "consolidation" FEATURE_CASE AND all-on (with consolidationEnabled) are byte-identical chunked vs all-at-once (pendingAtSpoke capture proven; FLOW-03 keystone).
    - `vitest run packages/simulation/test/determinism.unit.test.ts` exits 0 — golden 3920accc… + DET-01 off-path still green (no regression).
    - `grep -c "arriveConsolidationAtCenter" packages/simulation/src/engine.ts` returns ≥ 2 (function def + dispatch case).
    - `grep -c "arriveConsolidationAtCenter" packages/simulation/src/continuation.ts` returns ≥ 1 (SimTask DATA variant).
    - `grep -c "pendingBySpoke.get(.*push\|pendingBySpoke.get(dest" packages/simulation/src/engine.ts` returns ≥ 1 (re-stage into pendingBySpoke[destSpoke] — the cross-dock, FLOW-02).
  </acceptance_criteria>

  <done>
    Consolidation trailers carry real freight spoke→center; the center re-stages into pendingBySpoke[destSpoke]; empty returns are deterministic; the consolidation continuation-equivalence case is byte-identical; goldens preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine world-state → event stream | The deterministic event-queue core owns all freight movement; the integrity threat is double-drain (two trailers take the same package) or a non-resumable continuation, not untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Tampering | Double-drain (two trailers take the same packages) | mitigate | Atomic `manifest.splice(0, length)` removes-and-returns in one statement; idle trailers processed in stable trailerId order; one trailer empties the manifest, the next sees []. The 21-02 no-double-drain test asserts no packageId appears in two spoke-origin manifests. |
| T-21-11 | Tampering | Continuation divergence (chunked ≠ all-at-once) | mitigate | pendingAtSpoke captured in SimContinuation.world + the arriveConsolidationAtCenter SimTask is a DATA variant (never a closure); the dedicated consolidation FEATURE_CASE (chunk boundary mid-consolidation) proves byte-identity. |
| T-21-12 | Tampering | Golden drift via spurious RNG | mitigate | NO new RNG salt/substream; consolidation reuses existing freight; cadence/selection are deterministic (modular tick arithmetic + stable sort). The seed-42@10000 golden 3920accc… and the consolidationEnabled:false byte-identical case are re-asserted. |

No high-severity threats. Pure deterministic engine logic; no auth/crypto/external-input surface.
</threat_model>

<verification>
After this plan:
- `pnpm typecheck` green
- `pnpm vitest run packages/simulation` green — consolidation-determinism, continuation-equivalence (consolidation + all-on), determinism (golden + DET-01) all green
- The empty-return rule + the (no) RNG decision documented in the SUMMARY
</verification>

<success_criteria>
1. consolidationEnabled:false is byte-identical to pre-Phase-21 (golden + DET-01); no empty returns when off
2. consolidationEnabled:true drains real freight via atomic splice (no double-drain); center re-stages into pendingBySpoke[destSpoke]
3. pendingAtSpoke captured/restored; the consolidation continuation-equivalence case is byte-identical
4. Empty return is valid + deterministic
5. NO new RNG salt/substream; salt-collision test untouched
</success_criteria>

<output>
After completion, create `.planning/phases/21-bidirectional-freight-consolidation/21-04-SUMMARY.md` (record the empty-return rule + the no-new-RNG confirmation)
</output>
