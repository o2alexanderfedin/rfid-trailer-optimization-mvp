---
phase: 21-bidirectional-freight-consolidation
plan: 06
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/projections/src/detector.ts
  - packages/projections/src/detector-bound.unit.test.ts
autonomous: true
requirements:
  - FLOW-04

must_haves:
  truths:
    - "runDetection's projection reads (makeProjectionReads) are scoped to ACTIVE packages so detection cost does not scale with total-ever packages (the detection-cost-scales-with-state debt) under continuous bidirectional freight"
    - "The active predicate is defined explicitly (A3: no is_active column exists today) — scoped via a not-yet-terminal read predicate, with the choice documented; the detector pure core is untouched (DIP preserved)"
    - "A BOUNDED benchmark at ~1–5k packages (NOT 10k — GATE-HYGIENE) asserts detection time does not grow superlinearly with inactive state"
    - "Existing detection behavior is unchanged for active packages — the wrong-trailer/missed-unload detection results are identical for the active set"
  artifacts:
    - path: packages/projections/src/detector.ts
      provides: "active-scoped makeProjectionReads queries (filtered SELECTs replacing the unscoped selectAll); the active-predicate decision documented in JSDoc"
      contains: "active"
    - path: packages/projections/src/detector-bound.unit.test.ts
      provides: "BOUNDED perf assertion at ~1–5k packages + an equivalence assertion that active-set detection results are unchanged"
      contains: "detector"
  key_links:
    - from: packages/projections/src/detector.ts
      to: packages/projections/src/detector.ts
      via: "makeProjectionReads filters trailer_state/zone_estimate reads to active packages; the runDetection pure core is unchanged"
      pattern: "active"
---

<objective>
Scope `runDetection`'s projection reads to ACTIVE packages so detection cost stops scaling with total-ever packages (the detection-cost-scales-with-state tech debt, worsened by continuous bidirectional freight), with a bounded benchmark at ~1–5k packages.

Purpose: FLOW-04 detection bounding. Under continuous induction + bidirectional consolidation, the unscoped `selectAll()` reads in `makeProjectionReads` grow with every package ever. Scoping to active packages keeps detection cost bounded. The active predicate is a NET-NEW concept (no is_active column today — A3); this plan resolves it via a not-yet-terminal read predicate (no schema change, DIP-clean).

Output: detector.ts makeProjectionReads scoped to active packages (the pure core untouched); a bounded perf test (~1–5k, NOT 10k) + an active-set equivalence test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-CONTEXT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md
@.planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md
@.planning/phases/21-bidirectional-freight-consolidation/21-VALIDATION.md

<interfaces>
<!-- All line anchors verified against the live codebase this session. -->

From packages/projections/src/detector.ts:
- runDetection (line 120) reads four ports in parallel and runs the PURE detection core — do NOT change the core.
- makeProjectionReads (line 165) is the read adapter (DIP). The reads to scope:
  - readPlannedAssignments: `db.selectFrom("trailer_state").selectAll().execute()` (~line 174) — iterates assigned_package_ids.
  - readObserved: `db.selectFrom("zone_estimate").selectAll().execute()` (~line 186).
  - readDepartedHubs: a SECOND `db.selectFrom("trailer_state").selectAll().execute()` (~line 205), gated on status === "in_transit".
- The active concept: trailer_state.status (e.g. "idle" | "in_transit" | ...) and zone_estimate rows for packages still in flight. A package is ACTIVE while still moving toward its destination (not yet terminal). There is NO is_active column today (A3).

From packages/projections/src/schema.sql:
- trailer_state has a status column + idx_trailer_state_current_hub index. zone_estimate is keyed (package_id, trailer_id) with no terminal flag.
- IMPORTANT: Phase 22 introduces PackageDelivered (terminal) + projection purge. Phase 21 must NOT depend on that; choose an active predicate that works WITHOUT PackageDelivered (e.g. trailer in_transit / non-idle, or zone rows whose package has not arrived at its final dest). Document the predicate so Phase 22's purge composes cleanly.

From RESEARCH Pitfall 6 + A3: the read-scoping lives in makeProjectionReads; benchmark at a BOUNDED state size (~1–5k packages, NOT 10k — GATE-HYGIENE).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope makeProjectionReads to active packages + bounded benchmark + active-set equivalence (FLOW-04)</name>
  <files>
    packages/projections/src/detector.ts,
    packages/projections/src/detector-bound.unit.test.ts
  </files>

  <read_first>
    1. packages/projections/src/detector.ts — read runDetection (120-148) + makeProjectionReads (165+) FULLY; note the three selectAll reads (trailer_state ×2, zone_estimate ×1) and the in_transit gate in readDepartedHubs.
    2. packages/projections/src/schema.sql — read trailer_state (status column + index) + zone_estimate to confirm what an active predicate can filter on without a schema change.
    3. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 7 (the unscoped reads + the role-match: scoping lives in the adapter).
    4. .planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md Pitfall 6 (detection cost) + A3 (no is_active column — choose where active lives).
    5. Any existing detector test in packages/projections/ for the established in-memory snapshot / projection-fixture test shape.
  </read_first>

  <behavior>
    RESOLVE A3 (where active lives): scope the makeProjectionReads queries via a not-yet-terminal PREDICATE rather than adding an is_active column — this keeps the change a read-query concern (DIP-clean) and composes with Phase 22's future PackageDelivered purge without a schema migration. Active = a package still in flight toward its destination; concretely, filter to:
      - readPlannedAssignments / readDepartedHubs: trailer_state rows whose status can still produce a detection signal (e.g. `.where("status", "in", ["in_transit", ...])` or exclude idle trailers carrying no assigned packages).
      - readObserved: zone_estimate rows for packages still active. If no direct column exists, scope via the same active-trailer set or a bounded predicate; document the exact predicate chosen.
    The detection RESULTS for the active set must be IDENTICAL to the unscoped behavior — inactive/terminal packages produce no detection signal anyway, so scoping them out changes cost, not results. Document the active-predicate decision in the makeProjectionReads JSDoc + the SUMMARY (A3 resolved).

    RED tests first (detector-bound.unit.test.ts, BOUNDED ≤5k):
    - perf bound: build a projection state with ~1–5k packages where most are INACTIVE (terminal) and a small active set; assert runDetection time does not grow superlinearly with the inactive count (ratio-based: detection over 5k-mostly-inactive completes within a small constant multiple of detection over the active-only subset — NOT a flaky absolute-ms gate).
    - active-set equivalence: assert the detected exceptions are IDENTICAL between the scoped reads and a reference unscoped read over the SAME active set (scoping neither drops nor adds an exception for an active package).
    Keep state ≤5k (NOT 10k — GATE-HYGIENE). The perf assertion MUST be ratio-based / robust so it is not flaky.
  </behavior>

  <action>
    detector.ts: in makeProjectionReads, replace the unscoped selectAll reads with active-scoped queries (Kysely `.where(...)` predicates per <behavior>). Do NOT change runDetection's pure core or the DetectorReads port shape. Add a JSDoc note recording the active-predicate decision (A3 resolved; composes with Phase 22 purge).

    detector-bound.unit.test.ts: write the bounded perf test (~1–5k, ratio-based) + the active-set equivalence test. RED first, then GREEN.

    Commit: `feat(21-06): FLOW-04 detection active-scoping + bounded benchmark`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm vitest run packages/projections/src/detector-bound.unit.test.ts packages/projections</automated>
  </verify>

  <acceptance_criteria>
    - `vitest run packages/projections/src/detector-bound.unit.test.ts` exits 0 — the bounded perf assertion (~1–5k, ratio-based) + the active-set equivalence test are green (FLOW-04 VALIDATION row "detection bounded — is_active scoping benchmark at ~1–5k packages").
    - `pnpm vitest run packages/projections` exits 0 — the existing detector + reducer suites are unchanged (no detection regression for active packages).
    - `grep -c "\.where(" packages/projections/src/detector.ts` returns ≥ 2 (the previously-unscoped reads now filter to active).
    - The detector-bound test's largest package count is ≤ 5000 (GATE-HYGIENE — confirm by reading the test fixture size).
    - The active-predicate decision is recorded in the makeProjectionReads JSDoc (verify by reading the touched JSDoc) and does NOT reference PackageDelivered (Phase 22 independence).
    - `grep -c "is_active\|ALTER TABLE\|ADD COLUMN" packages/projections/src/schema.sql` returns 0 (no schema migration introduced — the predicate lives in the read adapter).
  </acceptance_criteria>

  <done>
    Detection reads are scoped to active packages (the pure core untouched); a bounded ratio-based benchmark proves cost no longer scales with inactive state; active-set results are unchanged; no schema migration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Projection reads → detection core | The adapter reads trusted projection state; the threat surface is a DoS-style cost growth (detection scaling with total-ever packages) and a correctness regression (scoping drops an active-package signal), not untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-17 | Denial of Service | Detection cost scales with total-ever packages | mitigate | makeProjectionReads filters to active packages so detection cost tracks the active set, not the historical total; the bounded ratio-based benchmark (~1–5k) witnesses the non-superlinear behavior. |
| T-21-18 | Tampering | Scoping silently drops an active-package detection signal | mitigate | The active-set equivalence test asserts detected exceptions are identical between scoped and unscoped reads over the same active set — scoping changes cost, not results. |
| T-21-19 | Tampering | SQL predicate correctness | mitigate | Parameterized Kysely `.where(...)` (no string concatenation); the predicate is documented and excludes only terminal/inactive rows that produce no signal. Low severity (internal projection reads). |

No high-severity threats. Internal projection-read adapter; no auth/crypto/external-input surface.
</threat_model>

<verification>
After this plan:
- `pnpm typecheck` green
- `pnpm vitest run packages/projections` green — detector-bound (perf + equivalence) + existing detector/reducer suites
- The active-predicate decision (A3) documented in JSDoc + SUMMARY, Phase-22-independent
</verification>

<success_criteria>
1. makeProjectionReads reads are scoped to active packages (the pure detection core untouched)
2. A bounded ratio-based benchmark at ~1–5k (NOT 10k) shows detection cost does not grow superlinearly with inactive state
3. Active-set detection results are identical (equivalence test green)
4. No schema migration / is_active column — the predicate lives in the read adapter; composes with Phase 22's purge
</success_criteria>

<output>
After completion, create `.planning/phases/21-bidirectional-freight-consolidation/21-06-SUMMARY.md` (record the A3 active-predicate decision)
</output>
