---
phase: 21-bidirectional-freight-consolidation
plan: 08
type: execute
wave: 3
depends_on:
  - 21-04-PLAN.md
  - 21-05-PLAN.md
files_modified:
  - packages/api/src/routes/hub-detail.ts
  - packages/api/src/routes/hub-detail.test.ts
  - packages/web/src/panels/HubBalance.tsx
  - packages/web/src/panels/HubBalance.test.tsx
autonomous: false
requirements:
  - FLOW-05

must_haves:
  truths:
    - "The hub-detail read API surfaces per-hub inbound/outbound inventory balance (cross-dock heat) from the existing hub_inventory projection (the same projection the optimizer consumes — Decision 3)"
    - "An operator panel widget displays the inbound/outbound balance numerically (cross-dock utilization), mirroring the MoneySlide fetch + pure-helper pattern"
    - "The read API returns a valid empty/zero balance for an unseen hub (no throw); the DTO is an explicit readonly wire shape with stable ordering"
    - "FLOW-05 is P2 — it ships in this phase; the full phase gate is green before sign-off"
  artifacts:
    - path: packages/api/src/routes/hub-detail.ts
      provides: "inbound/outbound balance counts added to the HubDetailDto from hub_inventory"
      contains: "inbound"
    - path: packages/web/src/panels/HubBalance.tsx
      provides: "operator panel widget showing inbound/outbound balance + pure exported helpers"
      contains: "balance"
  key_links:
    - from: packages/api/src/routes/hub-detail.ts
      to: packages/web/src/panels/HubBalance.tsx
      via: "GET /hubs/:id/detail returns inbound/outbound counts; the panel fetches + renders them"
      pattern: "inbound"
---

<objective>
FLOW-05 (P2): a per-hub inbound/outbound inventory balance display (cross-dock utilization heat) showing the consolidation value numerically — a read-API addition over the existing `hub_inventory` projection + an operator panel widget. Then run the full phase gate and sign off Phase 21.

Purpose: FLOW-05 surfaces the consolidation cross-dock value as a number (how much freight flows in vs out at the center). It is P2 (must still ship + be covered). This plan also carries the final full-gate verification for the phase.

Output: hub-detail read API + DTO carry inbound/outbound balance; an operator panel widget renders it; route + panel tests green; the full phase gate (build + typecheck + lint + test:all) is green; a human-verify checkpoint confirms the panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-CONTEXT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md
@.planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md
@.planning/phases/21-bidirectional-freight-consolidation/21-VALIDATION.md
@.planning/phases/21-bidirectional-freight-consolidation/21-04-SUMMARY.md
@.planning/phases/21-bidirectional-freight-consolidation/21-05-SUMMARY.md

<interfaces>
<!-- All line anchors verified against the live codebase this session. -->

From packages/api/src/routes/hub-detail.ts:
- HubDetailDto interface (line 91) — the explicit readonly wire shape; add inbound/outbound balance counts.
- registerHubDetailRoutes (line 200): GET /hubs/:id/detail (line 205); the projection reads (Promise.all over trailer_state etc., ~214-230). Add a hub_inventory read for the hub (`db.selectFrom("hub_inventory").where(...).execute()`) and surface inbound/outbound counts. The route is a PURE read over projections (no writes).
- The route returns a valid empty answer for an unseen hub (~232-234) and uses stable id-sorted output (~316-318) — preserve that discipline.
- hub_inventory projection columns: inbound / outbound / staged buckets (per the projections schema FND-07) — read the inbound + outbound counts for the balance.

From packages/web/src/panels/MoneySlide.tsx (the panel template):
- fetch via ../api/client.js + exported PURE helpers (formatDelta, winClass, comparisonRows) that are unit-tested separately; React 19, strict TS no any.
- The new HubBalance panel mirrors this: a fetch of the hub-detail DTO + pure exported helpers (e.g. a balance formatter / a heat class) that are unit-tested.

From packages/web/src/panels/ existing tests — the jsdom (ui) test lane shape for a panel (RTL render + pure-helper unit tests).

From RESEARCH Concern 9 / Pattern: FLOW-05 reads the SAME hub_inventory projection the optimizer consumes (Decision 3); lowest priority (P2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hub-detail read API inbound/outbound balance (FLOW-05)</name>
  <files>
    packages/api/src/routes/hub-detail.ts,
    packages/api/src/routes/hub-detail.test.ts
  </files>

  <read_first>
    1. packages/api/src/routes/hub-detail.ts — read HubDetailDto (91), registerHubDetailRoutes (200-323) incl. the projection reads (214-230), the unseen-hub empty answer (232-234), the stable id-sorted output (316-318).
    2. packages/projections/src/schema.sql — read the hub_inventory table (inbound/outbound/staged buckets) to confirm the columns/shape to count.
    3. packages/api/src/routes/hub-detail.test.ts — read for the established route-test shape (fixture projection state + DTO assertions).
    4. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 9 (the read-API route pattern + DTO discipline).
  </read_first>

  <behavior>
    - Add an inbound/outbound balance to HubDetailDto: explicit readonly fields, e.g. `readonly inventoryBalance: { readonly inbound: number; readonly outbound: number };` (Claude's discretion on exact shape — keep it explicit + readonly). Optionally a derived cross-dock heat ratio, computed in the route or as a pure helper.
    - In registerHubDetailRoutes, read the hub's hub_inventory row (`.where("hub_id", "=", hubId)`) and surface the inbound + outbound bucket counts. Preserve the unseen-hub valid-empty answer (zero balance, no throw) and the stable id-sorted output discipline.

    RED test first (hub-detail.test.ts): a hub with N inbound + M outbound packages in hub_inventory returns inventoryBalance { inbound: N, outbound: M }; an unseen hub returns a zero balance without throwing.
  </behavior>

  <action>
    hub-detail.ts: extend HubDetailDto with the inventoryBalance shape; read hub_inventory for the hub in the route and populate it; preserve the empty-hub + stable-ordering discipline. Write the RED route test, then GREEN.

    Commit: `feat(21-08): FLOW-05 hub-detail inbound/outbound balance`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm vitest run packages/api/src/routes/hub-detail.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `vitest run packages/api/src/routes/hub-detail.test.ts` exits 0 — balance-present + unseen-hub-zero tests green (FLOW-05 VALIDATION row "hub inbound/outbound balance in read API").
    - `grep -c "inventoryBalance\|inbound" packages/api/src/routes/hub-detail.ts` returns ≥ 1 (the balance surfaced on the DTO).
    - `grep -c "hub_inventory" packages/api/src/routes/hub-detail.ts` returns ≥ 1 (reads the same projection the optimizer consumes — Decision 3).
    - `pnpm typecheck` exits 0 (explicit readonly DTO clean).
  </acceptance_criteria>

  <done>
    The hub-detail read API surfaces inbound/outbound balance from hub_inventory; unseen hubs return a zero balance without error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Operator panel HubBalance widget + pure helpers (FLOW-05)</name>
  <files>
    packages/web/src/panels/HubBalance.tsx,
    packages/web/src/panels/HubBalance.test.tsx
  </files>

  <read_first>
    1. packages/web/src/panels/MoneySlide.tsx — read FULLY; the fetch via ../api/client.js + exported PURE helpers + React 19 strict-TS pattern is the template.
    2. An existing panel test in packages/web/src/panels/ — the jsdom (ui) RTL render + pure-helper unit-test shape.
    3. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 9 (the web panel pattern).
    4. packages/api/src/routes/hub-detail.ts (after Task 1) — the inventoryBalance DTO shape the panel fetches.
  </read_first>

  <behavior>
    - HubBalance.tsx: a panel widget that fetches the hub-detail DTO (via the api client) and displays the inbound/outbound balance numerically (cross-dock heat). Export PURE helpers (e.g. `formatBalance(inbound, outbound)` and a `heatClass(ratio)` returning a CSS class) that are unit-tested separately. React 19, strict TS, no any. Clean minimal UI (pragmatic — spacing/alignment/contrast per the developer profile), styling is Claude's discretion.

    RED tests first (HubBalance.test.tsx, jsdom ui lane): the pure helpers return the expected formatted strings / heat classes for representative balances; an RTL render with a mocked fetch shows the inbound/outbound numbers.
  </behavior>

  <action>
    HubBalance.tsx: implement the panel + the pure exported helpers mirroring MoneySlide.tsx. HubBalance.test.tsx: write the pure-helper unit tests + an RTL render test (mock the fetch). RED first, then GREEN.

    Commit: `feat(21-08): FLOW-05 HubBalance operator panel + pure helpers`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm vitest run --project ui packages/web/src/panels/HubBalance.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `vitest run --project ui packages/web/src/panels/HubBalance.test.tsx` exits 0 — pure-helper + RTL-render tests green (FLOW-05 VALIDATION row "operator panel").
    - `grep -c "export function\|export const" packages/web/src/panels/HubBalance.tsx` returns ≥ 2 (the component + ≥1 pure helper exported for unit testing).
    - `grep -ci "any" packages/web/src/panels/HubBalance.tsx | head` shows no `: any` usage (strict TS — confirm by reading; no explicit any).
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>

  <done>
    The operator panel displays the inbound/outbound cross-dock balance; pure helpers are unit-tested; React 19 strict-TS clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>FLOW-05 + Phase Gate: hub balance panel visible + full gate green</name>
  <what-built>
    FLOW-05 (P2): the operator UI now shows a per-hub inbound/outbound inventory balance (cross-dock utilization heat) from the live hub_inventory projection. This is the final Phase-21 checkpoint — it also gates on the FULL phase gate being green (build + typecheck + lint + test:all, including the seed-42@10000 golden 3920accc… and the consolidationEnabled:true continuation-equivalence case).
  </what-built>
  <how-to-verify>
    1. Run the full phase gate locally: `pnpm build && pnpm typecheck && pnpm lint && pnpm test:all`. Confirm it is GREEN, including the seed-42@10000 golden and the dedicated consolidation continuation-equivalence case (≈15 min per GATE-HYGIENE).
    2. Start the demo (API + web) with consolidationEnabled:true.
    3. Open a hub detail panel (click a hub on the map).
    4. Confirm the inbound/outbound balance is visible and numerically sensible (a center hub under active consolidation shows inbound from consolidation legs and outbound to distribution legs — the cross-dock heat).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue (gate red, panel missing/wrong numbers).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Read API → operator UI | GET /hubs/:id/detail is a pure read over projections; the threat surface is a malformed/empty-hub response and the (low) injection surface on the hub id param, not state mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-23 | Denial of Service | Unseen-hub request | mitigate | The route returns a valid zero balance for an unseen hub (no throw) — the established hub-detail discipline; the route test asserts it. |
| T-21-24 | Tampering | hub id param injection | mitigate | The id param is validated by the existing idParamsSchema (Fastify schema) and used in parameterized Kysely `.where("hub_id", "=", hubId)` — no string concatenation. |
| T-21-25 | Information Disclosure | hub balance counts | accept | inbound/outbound counts are aggregate simulation state; no PII; no ASVS L1 control implicated for an internal read API. |

No high-severity threats. Read-only route + a display panel; no auth/crypto/state-mutation surface.
</threat_model>

<verification>
After this plan:
- `pnpm typecheck` green
- `pnpm vitest run packages/api/src/routes/hub-detail.test.ts` + `--project ui packages/web/src/panels/HubBalance.test.tsx` green
- The FULL phase gate (`pnpm build && pnpm typecheck && pnpm lint && pnpm test:all`) green, including the seed-42@10000 golden + the consolidation continuation-equivalence case
- The human-verify checkpoint approves the panel + the green gate
</verification>

<success_criteria>
1. The hub-detail read API surfaces inbound/outbound balance from hub_inventory (Decision 3); unseen hubs return a zero balance
2. An operator panel renders the balance numerically with unit-tested pure helpers (React 19, strict TS)
3. Route + panel tests green
4. The full phase gate is green (build + typecheck + lint + test:all) including the golden + the consolidation continuation-equivalence case
</success_criteria>

<output>
After completion, create `.planning/phases/21-bidirectional-freight-consolidation/21-08-SUMMARY.md`
</output>
</content>
