---
phase: 21-bidirectional-freight-consolidation
plan: 03
type: tdd
wave: 2
depends_on:
  - 21-01-PLAN.md
files_modified:
  - packages/projections/src/reducers/hub-inventory.ts
  - packages/projections/src/reducers/package-location.ts
  - packages/projections/src/reducers/trailer-state.ts
  - packages/projections/src/reducers/zone-estimate.ts
  - packages/projections/src/reducers/driver-status.ts
  - packages/projections/src/reducers/driver-assignment.ts
  - packages/projections/src/reducers/trailer-fuel.ts
  - packages/projections/src/reducers/tag-registry.ts
  - packages/projections/src/reducers/exceptions.ts
  - packages/projections/src/reducers/geo-track.ts
  - packages/projections/src/reducers/audit-timeline.ts
  - packages/projections/src/reducers/hub-inventory.test.ts
autonomous: true
requirements:
  - FLOW-04

must_haves:
  truths:
    - "All 11 reducers handle PlanSuperseded exhaustively (pnpm build is the primary witness via assertNeverEvent/assertNeverAudit/assertNeverGeo) — 10 are no-ops, hub-inventory folds it"
    - "RESOLVED DESIGN DECISION (Open-Q1/A2): PlanAccepted STAYS A NO-OP for hub_inventory.staged (its payload carries NO packageIds — schemas.ts:230 — so it cannot stage). PlanSuperseded is the ONLY stage-mutating plan event: a dumb pure delete-then-apply that wipes the prior plan's staged scope (supersededPackageIds it carries) — items in the OLD plan but absent in the NEW are wiped, not stranded"
    - "The existing unload-scan staging path (PackageScanned scanType=unload → staged) STILL works for non-plan freight — a regression test proves both paths coexist"
    - "Staged is never double-counted: after PlanSuperseded for a scope, no superseded packageId remains in staged"
  artifacts:
    - path: packages/projections/src/reducers/hub-inventory.ts
      provides: "PlanAccepted remains a no-op for staged; PlanSuperseded delete-then-apply wipes supersededPackageIds from staged"
      contains: "PlanSuperseded"
    - path: packages/projections/src/reducers/hub-inventory.test.ts
      provides: "supersession delete-then-apply test + unload-scan-staging regression test + no-double-count test"
      contains: "PlanSuperseded"
  key_links:
    - from: packages/domain/src/events/domain-event.ts
      to: packages/projections/src/reducers/hub-inventory.ts
      via: "case PlanSuperseded folds supersededPackageIds out of staged (delete-then-apply)"
      pattern: "PlanSuperseded"
    - from: packages/projections/src/reducers/reducer.ts
      to: packages/projections/src/reducers/package-location.ts
      via: "assertNeverEvent exhaustiveness forces a PlanSuperseded case in every reducer"
      pattern: "PlanSuperseded"
---

<objective>
Fold `PlanSuperseded` into all 11 projection reducers and RESOLVE the supersession-target design decision (Open-Q1/A2): `PlanAccepted` stays a no-op for `hub_inventory.staged` (its payload carries NO packageIds), and `PlanSuperseded` is the only stage-mutating plan event — a dumb pure delete-then-apply (per D-21-1) that wipes the prior plan's staged scope, while preserving the existing unload-scan staging path for non-plan freight.

Purpose: This is the single most important FLOW-04 design decision (research Open Q1). The supersession MECHANISM is locked (explicit event + delete-then-apply); the TARGET projection is resolved here explicitly against the ACTUAL `PlanAccepted` contract, with a regression test that the unload-scan staging path still works. No double-counting at the center.

Output: 11 reducers handle PlanSuperseded (build-gated exhaustive); hub-inventory folds supersession; a reducer test proves delete-then-apply + unload-scan coexistence + no-double-count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-CONTEXT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md
@.planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md
@.planning/phases/21-bidirectional-freight-consolidation/21-VALIDATION.md
@.planning/phases/21-bidirectional-freight-consolidation/21-01-SUMMARY.md

<interfaces>
<!-- All line anchors verified against the live codebase this session. -->

From packages/projections/src/reducers/hub-inventory.ts:
- InventoryBucket = "inbound" | "outbound" | "staged".
- withoutPackage(hub, packageId) (lines 80-87) removes a package from inbound/outbound/staged — the deterministic remove primitive to reuse.
- placePackage(state, packageId, target | null) — places into a bucket or removes when target is null; the TrailerDeparted manifest-decrement uses `.reduce((acc, id) => placePackage(acc, id, null), state)` (the delete pattern to mirror for a holistic-scope wipe).
- bucketForScan: scanType "unload" → "staged" (lines 143-144) — the EXISTING staging path that must keep working.
- case "PlanGenerated": case "PlanAccepted": return state; (lines 211-212) — PlanAccepted is a NO-OP today and STAYS a no-op (it carries no packageIds to stage). DO NOT change PlanAccepted.
- default: return assertNeverEvent(event) (line 224).

From packages/projections/src/reducers/reducer.ts:
- assertNeverEvent(event: never): never (lines 37-39) — the build-gate default in 8 standard reducers + trailer-fuel.

From PATTERNS Concern 4 (the 10 no-op reducers): hub-inventory FOLDS it; the other 10 add `case "PlanSuperseded": return state;`. NOTE (verified in Phase 20): audit-timeline uses a LOCAL assertNeverAudit and returns `null` for no-ops; geo-track uses assertNeverGeo and returns `{ state, keyframes: [] }`. trailer-fuel uses assertNeverEvent. Match each reducer's existing no-op return shape.

From PlanAccepted payload (schemas.ts:230, added Phase 4): { epochId, scopeHash, planId, trailerId, occurredAt }. There are NO packageIds on PlanAccepted — so it CANNOT stage. The staged scope is wiped exclusively by PlanSuperseded's supersededPackageIds.

From PlanSuperseded payload (schemas.ts, added by 21-01): carries supersededPackageIds — the HOLISTIC prior-plan scope to delete-then-apply out of staged.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: hub-inventory PlanSuperseded delete-then-apply (PlanAccepted stays no-op) + regression test (FLOW-04 / Open-Q1 RESOLVED)</name>
  <files>
    packages/projections/src/reducers/hub-inventory.ts,
    packages/projections/src/reducers/hub-inventory.test.ts
  </files>

  <read_first>
    1. packages/projections/src/reducers/hub-inventory.ts — read FULLY; note withoutPackage (80-87), placePackage (106+), bucketForScan unload→staged (143-144), the PlanGenerated/PlanAccepted no-op (211-212), the TrailerDeparted manifest-decrement reduce pattern (185-196), assertNeverEvent default (224).
    2. packages/projections/src/reducers/hub-inventory.test.ts — read FULLY; note how existing cases construct events and assert bucket membership.
    3. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 4b — the delete-then-apply mandate + the DESIGN FLAG.
    4. .planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md Pitfall 4 + Open Question 1 (RESOLVED) — the staged-semantics reconciliation.
    5. packages/domain/src/events/schemas.ts — the PlanAccepted (line 230, NO packageIds) + PlanSuperseded (supersededPackageIds) payload shapes.
  </read_first>

  <behavior>
    Open-Q1/A2 is RESOLVED to the only contract-feasible path (consistent with D-21-1's dumb pure delete-then-apply and the actual PlanAccepted payload):

    DESIGN DECISION (record it in the reducer JSDoc): hub_inventory.staged holds BOTH (i) packages unloaded into the yard (PackageScanned scanType=unload — unchanged) AND (ii) the staged scope of an accepted plan. `PlanAccepted` STAYS A NO-OP for staged — its payload is `{ epochId, scopeHash, planId, trailerId, occurredAt }` (schemas.ts:230) and carries NO packageIds, so it has nothing to stage. `PlanSuperseded` is the ONLY stage-mutating plan event: it performs a dumb pure delete-then-apply removing the prior plan's `supersededPackageIds` from staged. Because PlanSuperseded carries the HOLISTIC prior scope (supersededPackageIds), a package present in the OLD plan but absent in the NEW is wiped (not stranded). The fresh plan's still-staged packages are re-staged by the EXISTING unload-scan path / the freight itself — not by PlanAccepted. There is NO domain-contract change to PlanAccepted in this plan. The no-double-count guarantee comes from PlanSuperseded clearing the stale staged scope (Pitfall 4: the double-count is stale `staged` lingering; wiping it on supersession is the fix).

    RED tests first (hub-inventory.test.ts):
    - delete-then-apply: after a PlanSuperseded with supersededPackageIds=[P1,P2] for a hub whose staged contains P1,P2,P3, staged contains only P3 (P1,P2 wiped; non-superseded P3 untouched).
    - regression (unload-scan staging coexists): a PackageScanned scanType=unload for P4 still lands P4 in staged after a PlanSuperseded for an unrelated scope (the two paths do not interfere).
    - no-double-count: a package counted once in inbound is not ALSO counted in staged after supersession (sum of buckets == physically-present count).
    - PlanAccepted no-op: a PlanAccepted leaves staged byte-identical (it carries no packageIds; it stages nothing) — asserts the contract.
  </behavior>

  <action>
    GREEN: In hub-inventory.ts, LEAVE the `case "PlanAccepted": return state;` no-op UNCHANGED. Add `case "PlanSuperseded":` that does a delete-then-apply: `event.payload.supersededPackageIds.reduce((acc, id) => placePackage(acc, id, null), state)` (or the equivalent withoutPackage fold over the scoped hub) — a dumb pure removal, NO epoch/scope comparison inside the reducer. Update the reducer JSDoc to record the staged-semantics decision (Open-Q1 RESOLVED: PlanAccepted stays no-op; PlanSuperseded delete-then-apply; unload-scan path unchanged). Keep bucketForScan unload→staged unchanged.

    Write the four RED tests in hub-inventory.test.ts first; confirm they fail; then GREEN. Commit: `feat(21-03): FLOW-04 hub-inventory PlanSuperseded delete-then-apply (Open-Q1 resolved)`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm vitest run packages/projections/src/reducers/hub-inventory.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `vitest run packages/projections/src/reducers/hub-inventory.test.ts` exits 0 — delete-then-apply + unload-scan-regression + no-double-count + PlanAccepted-no-op tests green (FLOW-04 VALIDATION row "supersession clears stale staged; no double-count").
    - `grep -c '"PlanSuperseded"' packages/projections/src/reducers/hub-inventory.ts` returns ≥ 1 (the fold case present).
    - `grep -c "supersededPackageIds" packages/projections/src/reducers/hub-inventory.ts` returns ≥ 1 (holistic-scope wipe uses the payload).
    - `grep -c "unload" packages/projections/src/reducers/hub-inventory.ts` returns ≥ 1 (the unload→staged path preserved).
    - The reducer JSDoc records the Open-Q1 staged-semantics resolution (PlanAccepted stays no-op; PlanSuperseded delete-then-apply) — verify by reading the touched JSDoc block.
  </acceptance_criteria>

  <done>
    hub_inventory.staged supersession is a dumb pure delete-then-apply driven by PlanSuperseded; PlanAccepted remains a no-op (no packageIds to stage); the unload-scan staging path is intact; no double-count; the design decision is documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: 10 no-op PlanSuperseded reducer cases (build-gated exhaustiveness)</name>
  <files>
    packages/projections/src/reducers/package-location.ts,
    packages/projections/src/reducers/trailer-state.ts,
    packages/projections/src/reducers/zone-estimate.ts,
    packages/projections/src/reducers/driver-status.ts,
    packages/projections/src/reducers/driver-assignment.ts,
    packages/projections/src/reducers/trailer-fuel.ts,
    packages/projections/src/reducers/tag-registry.ts,
    packages/projections/src/reducers/exceptions.ts,
    packages/projections/src/reducers/geo-track.ts,
    packages/projections/src/reducers/audit-timeline.ts
  </files>

  <read_first>
    1. packages/projections/src/reducers/package-location.ts — read the no-op case block + the existing PackageInducted no-op (added Phase 20) as the exact pattern.
    2. packages/projections/src/reducers/audit-timeline.ts — note it uses LOCAL assertNeverAudit and returns `null` for no-ops (NOT assertNeverEvent).
    3. packages/projections/src/reducers/geo-track.ts — note it uses assertNeverGeo and returns `{ state, keyframes: [] }` for no-ops.
    4. packages/projections/src/reducers/trailer-fuel.ts — note it uses assertNeverEvent.
    5. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 4 (the 10-reducer no-op list) + 20-PATTERNS.md "assertNeverEvent Exhaustiveness" (the precedent — PackageInducted was added the same way).
  </read_first>

  <behavior>
    Adding PlanSuperseded to the DomainEvent union (21-01) makes pnpm typecheck FAIL in these 10 reducers until each handles it. Add a no-op `case "PlanSuperseded":` to each, matching that reducer's EXISTING no-op return shape:
    - 8 standard reducers (package-location, trailer-state, zone-estimate, driver-status, driver-assignment, trailer-fuel, tag-registry, exceptions): `case "PlanSuperseded": return state;` in the no-op group before `default: return assertNeverEvent(event)`.
    - audit-timeline: `case "PlanSuperseded": return null;` (its no-op shape) — DO NOT touch the staged/inventory; supersession is purely a hub-inventory concern.
    - geo-track: `case "PlanSuperseded": return { state, keyframes: [] };` (its no-op shape).
    The build is the primary witness — `pnpm typecheck` lists any missing case.
  </behavior>

  <action>
    For each of the 10 reducers, add the PlanSuperseded no-op case matching its existing no-op return (state / null / { state, keyframes: [] }). Group it with the existing no-op cases (e.g. next to PlanAccepted / PackageInducted). Run `pnpm typecheck` to confirm zero missing cases across all 11 reducers (hub-inventory done in Task 1).

    Commit: `feat(21-03): FLOW-04 PlanSuperseded no-op cases in 10 reducers (exhaustiveness)`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm vitest run packages/projections</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm typecheck` exits 0 — no reducer is missing a PlanSuperseded case (the assertNeverEvent/assertNeverAudit/assertNeverGeo build gate is satisfied).
    - `pnpm vitest run packages/projections` exits 0 — all projection tests green including the existing reducer suites.
    - `grep -rl '"PlanSuperseded"' packages/projections/src/reducers/ | wc -l` returns ≥ 11 (every reducer handles it).
    - `grep -c '"PlanSuperseded": return null' packages/projections/src/reducers/audit-timeline.ts` returns 1 (correct no-op shape for audit-timeline).
    - `grep -c 'keyframes: \[\]' packages/projections/src/reducers/geo-track.ts` returns ≥ 1 (geo-track no-op shape; confirm the PlanSuperseded case uses it).
  </acceptance_criteria>

  <done>
    All 11 reducers exhaustively handle PlanSuperseded; pnpm typecheck and the projections suite are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Event log → projection state | Reducers fold trusted domain events (already validated at ingestion) into read-model state; the integrity threat is a stale/double-counted bucket, not untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Tampering | Double-count via stale staged | mitigate | PlanSuperseded delete-then-apply removes the prior plan's supersededPackageIds from staged; the no-double-count test asserts buckets sum to physically-present count. The dumb pure reducer (no epoch comparison) keeps the wipe deterministic and replay-from-zero clean (D-21-1). |
| T-21-08 | Tampering | Stranded freight (old-plan-only items not wiped) | mitigate | PlanSuperseded carries the HOLISTIC prior scope (supersededPackageIds), so items in the OLD plan but absent in the NEW are wiped; the delete-then-apply test asserts only non-superseded packages remain. |
| T-21-09 | Denial of Service | Reducer exhaustiveness regression | mitigate | assertNeverEvent/assertNeverAudit/assertNeverGeo make pnpm typecheck fail until every reducer handles PlanSuperseded — a missing case cannot ship. |

No high-severity threats. Reducers are pure functions over validated events; no auth/crypto/external-input surface.
</threat_model>

<verification>
After this plan:
- `pnpm typecheck` green (all 11 reducers exhaustive)
- `pnpm vitest run packages/projections` green (delete-then-apply + unload-scan regression + no-double-count + PlanAccepted-no-op + existing suites)
- The Open-Q1 staged-semantics decision is documented in the hub-inventory reducer JSDoc + the SUMMARY
</verification>

<success_criteria>
1. hub-inventory folds PlanSuperseded as a dumb pure delete-then-apply (no epoch comparison in the reducer); PlanAccepted stays a no-op (no packageIds to stage)
2. The existing unload-scan staging path still works (regression test green)
3. No double-count (test green)
4. All 10 other reducers add a PlanSuperseded no-op matching their existing return shape; pnpm typecheck green
5. Open-Q1 resolution recorded in JSDoc + SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/21-bidirectional-freight-consolidation/21-03-SUMMARY.md` (record the Open-Q1 staged-semantics resolution explicitly)
</output>
