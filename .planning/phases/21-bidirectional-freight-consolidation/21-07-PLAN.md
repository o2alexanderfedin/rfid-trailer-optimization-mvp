---
phase: 21-bidirectional-freight-consolidation
plan: 07
type: execute
wave: 3
depends_on:
  - 21-04-PLAN.md
files_modified:
  - packages/api/src/ws/envelope.ts
  - packages/api/src/ws/snapshots.ts
  - packages/api/src/sim/driver.ts
  - packages/web/src/map/coloring.ts
  - packages/web/src/map/layers.ts
  - packages/api/src/ws/envelope.test.ts
  - packages/api/src/ws/snapshots.test.ts
  - packages/web/src/map/consolidationLayer.test.ts
autonomous: false
requirements:
  - VIZ-12

must_haves:
  truths:
    - "TrailerKeyframe carries an optional additive direction: 'outbound' | 'consolidation'; derived in buildTrailerKeyframes from from_hub_id === MEMPHIS.hubId ? 'outbound' : 'consolidation'"
    - "trailerChanged re-emits when direction changes (the diff picks it up)"
    - "Consolidation trailers render with a distinct map style per direction (mirroring the inductionColoring zero-allocation StyleFunction pattern) and carry non-empty freight manifests"
    - "The API sim driver threads consolidationEnabled into simFeatureOpts so the live demo can run both directions (kept OFF for goldens)"
    - "Back-compat: direction is OPTIONAL + additive — older clients and snapshot payloads without it are unaffected"
  artifacts:
    - path: packages/api/src/ws/envelope.ts
      provides: "TrailerKeyframe.direction optional field + trailerChanged comparison"
      contains: "direction"
    - path: packages/api/src/ws/snapshots.ts
      provides: "direction derived from from_hub_id (MEMPHIS center) in buildTrailerKeyframes"
      contains: "direction"
    - path: packages/api/src/sim/driver.ts
      provides: "consolidationEnabled threaded into simFeatureOpts (driveSimulationOpenEnded)"
      contains: "consolidationEnabled"
    - path: packages/web/src/map/coloring.ts
      provides: "direction-keyed trailer style branch (distinct consolidation color/arrow)"
      contains: "direction"
    - path: packages/web/src/map/layers.ts
      provides: "upsertTrailerKeyframe threads the direction prop onto the feature"
      contains: "direction"
  key_links:
    - from: packages/api/src/ws/snapshots.ts
      to: packages/api/src/ws/envelope.ts
      via: "buildTrailerKeyframes sets keyframe.direction; trailerChanged compares it"
      pattern: "direction"
    - from: packages/api/src/ws/envelope.ts
      to: packages/web/src/map/layers.ts
      via: "direction prop on the ws TrailerKeyframe → feature.set('direction') → trailerStyle branch"
      pattern: "direction"
---

<objective>
VIZ-12: add `direction: 'outbound' | 'consolidation'` to the ws trailer tick, derive it from the leg's origin hub, re-emit on change, render a distinct map style per direction with non-empty consolidation manifests, and thread `consolidationEnabled` through the API sim driver so the live demo shows both directions.

Purpose: The realtime USA-map is the demo centerpiece. VIZ-12 makes the new consolidation flow visible alongside the unchanged distribution flow — a thin data+style delta (not net-new UI design). Styling is Claude's discretion (no UI-SPEC); keep it deterministic and minimal.

Output: ws envelope + snapshots carry direction; trailerChanged re-emits; the API driver threads consolidationEnabled; the web map styles consolidation distinctly; layer + envelope + snapshot tests green; a human-verify checkpoint confirms the live map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-CONTEXT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md
@.planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md
@.planning/phases/21-bidirectional-freight-consolidation/21-VALIDATION.md
@.planning/phases/21-bidirectional-freight-consolidation/21-04-SUMMARY.md
@.planning/phases/20-external-induction/20-05-PLAN.md

<interfaces>
<!-- All line anchors verified against the live codebase this session. -->

From packages/api/src/ws/envelope.ts:
- TrailerKeyframe (lines 47-59): fields id, routeId, departMs, etaMs, state, util?. Add `readonly direction?: "outbound" | "consolidation";` (OPTIONAL + additive — the util? precedent).
- trailerChanged(prev, next) (lines 273-281): compares routeId/departMs/etaMs/state/util. Add `|| prev.direction !== next.direction`.
- InductionEvent interface (line 128) + the additive TickPayload pattern (Phase 20) — direction is on the per-trailer keyframe, NOT a new TickPayload array.

From packages/api/src/ws/snapshots.ts:
- legRouteId(fromHubId, toHubId) (line 228). InflightLeg has from_hub_id/to_hub_id (lines 276-279).
- buildTrailerKeyframes (line 296): builds routeIdByTrip from legRouteId(trip.from_hub_id, trip.to_hub_id) (lines 304-307); constructs keyframes at ~335-350. Derive `direction = trip.from_hub_id === MEMPHIS.hubId ? "outbound" : "consolidation"` per trip and set keyframe.direction.
- MEMPHIS is the center (hubs[0]); import it from the simulation network barrel (the same source the engine uses for center).

From packages/api/src/sim/driver.ts:
- driveSimulationOpenEnded (line 827): simFeatureOpts object (lines 832-840) spreads rfid/overCarry/timing/hosEnabled/hosConfig/fleetPerSpoke/fuel conditionally. Add `...(opts.consolidationEnabled !== undefined ? { consolidationEnabled: opts.consolidationEnabled } : {})`. Add consolidationEnabled to the DriveSimulationOpenEndedOptions/DriveSimulationOptions interface (mirror hosEnabled?: boolean at line 185). KEEP it off by default (goldens).
- driveSimulationPaced also calls simulate() (lines 486/533) — thread consolidationEnabled there too if the live demo uses the paced path, mirroring how hosEnabled is spread.

From packages/web/src/map/coloring.ts:
- trailerStyle(feature) (lines 267-274): reads feature.get("state") from TRAILER_STYLE_CACHE (pre-allocated). Add a direction branch: read feature.get("direction") and return a distinct pre-allocated Style for "consolidation". Pre-allocate the new Style at module load (zero per-frame allocation — the leak guard).

From packages/web/src/map/layers.ts:
- upsertTrailerKeyframe (line 148): sets feature props (existing.set("state", ...), util at ~159-171). Add `if (keyframe.direction !== undefined) existing.set("direction", keyframe.direction);` on both the update + new-Feature paths.
- inductionColoring.ts / inductionLayer.test.ts (Phase 20) — the zero-allocation StyleFunction + layer-style test PRECEDENT to mirror for consolidationLayer.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ws direction field (envelope + snapshots) + trailerChanged + API driver threading (VIZ-12 data)</name>
  <files>
    packages/api/src/ws/envelope.ts,
    packages/api/src/ws/snapshots.ts,
    packages/api/src/sim/driver.ts,
    packages/api/src/ws/envelope.test.ts,
    packages/api/src/ws/snapshots.test.ts
  </files>

  <read_first>
    1. packages/api/src/ws/envelope.ts — read TrailerKeyframe (47-59) + trailerChanged (273-281) + the util? additive precedent.
    2. packages/api/src/ws/snapshots.ts — read legRouteId (228), InflightLeg (276-279), buildTrailerKeyframes (296-352) incl. the routeIdByTrip derivation and keyframe construction sites.
    3. packages/api/src/sim/driver.ts — read driveSimulationOpenEnded simFeatureOpts (832-840), the DriveSimulationOptions interface (hosEnabled at 185), and the paced simulate() calls (486/533).
    4. packages/api/src/ws/envelope.test.ts + snapshots.test.ts — read for the established diff/keyframe test shape.
    5. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 8 (the exact envelope/snapshots/trailerChanged excerpts) + 21-RESEARCH Pattern 6.
  </read_first>

  <behavior>
    - envelope.ts: add `readonly direction?: "outbound" | "consolidation";` to TrailerKeyframe (optional+additive). Add `|| prev.direction !== next.direction` to trailerChanged.
    - snapshots.ts: in buildTrailerKeyframes, derive direction per trip from `trip.from_hub_id === MEMPHIS.hubId ? "outbound" : "consolidation"` (import MEMPHIS from the simulation network barrel) and set keyframe.direction at the construction sites. Consolidation legs (from_hub_id !== center) ⇒ "consolidation"; distribution legs (from center) ⇒ "outbound".
    - driver.ts: add consolidationEnabled?: boolean to the DriveSimulationOptions interface; thread `...(opts.consolidationEnabled !== undefined ? { consolidationEnabled: opts.consolidationEnabled } : {})` into simFeatureOpts in driveSimulationOpenEnded AND into the paced simulate() calls if the live demo uses the paced path. KEEP default OFF (goldens unaffected).

    RED tests first:
    - envelope.test.ts: trailerChanged returns true when only direction differs.
    - snapshots.test.ts: a keyframe for a leg with from_hub_id === center has direction "outbound"; a leg with a spoke origin has direction "consolidation".
  </behavior>

  <action>
    envelope.ts: add the direction field + the trailerChanged comparison. snapshots.ts: import MEMPHIS; derive + set keyframe.direction. driver.ts: add the interface field + the conditional spread into simFeatureOpts (open-ended + paced). Write the RED envelope/snapshots tests, then GREEN.

    Commit: `feat(21-07): VIZ-12 ws TrailerKeyframe.direction + trailerChanged + driver threading`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm vitest run packages/api/src/ws/envelope.test.ts packages/api/src/ws/snapshots.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `vitest run packages/api/src/ws/envelope.test.ts packages/api/src/ws/snapshots.test.ts` exits 0 — direction-diff re-emit + direction-derivation tests green (VIZ-12 VALIDATION row "TrailerKeyframe.direction set; consolidation legs ⇒ 'consolidation'; diff re-emits on change").
    - `grep -c "direction" packages/api/src/ws/envelope.ts` returns ≥ 2 (field + trailerChanged comparison).
    - `grep -c '"consolidation"' packages/api/src/ws/snapshots.ts` returns ≥ 1 (the derivation).
    - `grep -c "consolidationEnabled" packages/api/src/sim/driver.ts` returns ≥ 2 (interface field + simFeatureOpts spread).
    - `pnpm typecheck` exits 0 (optional additive field clean under strict TS).
  </acceptance_criteria>

  <done>
    The ws tick carries direction (derived from the leg origin), re-emits on change, and the driver threads consolidationEnabled for the live demo (off for goldens).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web map distinct consolidation style + feature prop threading + layer test (VIZ-12 render)</name>
  <files>
    packages/web/src/map/coloring.ts,
    packages/web/src/map/layers.ts,
    packages/web/src/map/consolidationLayer.test.ts
  </files>

  <read_first>
    1. packages/web/src/map/coloring.ts — read trailerStyle (267-274) + the pre-allocated TRAILER_STYLE_CACHE / TRAILER_STYLE_DEFAULT (250-258).
    2. packages/web/src/map/layers.ts — read upsertTrailerKeyframe (148-175) incl. the feature.set("state"/"util") sites + createTrailerLayer (89-91).
    3. packages/web/src/map/inductionColoring.ts + inductionLayer.test.ts — the zero-allocation StyleFunction + layer-style TEST precedent to mirror.
    4. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 8 (Web map style + upsert excerpts) + "Zero-allocation OL StyleFunction" shared pattern.
  </read_first>

  <behavior>
    - coloring.ts: pre-allocate a distinct consolidation Style at module load (e.g. a different fill color/arrow than the outbound/distribution trailer style — Claude's discretion, deterministic, distinct from hub/stop/induction colors). In trailerStyle, branch on feature.get("direction"): "consolidation" ⇒ the consolidation Style; otherwise the existing state-keyed/default trailer style. Zero per-frame allocation (return cached references only — the leak guard).
    - layers.ts: in upsertTrailerKeyframe, thread `if (keyframe.direction !== undefined) existing.set("direction", keyframe.direction);` on BOTH the update and new-Feature property paths so the style function can read it.

    RED test first (consolidationLayer.test.ts, mirror inductionLayer.test.ts): a feature with direction "consolidation" resolves to the consolidation Style; a feature with direction "outbound" (or undefined) resolves to the default/state trailer style; the returned Style references are the SAME cached object across calls (zero-allocation).
  </behavior>

  <action>
    coloring.ts: add the pre-allocated consolidation Style + the direction branch in trailerStyle. layers.ts: thread the direction prop in upsertTrailerKeyframe (both paths). Write the RED layer-style test mirroring inductionLayer.test.ts, then GREEN.

    Commit: `feat(21-07): VIZ-12 consolidation map style + feature prop + layer test`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm vitest run --project ui packages/web/src/map/consolidationLayer.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `vitest run --project ui packages/web/src/map/consolidationLayer.test.ts` exits 0 — distinct-style-per-direction + zero-allocation cached-reference assertions green (VIZ-12 VALIDATION row "map renders distinct style per direction").
    - `grep -c "direction" packages/web/src/map/coloring.ts` returns ≥ 1 (the direction branch).
    - `grep -c "direction" packages/web/src/map/layers.ts` returns ≥ 1 (the upsert prop threading).
    - The consolidation Style is allocated ONCE at module load (verify by reading coloring.ts — a module-level const, not inside trailerStyle).
  </acceptance_criteria>

  <done>
    Consolidation trailers render with a distinct, pre-allocated style keyed on direction; the upsert threads the prop; the layer test proves distinct styling + zero allocation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>VIZ-12 Visual Confirmation: both directions + distinct consolidation styling + cross-dock trace on the live map</name>
  <what-built>
    Both flow directions now animate on the live map: consolidation trailers (spoke→center) render with a distinct style and non-empty freight manifests, alongside the unchanged center→spoke distribution trailers. The API sim driver can run consolidationEnabled:true for the live demo. (Automated tests cover the data + style; this checkpoint covers the visual judgment the demo centerpiece requires — VALIDATION Manual-Only row.)
  </what-built>
  <how-to-verify>
    1. Start the API + web dev servers (the standard local run) with the live demo configured for consolidationEnabled:true + inductionEnabled:true (the driver now threads the flag).
    2. Open the map in the browser.
    3. Confirm: consolidation trailers move spoke→center with a DISTINCT color/arrow from the distribution trailers, and both directions are active simultaneously.
    4. Confirm consolidation trailers carry NON-EMPTY freight manifests (not empty returns) when freight is staged.
    5. (FLOW-02 trace) Follow a package inducted at Spoke A: departure from Spoke A (consolidation) → arrival + re-sort at center → departure toward Spoke B (distribution) — the cross-dock value.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual issues (wrong color, empty manifests, only one direction visible, no cross-dock trace).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine snapshot → ws client | The ws protocol carries a one-way server→client state diff; direction is a derived discriminator, not user input; the threat surface is a back-compat break, not injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-20 | Tampering | Back-compat break (older clients / snapshot payloads) | mitigate | direction is OPTIONAL + additive (the util? precedent); snapshot payloads and older clients without it are unaffected; the diff only re-emits when direction actually changes. |
| T-21-21 | Denial of Service | Per-frame Style allocation (OL leak) | mitigate | The consolidation Style is pre-allocated once at module load; trailerStyle returns cached references only (zero per-frame allocation) — the layer test asserts cached-reference identity. |
| T-21-22 | Information Disclosure | ws trailer payload | accept | direction is a deterministic 'outbound'|'consolidation' discriminator derived from the leg origin; no PII; no ASVS L1 control implicated for a one-way simulation state diff. |

No high-severity threats. No auth/crypto/external-input surface; the ws stream is read-only server→client.
</threat_model>

<verification>
After this plan:
- `pnpm typecheck` green
- `pnpm vitest run packages/api/src/ws packages/web/src/map` green (envelope/snapshots/consolidationLayer)
- The human-verify checkpoint confirms both directions + distinct styling + non-empty manifests + the cross-dock trace on the live map
</verification>

<success_criteria>
1. TrailerKeyframe.direction (optional additive) derived from the leg origin; trailerChanged re-emits on change
2. The API driver threads consolidationEnabled (off for goldens)
3. The web map renders a distinct, pre-allocated consolidation style; upsert threads the prop
4. Layer/envelope/snapshot tests green; the human-verify checkpoint approves the live map
</success_criteria>

<output>
After completion, create `.planning/phases/21-bidirectional-freight-consolidation/21-07-SUMMARY.md`
</output>
</content>
