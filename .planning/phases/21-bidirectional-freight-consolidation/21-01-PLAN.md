---
phase: 21-bidirectional-freight-consolidation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/events/schemas.ts
  - packages/domain/src/events/domain-event.ts
  - packages/domain/src/events/contract.assert.ts
  - packages/domain/src/events/index.ts
  - packages/domain/src/index.ts
  - packages/domain/src/events/plan-superseded.test.ts
autonomous: true
requirements:
  - FLOW-04

must_haves:
  truths:
    - "PlanSuperseded exists in the DomainEvent closed union and the build gate (contract.assert.ts) enforces exhaustiveness at compile time"
    - "A well-formed PlanSuperseded event passes the validate() round-trip test without error and an extra payload field is rejected (.strict)"
    - "The PlanSuperseded payload carries holistic scope state (priorPlanId, epochId, scopeHash, trailerId, supersededPackageIds, reason) so the supersession reducer can wipe stale staged without stranding items"
    - "pnpm build && pnpm typecheck exit 0 after the 5-file ceremony lands atomically"
  artifacts:
    - path: packages/domain/src/events/schemas.ts
      provides: "planSupersededSchema via eventSchema factory; added to domainEventSchema discriminated union"
      contains: "planSupersededSchema"
    - path: packages/domain/src/events/domain-event.ts
      provides: "PlanSuperseded inferred type; member of DomainEvent union"
      contains: "PlanSuperseded"
    - path: packages/domain/src/events/contract.assert.ts
      provides: "case PlanSuperseded in assertExhaustive; type-equality proof covers the new member"
      contains: "case \"PlanSuperseded\""
    - path: packages/domain/src/events/index.ts
      provides: "PlanSuperseded type + planSupersededSchema re-exported"
    - path: packages/domain/src/index.ts
      provides: "barrel re-exports for PlanSuperseded type + schema"
    - path: packages/domain/src/events/plan-superseded.test.ts
      provides: "round-trip + strict-reject + unsupported-version tests for PlanSuperseded"
  key_links:
    - from: packages/domain/src/events/schemas.ts
      to: packages/domain/src/events/domain-event.ts
      via: "z.infer<typeof planSupersededSchema>"
      pattern: "PlanSuperseded"
    - from: packages/domain/src/events/domain-event.ts
      to: packages/domain/src/events/contract.assert.ts
      via: "assertExhaustive exhaustive switch + Exact<> proof"
      pattern: "case \"PlanSuperseded\""
---

<objective>
Add `PlanSuperseded` to the domain closed-union event system — the 5-file atomic ceremony (D-21-1, RESOLVED/LOCKED) that the supersession reducer (21-03) and the optimizer atomic-append (21-05) depend on.

Purpose: D-21-1 resolved the no-double-count mechanism (FLOW-04) as an EXPLICIT `PlanSuperseded` event (not a supersession-aware reducer). The optimizer emits it in the SAME atomic append as `PlanAccepted`; the hub-inventory reducer stays a dumb pure delete-then-apply. This plan lands and build-gates the event so every downstream plan can fold it.

Output: 5 modified domain files + 1 new test file; `PlanSuperseded` exported and build-gated; `validate()` round-trip + `.strict` reject tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-bidirectional-freight-consolidation/21-CONTEXT.md
@.planning/phases/21-bidirectional-freight-consolidation/21-RESEARCH.md
@.planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md
@.planning/phases/21-bidirectional-freight-consolidation/21-VALIDATION.md

<interfaces>
<!-- All line anchors verified against the live codebase this session. -->

From packages/domain/src/events/schemas.ts:
- `eventSchema(type, payload)` factory (lines 35-44) — wraps with `{ type: z.literal(type), schemaVersion, payload: payload.strict() }`. Call it; do NOT call `.strict()` again on the result.
- `planAcceptedSchema` (lines 230-239) is the closest payload analog: `eventSchema("PlanAccepted", z.object({ epochId: id, scopeHash: id, planId: id, trailerId: id, occurredAt }))`.
- `domainEventSchema` discriminated union — last member `packageInductedSchema` (added in Phase 20). Add `planSupersededSchema` after it.
- `id` constant and `occurredAt` constant are already in scope in this file.

From packages/domain/src/events/domain-event.ts:
- `DomainEvent` union (PackageInducted is the last member, added Phase 20). Add `| PlanSuperseded` after it.
- Import block imports schemas from "./schemas.js" — add `planSupersededSchema`.

From packages/domain/src/events/contract.assert.ts:
- `assertExhaustive` switch — `case "PackageInducted": return;` is the last case before `default`. Add `case "PlanSuperseded": return;` before `default`.
- Type-equality proof `Exact<Inferred, DomainEvent> = true` — NO manual edit; auto-covers the new member when domainEventSchema + DomainEvent are updated atomically. Missing either ⇒ `Type 'false' is not assignable to type 'true'`.

From packages/domain/src/events/index.ts + packages/domain/src/index.ts:
- Mirror the `PackageInducted` / `packageInductedSchema` re-export pattern exactly (type re-export block + schema re-export block in both files).
- `validateEvent` is exported from @mm/domain — used by the round-trip test.

From packages/domain/src/events/plan-events.test.ts (the test template):
- Round-trip pattern: `expect(validateEvent(planAccepted)).toEqual(planAccepted)` (or `.type` assertion).
- Strict-reject pattern: `expect(() => validateEvent({ ...ev, payload: { ...ev.payload, extra: 1 } })).toThrow(ValidationError)`.
- Unsupported-version pattern: an out-of-range `schemaVersion` throws.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: PlanSuperseded 5-file closed-union ceremony + round-trip/strict-reject test (FLOW-04 / D-21-1)</name>
  <files>
    packages/domain/src/events/schemas.ts,
    packages/domain/src/events/domain-event.ts,
    packages/domain/src/events/contract.assert.ts,
    packages/domain/src/events/index.ts,
    packages/domain/src/index.ts,
    packages/domain/src/events/plan-superseded.test.ts
  </files>

  <read_first>
    1. packages/domain/src/events/schemas.ts — read fully; note eventSchema factory (35-44), planAcceptedSchema (230-239), the domainEventSchema discriminatedUnion (packageInductedSchema is the last member), id + occurredAt constants.
    2. packages/domain/src/events/domain-event.ts — read fully; note the DomainEvent union (PackageInducted last member) + the schemas.js import block.
    3. packages/domain/src/events/contract.assert.ts — read fully; note assertExhaustive switch + the Exact<> type-equality proof.
    4. packages/domain/src/events/index.ts + packages/domain/src/index.ts — read fully; note the PackageInducted/packageInductedSchema re-export pattern in both.
    5. packages/domain/src/events/plan-events.test.ts — read fully; this is the test TEMPLATE (round-trip, strict-reject, unsupported-version). Note the existing planAccepted literal shape.
    6. .planning/phases/21-bidirectional-freight-consolidation/21-PATTERNS.md Concern 4 — the exact 5-step add sequence.
    7. .planning/phases/20-external-induction/20-PATTERNS.md Shared Patterns "5-File Closed-Union Extension Ceremony" — the identical ceremony Phase 20 used for PackageInducted (the precedent to mirror verbatim).
  </read_first>

  <behavior>
    RED phase first: write packages/domain/src/events/plan-superseded.test.ts mirroring plan-events.test.ts. It builds a well-formed PlanSuperseded literal, calls validateEvent(it), and asserts the round-trip; asserts an extra payload field throws ValidationError (.strict); asserts an unsupported schemaVersion throws. The test fails (type does not exist yet).

    GREEN phase: the 5-file ceremony makes it pass.

    PlanSuperseded payload (holistic scope state per D-21-1 — carries enough for the reducer to wipe stale staged WITHOUT stranding items present in the old plan but absent in the new):
    - epochId (id) — the superseding epoch
    - scopeHash (id) — the superseding scope hash
    - priorPlanId (id) — the plan being superseded (audit trail)
    - trailerId (id) — same optimizer-${trailerId} stream as PlanAccepted (so it rides the same atomic append)
    - supersededPackageIds (z.array(id)) — the HOLISTIC scope: every packageId the prior plan staged for this scope, so the reducer can delete-then-apply
    - reason (z.string().min(1)) — audit ("superseded by plan-43")
    - occurredAt (occurredAt constant) — VIRTUAL clock ISO, never Date.now()

    Test cases (mirror plan-events.test.ts):
    - Round-trips a well-formed PlanSuperseded.
    - Rejects an extra (unrecognized) payload field — .strict payload.
    - Rejects an unsupported schemaVersion.

    All 5 domain files + the test land in ONE commit. After commit run pnpm build && pnpm typecheck immediately — any missing ceremony step is caught here before Wave 2 begins.
  </behavior>

  <action>
    RED: Create packages/domain/src/events/plan-superseded.test.ts importing validateEvent + the PlanSuperseded type (and ValidationError) from @mm/domain. Build a well-formed PlanSuperseded literal: type "PlanSuperseded", schemaVersion 1, payload { epochId, scopeHash, priorPlanId, trailerId, supersededPackageIds: ["P00001"], reason, occurredAt }. Assert round-trip + strict-reject + unsupported-version. Run vitest on it — expect FAILURE (type absent).

    GREEN (5-file ceremony, per 21-PATTERNS Concern 4):
    1. schemas.ts — define `planSupersededSchema = eventSchema("PlanSuperseded", z.object({ epochId: id, scopeHash: id, priorPlanId: id, trailerId: id, supersededPackageIds: z.array(id), reason: z.string().min(1), occurredAt }))` after planAcceptedSchema; add planSupersededSchema to the domainEventSchema discriminatedUnion array after packageInductedSchema (per D-21-1).
    2. domain-event.ts — add planSupersededSchema to the schemas.js import; add `export type PlanSuperseded = z.infer<typeof planSupersededSchema>;`; append `| PlanSuperseded` to the DomainEvent union.
    3. contract.assert.ts — add `case "PlanSuperseded": return;` before default. Do NOT touch the Exact<> proof.
    4. events/index.ts — add PlanSuperseded to the type re-export block and planSupersededSchema to the schema re-export block.
    5. src/index.ts — add PlanSuperseded to the barrel type re-export and planSupersededSchema to the barrel schema re-export.

    Commit all 6 files together: `feat(21-01): FLOW-04/D-21-1 PlanSuperseded closed-union ceremony`.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm build && pnpm typecheck && pnpm vitest run packages/domain/src/events/plan-superseded.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm build` exits 0 — Turbo build succeeds with the 5 ceremony files updated atomically.
    - `pnpm typecheck` exits 0 — the Exact<Inferred, DomainEvent> = true type-equality proof passes; assertExhaustive covers PlanSuperseded.
    - `vitest run packages/domain/src/events/plan-superseded.test.ts` exits 0 — round-trip + strict-reject + unsupported-version green (FLOW-04, VALIDATION row "PlanSuperseded round-trips + .strict rejects extras").
    - `grep -c "planSupersededSchema" packages/domain/src/events/schemas.ts` returns ≥ 2 (schema defined + added to union).
    - `grep -c "PlanSuperseded" packages/domain/src/events/domain-event.ts` returns ≥ 2 (type + union member).
    - `grep -c '"PlanSuperseded"' packages/domain/src/events/contract.assert.ts` returns ≥ 1 (exhaustive switch case present).
    - `grep -c "supersededPackageIds" packages/domain/src/events/schemas.ts` returns ≥ 1 (holistic scope field present per D-21-1).
  </acceptance_criteria>

  <done>
    PlanSuperseded is a full member of the DomainEvent closed union; build + typecheck green; round-trip + strict-reject tests pass. The supersession reducer (21-03) and the optimizer atomic-append (21-05) may begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain types → consumers | All consumers import from @mm/domain; no user-supplied input crosses this boundary in Phase 21 (the simulation engine + optimizer construct all events) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | PlanSuperseded payload integrity | mitigate | Zod `.strict()` (applied inside eventSchema) rejects any extra/unrecognized payload field at the ingestion boundary; the strict-reject test asserts it. supersededPackageIds is `z.array(id)` (no arbitrary shape). |
| T-21-02 | Repudiation | Plan supersession auditability | mitigate | The explicit PlanSuperseded event (D-21-1) records priorPlanId + reason on the event log, giving a replayable audit trail ("freight unstaged because plan X superseded") — the reason a reducer-internal eviction was rejected. |
| T-21-03 | Information Disclosure | PlanSuperseded payload | accept | All fields are deterministically simulation/optimizer-generated, not user-supplied; no PII; no ASVS L1 control implicated for simulation-internal domain events. |

No high-severity threats. Phase 21 makes no changes to authentication, session management, or cryptography. ASVS L1 has no applicable controls for an internal simulation/read API's domain-event contract.
</threat_model>

<verification>
After this plan:
- `pnpm build && pnpm typecheck` green
- `vitest run packages/domain` green (all domain tests including new round-trip)
- `PlanSuperseded` importable from `@mm/domain` by Wave 2 plans (21-03 reducer, 21-05 optimizer)
</verification>

<success_criteria>
1. All 5 domain files updated atomically in one commit alongside the test
2. `pnpm build && pnpm typecheck` exit 0
3. `validate()` round-trip + `.strict` reject + unsupported-version tests for PlanSuperseded green
4. The payload carries holistic scope state (supersededPackageIds) per D-21-1
5. PlanSuperseded + planSupersededSchema importable from @mm/domain barrel
</success_criteria>

<output>
After completion, create `.planning/phases/21-bidirectional-freight-consolidation/21-01-SUMMARY.md`
</output>
</content>
</invoke>
