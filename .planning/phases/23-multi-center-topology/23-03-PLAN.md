---
phase: 23-multi-center-topology
plan: 03
type: tdd
wave: 2
depends_on: [23-01]
files_modified:
  - packages/simulation/src/network/centers.ts
  - packages/simulation/src/network/hubs.ts
autonomous: true
requirements: [HUB-04, NET-02, NET-03, NET-04]
user_setup: []

must_haves:
  truths:
    - "generateBigCityHubs() loads the committed JSON and returns a sorted readonly Hub[] (no clock/RNG/network)"
    - "pickRegionalCenters(hubs, count) selects `count` centers (the largest metro per freight-corridor+timezone partition), parameterized — never hard-coded, never collapsing to one"
    - "assignSpokesToNearestCenter assigns each spoke by partition then great-circle nearest, tie-broken by lowest stable center id, under a leg-length cap"
    - "buildBackbone returns a near-full-mesh of center<->center great-circle legs giving <=2-hop coast-to-coast routing"
    - "An anti-SPOF check confirms the center graph stays connected when any one center is removed"
    - "Dataset attribution (GeoNames CC BY 4.0) is shipped in the README/UI footer"
  artifacts:
    - path: "packages/simulation/src/network/centers.ts"
      provides: "Pure pickRegionalCenters + assignSpokesToNearestCenter + buildBackbone + isConnectedWithoutAnyCenter"
      min_lines: 80
    - path: "packages/simulation/src/network/hubs.ts"
      provides: "generateBigCityHubs() reading the committed JSON (parameterized hub source)"
      contains: "generateBigCityHubs"
  key_links:
    - from: "generateBigCityHubs"
      to: "us-big-cities.generated.json"
      via: "static import of the committed JSON (no runtime city-data dep)"
      pattern: "us-big-cities.generated"
    - from: "assignSpokesToNearestCenter"
      to: "haversineKm (from @mm/domain)"
      via: "great-circle nearest with leg-length cap + tie-break by id"
      pattern: "haversineKm"
---

<objective>
Build the pure multi-center topology functions (HUB-04 generator + NET-02 center selection +
NET-03 nearest-assignment + NET-04 backbone/anti-SPOF) and ship the dataset attribution. These are
deterministic functions of the static dataset from plan 23-01 — the byte-stable topology the route
builder and engine flow (plan 23-04) consume.

Purpose: Generalize the network from a single hard-wired Memphis center to a parameterized set of
regional centers over a near-full-mesh backbone, with stable, explainable, leg-capped spoke
assignment — the linchpin topology change, kept pure (no clock/RNG) so the bootstrap is reproducible.

Output: `network/centers.ts` (pickRegionalCenters / assignSpokesToNearestCenter / buildBackbone /
anti-SPOF check), `generateBigCityHubs()` in `network/hubs.ts`, and the GeoNames attribution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-multi-center-topology/23-CONTEXT.md

# The committed dataset shape this plan consumes (produced by plan 23-01):
@packages/simulation/src/network/hubs.ts

# The pure great-circle primitives to REUSE (do NOT swap greatCircle):
@packages/simulation/src/network/routes.ts

<interfaces>
<!-- From @mm/domain: Hub = { hubId, name, lat, lon }; haversineKm(a: Hub, b: Hub): number (REUSE — same basis as routes/twin). -->
<!-- The generated JSON row (plan 23-01) extends Hub with: state, population, rank, region, timezone. -->
<!-- New exports this plan must define (the contracts plan 23-04 consumes): -->
<!--   interface BigCityHub extends Hub { state; population; rank; region; timezone } -->
<!--   generateBigCityHubs(): readonly BigCityHub[]            // loads the committed JSON, sorted by hubId -->
<!--   pickRegionalCenters(hubs, count): readonly Hub[]        // count parameterized; largest metro per corridor+tz partition -->
<!--   assignSpokesToNearestCenter(spokes, centers, legCapKm): ReadonlyMap<string /*spokeHubId*/, string /*centerHubId*/> -->
<!--   buildBackbone(centers): readonly { fromHubId; toHubId }[]   // directed near-full-mesh pairs (geometry built in 23-04) -->
<!--   isConnectedWithoutAnyCenter(centers, backbone): boolean     // anti-SPOF: connected after removing each center -->
<!-- Determinism: every function is a pure function of its inputs; sort all id-keyed outputs; ties break by lowest id. -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: generateBigCityHubs + pickRegionalCenters (parameterized, partition-by-corridor+timezone)</name>
  <read_first>
    - packages/simulation/src/network/hubs.ts (USA_HUBS + the Hub shape + the static-const determinism comment)
    - packages/simulation/src/network/us-big-cities.generated.json (the dataset from plan 23-01)
    - packages/simulation/src/network/routes.ts (haversineKm re-export)
  </read_first>
  <files>packages/simulation/src/network/hubs.ts, packages/simulation/src/network/centers.ts</files>
  <behavior>
    - generateBigCityHubs(): returns the committed JSON's hubs as readonly BigCityHub[], sorted by hubId, with no I/O beyond the single static JSON import. Test: count in [80,130]; sorted ascending; every hub in the continental envelope; pure (two calls return deeply-equal arrays).
    - pickRegionalCenters(hubs, count): partitions hubs by (freight-corridor/region + timezone) band, then picks the largest-population hub per partition, returning exactly `count` centers (when count <= partition count) chosen by a documented, deterministic rule; ties break by lowest hubId. Test: count is honored (e.g. 4, 6, 8 all return that many distinct centers); never returns < 2; output sorted by hubId; reordering the input hub array does NOT change the chosen set (stable).
  </behavior>
  <action>
    Add `generateBigCityHubs()` to `network/hubs.ts`: `import bigCities from "./us-big-cities.generated.json" with { type: "json" }` (or the repo's existing JSON-import convention — match how `road-geometry.generated.json` is loaded, adapting to a static import), assert-shape it into `readonly BigCityHub[]`, sort by hubId. Keep `USA_HUBS` untouched (the flags-off path still uses it). Create `network/centers.ts` with `pickRegionalCenters(hubs, count)`: group by `region`+`timezone` (a documented corridor proxy), order partitions deterministically, pick the max-population hub per partition (tie -> lowest hubId), and select `count` of them by a documented stable rule. The `count` is a plain parameter (NEVER a literal) — its concrete default is chosen in plan 23-05. Reuse `haversineKm` for any distance need. No clock/RNG.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation exec vitest run network/centers 2>&1 | tail -20 && pnpm --filter @mm/simulation exec vitest run -t "generateBigCityHubs" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - generateBigCityHubs returns [80,130] sorted in-envelope hubs, pure, no extra I/O.
    - pickRegionalCenters(hubs, count) returns exactly `count` distinct centers, sorted, stable under input reordering, never < 2.
    - `count` is a function parameter — `grep -nE "pickRegionalCenters\\([^,]+,\\s*[0-9]" packages/simulation/src` finds NO hard-coded literal call inside the module.
  </acceptance_criteria>
  <done>generateBigCityHubs + a parameterized, partition-based, deterministic pickRegionalCenters exist and their unit tests pass.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: assignSpokesToNearestCenter (leg cap, tie-break by id) + buildBackbone + anti-SPOF</name>
  <read_first>
    - packages/simulation/src/network/centers.ts (Task 1 outputs)
    - packages/simulation/src/network/routes.ts (haversineKm, routeId conventions)
  </read_first>
  <files>packages/simulation/src/network/centers.ts</files>
  <behavior>
    - assignSpokesToNearestCenter(spokes, centers, legCapKm): each spoke -> its center, chosen by the SAME region/timezone partition first, then great-circle nearest, tie-broken by lowest center hubId. A spoke whose nearest in-partition center exceeds legCapKm falls back to the global great-circle nearest center within the cap (documented rule). Test: deterministic (same map twice); a coordinate nudge below 6dp does NOT re-partition; every assigned leg <= legCapKm OR is flagged by the documented overflow rule; ties resolve to the lowest id.
    - buildBackbone(centers): returns the directed near-full-mesh center<->center pairs (both directions), in stable sorted order. Test: for n centers, count === n*(n-1); ids sorted; no self-pairs.
    - isConnectedWithoutAnyCenter(centers, backbone): true iff removing ANY single center leaves the remaining center graph connected. Test: a full mesh of >=3 centers passes; a star-of-centers (one hub-of-hubs) FAILS (proves the check catches re-centralization/SPOF); a 2-hop-max property holds for the full mesh.
  </behavior>
  <action>
    Add `assignSpokesToNearestCenter`, `buildBackbone`, and `isConnectedWithoutAnyCenter` to `network/centers.ts`. Implement assignment as a pure function: for each spoke (iterated in sorted hubId order), compute `haversineKm` to each center, pick the in-partition nearest under `legCapKm`, else the global nearest under the cap, tie-broken by lowest center hubId; return a `ReadonlyMap<spokeHubId, centerHubId>`. Implement `buildBackbone` as all directed center pairs (full mesh — cheap at <=8 centers per NET-04), returned sorted. Implement `isConnectedWithoutAnyCenter` via a pure BFS/union-find over the backbone adjacency, looping the removal of each center. Round coordinates to 6dp before any compare that could flip an assignment (anti-P2, mirroring `hubCoordsChecksum`). No clock/RNG.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation exec vitest run network/centers 2>&1 | tail -25 && pnpm --filter @mm/simulation typecheck</automated>
  </verify>
  <acceptance_criteria>
    - assignSpokesToNearestCenter is deterministic, leg-capped, tie-broken by lowest id, and stable under sub-6dp coordinate nudges.
    - buildBackbone(n centers) returns n*(n-1) directed sorted pairs, no self-pairs.
    - isConnectedWithoutAnyCenter returns true for a full mesh and FALSE for a hub-of-hubs star (anti-SPOF + anti-re-centralization witness); coast-to-coast is <=2 hops in the mesh.
    - `pnpm --filter @mm/simulation typecheck` clean.
  </acceptance_criteria>
  <done>Pure nearest-assignment (leg-capped, id-tie-broken), full-mesh backbone, and an anti-SPOF connectivity check exist with passing tests and clean typecheck.</done>
</task>

<task type="execute">
  <name>Task 3: Ship GeoNames CC BY 4.0 dataset attribution (HUB-04)</name>
  <read_first>
    - README.md (the existing OpenStreetMap attribution lines ~L62/L78 — match their style)
    - packages/web/src/map/MapView.tsx (the OSM TileLayer; the existing basemap attribution surface)
  </read_first>
  <files>README.md, packages/web/src/map/MapView.tsx</files>
  <action>
    Add the required "city data (c) GeoNames, CC BY 4.0" attribution (per HUB-04 + the locked CONTEXT decision that `all-the-cities`/GeoNames was chosen) alongside the existing OpenStreetMap credit: a line in the README features/data section, and a visible credit in the map UI footer/attribution (extend the existing OSM `Attribution`/credit in `MapView.tsx`, or the page footer if that is where the OSM credit renders). Keep it text-only; no new dependency. If a dedicated footer component does not exist, add the credit next to where the OSM "(c) OpenStreetMap" attribution already appears.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && grep -rn "GeoNames" README.md packages/web/src && pnpm --filter @mm/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - "GeoNames" + "CC BY 4.0" appears in BOTH the README and a visible UI/map attribution surface.
    - `pnpm --filter @mm/web typecheck` clean (no regression to the map component).
  </acceptance_criteria>
  <done>GeoNames CC BY 4.0 attribution ships in the README and the UI footer/map attribution; web typecheck stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static dataset -> topology functions | Pure functions of committed data; the integrity concern is determinism (stable partition/assignment), not external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | non-deterministic center selection / spoke flip from coordinate noise (anti-P12) | mitigate | Round to 6dp before any assignment-deciding compare; tie-break by lowest stable id; a committed partition snapshot (plan 23-05) makes any change a visible diff. |
| T-23-07 | Denial of service | backbone SPOF / re-centralization re-introducing the single-star bottleneck (anti-P14) | mitigate | `isConnectedWithoutAnyCenter` anti-SPOF check + the hub-of-hubs-FAILS test; near-full-mesh keeps coast-to-coast <=2 hops. |
| T-23-08 | Repudiation | missing dataset attribution (license non-compliance) | mitigate | HUB-04 ships the GeoNames CC BY 4.0 credit in README + UI, asserted by grep in verify. |
</threat_model>

<verification>
- All `network/centers.ts` unit tests green; `generateBigCityHubs` returns an in-envelope sorted set.
- Anti-SPOF check passes a full mesh, fails a star.
- GeoNames attribution present in README + UI; `@mm/simulation` + `@mm/web` typecheck clean.
</verification>

<success_criteria>
Pure, deterministic multi-center topology functions exist: a JSON-backed `generateBigCityHubs`, a
parameterized partition-based `pickRegionalCenters`, a leg-capped id-tie-broken
`assignSpokesToNearestCenter`, a near-full-mesh `buildBackbone`, and an anti-SPOF connectivity check —
plus the shipped GeoNames CC BY 4.0 attribution.
</success_criteria>

<output>
After completion, create `.planning/phases/23-multi-center-topology/23-03-SUMMARY.md`
</output>
