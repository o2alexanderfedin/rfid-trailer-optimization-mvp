---
phase: 23-multi-center-topology
plan: 05
type: tdd
wave: 4
depends_on: [23-01, 23-02, 23-03, 23-04]
autonomous: false
files_modified:
  - packages/simulation/test/continental-determinism.unit.test.ts
  - packages/simulation/test/determinism.unit.test.ts
  - packages/simulation/test/hub-dataset-drift.unit.test.ts
  - packages/simulation/src/network/centers.ts
  - packages/simulation/src/network/center-partition.snapshot.json
requirements: [HUB-01, NET-02, DET-01]
user_setup: []

must_haves:
  truths:
    - "With continentalTopology absent AND explicit-false, the seed-42 10k-tick golden is byte-identical to 3920accc..."
    - "continentalTopology:false is byte-identical to the flag being absent"
    - "The continental model captures its own new golden on a small (12-20-hub) deterministic fixture"
    - "The center count is chosen empirically from a real continental run validating trailer-fill/consolidation; the chosen value + rationale are recorded in a committed partition snapshot"
    - "A drift-guard test re-derives the dataset checksum and the center partition and asserts they match the committed snapshot"
    - "The network never collapses to a single primary center"
  artifacts:
    - path: "packages/simulation/test/continental-determinism.unit.test.ts"
      provides: "New continental golden + flags-off two-part gate (DET-01)"
      contains: "continentalTopology"
    - path: "packages/simulation/src/network/center-partition.snapshot.json"
      provides: "Committed center-count decision + partition snapshot"
      contains: "centerCount"
    - path: "packages/simulation/test/hub-dataset-drift.unit.test.ts"
      provides: "Dataset checksum + partition drift guard"
      contains: "hubsChecksum"
  key_links:
    - from: "determinism.unit.test.ts"
      to: "3920accc..."
      via: "continentalTopology absent + explicit-false both hash to the golden"
      pattern: "3920accc"
    - from: "center-partition.snapshot.json"
      to: "pickRegionalCenters chosen count"
      via: "committed empirical decision asserted by the drift guard"
      pattern: "centerCount"
---

<objective>
Close the determinism keystone for Phase 23: assert the two-part flags-off gate (DET-01) — both
`continentalTopology:false === absent` AND `absent => seed-42 10k golden 3920accc...` — capture the new
continental model's own golden on a small 12-20-hub fixture, choose the regional-center count
empirically from a real continental run (NET-02), and commit a partition snapshot + drift guard so the
topology is reproducible and any change is a visible diff.

Purpose: This is the witness that the whole phase preserved byte-identical replay when off and produced
a reproducible new model when on. The center count was deliberately deferred to this phase to be chosen
against real trailer-fill/consolidation behavior, not guessed — and recorded so it is auditable.

Output: continental + flags-off determinism tests, the new continental golden, the committed
center-partition snapshot (with the empirical decision), and the dataset/partition drift guard.

This plan has a `checkpoint:decision` for the empirically-chosen center count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-multi-center-topology/23-CONTEXT.md
@.planning/phases/23-multi-center-topology/23-03-SUMMARY.md
@.planning/phases/23-multi-center-topology/23-04-SUMMARY.md

# The existing golden harness + the two-part flags-off gate pattern to extend:
@packages/simulation/test/determinism.unit.test.ts

# The topology functions whose chosen count is recorded:
@packages/simulation/src/network/centers.ts

<interfaces>
<!-- The golden constant (determinism.unit.test.ts): 3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861 -->
<!--   = sha256(JSON.stringify(simulate({ seed: 42, durationTicks: 10000 }))). -->
<!-- The two-part gate pattern (extend it for continentalTopology, mirroring outboundDeliveryEnabled): -->
<!--   (a) explicit `continentalTopology: false` === flag absent over a short run; -->
<!--   (b) flag ABSENT => the 10k stream still hashes to 3920accc... . -->
<!-- New continental golden: a SMALL fixed fixture (12-20 hubs, a small centerCount, fixed seed + durationTicks) -->
<!--   so the hash is fast; the full ~100-hub run is a perf concern, NOT a golden (per CONTEXT deferred items). -->
<!-- The fixture must be a deterministic subset/construction (a fixed 12-20-hub BigCityHub[] + fixed centerCount) so -->
<!--   the captured hash is reproducible same-seed; capture it ONLY after proving same-seed reproducibility first. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Choose the regional-center count (empirical, NET-02)</name>
  <decision>The regional-center count for the continental topology (NET-02 — deliberately deferred to this phase to be chosen empirically).</decision>
  <context>
    The research envelope is ~4-8 with a near-full-mesh backbone (default ~5-6). The count must be chosen
    from a REAL continental run that validates trailer-fill/consolidation, never collapse to a single
    primary, and keep the near-full-mesh cheap + per-center fan-out bounded. Before this checkpoint, run a
    continental simulation at a few candidate counts (e.g. 4, 6, 8) and report, per count: hub-count,
    per-center spoke fan-out (min/median/max), max spoke->center leg vs the leg cap, backbone leg count
    (n*(n-1)), the anti-SPOF result, and a trailer-fill/consolidation proxy (e.g. avg packages per
    consolidation trailer at the center). Recommend the count that keeps fan-out bounded and trailer-fill
    healthy on a near-full mesh.
  </context>
  <options>
    <option id="centers-4">
      <name>4 centers</name>
      <pros>Fewest backbone legs (12 directed); minimal map clutter; cheapest mesh.</pros>
      <cons>Highest per-center fan-out; risk of thin consolidation / over-concentration; longer spoke legs.</cons>
    </option>
    <option id="centers-6">
      <name>6 centers (research default)</name>
      <pros>Balanced fan-out; <=2-hop coast-to-coast; near-full mesh stays cheap (30 directed legs); healthy trailer-fill.</pros>
      <cons>Slightly more backbone legs to render than 4.</cons>
    </option>
    <option id="centers-8">
      <name>8 centers</name>
      <pros>Lowest per-center fan-out; shortest spoke legs; strongest consolidation locality.</pros>
      <cons>56 directed backbone legs (more render/scope cost); approaches the FEATURES upper bound.</cons>
    </option>
  </options>
  <resume-signal>Select: centers-4, centers-6, centers-8, or specify another count in [4,8] with rationale.</resume-signal>
</task>

<task type="execute">
  <name>Task 1: Record the empirical center-count decision + committed partition snapshot</name>
  <read_first>
    - packages/simulation/src/network/centers.ts (pickRegionalCenters, assignSpokesToNearestCenter, buildBackbone, isConnectedWithoutAnyCenter)
    - packages/simulation/src/network/routes.ts (hubCoordsChecksum)
  </read_first>
  <files>packages/simulation/src/network/centers.ts, packages/simulation/src/network/center-partition.snapshot.json</files>
  <action>
    Set the engine's `centerCount` const (plan 23-04) to the value chosen at the checkpoint, with a one-line
    rationale comment citing the run metrics. Emit a committed `center-partition.snapshot.json` capturing:
    the chosen `centerCount` + rationale summary, the selected center hubIds, the full spoke->center
    assignment map, the backbone leg ids, the anti-SPOF result, the `hubsChecksum`, and a `partitionChecksum`
    (a stable digest of the sorted assignment map). Add a small exported helper in `centers.ts` that
    re-derives the partition (so the drift guard can recompute it). Assert here that `centerCount >= 2`
    (never a single primary) and that `isConnectedWithoutAnyCenter` is true for the chosen set.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && node -e "const s=require('./packages/simulation/src/network/center-partition.snapshot.json'); if(s.centerCount<2) throw new Error('single primary'); if(!s.partitionChecksum||!s.hubsChecksum) throw new Error('no checksum'); if(!s.antiSpof) throw new Error('SPOF'); console.log('OK count',s.centerCount)"</automated>
  </verify>
  <acceptance_criteria>
    - center-partition.snapshot.json exists, committed, with centerCount (>=2), rationale, center ids, assignment map, backbone ids, antiSpof:true, hubsChecksum, partitionChecksum.
    - The engine `centerCount` const equals the snapshot's centerCount.
  </acceptance_criteria>
  <done>The empirically-chosen center count + full partition are recorded in a committed, checksummed snapshot; the network has >=2 centers and survives any single-center removal.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Dataset + partition drift guard</name>
  <read_first>
    - packages/simulation/src/network/centers.ts (the re-derive helper from Task 1)
    - packages/simulation/src/network/us-big-cities.generated.json + center-partition.snapshot.json
  </read_first>
  <files>packages/simulation/test/hub-dataset-drift.unit.test.ts</files>
  <behavior>
    - Re-deriving `hubCoordsChecksum(generateBigCityHubs())` equals the committed `hubsChecksum` (dataset drift guard).
    - Re-deriving the center partition (pickRegionalCenters + assignSpokesToNearestCenter at the snapshot's centerCount) equals the committed `partitionChecksum` and assignment map (partition drift guard).
    - The generated hub set is in [80,130], continental envelope, unique ids (re-asserts HUB-01/02/03 invariants as a guard).
  </behavior>
  <action>
    Write `hub-dataset-drift.unit.test.ts` asserting the dataset checksum and the partition checksum match
    the committed snapshots, and re-asserting the envelope/count/uniqueness invariants. This is the
    supply-chain-of-data integrity guard (a silent data swap or a partition change becomes a red test, not a
    silent re-route).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation exec vitest run hub-dataset-drift 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Dataset checksum + partition checksum match the committed snapshots.
    - Count/envelope/uniqueness invariants re-asserted green.
  </acceptance_criteria>
  <done>A drift guard proves the committed dataset + center partition are reproducible; any change to either is a visible failing diff.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Flags-off two-part gate (DET-01) + the new continental golden</name>
  <read_first>
    - packages/simulation/test/determinism.unit.test.ts (the DET-01 two-part gate + the 10k golden + the consolidation/outbound false===absent pattern)
  </read_first>
  <files>packages/simulation/test/determinism.unit.test.ts, packages/simulation/test/continental-determinism.unit.test.ts</files>
  <behavior>
    - DET-01 part (a): `simulate({ seed: 42, durationTicks: 500, continentalTopology: false })` is byte-identical to the flag being absent.
    - DET-01 part (b): `simulate({ seed: 42, durationTicks: 10000 })` (flag absent) still hashes to 3920accc... .
    - New continental golden: a small fixed 12-20-hub fixture + fixed centerCount + fixed seed/durationTicks hashes to a freshly-captured committed SHA-256; the SAME run twice in-process hashes identically (reproducibility proven BEFORE the golden is committed — never bake a non-reproducible hash).
    - The continental-on stream genuinely differs from the legacy stream (sanity: the new golden != 3920accc...).
  </behavior>
  <action>
    Add the `continentalTopology: false === absent` case to the existing DET-01 `describe` in
    `determinism.unit.test.ts` (mirror the `outboundDeliveryEnabled: false` case), and an explicit
    `continentalTopology ABSENT => 3920accc...` 10k assertion. Create
    `continental-determinism.unit.test.ts` (mirroring `consolidation-determinism.unit.test.ts`): define the
    fixed 12-20-hub fixture + centerCount, FIRST assert same-seed in-process reproducibility (two runs hash
    equal), THEN capture + commit the continental golden constant and assert the run hashes to it. Keep the
    fixture hub count small for a fast hash (per CONTEXT — the ~100-hub run is a perf concern, not a golden).
    Keep great-circle transcendentals out of the hashed payload (decisions hash ids/integers; geometry is
    for the map).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation exec vitest run determinism 2>&1 | tail -30 && pnpm --filter @mm/simulation exec vitest run continental-determinism 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `continentalTopology: false` === absent (part a) PASSES.
    - flag-absent 10k golden === 3920accc... (part b) PASSES — the non-negotiable regression gate.
    - The continental run is same-seed reproducible AND hashes to the committed new golden; the new golden != 3920accc... .
  </acceptance_criteria>
  <done>The DET-01 two-part flags-off gate holds (false===absent AND absent=>3920accc...) and the continental model has its own committed, reproducible golden on a small fixture.</done>
</task>

<task type="execute">
  <name>Task 4: Full phase gate — build + typecheck + lint + tests across touched packages</name>
  <read_first>
    - .planning/phases/23-multi-center-topology/23-01-SUMMARY.md through 23-04-SUMMARY.md (what landed)
  </read_first>
  <files>packages/simulation/test/continental-determinism.unit.test.ts</files>
  <action>
    Run the project's standard verification gate per touched package — build, typecheck (the SEPARATE gate
    that catches test-file TS errors build/lint/vitest miss), lint, tests — for @mm/simulation,
    @mm/projections, @mm/optimizer, and @mm/web typecheck. Run ONE package gate at a time and clean up
    between them (per the v2.1 OOM lesson: a full-monorepo vitest gate OOMs from leftover workers/
    testcontainers). Fix any cross-package typecheck/lint fallout from the topology generalization (e.g. an
    additive `centerHubId` field surfacing in a consumer's exhaustive type). Do NOT weaken any determinism
    assertion to make a gate pass.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && for p in @mm/simulation @mm/projections @mm/optimizer; do echo "== $p =="; pnpm --filter $p build && pnpm --filter $p typecheck && pnpm --filter $p lint && pnpm --filter $p exec vitest run 2>&1 | tail -5 || exit 1; done && pnpm --filter @mm/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Each of @mm/simulation, @mm/projections, @mm/optimizer passes build + typecheck + lint + tests.
    - @mm/web typecheck is clean.
    - No determinism assertion was weakened to pass a gate.
  </acceptance_criteria>
  <done>The full phase passes build + typecheck + lint + tests for every touched package, one gate at a time, with no weakened determinism assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag off -> legacy golden | The seed-42 10k golden is the keystone integrity boundary; the gate proves the phase did not perturb it. |
| committed snapshot -> reproducibility | The dataset + partition checksums are the integrity record; the drift guard is the verifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-12 | Tampering | a non-reproducible golden baked in (a flake frozen forever, anti-P2/P8) | mitigate | Prove same-seed in-process reproducibility BEFORE committing the continental golden; capture-env note carried (cross-arch contingency documented next to the hash). |
| T-23-13 | Tampering | flags-off drift undetected | mitigate | DET-01 two-part gate (false===absent AND absent=>3920accc...) runs in CI; any drift is a red test. |
| T-23-14 | Tampering | silent center-count / partition change | mitigate | Committed center-partition.snapshot.json + the drift-guard test re-deriving both checksums. |
</threat_model>

<verification>
- DET-01 two-part gate green (false===absent; absent=>3920accc...).
- Continental golden reproducible same-seed + committed; != legacy golden.
- Dataset + partition drift guard green; centerCount >= 2; anti-SPOF true.
- Per-package build + typecheck + lint + tests green (one gate at a time); @mm/web typecheck clean.
</verification>

<success_criteria>
The DET-01 two-part flags-off gate holds, the continental model has its own committed reproducible golden
on a small fixture, the regional-center count is empirically chosen and recorded in a committed checksummed
partition snapshot (never a single primary), a drift guard makes any dataset/partition change visible, and
every touched package passes the full project gate.
</success_criteria>

<output>
After completion, create `.planning/phases/23-multi-center-topology/23-05-SUMMARY.md`
</output>
