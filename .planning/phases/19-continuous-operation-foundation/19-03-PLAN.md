---
phase: 19-continuous-operation-foundation
plan: 03
type: tdd
wave: 1
depends_on: [19-02]
files_modified:
  - packages/simulation/test/determinism.unit.test.ts
  - packages/simulation/test/network.unit.test.ts
  - packages/simulation/test/fuel-determinism.unit.test.ts
autonomous: true
requirements: [DET-01, DET-02]

must_haves:
  truths:
    - "simulate({ seed: 42, durationTicks: 10000 }) produces a real SHA-256 hash that matches LONG_RUN_GOLDEN_SHA256 committed in the test file"
    - "The committed hash is the TRUE hash from actually running the 10k-tick simulation — not a placeholder"
    - "buildRoutes() emits both a center→spoke AND spoke→center RouteRegistered event for every spoke (VQ#5 verification)"
    - "All 5 existing salt constants remain pairwise-distinct (salt-collision regression passes)"
    - "All pre-existing tests in determinism.unit.test.ts still PASS (DET-01 finite path byte-identical)"
  artifacts:
    - path: "packages/simulation/test/determinism.unit.test.ts"
      provides: "DET-02 describe block with committed real SHA-256 hash (not PLACEHOLDER)"
      contains: "LONG_RUN_GOLDEN_SHA256"
    - path: "packages/simulation/test/network.unit.test.ts"
      provides: "VQ#5 verification: RouteRegistered emitted for both directions per spoke"
      contains: "RouteRegistered"
    - path: "packages/simulation/test/fuel-determinism.unit.test.ts"
      provides: "Regression: 5-salt pairwise-distinct assertion still passes unchanged"
  key_links:
    - from: "packages/simulation/test/determinism.unit.test.ts"
      to: "packages/simulation/src/engine.ts (simulate function)"
      via: "simulate({ seed: 42, durationTicks: 10000 })"
      pattern: "simulate.*seed.*42.*10000"
    - from: "packages/simulation/test/network.unit.test.ts"
      to: "packages/simulation/src/network/routes.ts (buildRoutes)"
      via: "assert both directions per spoke"
      pattern: "RouteRegistered.*spoke.*center"
---

<objective>
GREEN phase for DET-02 (commit the real 10k-tick hash) plus verification tests for VQ#5 (bidirectional routes) and salt-collision regression. This plan generates the actual SHA-256 hash and commits it, replacing the PLACEHOLDER from plan-01.

Purpose: Seals the long-run determinism guarantee with a concrete hash constant. Verifies that bidirectional route registration already works (VQ#5 confirmed pre-implemented) and that salt constants remain collision-free.

Output: determinism.unit.test.ts with committed real hash. network.unit.test.ts extended with bidirectional assertion. Fuel-determinism regression confirmed passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-continuous-operation-foundation/19-RESEARCH.md
@.planning/phases/19-continuous-operation-foundation/19-PATTERNS.md

<interfaces>
<!-- Key sources to read before implementing -->

From packages/simulation/test/determinism.unit.test.ts (appended in plan-01):
```typescript
// The PLACEHOLDER to replace with the real hash:
const LONG_RUN_GOLDEN_SHA256 = "PLACEHOLDER_REPLACE_AFTER_FIRST_RUN";

describe("10k-tick determinism golden (DET-02)", () => {
  it("simulate({ seed: 42, durationTicks: 10000 }) produces a committed SHA-256 hash", () => {
    const stream = simulate({ seed: 42, durationTicks: 10000 });
    const hash = createHash("sha256").update(JSON.stringify(stream)).digest("hex");
    expect(hash).toBe(LONG_RUN_GOLDEN_SHA256);
  });
});
```

From packages/simulation/src/network/routes.ts (lines 283-306 — already bidirectional per VQ#5):
```typescript
for (let i = 1; i < hubs.length; i += 1) {
  const spoke = hubs[i]!;
  routes.push({ routeId: routeId(center.hubId, spoke.hubId), fromHubId: center.hubId, toHubId: spoke.hubId, ... });
  routes.push({ routeId: routeId(spoke.hubId, center.hubId), fromHubId: spoke.hubId, toHubId: center.hubId, ... });
}
```

From packages/simulation/test/fuel-determinism.unit.test.ts (lines 43-56):
```typescript
// 5-salt pairwise-distinct assertion — DO NOT MODIFY, just verify it passes
const salts = [RFID_RNG_SALT, OVER_CARRY_RNG_SALT, TIMING_RNG_SALT, HOS_RNG_SALT, FUEL_RNG_SALT];
expect(new Set(salts).size).toBe(5);
```

From packages/simulation/test/network.unit.test.ts — existing tests that can be extended:
(read the file to find the current describe blocks before adding assertions)
```typescript
// Existing: tests for hub registration, route existence etc.
// ADD: assertion that for each spoke, both directions are registered
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Generate real DET-02 hash and commit it to determinism.unit.test.ts (GREEN for DET-02)</name>
  <files>packages/simulation/test/determinism.unit.test.ts</files>
  <behavior>
    - The LONG_RUN_GOLDEN_SHA256 constant is replaced with the real 64-character hex SHA-256 string
    - The DET-02 test PASSES (hash matches)
    - All 7 pre-existing tests in the file still PASS (byte-identical finite path)
    - The DET-01 flags-off regression test PASSES
    - NOTE: the "generate real hash" step requires running simulate({ seed:42, durationTicks:10000 }) once and capturing the output hash — do this via a one-off script or temporary test that prints the hash, then commit the printed value
  </behavior>
  <read_first>
    - /Volumes/Unitek-B/Projects/jobs/intelliswift/packages/simulation/test/determinism.unit.test.ts (full file — read after plan-01 edits)
    - /Volumes/Unitek-B/Projects/jobs/intelliswift/packages/simulation/src/engine.ts lines 1251-1255 (simulate wrapper — confirm it exists and is callable)
  </read_first>
  <action>Step 1: Generate the real hash. Create a temporary one-off Bash script that runs:

    node --input-type=module <<'EOF'
    import { createHash } from 'node:crypto';
    const { simulate } = await import('./packages/simulation/src/engine.js');
    // If .js does not exist, use tsx:
    EOF

    OR more reliably: add a temporary throwaway test that PRINTS the hash (does not assert) — run it with pnpm vitest run and capture stdout:

    Temporarily change the DET-02 it() body to `console.log(hash); expect(true).toBe(true)` — run pnpm vitest run on that file — read the printed hash from stdout — then replace the body back with the real assertion `expect(hash).toBe("THE-REAL-HASH")` and update LONG_RUN_GOLDEN_SHA256 with the captured value.

    IMPORTANT: The engine.ts must build successfully (plan-02 must be complete) before this task runs. Verify pnpm build is green before attempting to import engine.

    After capturing the hash: replace `"PLACEHOLDER_REPLACE_AFTER_FIRST_RUN"` in determinism.unit.test.ts with the real 64-char hex string. The constant must be at the module level, not inside the describe block.

    Cross-architecture note (from RESEARCH VQ#9 / Pitfall 3): If this CI environment is x86 and production CI is ARM (or vice versa), the hash may diverge. Document the architecture used in a JSDoc comment on LONG_RUN_GOLDEN_SHA256. If hashes diverge on CI, the contingency is to replace sampleLogNormal with an integer lookup table (plan action for that contingency: see RESEARCH Pitfall 3 — do NOT implement the lookup table now unless the hash actually fails on CI).</action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm vitest run packages/simulation/test/determinism.unit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pnpm vitest run packages/simulation/test/determinism.unit.test.ts shows ALL tests PASSING including the DET-02 "10k-tick determinism golden" test
    - LONG_RUN_GOLDEN_SHA256 is a 64-character hex string (not "PLACEHOLDER_REPLACE_AFTER_FIRST_RUN")
    - The hash matches the actual output of simulate({ seed: 42, durationTicks: 10000 }) — confirmed by the test passing
    - grep -v "PLACEHOLDER" packages/simulation/test/determinism.unit.test.ts | grep "LONG_RUN_GOLDEN_SHA256" returns a line with a real hex value
    - DET-01 KEYSTONE GATE: all 7 pre-existing tests still PASS (finite path byte-identical)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VQ#5 bidirectional route verification test + salt-collision regression</name>
  <files>
    packages/simulation/test/network.unit.test.ts
    packages/simulation/test/fuel-determinism.unit.test.ts
  </files>
  <behavior>
    network.unit.test.ts — new assertion(s):
    - "buildRoutes() emits RouteRegistered for both center→spoke AND spoke→center for every spoke" — run simulate({ seed:42, durationTicks:100 }), filter events by type==="RouteRegistered", for each spoke hubId assert BOTH routeId(center, spoke) AND routeId(spoke, center) appear in the event stream
    - "RouteRegistered spoke→center route has fromHubId === spoke.hubId and toHubId === center.hubId" — assert payload fields match expected direction

    fuel-determinism.unit.test.ts — NO CHANGES; only run it and confirm it still passes. The salt-collision assertion at lines 43-56 (`expect(new Set(salts).size).toBe(5)`) already covers the 5 existing salts. No new salt constants are added in Phase 19.
  </behavior>
  <read_first>
    - /Volumes/Unitek-B/Projects/jobs/intelliswift/packages/simulation/test/network.unit.test.ts (full file — to avoid duplicating existing assertions)
    - /Volumes/Unitek-B/Projects/jobs/intelliswift/packages/simulation/src/network/routes.ts lines 283-306 (already bidirectional — confirmed by VQ#5)
    - /Volumes/Unitek-B/Projects/jobs/intelliswift/packages/simulation/test/fuel-determinism.unit.test.ts lines 43-56 (salt-collision assertion — do not modify)
    - /Volumes/Unitek-B/Projects/jobs/intelliswift/packages/domain/src/events (RouteRegistered schema — for exact payload field names)
  </read_first>
  <action>Read network.unit.test.ts to understand existing test structure. Add a new describe block: "bidirectional route registration (VQ#5 verification)". Inside: run simulate with a small durationTicks (100 is enough to capture all bootstrap RouteRegistered events). Filter for type === "RouteRegistered". The 10 hubs = 1 center + 9 spokes → 9 spoke-legs × 2 directions = 18 RouteRegistered events. Assert events.length === 18 (or >= 18). For the first spoke found in the payload, assert both directions appear: one with fromHubId === centerId and one with fromHubId === spokeId. Assert the two routeIds are not equal (opposite directions have different IDs). Import simulate from "../src/engine.js" if not already imported. Use the same `import { describe, expect, it } from "vitest"` pattern as the existing file. Do NOT modify fuel-determinism.unit.test.ts — only run it to confirm no regression.</action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm vitest run packages/simulation/test/network.unit.test.ts packages/simulation/test/fuel-determinism.unit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pnpm vitest run on network.unit.test.ts: all existing tests PASS + new "bidirectional route registration (VQ#5)" tests PASS
    - Confirmed: 18 RouteRegistered events emitted (9 spokes × 2 directions)
    - pnpm vitest run on fuel-determinism.unit.test.ts: all tests PASS unchanged (5-salt collision test passes)
    - pnpm test (full unit suite) green
    - DET-01 KEYSTONE GATE: determinism.unit.test.ts still 100% green after network.unit.test.ts changes (no interference)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface. Test files only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | — | SHA-256 hash in test fixture | accept | node:crypto createHash is used for test fixture comparison only, not for user-facing cryptography. The hash is a determinism assertion, not a security control. No new attack surface. |
</threat_model>

<verification>
Full gate for plan-03:
- pnpm build — green
- pnpm typecheck — green
- pnpm vitest run packages/simulation/test/determinism.unit.test.ts — ALL tests GREEN including DET-02 with committed hash
- pnpm vitest run packages/simulation/test/network.unit.test.ts — bidirectional RouteRegistered assertion PASSES
- pnpm vitest run packages/simulation/test/fuel-determinism.unit.test.ts — salt-collision assertion PASSES (5 salts pairwise-distinct)
- pnpm test — unit suite 100% green
</verification>

<success_criteria>
- LONG_RUN_GOLDEN_SHA256 = real 64-char hex hash (not PLACEHOLDER)
- DET-02 test PASSES (simulate({ seed:42, durationTicks:10000 }) hash matches)
- VQ#5 verification PASSES (18 RouteRegistered events, both directions per spoke)
- Salt-collision regression PASSES (5 existing salts remain pairwise-distinct)
- DET-01 KEYSTONE: all pre-existing determinism tests byte-identical
</success_criteria>

<output>
After completion, create .planning/phases/19-continuous-operation-foundation/19-03-SUMMARY.md
</output>
