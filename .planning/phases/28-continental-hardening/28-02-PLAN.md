---
phase: 28-continental-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "28-01"
files_modified:
  - packages/simulation/test/coordinator-determinism.unit.test.ts
autonomous: true
requirements:
  - DET-02

must_haves:
  truths:
    - "Shuffling the per-tick coordinator/center iteration order yields a byte-identical emitted suggestion batch (the engine already sorts by centerId — this test proves it)"
    - "The sorted coordinator batch order is the codepoint-sorted centerId order regardless of input permutation"
    - "The shuffle-invariance test imports COORDINATOR_ON_GOLDEN_SHA256 and FLAGS_OFF_GOLDEN_SHA256 from goldens.ts (no bare literals)"
    - "All 5 goldens stay byte-identical after this change"
  artifacts:
    - path: "packages/simulation/test/coordinator-determinism.unit.test.ts"
      provides: "Coordinator agent-order-shuffle batch test (the GAP-1 witness)"
      contains: "coordinator-order-shuffle golden"
  key_links:
    - from: "packages/simulation/test/coordinator-determinism.unit.test.ts"
      to: "packages/simulation/src/coordinator/rng.ts"
      via: "deriveCoordinatorRng import — builds per-center substream from seed + centerId"
    - from: "coordinator-order-shuffle describe block"
      to: "sortAgentsByStableId (via codepoint sort inline)"
      via: "the test mirrors the engine's centerId-sort primitive directly"
---

<objective>
Add the coordinator agent-order-shuffle batch test to coordinator-determinism.unit.test.ts, mirroring ooda-determinism.unit.test.ts:49-105. The test shuffles the per-tick center/coordinator set, calls decideCoordinatorSuggestions on each center with its deriveCoordinatorRng substream, and asserts the emitted suggestion batch is byte-identical regardless of input order — proving the engine's centerId sort renders per-tick coordinator iteration order irrelevant.

Purpose: GAP-1 from the determinism-test inventory (28-CONTEXT.md). The OODA agent pass has this coverage; the coordinator pass does not. This closes the gap.

Output: New describe block appended to coordinator-determinism.unit.test.ts. No new file, no production changes, no golden changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-continental-hardening/28-CONTEXT.md
@.planning/phases/28-continental-hardening/28-01-SUMMARY.md

<interfaces>
<!-- Template: ooda-determinism.unit.test.ts lines 49-105 (the OODA shuffle pattern to mirror) -->

OODA shuffle pattern (the structural template):
  const ids = ["H05", "T003", "A1", ...];  // representative mixed agent set
  const SEED = 42;
  const batch = (order: readonly string[]): string => {
    const agents = sortAgentsByStableId(order.map(stableId => ({ stableId })));
    const decisions = agents.map(a => decideTruck(obsFor(a.stableId), deriveAgentRng(SEED, a.stableId)));
    return JSON.stringify({ order: agents.map(a => a.stableId), decisions });
  };
  it("shuffling yields byte-identical batch") => { ... }
  it("sorted batch order is codepoint-sorted stable-id order") => { ... }

Coordinator analog (adapt the OODA pattern):
  - Replace sortAgentsByStableId with inline codepoint sort (sort by centerId string):
      const sorted = (order: readonly string[]) => [...order].sort((a, b) => a < b ? -1 : a > b ? 1 : 0)
  - Replace decideTruck + deriveAgentRng with decideCoordinatorSuggestions + deriveCoordinatorRng
  - Replace AgentObservation with CoordinatorObservation
  - Center ids instead of agent ids

From packages/simulation/src/coordinator/index.ts (all needed exports):
  deriveCoordinatorRng(seed: number, centerId: string): Rng
  decideCoordinatorSuggestions(obs: CoordinatorObservation, rng: Rng, simTimeMs: number, opts: ...): CoordinatorSuggestion[]

From packages/simulation/src/coordinator/observe.ts (type):
  export interface CoordinatorObservation {
    centerId: string;
    simTimeMs: number;
    spokes: ObservedSpoke[];
    trucks: ObservedTruck[];
  }

From packages/simulation/src/coordinator/coordinator.ts:
  export function decideCoordinatorSuggestions(obs: CoordinatorObservation, rng: Rng, guardState: ..., simTimeMs: number): CoordinatorSuggestion[]
  // IMPORTANT: check the actual decideCoordinatorSuggestions signature in source before writing the test
  // The call site in coordinator-determinism.unit.test.ts currently has NO direct call — only simulate()

From packages/simulation/test/coordinator-determinism.unit.test.ts (existing file structure):
  - 3 describe blocks: salt-collision, coordinator-on golden, stream non-trivial counts
  - Imports: createHash, describe/expect/it, FuelConfig, salts, simulate from engine, COORDINATOR_RNG_SALT from coordinator/index
  - After Plan 28-01: also imports FLAGS_OFF_GOLDEN_SHA256/OODA_ON_GOLDEN_SHA256/COORDINATOR_ON_GOLDEN_SHA256 from "./goldens.js"

From packages/simulation/test/goldens.ts (created in 28-01):
  export const FLAGS_OFF_GOLDEN_SHA256 = "3920accc...";
  export const OODA_ON_GOLDEN_SHA256 = "94689f99...";
  export const COORDINATOR_ON_GOLDEN_SHA256 = "edfa5a6d...";
  export const OPTIMIZER_ON_GOLDEN_SHA256 = "162efbd8...";
  export const CONTINENTAL_GOLDEN_SHA256 = "8f91b13f...";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read decideCoordinatorSuggestions signature and CoordinatorObservation shape</name>
  <files>packages/simulation/src/coordinator/coordinator.ts, packages/simulation/src/coordinator/observe.ts</files>
  <action>
Read packages/simulation/src/coordinator/coordinator.ts and packages/simulation/src/coordinator/observe.ts to extract:
1. The exact signature of decideCoordinatorSuggestions (parameters, types, any guard-state argument)
2. The exact shape of CoordinatorObservation and ObservedSpoke / ObservedTruck
3. Any guard-state parameter needed for the call (the Phase-24 ooda analog just calls decideTruck(obs, rng) — check if the coordinator version needs additional args like guard maps)

This read-first task ensures the test in Task 2 uses the correct call signature rather than guessing. Do NOT write any code in this task — record findings only.
  </action>
  <verify>
    <automated>grep -n "export function decideCoordinatorSuggestions\|CoordinatorObservation\|ObservedSpoke\|ObservedTruck" /Volumes/Unitek-B/Projects/jobs/intelliswift/packages/simulation/src/coordinator/coordinator.ts /Volumes/Unitek-B/Projects/jobs/intelliswift/packages/simulation/src/coordinator/observe.ts 2>/dev/null | head -30</automated>
  </verify>
  <done>Exact decideCoordinatorSuggestions call signature and CoordinatorObservation interface confirmed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append coordinator-order-shuffle describe block to coordinator-determinism.unit.test.ts</name>
  <files>packages/simulation/test/coordinator-determinism.unit.test.ts</files>
  <action>
Append a new describe block to coordinator-determinism.unit.test.ts after the existing 3 describe blocks. This is the GAP-1 closure. The block title is "coordinator-order-shuffle golden (COORD-01, Pitfall 1+4)".

Pattern to follow: ooda-determinism.unit.test.ts:49-105 exactly, adapted for coordinators:

1. Choose a representative set of center ids. Use 6 center ids that mirror the production setup: e.g. ["CTR-NE", "CTR-SE", "CTR-MW", "CTR-SW", "CTR-NW", "CTR-CENTRAL"] (or whichever centerId strings match the production fixture — check what center ids the engine uses; if they are not deterministic across runs, use synthetic strings that spell out the domain intent).

2. Build an obsFor(centerId) factory that constructs a minimal but non-trivial CoordinatorObservation for a given centerId. The observation must exercise the congestion-threshold so decideCoordinatorSuggestions actually produces suggestions (not a vacuously-empty batch). Use the exact type from the source read in Task 1.

3. Build a batch(order) function (matching the OODA analog):
   - Sort the input order array by codepoint (the centerId codepoint sort — same semantics as sortAgentsByStableId but for center ids).
   - For each sorted centerId: call decideCoordinatorSuggestions(obsFor(centerId), deriveCoordinatorRng(SEED, centerId), ...) with whatever guard-state/simTimeMs the function requires (use minimal/clean state so the call succeeds; no real guard state needed for the shuffle-invariance proof).
   - Return JSON.stringify({ order: sortedIds, suggestions }) as the batch string.

4. Two it() assertions:
   a. "shuffling the per-tick center INPUT order yields a byte-identical batch"
      - Compute inOrder = batch(centerIds)
      - Assert batch([...centerIds].reverse()) === inOrder
      - Assert batch([rotate of centerIds]) === inOrder
      - Assert batch([another permutation]) === inOrder
   b. "the sorted batch order is the codepoint-sorted centerId order (input-independent)"
      - The sorted order from batch(reversed) equals [...centerIds].sort()

Add the required import for decideCoordinatorSuggestions, deriveCoordinatorRng, and CoordinatorObservation (and any other types needed from Task 1 findings) if not already imported. Do NOT add a bare golden string literal — if the batch test needs a golden, derive it via same-run comparison rather than a hardcoded SHA. (The OODA shuffle test does NOT bake a separate golden for the shuffle batch — it only asserts invariance across permutations. Mirror that approach exactly.)

The block must include a JSDoc comment explaining it mirrors ooda-determinism.unit.test.ts:49-105 for the per-center coordinator set (GAP-1 from the determinism-test inventory, 28-CONTEXT.md).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation test -- --run --reporter=verbose 2>&1 | grep -A 3 "coordinator-order-shuffle"</automated>
  </verify>
  <done>
New describe block "coordinator-order-shuffle golden" is present in coordinator-determinism.unit.test.ts with 2 passing it() assertions. pnpm --filter @mm/simulation test -- --run is green. All 5 goldens unchanged (FLAGS_OFF_GOLDEN_SHA256 / OODA_ON_GOLDEN_SHA256 / COORDINATOR_ON_GOLDEN_SHA256 / OPTIMIZER_ON_GOLDEN_SHA256 / CONTINENTAL_GOLDEN_SHA256 all still match their committed values — confirmed by the existing determinism tests passing).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness | New describe block is test-only; zero production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Tampering | shuffle-invariance assertion weakened | mitigate | Test asserts byte-identical JSON across 3+ permutations (not a probabilistic check); a change to coordinator sort order fails immediately |
| T-28-04 | Information Disclosure | none | accept | test-only; no secrets |

No production code changes.
</threat_model>

<verification>
cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation test -- --run 2>&1 | tail -20

All tests green including the 2 new it() assertions in "coordinator-order-shuffle golden". No golden values changed:
  grep -c "COORDINATOR_ON_GOLDEN_SHA256\|FLAGS_OFF_GOLDEN_SHA256" packages/simulation/test/coordinator-determinism.unit.test.ts
should be > 0 (using canonical names from goldens.ts, not bare literals).
</verification>

<success_criteria>
- "coordinator-order-shuffle golden (COORD-01, Pitfall 1+4)" describe block present in coordinator-determinism.unit.test.ts
- 2 it() assertions: shuffle yields byte-identical batch; sorted order is codepoint-sorted centerIds
- Both assertions pass in pnpm --filter @mm/simulation test -- --run
- No bare golden string literals added (only named imports from goldens.ts or relative-equivalence assertions)
- All 5 committed goldens pass (determinism suite green)
</success_criteria>

<output>
After completion, create .planning/phases/28-continental-hardening/28-02-SUMMARY.md
</output>
