---
phase: 28-continental-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/simulation/test/goldens.ts
  - packages/simulation/test/determinism.unit.test.ts
  - packages/simulation/test/ooda-determinism.unit.test.ts
  - packages/simulation/test/coordinator-determinism.unit.test.ts
  - packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts
  - packages/simulation/test/continental-determinism.unit.test.ts
  - packages/simulation/test/coordinator-engine.unit.test.ts
autonomous: true
requirements:
  - DET-02

must_haves:
  truths:
    - "All 5 golden SHA-256 constants live in exactly one canonical file (goldens.ts) and nowhere else as bare string literals"
    - "Every determinism-family test file imports its golden constants from goldens.ts"
    - "The two-part flags-off gate (false===absent AND absent=>3920accc) for all 4 v3.0 flags lives in determinism.unit.test.ts and is not duplicated in coordinator-engine or ooda-engine tests"
    - "The cross-arch capture-env note and integer-LUT contingency are consolidated into goldens.ts as the provenance block"
    - "All 5 goldens remain byte-identical after the refactor: the test suite is green before and after"
  artifacts:
    - path: "packages/simulation/test/goldens.ts"
      provides: "Single canonical home for all 5 golden SHA-256 constants + capture provenance + cross-arch notes"
      exports:
        - FLAGS_OFF_GOLDEN_SHA256
        - OODA_ON_GOLDEN_SHA256
        - COORDINATOR_ON_GOLDEN_SHA256
        - OPTIMIZER_ON_GOLDEN_SHA256
        - CONTINENTAL_GOLDEN_SHA256
  key_links:
    - from: "packages/simulation/test/determinism.unit.test.ts"
      to: "packages/simulation/test/goldens.ts"
      via: "named import of FLAGS_OFF_GOLDEN_SHA256 / OODA_ON_GOLDEN_SHA256 / COORDINATOR_ON_GOLDEN_SHA256 / OPTIMIZER_ON_GOLDEN_SHA256"
    - from: "packages/simulation/test/ooda-determinism.unit.test.ts"
      to: "packages/simulation/test/goldens.ts"
      via: "named import of FLAGS_OFF_GOLDEN_SHA256 / OODA_ON_GOLDEN_SHA256"
    - from: "packages/simulation/test/coordinator-determinism.unit.test.ts"
      to: "packages/simulation/test/goldens.ts"
      via: "named import of FLAGS_OFF_GOLDEN_SHA256 / OODA_ON_GOLDEN_SHA256 / COORDINATOR_ON_GOLDEN_SHA256"
    - from: "packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts"
      to: "packages/simulation/test/goldens.ts"
      via: "named import of all 4 named-model goldens"
    - from: "packages/simulation/test/continental-determinism.unit.test.ts"
      to: "packages/simulation/test/goldens.ts"
      via: "named import of FLAGS_OFF_GOLDEN_SHA256 / CONTINENTAL_GOLDEN_SHA256"
    - from: "packages/simulation/test/coordinator-engine.unit.test.ts"
      to: "packages/simulation/test/goldens.ts"
      via: "named import of FLAGS_OFF_GOLDEN_SHA256 (replaces bare local const GOLDEN)"
---

<objective>
Extract the 5 golden SHA-256 constants into a single canonical module (goldens.ts), consolidate the cross-arch capture-env provenance block there, import everywhere, and remove all duplicated bare string literals — including the redundant two-part flags-off copy in coordinator-engine.unit.test.ts.

Purpose: A refactor typo in any golden constant now fails ALL tests loudly instead of silently drifting. "One place to audit" is the DET-02 keystone for this phase. No golden values change; no production code changes; no new tests yet (those land in 28-02 and 28-03 and will import from goldens.ts).

Output: goldens.ts (new) + 6 edited test files (string literals replaced with named imports).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-continental-hardening/28-CONTEXT.md

<interfaces>
<!-- Golden SHA-256 constants to be canonicalized. All verified green in the test suite. -->

From packages/simulation/test/determinism.unit.test.ts (line 125-126):
  FLAGS_OFF: "3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861"
  Captured: simulate({ seed: 42, durationTicks: 10000 }) on x86_64 (darwin)

From packages/simulation/test/ooda-determinism.unit.test.ts (line 206-208):
  OODA_ON: "94689f9989c0019edff27134dad0ef4cfb07c15c9c308ef4b40c38e848f4e608"
  Captured: simulate({ seed: 42, durationTicks: 10000, oodaAgentsEnabled: true, ... }) on x86_64

From packages/simulation/test/coordinator-determinism.unit.test.ts (line 112-116):
  COORDINATOR_ON: "edfa5a6d40b36e3774797b60d7bd99b5a8af7cce97adb1e775bad0b56b514adc"
  Captured: Phase-25 all-on stack on arm64 (darwin)

From packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts (line 114-125):
  OPTIMIZER_ON: "162efbd8c02f64c7fed96e142ec9d26c3b26c283c44bf80979a67dc9d6d3f233"
  Captured: Phase-26/P27-A all-on + coordinatorUsesOptimizer:true on arm64 (darwin)
  NOTE: DIFFERS from COORDINATOR_ON (route-aware divergence, 3 pins removed in P27-04)

From packages/simulation/test/continental-determinism.unit.test.ts (line 78):
  CONTINENTAL: "8f91b13f06e8481b5d80f0beb3c36b9307abad21242bdc1696b8769175db6644"
  Captured: 14-hub fixture topology artifact on x86_64 (darwin), node v23

Duplicates to remove:
  - coordinator-engine.unit.test.ts:229  local const GOLDEN = "3920accc..."
  - ooda-determinism.unit.test.ts:206-208 local const OODA_ON_GOLDEN_SHA256, FLAGS_OFF_GOLDEN_SHA256
  - coordinator-determinism.unit.test.ts:112-116 local consts (3 names)
  - coordinator-optimizer-determinism.unit.test.ts:114-125 local consts (4 names)
  - continental-determinism.unit.test.ts:98 inline "3920accc..." in assertion

Cross-arch provenance (to consolidate into goldens.ts):
  - x86_64: FLAGS_OFF (3920accc), OODA_ON (94689f99), CONTINENTAL (8f91b13f), + node v23
  - arm64:  COORDINATOR_ON (edfa5a6d), OPTIMIZER_ON (162efbd8)
  - Integer-LUT contingency: only if CI hash fails; do NOT pre-apply
  - Reproducibility protocol: in-process ×2 + 2 separate node processes before baking
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create packages/simulation/test/goldens.ts — the canonical golden module</name>
  <files>packages/simulation/test/goldens.ts</files>
  <action>
Create a new TypeScript module at packages/simulation/test/goldens.ts. This file is pure constants — no imports, no logic. It is the single auditable home for all 5 v3.0 golden SHA-256 constants.

Structure the file as follows:

1. A top-of-file JSDoc comment (DET-02 canonical home) explaining the module's purpose: single source of truth, import this everywhere, a refactor typo fails all tests loudly.

2. A "CAPTURE PROVENANCE" block (as a JSDoc or block comment) documenting the cross-arch capture environment and the integer-LUT contingency note. Include:
   - FLAGS_OFF and OODA_ON and CONTINENTAL: captured on x86_64 (darwin); CONTINENTAL on node v23
   - COORDINATOR_ON and OPTIMIZER_ON: captured on arm64 (darwin)
   - All prior goldens (3920accc / 94689f99 / 8f91b13f) verified GREEN on arm64 host (float path arch-stable)
   - The integer lookup-table contingency: if a multi-arch CI run produces a different hash, replace the log-normal sampler with an integer LUT; do NOT do this unless the hash actually fails on CI
   - The reproducibility-first protocol: run twice in-process AND across 2 separate node processes before baking; never commit a non-reproducible golden
   - Per-golden capture config note (seed/durationTicks/flags from which golden was derived — brief, ≤1 line each)

3. Five exported string constants in this order, each preceded by a 1-2 line comment naming the model, the phase it was first captured in, and the full config fingerprint (seed, durationTicks, flags):
   - FLAGS_OFF_GOLDEN_SHA256 = "3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861"
     (simulate({ seed: 42, durationTicks: 10000 }), no v3.0 flags; Phase 19/DET-02)
   - OODA_ON_GOLDEN_SHA256 = "94689f9989c0019edff27134dad0ef4cfb07c15c9c308ef4b40c38e848f4e608"
     (Phase-24 all-on: oodaAgentsEnabled + hos + fuel + induction + consolidation)
   - COORDINATOR_ON_GOLDEN_SHA256 = "edfa5a6d40b36e3774797b60d7bd99b5a8af7cce97adb1e775bad0b56b514adc"
     (Phase-25 all-on: coordinatorsEnabled + OODA-on stack; rule-based reroute)
   - OPTIMIZER_ON_GOLDEN_SHA256 = "162efbd8c02f64c7fed96e142ec9d26c3b26c283c44bf80979a67dc9d6d3f233"
     (Phase-26/P27-A: coordinatorUsesOptimizer + full stack; route-aware-divergent, != COORDINATOR_ON)
   - CONTINENTAL_GOLDEN_SHA256 = "8f91b13f06e8481b5d80f0beb3c36b9307abad21242bdc1696b8769175db6644"
     (Phase-23: 14-hub fixture topology artifact, centerCount=4; NOT a simulate() run hash)

Use `export const` for each. No default export. File must have zero imports (it is a pure-data leaf so any test file can import it without pulling in engine code).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation exec tsc --noEmit --project tsconfig.json 2>&1 | head -20 || pnpm typecheck 2>&1 | grep "goldens" | head -10</automated>
  </verify>
  <done>File exists, exports all 5 named constants with exact SHA values, has provenance block, compiles cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor all 6 test files to import from goldens.ts — remove all bare string literal duplicates</name>
  <files>
    packages/simulation/test/determinism.unit.test.ts,
    packages/simulation/test/ooda-determinism.unit.test.ts,
    packages/simulation/test/coordinator-determinism.unit.test.ts,
    packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts,
    packages/simulation/test/continental-determinism.unit.test.ts,
    packages/simulation/test/coordinator-engine.unit.test.ts
  </files>
  <action>
For each of the 6 test files, add an import from "./goldens.js" for the named constants used in that file, then replace every bare golden string literal with the named constant. Read each file fully before editing it.

File-by-file changes (each file is touched once):

DETERMINISM.UNIT.TEST.TS:
- Add to imports: import { FLAGS_OFF_GOLDEN_SHA256, OODA_ON_GOLDEN_SHA256, COORDINATOR_ON_GOLDEN_SHA256, OPTIMIZER_ON_GOLDEN_SHA256 } from "./goldens.js";
- Remove the local const LONG_RUN_GOLDEN_SHA256 (line ~125-126). Replace all uses with FLAGS_OFF_GOLDEN_SHA256.
- In the COORDINATOR_ON sub-block (line ~413-416): remove local const COORDINATOR_ON_GOLDEN_SHA256 and OODA_ON_GOLDEN_SHA256. Replace uses with the canonical imports.
- In the OPTIMIZER sub-block references to edfa5a6d: replace inline "edfa5a6d..." string (if bare) with COORDINATOR_ON_GOLDEN_SHA256.
- All remaining bare "3920accc..." inline strings (the ones inside test body strings like assertions) also become FLAGS_OFF_GOLDEN_SHA256 references. Cross-check: after edit, grep for "3920accc\|94689f99\|edfa5a6d\|162efbd8\|8f91b13f" in this file should return zero code-line matches (only comment mentions are acceptable — comments explaining the model are fine; bare const/assignment literals must be gone).

OODA-DETERMINISM.UNIT.TEST.TS:
- Add import: import { FLAGS_OFF_GOLDEN_SHA256, OODA_ON_GOLDEN_SHA256 } from "./goldens.js";
- Remove local const OODA_ON_GOLDEN_SHA256 (line ~206) and FLAGS_OFF_GOLDEN_SHA256 (line ~208). Replace uses.
- After edit: zero bare golden literals on code lines.

COORDINATOR-DETERMINISM.UNIT.TEST.TS:
- Add import: import { FLAGS_OFF_GOLDEN_SHA256, OODA_ON_GOLDEN_SHA256, COORDINATOR_ON_GOLDEN_SHA256 } from "./goldens.js";
- Remove the 3 local golden consts (lines ~112-116). Replace uses.
- After edit: zero bare golden literals on code lines.

COORDINATOR-OPTIMIZER-DETERMINISM.UNIT.TEST.TS:
- Add import: import { FLAGS_OFF_GOLDEN_SHA256, OODA_ON_GOLDEN_SHA256, COORDINATOR_ON_GOLDEN_SHA256, OPTIMIZER_ON_GOLDEN_SHA256 } from "./goldens.js";
- Remove the 4 local golden consts (lines ~114-125). The file's COORDINATOR_OPTIMIZER_ON_GOLDEN_SHA256 local becomes OPTIMIZER_ON_GOLDEN_SHA256 from the import. Replace uses.
- After edit: zero bare golden literals on code lines.

CONTINENTAL-DETERMINISM.UNIT.TEST.TS:
- Add import: import { FLAGS_OFF_GOLDEN_SHA256, CONTINENTAL_GOLDEN_SHA256 } from "./goldens.js";
- Remove local const CONTINENTAL_GOLDEN_SHA256 (line ~78). Replace uses.
- The inline "3920accc..." string at line ~98 (inside the assertion that CONTINENTAL differs from flags-off) becomes FLAGS_OFF_GOLDEN_SHA256.
- After edit: zero bare golden literals on code lines.

COORDINATOR-ENGINE.UNIT.TEST.TS:
- Add import: import { FLAGS_OFF_GOLDEN_SHA256 } from "./goldens.js";
- Remove local const GOLDEN at line ~229. Replace the two uses (the const assignment AND the .toBe(GOLDEN) call) with FLAGS_OFF_GOLDEN_SHA256.
- The redundant two-part flags-off gate tests that DUPLICATE what determinism.unit.test.ts already has (the "coordinatorsEnabled ABSENT is byte-identical to the seed-42 10k golden" body): keep the tests but replace the string literal with FLAGS_OFF_GOLDEN_SHA256. Do NOT delete these tests — only remove the bare literal.
- After edit: zero bare golden literals on code lines.

CRITICAL: Do not change any test assertion logic, test structure, describe/it text, or comments. Only the string literal constant → named import substitution. The cross-arch capture provenance comments in the individual test files (e.g. "Captured on arm64..." lines) may stay as-is; the master block in goldens.ts now unifies them, but the per-file commentary is still useful context.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation test -- --run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
All determinism-family tests pass. grep for bare golden literals:
  grep -v "^[ \t]*//" packages/simulation/test/determinism.unit.test.ts packages/simulation/test/ooda-determinism.unit.test.ts packages/simulation/test/coordinator-determinism.unit.test.ts packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts packages/simulation/test/continental-determinism.unit.test.ts packages/simulation/test/coordinator-engine.unit.test.ts | grep -c "3920accc\|94689f99\|edfa5a6d\|162efbd8\|8f91b13f"
should equal 0.
All 5 goldens stay byte-identical (test suite green).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness | All changes are test file refactors; no production code path changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | goldens.ts constant values | mitigate | SHA constants are read-only exported strings; the test suite fails immediately if any value drifts — each test asserts the exact hash |
| T-28-02 | Repudiation | cross-arch provenance notes lost in refactor | accept | Provenance block in goldens.ts is the canonical record; per-file comments retained in individual test files for context |

No production code changes → no new attack surface. Threat surface is minimal (test/dev context only).
</threat_model>

<verification>
Run the full simulation test suite:
  cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm --filter @mm/simulation test -- --run 2>&1 | tail -20

Confirm all determinism tests pass (green). Then confirm zero bare golden literals remain on non-comment lines in the 6 edited files:
  grep -v "^[ \t]*//" packages/simulation/test/determinism.unit.test.ts packages/simulation/test/ooda-determinism.unit.test.ts packages/simulation/test/coordinator-determinism.unit.test.ts packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts packages/simulation/test/continental-determinism.unit.test.ts packages/simulation/test/coordinator-engine.unit.test.ts | grep "3920accc\|94689f99\|edfa5a6d\|162efbd8\|8f91b13f"
should produce no output.

Confirm goldens.ts exports all 5 names:
  grep -c "^export const" packages/simulation/test/goldens.ts
should equal 5.
</verification>

<success_criteria>
- goldens.ts exists, exports exactly 5 named SHA constants, has the provenance block, zero imports
- All 6 test files import from "./goldens.js" for any golden constant they reference
- Zero bare golden string literals remain on non-comment code lines in those 6 files
- pnpm --filter @mm/simulation test -- --run passes with all tests green (no golden changes)
- pnpm typecheck passes
</success_criteria>

<output>
After completion, create .planning/phases/28-continental-hardening/28-01-SUMMARY.md
</output>
