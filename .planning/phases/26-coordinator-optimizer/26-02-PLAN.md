---
phase: 26-coordinator-optimizer
plan: 02
type: tdd
wave: 2
depends_on: [26-01]
files_modified:
  - packages/simulation/src/engine.ts
  - packages/simulation/test/coordinator-optimizer.unit.test.ts
  - packages/api/src/sim/driver.ts
  - packages/api/src/server.ts
autonomous: true
requirements: [COORD-06]
must_haves:
  truths:
    - "With coordinatorUsesOptimizer on (a sub-flag of coordinatorsEnabled), the stepCoordinators REROUTE branch is sourced from a scoped pure runEpoch over a per-center twin built from in-engine fold state — hold/consolidate/dispatch stay rule-based"
    - "Each coordinator's per-center epoch scope is partitionScopeByCenter's slice for its center (scope ⊆ that center's affected hubs); a single event's scope is independent of total network size (NET-05 now governs a LIVE epoch)"
    - "When the deterministic horizon/size cap is exceeded, the reroute branch falls back to the Phase-25 rule-based decideCoordinatorSuggestions reroute — a documented deterministic threshold, never wall-clock"
    - "The global RollingLoop is disabled under the coordinator flag so the per-center coordinators and the global optimizer never double-plan"
    - "coordinatorUsesOptimizer absent/false ⇒ NO runEpoch call, NO partition, NO behavior change — the Phase-25 coordinator path is untouched"
  artifacts:
    - path: "packages/simulation/src/engine.ts"
      provides: "coordinatorUsesOptimizer SimulateOptions flag + the optimizer-backed reroute branch in stepCoordinators (partition + buildCenterTwinFromFold + runEpoch + epochResultToRerouteSuggestions + fallback)"
      contains: "coordinatorUsesOptimizer"
    - path: "packages/simulation/test/coordinator-optimizer.unit.test.ts"
      provides: "tests: reroute sourced from optimizer, scope-size-invariant live epoch, fallback trigger, off-path inertness"
    - path: "packages/api/src/sim/driver.ts"
      provides: "global RollingLoop disabled when the coordinator flag is on (loop.tick gated)"
  key_links:
    - from: "packages/simulation/src/engine.ts"
      to: "@mm/optimizer runEpoch + partitionScopeByCenter + detectAffectedScope"
      via: "in-fold synchronous call inside stepCoordinators"
      pattern: "runEpoch|partitionScopeByCenter"
    - from: "packages/simulation/src/engine.ts"
      to: "packages/simulation/src/coordinator/optimize.ts"
      via: "buildCenterTwinFromFold + epochResultToRerouteSuggestions"
      pattern: "buildCenterTwinFromFold|epochResultToRerouteSuggestions"
    - from: "packages/api/src/sim/driver.ts"
      to: "RollingLoop.tick"
      via: "gated no-op when coordinatorUsesOptimizer is on"
      pattern: "coordinatorUsesOptimizer|loop"
---

<objective>
Wire the Plan-01 pure adapter into the engine: under the new sub-flag `coordinatorUsesOptimizer`, the `stepCoordinators` REROUTE branch is generated by a scoped pure `runEpoch` over a per-center twin (partition → fold-slice → twin → runEpoch → translate), with a deterministic fallback to the Phase-25 rule-based reroute; keep hold/consolidate/dispatch rule-based; wire `partitionScopeByCenter` (NET-05) as the per-center epoch scope; and disable the global `RollingLoop` under the coordinator flag so the two never double-plan.

Purpose: This is COORD-06's core — the coordinator invokes the proven optimizer IP as a per-center, in-fold, pure suggestion engine, giving route-aware reroute suggestions instead of the rule-based congestion heuristic, while NET-05's scope-size invariant (proven at 500 hubs in P23) now governs a live epoch. The two-part flags-off gate + golden are Plan 03 — this plan establishes the behavior; Plan 03 pins it.

Output: a flag-gated optimizer-backed reroute path in `engine.ts`, the global loop disabled under the flag in the API driver, and an engine-level test suite proving the behavior + off-path inertness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-coordinator-optimizer/26-CONTEXT.md
@.planning/phases/26-coordinator-optimizer/26-01-SUMMARY.md

<read_first>
- packages/simulation/src/engine.ts lines 201-460 — the `SimulateOptions` interface (add `coordinatorUsesOptimizer` after `coordinatorsEnabled`, strict `=== true`, default OFF, mirroring the `coordinatorsEnabled` doc block at ~433-454). Read only this range.
- packages/simulation/src/engine.ts lines 2189-2378 — the FULL `stepCoordinators` body: OBSERVE (frozen fold-map snapshot), `coordinatorCenterIds`, `observeCenter`, the per-center DECIDE loop that calls `decideCoordinatorSuggestions`, the GUARD filter (lease→prune→backoff→hysteresis), emit + lease + pendingSuggestionsByTarget. The optimizer-backed reroute branch REPLACES only the reroute candidates feeding the guard filter; hold/consolidate/dispatch candidates stay from the rule-based generator. Read only this range.
- packages/simulation/src/engine.ts lines 1056-1145 — the fold state the per-center twin reads: `center`, `centerIds`, `spokes`, `routes` (buildRoutes output: TwinRoute-compatible legs), `centerOf(spokeHubId)`, `legMilesFor`, `trailerRoster` (trailerId/driverId/spoke). Plus `activeTripByTrailer` (~855), `pendingBySpoke`/`pendingAtSpoke` (~1464-1484) — the freight manifests. Read only these ranges; do NOT re-read the whole engine.
- packages/optimizer/src/rolling/scope.ts — `detectAffectedScope(events, epoch)` + `partitionScopeByCenter(scope, centerOf, events)` returning `ReadonlyMap<centerHubId, OptimizerScope>`. The per-center slice for `centerId` is the live epoch scope; its size is independent of total network size (the NET-05 invariant). centerOf here is a `ReadonlyMap<hubId, centerHubId>` — build it from the engine's continentalCenterOf (or hubs[0] collapse off-continental).
- packages/optimizer/src/rolling/epoch.ts lines 536-556 — `runEpoch(epoch, input, weights)` + the empty-scope early return (scope.trailerIds.length === 0 ⇒ empty recommendations). Read only this range.
- packages/simulation/src/coordinator/optimize.ts (from Plan 01) — `buildCenterTwinFromFold(slice, nowMin)` + `epochResultToRerouteSuggestions(result, currentNextHubByTrailer)` + `CenterFoldSlice`.
- packages/api/src/sim/driver.ts lines 89-105 — `makeSimRunner` / the `loop.tick` per-tick call; lines 528-600 + 895-960 — where `driveSimulation*` pass `loop` through. The gate: when the run's coordinator-optimizer flag is on, `makeSimRunner` must return the no-op runner (the global loop is disabled). Read only those ranges.
- packages/api/src/server.ts lines 180-210 — where `new RollingLoop({...})` is constructed and handed to the driver; this is the composition root that decides whether the loop is wired. Read only this range.
- packages/simulation/test/coordinator-engine.unit.test.ts — the existing engine-level coordinator integration test patterns (how a run is driven, how suggestion kinds are counted) to mirror for the new optimizer-backed suite. Read the top ~80 lines for the harness shape.
</read_first>

<interfaces>
From @mm/optimizer (barrel):
```
function detectAffectedScope(events, epoch): OptimizerScope
function partitionScopeByCenter(scope: OptimizerScope, centerOf: ReadonlyMap<string,string>, events): ReadonlyMap<string, OptimizerScope>
function runEpoch(epoch: Epoch, input: EpochInput, weights: ObjectiveWeights): EpochResult
interface OptimizerScope { hubIds; trailerIds; horizonStartMin; horizonEndMin; timeStepMin }
```
From packages/simulation/src/coordinator/optimize.ts (Plan 01):
```
function buildCenterTwinFromFold(slice: CenterFoldSlice, nowMin: number): TwinSnapshot
function epochResultToRerouteSuggestions(result: EpochResult, currentNextHubByTrailer: ReadonlyMap<string,string>): readonly CoordinatorSuggestion[]
interface CenterFoldSlice { centerId; spokeHubIds; trailers; legs }  // exact shape per Plan 01
```
Engine fold state already in scope inside stepCoordinators: `spokes`, `routes`, `centerOf`, `trailerRoster`, `activeTripByTrailer`, `pendingBySpoke`, `pendingAtSpoke`, `legMilesFor`, `seed`, `MS_PER_TICK`, `centerIds`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coordinatorUsesOptimizer flag + the optimizer-backed reroute branch in stepCoordinators</name>
  <files>packages/simulation/src/engine.ts, packages/simulation/test/coordinator-optimizer.unit.test.ts</files>
  <action>
    Add `coordinatorUsesOptimizer?: boolean` to `SimulateOptions` (strict `=== true`, default OFF, sub-flag of coordinatorsEnabled — document that it has NO effect unless coordinatorsEnabled is also on). Read it as `const coordinatorUsesOptimizer = coordinatorsEnabled && opts.coordinatorUsesOptimizer === true;` near the existing `coordinatorsEnabled` read.

    In `stepCoordinators`, split the candidate generation: keep `decideCoordinatorSuggestions(obs, rng)` producing hold/consolidate/dispatch candidates (filter its output to NON-reroute kinds when the sub-flag is on). When `coordinatorUsesOptimizer` is on, generate the REROUTE candidates from the optimizer instead (per CONTEXT decision D: optimizer REPLACES rule-based reroute only):
      1. Build the events batch for the partition input — the suggestion-driving events the coordinator reacts to this pass. Use the same deterministic, integer/string fold-derived signal the rule-based reroute reads (the in-region trucks' next-hub congestion); construct a minimal synthetic scope or reuse the affected-scope detection over this center's recent in-region events. Document the exact event source. Call `detectAffectedScope(events, epoch)` where `epoch = { epochId: `coord-${centerId}-${tick}`, nowMin: tick (tick is sim-minutes — 1 tick = 1 sim-min per MS_PER_TICK), freezeWindowMin: <named const> }`.
      2. `partitionScopeByCenter(scope, centerOfMap, events)` and take ONLY this center's slice (`.get(centerId)`). centerOfMap is a `ReadonlyMap<hubId, centerHubId>` derived from continentalCenterOf (or every hub → hubs[0] off-continental). This is the NET-05 wiring — the per-center epoch scope.
      3. Build the `CenterFoldSlice` from the FROZEN observation + fold maps (spokes, in-region trailerRoster entries, their activeTripByTrailer, pendingBySpoke/pendingAtSpoke counts as block volumes, routes restricted to this center's legs, legMilesFor for distanceMiles). Call `buildCenterTwinFromFold(slice, tick)`.
      4. Call `runEpoch(epoch, { events, twinSnapshot: twin }, DEFAULT_WEIGHTS)` synchronously in-fold (the optimizer's own default weights). This runs at a deterministic tick in sorted-centerId order over pure inputs — replay-safe (the core is no-RNG/no-clock per rolling/types.ts).
      5. `epochResultToRerouteSuggestions(result, currentNextHubByTrailer)` where currentNextHubByTrailer maps each in-region trailerId → its activeTripByTrailer toHubId. Merge these reroute candidates with the rule-based non-reroute candidates, preserving the existing sorted/guard-filter ordering (reroute candidates sorted by targetAgentId, then the existing per-spoke order for the rest — keep the suggestionId surviving-index scheme byte-stable).

    The merged candidate list flows through the EXISTING guard filter (lease→prune→backoff→hysteresis) + emit + lease + pendingSuggestionsByTarget UNCHANGED. NO new RNG substream beyond the existing per-center deriveCoordinatorRng. NO Date.now/Math.random/async. Every write stays gated under coordinatorsEnabled (already true) — the sub-flag only changes the SOURCE of reroute candidates.

    Add the named freeze-window + horizon constants near the other COORDINATOR_* consts. Import runEpoch/detectAffectedScope/partitionScopeByCenter from @mm/optimizer and the Plan-01 functions from ./coordinator/index.js.

    TEST (coordinator-optimizer.unit.test.ts): drive a run with coordinatorsEnabled + coordinatorUsesOptimizer + the all-on stack (OODA on, so activeTripByTrailer is populated — the reroute source). Assert: reroute ActionSuggested events ARE emitted; the run is same-seed byte-identical (deep-equal two runs); hold/consolidate/dispatch still appear (rule-based path intact). Assert a scope-size-invariance witness: the per-center reroute decisions for a fixed center do not change when extra unrelated centers/hubs are added (the live NET-05 invariant) — construct two configs differing only in unrelated network size and assert the target center's reroute suggestion set is identical.
  </action>
  <verify>
    <automated>pnpm --filter @mm/simulation exec vitest run test/coordinator-optimizer.unit.test.ts</automated>
  </verify>
  <done>coordinatorUsesOptimizer flag exists (strict === true, default off); the optimizer-backed reroute branch calls runEpoch in-fold over a per-center partitioned twin; hold/consolidate/dispatch stay rule-based; reroute events emitted; same-seed byte-identical; scope-size-invariance witness GREEN. `pnpm typecheck` clean; `pnpm --filter @mm/simulation lint` exit 0 (engine.ts is NOT under coordinator/** but the in-fold call must still be Date.now/Math.random-free — the existing engine purity discipline holds).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Deterministic horizon/size-cap fallback to the rule-based reroute</name>
  <files>packages/simulation/src/engine.ts, packages/simulation/test/coordinator-optimizer.unit.test.ts</files>
  <behavior>
    - A named deterministic cap (e.g. `COORDINATOR_OPTIMIZER_MAX_SCOPE_HUBS` / `..._MAX_TRAILERS`, integer, baked into the golden) bounds the per-center epoch. When this center's partitioned slice exceeds the cap, the reroute branch falls back to the Phase-25 rule-based `decideCoordinatorSuggestions` reroute candidates INSTEAD of calling runEpoch — a documented, deterministic threshold (per CONTEXT decision: "deterministic, documented threshold — not wall-clock").
    - Test: a small in-region scope (under the cap) ⇒ the reroute candidates come from runEpoch (optimizer path); the test distinguishes the paths by a path-witness (e.g. a counter/flag the test can observe, or asserting the optimizer path is taken for a sub-cap scope and the rule-based path for an over-cap scope by their distinct candidate signatures).
    - Test: an over-cap scope ⇒ the reroute candidates come from the rule-based generator; the run is still same-seed byte-identical; NO runEpoch is called for that center (no wall-clock dependence — the decision is purely the integer scope size vs the cap).
    - Test: the fallback is DETERMINISTIC — same seed + same network ⇒ the same set of centers take the optimizer path and the same set fall back (the threshold is a pure function of the partitioned scope size).
  </behavior>
  <action>Add the named integer cap constants near the COORDINATOR_* consts. In the optimizer-backed reroute branch, after computing this center's partitioned slice, compare `slice.hubIds.length` / `slice.trailerIds.length` against the caps; if exceeded, take the rule-based reroute candidates (reuse decideCoordinatorSuggestions's reroute output — do NOT call runEpoch). Document why (in-fold budget) and that the threshold is the partitioned scope SIZE (an integer), never wall-clock timing. Keep the rest of the merge/guard/emit path identical. Expose a minimal, test-only path witness if needed (a returned/observable per-pass counter of optimizer-vs-fallback decisions) without changing the event stream — prefer asserting via the candidate signatures so no production observability is added. NO Date.now/Math.random/async.</action>
  <verify>
    <automated>pnpm --filter @mm/simulation exec vitest run test/coordinator-optimizer.unit.test.ts</automated>
  </verify>
  <done>RED commit precedes GREEN; sub-cap scope ⇒ optimizer path; over-cap scope ⇒ rule-based fallback with no runEpoch call; the fallback decision is a pure function of integer scope size (deterministic, same-seed identical). `pnpm typecheck` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Disable the global RollingLoop under the coordinator-optimizer flag (no double-plan)</name>
  <files>packages/api/src/sim/driver.ts, packages/api/src/server.ts</files>
  <action>
    Per CONTEXT decision + ROADMAP success criterion 3: the global `RollingLoop` must be disabled when the per-center coordinators are the plan source, so the two never double-plan. Thread the run's `coordinatorUsesOptimizer` (and/or `coordinatorsEnabled`) flag into the API driver. In `makeSimRunner` (driver.ts ~97-105) — OR at the server composition root (server.ts ~185-207 where `new RollingLoop` is built and passed) — gate the loop: when the coordinator-optimizer flag is on, pass `loop: undefined` / return the no-op runner so `loop.tick` is never called. Prefer gating at the composition root (server.ts) so `RollingLoop` simply isn't wired when coordinators own planning — cleaner than a per-tick branch. Document the decision as: under the coordinator flag the coordinators are the primary (and only) plan source; the global loop is the non-continental / flags-off path.

    Add/extend an API test (or a focused assertion in an existing driver/server test) proving: with the coordinator-optimizer flag on, `loop.tick` is NOT invoked (the global loop is disabled); with it off, the loop runs as today (backward-compatible — the existing live-loop / rolling-service tests stay green). Do NOT introduce async-queue or any wall-clock coupling. This is API-plumbing only — it touches NO deterministic core file and emits NO new event.
  </action>
  <verify>
    <automated>pnpm --filter @mm/api exec vitest run src/optimizer/live-loop.test.ts src/sim/driver.test.ts</automated>
  </verify>
  <done>The global RollingLoop is not wired/called when coordinatorUsesOptimizer is on; off ⇒ unchanged (existing API tests green). `pnpm typecheck` clean; `pnpm --filter @mm/api lint` exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fold maps → in-fold runEpoch | live engine fold state crosses into a synchronous pure optimizer call inside the deterministic core; any clock/RNG/async leak breaks the Plan-03 golden |
| global loop vs coordinators | two independent plan sources; if both run they double-plan (conflicting suggestions) |
| sub-flag boundary | coordinatorUsesOptimizer must change ONLY the reroute source; bleeding into the off path breaks the two-part gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | in-fold runEpoch determinism | mitigate | runEpoch is pure/no-RNG/no-clock (rolling/types.ts); called at a deterministic tick in sorted-centerId order over pure fold-derived inputs; default weights; same-seed byte-identical test (Plan 03 pins the golden) |
| T-26-05 | EoP | global loop + coordinators double-plan | mitigate | global RollingLoop disabled under the flag (Task 3); test asserts loop.tick not invoked when on |
| T-26-06 | Tampering | sub-flag off-path bleed | mitigate | strict `=== true` gate; coordinatorUsesOptimizer only swaps the reroute SOURCE; off ⇒ no runEpoch/partition; Plan 03 proves byte-identical to the Phase-25 golden edfa5a6d |
| T-26-07 | DoS | unbounded in-fold optimizer cost | mitigate | named integer scope-size cap (Task 2) ⇒ deterministic rule-based fallback; short freeze-window horizon; per-center bounded scope (NET-05) |
| T-26-08 | DoS | scope grows with network size | mitigate | partitionScopeByCenter slice size independent of total network (scope-size-invariance witness, Task 1) |
</threat_model>

<verification>
- `pnpm --filter @mm/simulation exec vitest run test/coordinator-optimizer.unit.test.ts` — GREEN
- `pnpm --filter @mm/api exec vitest run src/optimizer/live-loop.test.ts src/sim/driver.test.ts` — GREEN
- `pnpm typecheck` — clean
- `pnpm --filter @mm/simulation lint` + `pnpm --filter @mm/api lint` — exit 0
- Run vitest lanes ONE AT A TIME with `pkill -f vitest` between (the v2-gate OOM memory — no exit 137)
</verification>

<success_criteria>
- coordinatorUsesOptimizer sub-flag gates the optimizer-backed reroute branch (strict === true, default off)
- Reroute candidates come from a scoped pure runEpoch over a per-center partitioned twin; hold/consolidate/dispatch stay rule-based
- partitionScopeByCenter wired as the per-center epoch scope (NET-05 live), scope-size invariant
- Deterministic integer-scope-size fallback to rule-based reroute
- Global RollingLoop disabled under the flag (no double-plan); off path unchanged
- Same-seed byte-identical (the golden is Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/26-coordinator-optimizer/26-02-SUMMARY.md`
</output>
