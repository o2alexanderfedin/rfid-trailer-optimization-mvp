---
phase: 26-coordinator-optimizer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/simulation/src/coordinator/optimize.ts
  - packages/simulation/src/coordinator/optimize.unit.test.ts
  - packages/simulation/src/coordinator/index.ts
autonomous: true
requirements: [COORD-06]
must_haves:
  truths:
    - "A pure builder turns one center's in-engine fold state (its spokes, in-region trailers, their active trips + freight manifests) into a small @mm/optimizer TwinSnapshot scoped to that center — built from fold maps, never a full event-log scan"
    - "A pure translator turns an EpochResult into reroute CoordinatorSuggestions: a trailer whose optimizer-chosen next leg differs from its current next hub yields one reroute suggestion (integer/string params only — toHubId), and a trailer the epoch leaves unchanged yields none"
    - "Same fold inputs twice => byte-identical TwinSnapshot and byte-identical suggestion list (pure, no Date.now/Math.random, no async)"
  artifacts:
    - path: "packages/simulation/src/coordinator/optimize.ts"
      provides: "buildCenterTwinFromFold + epochResultToRerouteSuggestions (pure, in-fold optimizer adapter)"
      min_lines: 80
    - path: "packages/simulation/src/coordinator/optimize.unit.test.ts"
      provides: "RED-first tests: twin shape from fold, reroute translation, purity/determinism, empty-scope no-op"
  key_links:
    - from: "packages/simulation/src/coordinator/optimize.ts"
      to: "@mm/optimizer runEpoch / TwinSnapshot / EpochResult"
      via: "import of the pure rolling contract types"
      pattern: "TwinSnapshot|EpochResult|EpochInput"
---

<objective>
Build the PURE in-fold adapter between one center's engine fold state and the proven `@mm/optimizer` core: (1) `buildCenterTwinFromFold` assembles a small `TwinSnapshot` scoped to one center from plain fold inputs, and (2) `epochResultToRerouteSuggestions` translates an `EpochResult` into the closed-union `CoordinatorSuggestion[]` for the REROUTE kind only. No engine wiring yet — this is the testable, pure contract Plan 02 calls.

Purpose: Reusing the hardest-won v1 optimization IP (SSP min-cost-flow + VRPTW + selectPlan) as a per-center suggestion engine REQUIRES a fold→twin bridge and a result→suggestion bridge. Isolating them as pure functions makes the determinism keystone provable here, before any engine state or flag enters the picture (interface-first ordering — Plan 02 receives a tested contract, not a scavenger hunt).

Output: `packages/simulation/src/coordinator/optimize.ts` exporting two pure functions + their input types, re-exported from `coordinator/index.ts`, with a RED-first unit suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-coordinator-optimizer/26-CONTEXT.md

<read_first>
- packages/optimizer/src/rolling/types.ts — the FULL rolling contract: `TwinSnapshot` (hubs, optional centerHubId, routes, trailers), `TwinTrailer` (trailerId, currentHubId, departureMin, capacity, route: TwinStop[], blocks: TwinBlock[], optional driver/milesSinceRefuel), `TwinStop` (hubId, stopIndex), `TwinBlock` (blockId, nextUnloadHubId, volume, optional deadlineMin), `TwinRoute` (routeId, fromHubId, toHubId, travelMin, capacity, optional distanceMiles), `Epoch` (epochId, nowMin, freezeWindowMin), `EpochInput` (events, twinSnapshot, optional timing/fuelConfig), `EpochResult` (epochId, scopeHash, generated, accepted, recommendations: EpochRecommendation[], optional freightAssignment).
- packages/optimizer/src/rolling/epoch.ts lines 536-606 — `runEpoch(epoch, input, weights): EpochResult` signature + the empty-scope early return (scope.trailerIds.length === 0 ⇒ no plan, empty recommendations) + how `accepted`/recommendations are produced. (Read only this range; do NOT re-read the whole 800+ line file.)
- packages/optimizer/src/rolling/index.ts — the barrel: `runEpoch`, `detectAffectedScope`, `partitionScopeByCenter`, `buildTwin`, `scopeHash`, `isFrozen` + the exported types. This is the ONLY import surface — never reach into rolling internals.
- packages/simulation/src/coordinator/coordinator.ts — the `CoordinatorSuggestion` closed union (reroute|hold|consolidate|dispatch) the translator must produce; reroute carries `{ targetAgentId, toHubId }`. The translator emits ONLY reroute entries.
- packages/simulation/src/coordinator/observe.ts — `ObservedSpoke` / `ObservedTruck` / `CoordinatorObservation`: the integer/string frozen surface that already exists; the fold inputs the twin builder reads must be the SAME shape family (plain data, no live refs).
- packages/api/src/optimizer/twin-snapshot.ts lines 1-60 + 328-360 — the EXISTING full-scan twin assembler header + how it shapes `TwinTrailer`/`TwinRoute` from projections. MIRROR its OUTPUT shape but build from a small per-center FOLD slice (NOT readAll / Postgres). Read only those ranges.
- packages/optimizer/src/objective/types.ts — `ObjectiveWeights` (the third runEpoch arg). Find the DEFAULT weights export the optimizer ships (grep `DEFAULT.*WEIGHTS|defaultWeights` in packages/optimizer/src/objective/); the adapter uses the optimizer's own default so suggestions match the v1 IP exactly.
</read_first>

<interfaces>
<!-- The pure rolling contract the adapter targets. Use these directly. -->

From packages/optimizer/src/rolling (barrel):
```
function runEpoch(epoch: Epoch, input: EpochInput, weights: ObjectiveWeights): EpochResult
interface Epoch { epochId: string; nowMin: number; freezeWindowMin: number }
interface EpochInput { events: readonly DomainEvent[]; twinSnapshot: TwinSnapshot; timing?: TimingConfig; fuelConfig?: FuelConfig }
interface TwinSnapshot { hubs: readonly string[]; centerHubId?: string; routes: readonly TwinRoute[]; trailers: readonly TwinTrailer[] }
interface TwinTrailer { trailerId: string; currentHubId: string; departureMin: number; capacity: number; route: readonly TwinStop[]; blocks: readonly TwinBlock[]; driver?: TwinDriver; milesSinceRefuel?: number }
interface TwinStop { hubId: string; stopIndex: number }
interface TwinBlock { blockId: string; nextUnloadHubId: string; volume: number; deadlineMin?: number }
interface TwinRoute { routeId: string; fromHubId: string; toHubId: string; travelMin: number; capacity: number; distanceMiles?: number }
interface EpochRecommendation { trailerId: string; planId: string; feasible: boolean; objectiveCost: number; breakdown: ObjectiveBreakdown; frozen: boolean; repairRecommendations?: ... }
interface EpochResult { epochId; scopeHash; generated; accepted; recommendations: readonly EpochRecommendation[]; freightAssignment? }
```

From packages/simulation/src/coordinator/coordinator.ts:
```
type CoordinatorSuggestion =
  | { kind: "reroute"; targetAgentId: string; toHubId: string }
  | { kind: "hold"; targetAgentId: string }
  | { kind: "consolidate"; targetAgentId: string }
  | { kind: "dispatch"; targetAgentId: string; toHubId: string }
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: buildCenterTwinFromFold — assemble a per-center TwinSnapshot from plain fold inputs</name>
  <files>packages/simulation/src/coordinator/optimize.ts, packages/simulation/src/coordinator/optimize.unit.test.ts</files>
  <behavior>
    - Define a plain-data `CenterFoldSlice` input type (integer/string only, no live refs): centerId, the center's spoke hub ids, the in-region trailers (trailerId, currentHubId, optional active-trip toHubId, capacity, the trailer's remaining route stops as {hubId, stopIndex}, its load blocks as {blockId, nextUnloadHubId, volume}), and the in-scope route legs ({routeId, fromHubId, toHubId, travelMin, capacity, optional distanceMiles}). This is the SAME plain-data family as CoordinatorObservation — Plan 02 fills it from the engine fold maps.
    - Test: a slice with two spokes + one trailer + two route legs produces a TwinSnapshot whose `hubs` are the sorted unique hub ids in the slice, `centerHubId` is the slice's centerId, `routes` mirror the slice legs 1:1, and `trailers` carry the trailer's route (TwinStop[] sorted by stopIndex) + blocks (TwinBlock[]). departureMin is derived from the input nowMin + the trailer's scheduled-departure offset (integer minutes), NEVER Date.now.
    - Test (scope-size invariance, the NET-05 thesis): a slice for center A produces a twin whose hubs/trailers are independent of how many hubs exist in centers B/C — the same center-A slice yields a byte-identical twin regardless of total network size (the input is already the partitioned slice).
    - Test (determinism/purity): same CenterFoldSlice twice ⇒ deep-equal TwinSnapshot AND byte-identical JSON.stringify; the source slice arrays are not mutated (frozen-in ⇒ frozen-out).
    - Test (empty): a slice with no trailers ⇒ a twin with empty trailers + (still) its hubs/routes (the runEpoch empty-scope path handles it downstream).
  </behavior>
  <action>Create `buildCenterTwinFromFold(slice: CenterFoldSlice, nowMin: number): TwinSnapshot` in `coordinator/optimize.ts`. It is a pure projection from the partitioned per-center slice (per CONTEXT decision: built from FOLD state, not a full-scan; mirror twin-snapshot.ts OUTPUT shape but scoped). Sort `hubs` and each trailer's `route` by stable key (hubId / stopIndex). Map slice legs to `TwinRoute` and slice trailers to `TwinTrailer` with no recomputation beyond the documented integer derivations. Use the @mm/optimizer barrel types only (no reach into internals). NO Date.now, NO Math.random, NO async. Keep all numerics integer at this boundary (round any minutes input upstream — this builder receives already-integer fold values). Define and export `CenterFoldSlice` + its element types alongside the function.</action>
  <verify>
    <automated>pnpm --filter @mm/simulation exec vitest run src/coordinator/optimize.unit.test.ts</automated>
  </verify>
  <done>RED commit precedes GREEN; the suite proves twin shape from a fold slice, scope-size invariance, purity/determinism (deep-equal + byte-identical), and the empty-trailers case. `pnpm typecheck` clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: epochResultToRerouteSuggestions — translate an EpochResult into reroute CoordinatorSuggestions</name>
  <files>packages/simulation/src/coordinator/optimize.ts, packages/simulation/src/coordinator/optimize.unit.test.ts, packages/simulation/src/coordinator/index.ts</files>
  <behavior>
    - Define the translator input: the EpochResult plus a `currentNextHubByTrailer: ReadonlyMap<string,string>` (each in-region trailer's CURRENT next hub from its active trip) so the translator can compare the optimizer's chosen next leg against the status quo.
    - Test: for a trailer whose optimizer-implied next leg (derived deterministically from the epoch result — the accepted plan / the trailer's recommendation route head, documented below) differs from its current next hub, emit exactly one `{ kind: "reroute", targetAgentId: trailerId, toHubId: <optimizer next hub> }`. params are string-only.
    - Test: for a trailer the epoch left frozen, infeasible-with-no-change, or whose optimizer next hub EQUALS its current next hub ⇒ NO suggestion (no churn, anti-P7).
    - Test: a trailer NOT in currentNextHubByTrailer (no active trip, between legs) ⇒ NO reroute suggestion (the reroute kind requires a current next hub to differ from).
    - Test: the output list is sorted by targetAgentId (byte-stable) and is a pure function of (result, map) — same inputs twice ⇒ deep-equal + byte-identical JSON.
    - Test: an empty-scope EpochResult (recommendations: []) ⇒ [] (the fallback-trigger substrate Plan 02 reads — "the optimizer suggested nothing").
  </behavior>
  <action>Create `epochResultToRerouteSuggestions(result: EpochResult, currentNextHubByTrailer: ReadonlyMap<string,string>): readonly CoordinatorSuggestion[]` in `coordinator/optimize.ts`. Derive each trailer's optimizer-chosen next hub deterministically from the EpochResult — document the exact derivation in a doc comment (Claude's discretion per CONTEXT, following the existing optimizer-recommendation + Phase-25 generation patterns: e.g. the first leg of the trailer's selected route in the accepted plan, falling back to the per-trailer recommendation when no plan was accepted). Emit a reroute ONLY when that hub differs from `currentNextHubByTrailer.get(trailerId)` AND the trailer is not frozen. Sort the result by targetAgentId. Return `CoordinatorSuggestion[]` typed exactly as the existing closed union (reroute branch only). Re-export both new functions + `CenterFoldSlice` from `coordinator/index.ts`. NO Date.now/Math.random/async.</action>
  <verify>
    <automated>pnpm --filter @mm/simulation exec vitest run src/coordinator/optimize.unit.test.ts</automated>
  </verify>
  <done>RED commit precedes GREEN; the suite proves the differs⇒reroute / same-or-frozen⇒none / no-current-hub⇒none / sorted-stable / empty⇒[] cases with purity. index.ts re-exports the new surface. `pnpm typecheck` clean; `pnpm --filter @mm/simulation lint` exit 0 (the file lives under coordinator/** so the DET-03 ESLint guard already covers it — no Date.now/Math.random/async-queue/kysely).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fold state → optimizer twin | in-engine fold maps cross into the pure @mm/optimizer contract; any float/clock/RNG leak here would poison the (Plan 03) golden |
| optimizer result → advisory suggestion | the EpochResult crosses back into the coordinator's closed-union payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | buildCenterTwinFromFold | mitigate | integer/string-only fold inputs; sort hubs/stops by stable key; no Date.now/Math.random/async (proven by purity test + the coordinator/** DET-03 ESLint guard); deep-equal + byte-identical determinism test |
| T-26-02 | Tampering | epochResultToRerouteSuggestions | mitigate | string-only reroute params; output sorted by targetAgentId; same-input byte-identical test; only the reroute kind is produced (hold/consolidate/dispatch stay rule-based — no scope creep) |
| T-26-03 | DoS | scope-size growth | accept | the builder receives the ALREADY-partitioned per-center slice (NET-05); scope-size-invariance test proves the twin is independent of total network size — the live wiring is Plan 02's concern |
</threat_model>

<verification>
- `pnpm --filter @mm/simulation exec vitest run src/coordinator/optimize.unit.test.ts` — all tests GREEN
- `pnpm typecheck` — clean (esbuild strips types; typecheck is the exhaustiveness proof for the closed union)
- `pnpm --filter @mm/simulation lint` — exit 0 (coordinator/** DET-03 guard covers the new file)
- No engine.ts change in this plan (file ownership: this plan owns only coordinator/optimize.ts + its test + the index re-export)
</verification>

<success_criteria>
- buildCenterTwinFromFold produces a scoped TwinSnapshot from a plain fold slice, scope-size-invariant, pure
- epochResultToRerouteSuggestions produces reroute-only CoordinatorSuggestions, sorted, pure, with no-churn + empty-scope cases
- Both functions are byte-identical-reproducible and free of Date.now/Math.random/async (DET-03)
- index.ts exposes the new surface for Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/26-coordinator-optimizer/26-01-SUMMARY.md`
</output>
