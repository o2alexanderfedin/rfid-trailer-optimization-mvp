---
phase: 26-coordinator-optimizer
plan: 03
type: tdd
wave: 3
depends_on: [26-02]
files_modified:
  - packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts
  - packages/simulation/test/determinism.unit.test.ts
  - packages/simulation/test/coordinator-optimizer-continuation.unit.test.ts
autonomous: true
requirements: [COORD-06]
must_haves:
  truths:
    - "Two-part flags-off gate for coordinatorUsesOptimizer: coordinatorUsesOptimizer:false === absent over a short run AND with it absent, replay is byte-identical to the Phase-25 coordinator golden edfa5a6d… (and the flags-off 3920accc… + OODA-on 94689f99… goldens stay intact)"
    - "A NEW optimizer-backed coordinator golden is captured reproducibility-first (run twice in-process AND across two separate node process invocations ⇒ identical) BEFORE the literal is baked; it differs from edfa5a6d…, 94689f99…, and 3920accc…"
    - "Continuation-equivalence: a coordinatorUsesOptimizer-on run driven in chunks (1/7/23/500) is byte-identical to all-at-once (the in-fold runEpoch path serializes correctly across a chunk boundary)"
    - "The optimizer-backed stream carries non-trivial route-aware reroute suggestions and every emitted event passes the domain validateEvent boundary"
  artifacts:
    - path: "packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts"
      provides: "COORDINATOR_OPTIMIZER_GOLDEN_SHA256 (reproducibility-first) + != the 3 prior goldens + validateEvent + non-trivial reroute counts"
    - path: "packages/simulation/test/determinism.unit.test.ts"
      provides: "the coordinatorUsesOptimizer two-part flags-off gate (false===absent + absent⇒edfa5a6d + flags-off⇒3920accc) consolidated in the canonical DET file"
    - path: "packages/simulation/test/coordinator-optimizer-continuation.unit.test.ts"
      provides: "chunked==all-at-once at 1/7/23/500 for the optimizer-on model"
  key_links:
    - from: "packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts"
      to: "simulate({ coordinatorUsesOptimizer + all-on stack })"
      via: "sha256(JSON.stringify(stream)) reproducibility-first capture"
      pattern: "coordinatorUsesOptimizer|sha256|createHash"
    - from: "packages/simulation/test/determinism.unit.test.ts"
      to: "the 3920accc / edfa5a6d goldens"
      via: "two-part gate (false===absent AND absent⇒prior golden)"
      pattern: "edfa5a6d|3920accc|coordinatorUsesOptimizer"
---

<objective>
Pin the optimizer-backed coordinator model to the determinism keystone: (1) the two-part flags-off gate for `coordinatorUsesOptimizer` (false === absent AND absent ⇒ byte-identical to the Phase-25 coordinator golden `edfa5a6d…`, with `3920accc…`/`94689f99…` intact); (2) a NEW optimizer-backed coordinator golden captured reproducibility-first; and (3) continuation-equivalence (chunked == all-at-once) for the optimizer-on model.

Purpose: COORD-06's determinism facet + the milestone keystone — the new model changes the event stream (route-aware reroute suggestions differ from rule-based), so it gets its OWN golden, captured only after reproducibility + the flags-off gate are proven first; and the in-fold runEpoch path must survive a chunk boundary (the continuation witness that the new behavior is fully captured in SerializedWorldState — Plan 02 added no new persisted state, so this proves the existing serialization already covers it).

Output: three test files pinning the gate, the new golden (reproducibility-first), and continuation-equivalence — the full Phase-26 determinism gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-coordinator-optimizer/26-CONTEXT.md
@.planning/phases/26-coordinator-optimizer/26-02-SUMMARY.md

<read_first>
- packages/simulation/test/coordinator-determinism.unit.test.ts — the Phase-25 golden test to MIRROR EXACTLY: the `sha` helper, the reproducibility-first capture discipline, the COORDINATOR_ON_GOLDEN_SHA256 = edfa5a6d… literal + its capture-env / cross-arch note, the non-trivial-counts + validateEvent assertions, and the all-on stack config it selected (LEGACY single-center all-on, because continental yields 0 rejects). Reuse that SAME config + add coordinatorUsesOptimizer.
- packages/simulation/test/determinism.unit.test.ts lines 1-70 + grep for the existing `coordinatorsEnabled` two-part gate block (the a/b/c triple consolidated by Plan 25-05) — add the coordinatorUsesOptimizer triple mirroring it: (a) false===absent short run, (b) absent ⇒ edfa5a6d (the Phase-25 coordinator golden, since coordinatorUsesOptimizer absent means the rule-based coordinator path), (c) false===absent over 10k. Read the gate block + the OPTS const.
- packages/simulation/test/coordinator-continuation.unit.test.ts — the Phase-25 continuation-equivalence harness (runToHorizon chunked vs all-at-once at chunk 1/7/23/500, the off-path [] witness). MIRROR it with coordinatorUsesOptimizer on. Read the whole file (it is the template).
- packages/simulation/test/coordinator-optimizer.unit.test.ts (from Plan 02) — the optimizer-on run config + how the harness drives it; reuse the same flag set.
- .planning/phases/25-coordination-centers/25-05-SUMMARY.md — the reproducibility-first capture protocol (in-process twice + two separate node process invocations BEFORE baking) + the capture-env/integer-LUT cross-arch contingency note format to replicate next to the new literal.
</read_first>

<interfaces>
The three prior goldens (do NOT change them — assert they stay intact):
```
flags-off  seed-42 10k : 3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861
OODA-on              : 94689f99…
coordinator-on (P25) : edfa5a6d40b36e3774797b60d7bd99b5a8af7cce97adb1e775bad0b56b514adc
```
The new literal to capture + bake:
```
COORDINATOR_OPTIMIZER_GOLDEN_SHA256 = <captured reproducibility-first; != all three above>
```
sha helper: `createHash("sha256").update(JSON.stringify(simulate(opts))).digest("hex")`
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: The coordinatorUsesOptimizer two-part flags-off gate</name>
  <files>packages/simulation/test/determinism.unit.test.ts</files>
  <behavior>
    - (a) `coordinatorUsesOptimizer: false` === absent over a seed-42 short run (byte-identical stream) — the strict `=== true` gate witness.
    - (b) With `coordinatorUsesOptimizer` ABSENT but `coordinatorsEnabled` on (the all-on stack), the seed-42 stream is byte-identical to the Phase-25 coordinator golden `edfa5a6d…` (absent ⇒ the rule-based coordinator path is untouched).
    - (c) With ALL v3.0 flags absent + explicit-false (including coordinatorUsesOptimizer:false), the seed-42 10k golden is byte-identical to `3920accc…` (the master flags-off gate re-asserted) AND the OODA-on golden `94689f99…` is intact.
    - (d) `coordinatorUsesOptimizer: false` === absent over a 10k run on the all-on stack (the longer false===absent witness).
  </behavior>
  <action>Add the coordinatorUsesOptimizer two-part gate block to `determinism.unit.test.ts`, mirroring the existing `coordinatorsEnabled` a/b/c triple consolidated by Plan 25-05. Use the same all-on stack config as `coordinator-determinism.unit.test.ts` for the absent⇒edfa5a6d assertion. Re-assert 3920accc + 94689f99 unchanged (these tests likely already exist — extend, do not duplicate). This is the gate that proves the sub-flag is byte-identically inert when off.</action>
  <verify>
    <automated>pnpm --filter @mm/simulation exec vitest run test/determinism.unit.test.ts</automated>
  </verify>
  <done>The a/b/c/d coordinatorUsesOptimizer gate is GREEN: false===absent (short + 10k), absent⇒edfa5a6d, flags-off⇒3920accc, OODA-on⇒94689f99 all intact.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: The new optimizer-backed coordinator golden (reproducibility-first)</name>
  <files>packages/simulation/test/coordinator-optimizer-determinism.unit.test.ts</files>
  <behavior>
    - simulate({ seed 42, 10k, the Phase-25 all-on stack + coordinatorUsesOptimizer: true }) hashes to a NEW committed SHA-256.
    - Reproducibility-first: the config run twice in-process ⇒ equal, AND across two separate node process invocations ⇒ equal, BEFORE the literal is baked (replicate the 25-05 protocol).
    - The new golden != edfa5a6d… (the optimizer-backed reroute changed the decisions — route-aware ≠ rule-based congestion heuristic), != 94689f99…, != 3920accc….
    - The optimizer-on stream carries NON-TRIVIAL reroute ActionSuggested counts (the route-aware suggestions are observably present) and every emitted event passes the domain `validateEvent` boundary.
  </behavior>
  <action>Create `coordinator-optimizer-determinism.unit.test.ts` mirroring `coordinator-determinism.unit.test.ts` EXACTLY. CAPTURE the golden reproducibility-first: run the config twice in-process (assert equal), run it across two separate `node` process invocations (assert equal) — do this BEFORE writing the literal. Document the capture-env (arch/os/event-count) + the integer-LUT cross-arch contingency note next to the literal, exactly as 25-05 did. Bake `COORDINATOR_OPTIMIZER_GOLDEN_SHA256`. Assert it differs from all three prior goldens + the non-trivial reroute count + validateEvent. If the chosen all-on stack does NOT exercise the optimizer reroute path richly enough (e.g. the continental config under-triggers, the known 25-02/25-05 finding), select + DOCUMENT the config that genuinely exercises the optimizer-backed reroute (Claude's discretion, following 25-05's empirical config-selection precedent).</action>
  <verify>
    <automated>pnpm --filter @mm/simulation exec vitest run test/coordinator-optimizer-determinism.unit.test.ts</automated>
  </verify>
  <done>RED (literal-not-yet-present) → captured-reproducibility-first → GREEN. The golden is baked AFTER in-process-twice + two-process-invocation identity is proven; != the three prior goldens; non-trivial reroute counts; validateEvent passes; capture-env note present.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Continuation-equivalence for the optimizer-on model</name>
  <files>packages/simulation/test/coordinator-optimizer-continuation.unit.test.ts</files>
  <behavior>
    - A coordinatorUsesOptimizer-on run driven in CHUNKS via runToHorizon at chunk sizes 1, 7, 23, 500 is byte-identical to the all-at-once run (the in-fold runEpoch path + the per-center partition are recomputed each pass from fold state, so a chunk boundary mid-run reproduces the same reroute suggestions).
    - The off path ([] / no optimizer call) remains byte-identical to pre-Phase-26 (the sub-flag off ⇒ no new state, no new behavior across a boundary).
    - JSON round-trip of the continuation (capture → serialize → restore → resume) yields the same final hash (the existing SerializedWorldState already covers the optimizer path because Plan 02 added NO new persisted state — this is the witness).
  </behavior>
  <action>Create `coordinator-optimizer-continuation.unit.test.ts` mirroring `coordinator-continuation.unit.test.ts`. Drive the optimizer-on config chunked at 1/7/23/500 and assert byte-identical to all-at-once. Assert the off path is byte-identical to pre-Phase-26 (a [] witness). Include the JSON-round-trip continuation equivalence. If the chunked run reveals a serialization gap (the in-fold runEpoch reads transient per-pass state that must survive a boundary), STOP and document it as a Rule-3 blocking deviation — but per Plan 02 the runEpoch path is recomputed purely from already-serialized fold state each pass, so NO new SerializedWorldState field is expected (assert that — the continuation holds with the existing fields).</action>
  <verify>
    <automated>pnpm --filter @mm/simulation exec vitest run test/coordinator-optimizer-continuation.unit.test.ts</automated>
  </verify>
  <done>chunked==all-at-once at 1/7/23/500 for the optimizer-on model; off path byte-identical; JSON-round-trip equivalent; no new SerializedWorldState field required (the optimizer path is recomputed from existing serialized fold state).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sub-flag off ⇒ prior golden | coordinatorUsesOptimizer absent must reproduce edfa5a6d / 3920accc byte-for-byte |
| in-fold runEpoch ⇒ golden | the new model's stream must be reproducible (replay + cross-process) before baking |
| chunk boundary ⇒ optimizer path | the in-fold runEpoch must reproduce across a continuation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09 | Tampering | flags-off drift | mitigate | the two-part gate (false===absent + absent⇒edfa5a6d) + 3920accc/94689f99 re-asserted (Task 1) |
| T-26-10 | Repudiation | non-reproducible new golden | mitigate | reproducibility-first capture (in-process twice + two separate node processes) BEFORE baking (Task 2), per the 25-05 protocol |
| T-26-11 | Tampering | optimizer state lost across a chunk boundary | mitigate | continuation-equivalence at 1/7/23/500 (Task 3); the runEpoch path is recomputed from already-serialized fold state ⇒ no new persisted field |
| T-26-12 | Tampering | cross-arch float divergence in the optimizer path | accept | the optimizer core is integer-arithmetic (rolling/types.ts); the capture-env + integer-LUT contingency note is documented next to the literal (the prior goldens verify on this arm64 host) |
</threat_model>

<verification>
- `pnpm --filter @mm/simulation exec vitest run test/determinism.unit.test.ts` — GREEN (gate)
- `pnpm --filter @mm/simulation exec vitest run test/coordinator-optimizer-determinism.unit.test.ts` — GREEN (new golden)
- `pnpm --filter @mm/simulation exec vitest run test/coordinator-optimizer-continuation.unit.test.ts` — GREEN (continuation)
- FULL phase gate: `pnpm build` (turbo) 10/10; `pnpm typecheck` clean; `pnpm lint` exit 0 (coordinator/** + ooda/** DET-03 guards active); the four goldens (3920accc / 94689f99 / edfa5a6d / new) all verified
- Run vitest lanes ONE AT A TIME with `pkill -f vitest` between (v2-gate OOM memory — no exit 137)
</verification>

<success_criteria>
- coordinatorUsesOptimizer two-part flags-off gate green (false===absent + absent⇒edfa5a6d); 3920accc + 94689f99 intact
- New optimizer-backed coordinator golden captured reproducibility-first, != all three prior goldens, non-trivial reroute counts, validateEvent
- Continuation-equivalence (chunked==all-at-once at 1/7/23/500) for the optimizer-on model; no new SerializedWorldState field
- Full Phase-26 determinism gate green
</success_criteria>

<output>
After completion, create `.planning/phases/26-coordinator-optimizer/26-03-SUMMARY.md`
</output>
