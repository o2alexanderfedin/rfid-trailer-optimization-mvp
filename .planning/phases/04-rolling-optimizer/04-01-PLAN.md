---
phase: 04-rolling-optimizer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/events/schemas.ts
  - packages/domain/src/events/domain-event.ts
  - packages/domain/src/events/contract.assert.ts
  - packages/domain/src/events/index.ts
  - packages/domain/src/events/plan-events.test.ts
autonomous: true
requirements: [OPT-04]
must_haves:
  truths:
    - "A PlanGenerated event and a PlanAccepted event exist as members of the closed DomainEvent union"
    - "The build gate (tsc -b) still fails if a union member lacks a contract.assert case or the zod union drifts"
    - "validateEvent accepts a well-formed PlanGenerated/PlanAccepted and rejects a malformed one"
  artifacts:
    - path: "packages/domain/src/events/schemas.ts"
      provides: "planGeneratedSchema + planAcceptedSchema (zod) added to domainEventSchema union"
      contains: "planAcceptedSchema"
    - path: "packages/domain/src/events/domain-event.ts"
      provides: "PlanGenerated + PlanAccepted union members"
      contains: "PlanAccepted"
    - path: "packages/domain/src/events/contract.assert.ts"
      provides: "exhaustiveness cases for the two new events"
      contains: "PlanAccepted"
  key_links:
    - from: "packages/domain/src/events/domain-event.ts"
      to: "packages/domain/src/events/schemas.ts"
      via: "z.infer member type inferred from each new schema"
      pattern: "z\\.infer<typeof planAcceptedSchema>"
    - from: "packages/domain/src/events/contract.assert.ts"
      to: "DomainEvent union"
      via: "switch case per discriminant + assertNever default"
      pattern: "case \"PlanAccepted\""
---

<objective>
Extend the Phase-1 closed `DomainEvent` union with the two plan-lifecycle events the optimizer needs — `PlanGenerated` (a candidate plan was produced over the twin) and `PlanAccepted` (the ONE operational side effect when a plan is committed). This is the domain contract every later Phase-4 plan emits against.

Purpose: OPT-04 requires that evaluating candidates produces NO side effects until accept, at which point exactly one `PlanAccepted` event is appended. That event must be a first-class member of the closed, versioned union with zod validation and contract.assert exhaustiveness — not an untyped blob.
Output: Two new union members + zod schemas + contract.assert cases + a validation test. Build gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rolling-optimizer/04-CONTEXT.md
@.planning/phases/04-rolling-optimizer/04-RESEARCH.md

<read_first>
- packages/domain/src/events/domain-event.ts — the closed union + `assertNever`. You ADD `PlanGenerated`/`PlanAccepted` as `z.infer` members and to the `DomainEvent` union.
- packages/domain/src/events/schemas.ts — one zod schema per event; `domainEventSchema` is the discriminated union and `PHASE1_SCHEMA_VERSION`. Add two `EventEnvelope`-shaped schemas (`{ type, schemaVersion, payload }`) and add them to the union.
- packages/domain/src/events/contract.assert.ts — the COMPILE-TIME fixture. Add a `case` for each new discriminant in `assertExhaustive`; the `Exact<Inferred, DomainEvent>` proof then forces schema/union parity.
- packages/domain/src/events/index.ts — re-exports; add the two new type names + two schema names.
- packages/domain/src/ingestion/validate.ts — `validateEvent` uses `domainEventSchema`; once the union includes the new members it validates them for free.
</read_first>

<interfaces>
Envelope shape (established Phase 1): `interface EventEnvelope<TType, TPayload> { readonly type: TType; readonly schemaVersion: number; readonly payload: TPayload; }`

Payload fields to model (keep primitive + deterministic — no Date.now in domain):
- `PlanGenerated.payload`: `{ epochId: string; scopeHash: string; planId: string; trailerId: string; objectiveCost: number; feasible: boolean; occurredAt: string }`
- `PlanAccepted.payload`: `{ epochId: string; scopeHash: string; planId: string; trailerId: string; occurredAt: string }`

(`objectiveCost` is the weighted-objective value from Plan 05; `scopeHash` is the idempotency key from Plan 06. These are opaque strings/numbers at the domain layer — no optimizer import.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PlanGenerated + PlanAccepted zod schemas and union members</name>
  <files>packages/domain/src/events/schemas.ts, packages/domain/src/events/domain-event.ts, packages/domain/src/events/plan-events.test.ts</files>
  <behavior>
    - validateEvent({ type: "PlanAccepted", schemaVersion: 1, payload: {valid...} }) returns the typed event (ok).
    - validateEvent on a PlanGenerated missing `objectiveCost` (or with a non-number) is rejected.
    - The `Exact<Inferred, DomainEvent>` proof in contract.assert continues to hold (proven at build, but add a type-level test asserting both new members are assignable to DomainEvent).
  </behavior>
  <action>Add `planGeneratedSchema` and `planAcceptedSchema` to schemas.ts as `z.object({ type: z.literal(...), schemaVersion: z.literal(PHASE1_SCHEMA_VERSION), payload: z.object({...}) })`, modeling the payload fields from `<interfaces>`. Add both to the `domainEventSchema` discriminated union. In domain-event.ts add `export type PlanGenerated = z.infer<typeof planGeneratedSchema>` and `PlanAccepted`, and add both to the `DomainEvent = | ...` union. Keep determinism: `occurredAt` is a payload string the caller supplies (the sim/epoch clock), never `Date.now()`. Write plan-events.test.ts proving validateEvent accept/reject per the behavior block.</action>
  <verify>
    <automated>pnpm --filter @mm/domain build && pnpm vitest run packages/domain/src/events/plan-events.test.ts</automated>
  </verify>
  <done>Both events validate when well-formed, reject when malformed; `pnpm --filter @mm/domain build` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend contract.assert exhaustiveness + re-exports</name>
  <files>packages/domain/src/events/contract.assert.ts, packages/domain/src/events/index.ts</files>
  <action>In contract.assert.ts add `case "PlanGenerated":` and `case "PlanAccepted":` to `assertExhaustive` before the `default: assertNever(event)`. Do NOT touch the `Exact<Inferred, DomainEvent>` proof — it should still compile because Task 1 kept schema and union in parity (if it does NOT compile, the schemas/union drifted: fix Task 1, do not weaken the proof). In index.ts add `PlanGenerated`, `PlanAccepted` to the type re-export block and `planGeneratedSchema`, `planAcceptedSchema` to the schema re-export block.</action>
  <verify>
    <automated>pnpm --filter @mm/domain build && pnpm -r build</automated>
  </verify>
  <done>contract.assert compiles with the two new cases; the full turbo `pnpm -r build` stays green (no downstream package broke against the widened union).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→domain ingestion | Untyped event payloads cross `validateEvent` into the closed union |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | PlanAccepted payload | mitigate | zod schema validates every field at the ingestion boundary; no `any`, no passthrough |
| T-04-02 | Information disclosure | plan events | accept | events carry ids + costs only, no PII; demo-scoped simulated data |
</threat_model>

<verification>
- `pnpm --filter @mm/domain build` green (contract.assert + Exact proof hold).
- `pnpm -r build` green (no downstream consumer breaks on the widened union).
- plan-events.test.ts: well-formed accepted, malformed rejected.
</verification>

<success_criteria>
PlanGenerated + PlanAccepted are members of the closed, versioned, contract-asserted DomainEvent union, zod-validated through validateEvent, with the build gate enforcing schema/union parity.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rolling-optimizer/04-01-SUMMARY.md`
</output>
