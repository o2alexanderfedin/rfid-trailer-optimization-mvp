---
phase: 04-rolling-optimizer
plan: 03
type: tdd
wave: 2
depends_on: [02]
files_modified:
  - packages/optimizer/src/flow/index.ts
  - packages/optimizer/src/flow/types.ts
  - packages/optimizer/src/flow/min-cost-flow.ts
  - packages/optimizer/src/flow/min-cost-flow.test.ts
  - packages/optimizer/src/flow/glpk-oracle.test.ts
  - packages/optimizer/src/flow/assign-freight.ts
  - packages/optimizer/src/flow/assign-freight.test.ts
autonomous: true
requirements: [OPT-02]
must_haves:
  truths:
    - "A pure-TS Successive Shortest Path min-cost flow solver returns the optimal flow per edge for a graph with supplies/demands"
    - "KEYSTONE: SSP optimal cost EQUALS glpk.js's exact LP optimum on a battery of random small instances (the correctness oracle) + hand-computed fixtures"
    - "assignFreight maps freight blocks to route legs over the time-expanded graph by min-cost flow, minimizing transport+waiting+handling+SLA-lateness+missed-connection cost under edge/hub capacity"
    - "the solver is deterministic and uses integer costs/capacities (no Date.now/Math.random, no float drift)"
  artifacts:
    - path: "packages/optimizer/src/flow/min-cost-flow.ts"
      provides: "MinCostFlowSSP solver (residual graph, Bellman-Ford initial potentials, Dijkstra-with-potentials)"
      exports: ["minCostFlow"]
      min_lines: 40
    - path: "packages/optimizer/src/flow/glpk-oracle.test.ts"
      provides: "THE keystone: SSP == glpk.js exact optimum on random instances + fixtures"
      contains: "glpk"
    - path: "packages/optimizer/src/flow/assign-freight.ts"
      provides: "assignFreight(graph, supplies) → freight-to-leg assignment via minCostFlow"
      exports: ["assignFreight"]
  key_links:
    - from: "packages/optimizer/src/flow/assign-freight.ts"
      to: "packages/optimizer/src/graph/types.ts"
      via: "consumes TimeExpandedGraph from Plan 02"
      pattern: "TimeExpandedGraph"
    - from: "packages/optimizer/src/flow/glpk-oracle.test.ts"
      to: "packages/optimizer/src/flow/min-cost-flow.ts"
      via: "asserts SSP optimum == glpk exact LP optimum"
      pattern: "glpk"
---

<objective>
Build the **min-cost-flow freight assignment** (OPT-02) — the concentrated engineering risk of the phase — as a pure-TS **Successive Shortest Path** solver, and validate it against the **glpk.js exact-LP correctness oracle** (THE Phase-4 keystone test). Then assign freight blocks to route legs over the Plan-02 time-expanded graph.

Purpose: There is no maintained JS min-cost-flow library, so we hand-roll SSP (residual graph, Bellman-Ford for initial potentials then Dijkstra-with-potentials per the research blueprint). Correctness is non-negotiable, so glpk.js (WASM, exact LP) is held purely as a TEST oracle: SSP's optimal cost must equal glpk's exact optimum on a battery of random small instances plus hand-computed fixtures. Integer costs/capacities defend against P12 numerical drift.
Output: A tested SSP solver, the glpk oracle keystone test, and `assignFreight` mapping freight to legs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rolling-optimizer/04-CONTEXT.md
@.planning/phases/04-rolling-optimizer/04-RESEARCH.md
@.planning/phases/04-rolling-optimizer/04-VALIDATION.md

<read_first>
- packages/optimizer/src/graph/types.ts (from Plan 02) — `TimeExpandedGraph`, `FlowEdge`, `FlowNode`, `EdgeKind`. assignFreight consumes this; the solver operates on the residual form of these edges.
- 04-RESEARCH.md "Google AI Mode Consult" — the SSP blueprint: `Edge{to,capacity,flow,cost,rev}`, `addEdge` creating a paired reverse edge, push flow along successive shortest paths; Bellman-Ford for initial potentials (handles any non-negative-cycle graph), Dijkstra-with-potentials thereafter for speed.
- packages/load-planner/src/scoring.ts — cost conventions (integer-friendly). Do not reuse scorePlan here; the flow cost model is the edge `cost` integers from Plan 02.
</read_first>

<interfaces>
Define in src/flow/types.ts:
```
export interface Supply { readonly nodeId: string; readonly amount: number; } // >0 source, <0 sink; sum must be 0
export interface FlowResult { readonly totalCost: number; readonly flowByEdgeId: ReadonlyMap<string, number>; readonly feasible: boolean; }
export function minCostFlow(graph: TimeExpandedGraph, supplies: readonly Supply[]): FlowResult;
```
assignFreight in src/flow/assign-freight.ts:
```
export interface FreightAssignment { readonly blockId: string; readonly legEdgeIds: readonly string[]; readonly cost: number; }
export function assignFreight(graph: TimeExpandedGraph, freightSupplies: readonly Supply[]): readonly FreightAssignment[];
```
glpk.js usage (TEST ONLY): build the equivalent min-cost-flow LP (minimize Σ cost·flow s.t. flow conservation per node + 0<=flow<=capacity), solve to exact optimum, compare totalCost to SSP within integer tolerance (== for integer inputs).
</interfaces>
</context>

<feature>
  <name>Min-cost flow (SSP) + glpk oracle + freight assignment (OPT-02)</name>
  <files>packages/optimizer/src/flow/min-cost-flow.ts, packages/optimizer/src/flow/glpk-oracle.test.ts, packages/optimizer/src/flow/assign-freight.ts</files>
  <behavior>
    minCostFlow:
    - Hand fixture: a known 4-node diamond graph returns the hand-computed optimal cost + the expected per-edge flow.
    - Infeasible supply (demand exceeds total capacity across any cut) returns `feasible: false`.
    - Flow conservation holds at every node (inflow − outflow == supply) on every passing fixture.
    - Determinism: same (graph, supplies) ⇒ identical totalCost + flowByEdgeId.
    KEYSTONE glpk oracle:
    - For N (>= 20) randomly generated small graphs (seeded RNG IN THE TEST, with capacities/costs as small ints), SSP.totalCost EQUALS glpk.js exact LP optimum.
    - The hand fixtures also match glpk.
    assignFreight:
    - Freight supplies over the time-expanded graph produce per-block leg-edge sequences whose total equals minCostFlow.totalCost.
    - Respects edge capacities (no assignment exceeds an edge's capacity).
  </behavior>
  <implementation>
    GREEN: implement `MinCostFlowSSP` per the research blueprint — residual edges with paired reverse edges; initialize potentials with Bellman-Ford; repeatedly find the shortest residual path (Dijkstra + potentials), push the bottleneck flow, update potentials, until demand is met or no augmenting path exists (then `feasible:false`). All arithmetic on integers. Wrap as `minCostFlow(graph, supplies)`. Implement `assignFreight` by constructing supplies from freight blocks, running minCostFlow, and decomposing the resulting flow into per-block leg-edge paths. For the oracle test, install glpk.js (devDependency from Plan 02), translate each instance to its LP, solve, compare. Seed all randomness inside the test only.
    REFACTOR: extract the residual-graph + Dijkstra-with-potentials into named helpers; keep minCostFlow readable.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD Successive Shortest Path min-cost flow solver</name>
  <files>packages/optimizer/src/flow/types.ts, packages/optimizer/src/flow/min-cost-flow.ts, packages/optimizer/src/flow/min-cost-flow.test.ts</files>
  <action>RED: write min-cost-flow.test.ts with the hand-fixture, infeasibility, conservation, and determinism cases (commit `test(04-03): failing SSP min-cost-flow tests`). GREEN: implement the SSP solver per `<implementation>` (commit `feat(04-03): SSP min-cost flow`). REFACTOR (commit if applied). Define src/flow/types.ts contracts first.</action>
  <verify>
    <automated>pnpm --filter @mm/optimizer build && pnpm vitest run packages/optimizer/src/flow/min-cost-flow.test.ts</automated>
  </verify>
  <done>Solver returns optimal cost + conserving flows on fixtures; infeasible instances flagged; deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KEYSTONE — glpk.js exact-LP correctness oracle</name>
  <files>packages/optimizer/src/flow/glpk-oracle.test.ts</files>
  <action>Write THE keystone test: a seeded generator produces >= 20 random small min-cost-flow instances; for each, build the equivalent LP and solve with glpk.js (WASM) to its exact optimum, then assert `minCostFlow(...).totalCost === glpkOptimum` (integer equality). Include the hand fixtures from Task 1 in the comparison. glpk.js is imported ONLY in this test file (it is a devDependency; assert nothing in src imports it). This gates the hardest algorithm against an independent exact solver.</action>
  <verify>
    <automated>pnpm vitest run packages/optimizer/src/flow/glpk-oracle.test.ts && grep -rL "glpk" packages/optimizer/src/flow/min-cost-flow.ts >/dev/null && ! grep -rn "glpk" packages/optimizer/src/**/*.ts | grep -v "\.test\.ts"</automated>
  </verify>
  <done>SSP optimum == glpk exact optimum across all random instances + fixtures; glpk appears only in *.test.ts, never in src production files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD assignFreight over the time-expanded graph</name>
  <files>packages/optimizer/src/flow/assign-freight.ts, packages/optimizer/src/flow/assign-freight.test.ts, packages/optimizer/src/flow/index.ts</files>
  <action>RED → GREEN → REFACTOR `assignFreight(graph, freightSupplies)`: build supplies, run minCostFlow over the Plan-02 graph, decompose flow into per-block leg-edge paths respecting capacities. Export minCostFlow + assignFreight + flow types from src/flow/index.ts (the flow barrel set up by Plan 02; the root src/index.ts already re-exports it — DO NOT edit the root or any other plan's barrel).</action>
  <verify>
    <automated>pnpm --filter @mm/optimizer build && pnpm vitest run packages/optimizer/src/flow/assign-freight.test.ts</automated>
  </verify>
  <done>Freight assigned to legs at total = minCostFlow optimum, no edge over capacity; exported from package index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph→solver | Trusted graph from Plan 02 enters the SSP solver; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | SSP optimum correctness | mitigate | glpk.js exact-LP oracle gates every result on random instances + fixtures (the keystone) |
| T-04-06 | Denial of service | solver runtime on large graphs | mitigate | scope-pruned, coarse-time graph (P9) keeps instances small; runtime tracked as KPI |
| T-04-07 | Tampering | float drift | mitigate | integer costs/capacities end-to-end (P12); oracle catches any drift |
</threat_model>

<verification>
- `pnpm vitest run packages/optimizer/src/flow` green.
- KEYSTONE: glpk oracle test passes (SSP == exact LP) on all random instances + fixtures.
- glpk.js referenced ONLY in *.test.ts (verified by grep in Task 2).
- Solver deterministic, integer-valued.
</verification>

<success_criteria>
A pure-TS SSP min-cost-flow solver, proven optimal against the glpk.js exact-LP oracle, assigns freight blocks to route legs over the time-expanded graph under capacity — the validated freight-assignment layer.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rolling-optimizer/04-03-SUMMARY.md`
</output>
