---
phase: 25-coordination-centers
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/events/schemas.ts
  - packages/domain/src/events/domain-event.ts
  - packages/domain/src/events/index.ts
  - packages/domain/src/events/contract.assert.ts
  - packages/domain/src/index.ts
  - packages/domain/test/events.unit.test.ts
  - packages/domain/src/events/suggestion-events.test.ts
  - packages/optimizer/src/rolling/scope.ts
  - packages/projections/src/reducers/audit-timeline.ts
  - packages/projections/src/reducers/exceptions.ts
  - packages/projections/src/reducers/delivery-kpi.ts
  - packages/projections/src/reducers/driver-assignment.ts
  - packages/projections/src/reducers/driver-status.ts
  - packages/projections/src/reducers/geo-track.ts
  - packages/projections/src/reducers/hub-inventory.ts
  - packages/projections/src/reducers/package-location.ts
  - packages/projections/src/reducers/tag-registry.ts
  - packages/projections/src/reducers/trailer-fuel.ts
  - packages/projections/src/reducers/trailer-state.ts
  - packages/projections/src/reducers/zone-estimate.ts
  - packages/projections/src/runner/inline.ts
  - packages/simulation/src/coordinator/canonical.ts
  - packages/simulation/src/coordinator/canonical.unit.test.ts
autonomous: true
requirements: [COORD-02]

must_haves:
  truths:
    - "ActionSuggested / SuggestionAccepted / SuggestionRejected are members of the closed DomainEvent union and validate via zod"
    - "All three new events are classified SCOPE-NEUTRAL in scope.ts (named in hubsOf, never re-scoping)"
    - "The ActionSuggested hashed payload routes through a fixed-key-order canonicalizer (no key-order drift)"
    - "pnpm typecheck is clean — every exhaustive switch names the three new members (the Phase-22/24 trap)"
  artifacts:
    - path: "packages/domain/src/events/schemas.ts"
      provides: "actionSuggestedSchema / suggestionAcceptedSchema / suggestionRejectedSchema zod schemas in the domainEventSchema discriminated union"
      contains: "actionSuggestedSchema"
    - path: "packages/domain/src/events/domain-event.ts"
      provides: "ActionSuggested / SuggestionAccepted / SuggestionRejected types added to the DomainEvent union"
      contains: "ActionSuggested"
    - path: "packages/optimizer/src/rolling/scope.ts"
      provides: "the three new events classified scope-neutral in hubsOf (return [])"
      contains: "ActionSuggested"
    - path: "packages/simulation/src/coordinator/canonical.ts"
      provides: "canonicalizeSuggestionPayload — fixed-key-order canonicalizer for the ActionSuggested payload"
      exports: ["canonicalizeSuggestionPayload"]
  key_links:
    - from: "packages/domain/src/events/domain-event.ts"
      to: "packages/domain/src/events/contract.assert.ts"
      via: "type-equality + exhaustiveness proof (assertNever)"
      pattern: "case \"ActionSuggested\""
    - from: "packages/optimizer/src/rolling/scope.ts"
      to: "the suggestion events"
      via: "scope-neutral classification mirroring PlanGenerated/PlanAccepted/PlanSuperseded"
      pattern: "case \"ActionSuggested\""
---

<objective>
Add the three advisory coordination events — `ActionSuggested`, `SuggestionAccepted`, `SuggestionRejected` — to the closed domain event union, thread them through zod + EVERY exhaustive switch across domain/optimizer/projections, classify all three SCOPE-NEUTRAL in `scope.ts`, and pin the `ActionSuggested` payload through a fixed-key-order canonicalizer. This is the event SUBSTRATE for the whole phase (COORD-02): the coordinator (Plan 02) emits these, the agent handshake (Plan 03) consumes them, and the determinism gate (Plan 05) goldens them. It adds NO engine behavior — every new switch case is a no-op/audit case, so the seed-42 10k golden stays byte-identical to `3920accc…`.

Purpose: every downstream plan depends on these event types existing and being correctly classified. Getting the closed-union threading + scope-neutral classification right NOW (proven by `pnpm typecheck`, not vitest — the esbuild-strips-types trap from Phase 22/24) prevents a re-plan feedback storm later (Pitfall 11) and prevents a flags-off golden drift (Pitfall 8).
Output: three new domain events (closed union + zod + exhaustive switches + scope-neutral), `coordinator/canonical.ts` with `canonicalizeSuggestionPayload`, and a discriminator-count bump in the domain test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-coordination-centers/25-CONTEXT.md

<interfaces>
<!-- Extracted from the codebase. Mirror these EXACTLY; do not explore to re-derive. -->

The closed union member-add pattern (TrailerDiverted, Phase 24) — touch ALL of these:
- packages/domain/src/events/schemas.ts: `eventSchema("Type", payloadZod)` factory; each `*Schema` is exported AND added to the `domainEventSchema` discriminated-union array.
- packages/domain/src/events/domain-event.ts:190-223: `export type DomainEvent = ... | TrailerDiverted;` — add the three members.
- packages/domain/src/events/index.ts + packages/domain/src/index.ts: re-export the new types + schemas (the trailer-diverted test failed until the MAIN index re-exported them).
- packages/domain/src/events/contract.assert.ts:25-58: `assertExhaustive(event)` switch whose `default` is `assertNever(event)` — add three cases.
- packages/domain/test/events.unit.test.ts:197 `describeEvent` switch (default assertNever) + the `ALL_EVENTS` describeEvent list (line ~295) + "DomainEventType is the union of the 26 literal discriminators" (line 319 — becomes 29).

scope-neutral classification (mirror PlanGenerated/PlanAccepted/PlanSuperseded):
- packages/optimizer/src/rolling/scope.ts:63-87: the grouped scope-neutral `case` block returning `[]` in `hubsOf`; `default: never` exhaustiveness at line 88. `trailersOf` (line 97) has its own `default: return []` (no exhaustiveness guard — the three events fall through, correct).

Reducer exhaustive switches (12 reducers + inline.ts) — TrailerDiverted is named as a no-op/audit case in each. Pattern in audit-timeline.ts:222 and exceptions.ts. inline.ts uses `affected*` helper switches whose `default: never` (inline.ts:399) forces classification. For THIS plan the three suggestion events are no-op in EVERY reducer (the COORD-03 reject SURFACING lands in Plan 03; here they only need to be classified to keep the build green + golden byte-identical).

The canonicalizer pattern (packages/simulation/src/ooda/canonical.ts):
```
export function canonicalizeOodaPayload(payload: TrailerDiverted["payload"]): TrailerDiverted["payload"] {
  return { trailerId, tripId, fromHubId, toHubId, reason, occurredAt }; // fixed key order, values untouched
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the three suggestion events (zod + closed union + re-exports)</name>
  <files>packages/domain/src/events/schemas.ts, packages/domain/src/events/domain-event.ts, packages/domain/src/events/index.ts, packages/domain/src/index.ts, packages/domain/src/events/suggestion-events.test.ts</files>
  <read_first>
    - packages/domain/src/events/schemas.ts (the eventSchema factory + domainEventSchema array — the file being modified)
    - packages/domain/src/events/domain-event.ts:130-223 (TruckRefueled/TrailerDiverted type pattern + DomainEvent union — the analog)
    - packages/domain/src/events/index.ts + packages/domain/src/index.ts (re-export pattern)
    - packages/domain/src/events/trailer-diverted.test.ts (the new-event test shape to mirror in suggestion-events.test.ts)
  </read_first>
  <behavior>
    - Test (RED): import { actionSuggestedSchema, suggestionAcceptedSchema, suggestionRejectedSchema } from "@mm/domain" — module-missing until added.
    - Test: validateEvent accepts a well-formed ActionSuggested { suggestionId, coordinatorId, targetAgentId, kind:"reroute", params:{...}, issuedAtSimMs, ttlSimMs } and rejects a malformed one (missing ttlSimMs, or kind not in the closed enum).
    - Test: validateEvent accepts SuggestionAccepted { suggestionId, occurredAt } and SuggestionRejected { suggestionId, reasonCode, occurredAt }; rejects a reasonCode outside the closed enum.
    - Test: ActionSuggested.kind is the closed enum reroute|hold|consolidate|dispatch; SuggestionRejected.reasonCode is the closed enum hos|fuel|dock|infeasible (mirror the reject-with-reason set the Phase-24 feasibility verdict produces: mustRest/mustRefuel/dock/none).
  </behavior>
  <action>
    Add three zod schemas via the existing `eventSchema(type, payloadZod)` factory in schemas.ts. ActionSuggested payload (fixed declaration order): `suggestionId` (string), `coordinatorId` (string), `targetAgentId` (string), `kind` (`z.enum(["reroute","hold","consolidate","dispatch"])`), `params` (a small closed object — at Claude's discretion per the kind, e.g. `{ toHubId?: string }` for reroute; keep it integer/string-only, NO float geometry, NO RNG, per Pitfall 1), `issuedAtSimMs` (number — sim-time epoch ms from the virtual clock), `ttlSimMs` (number). SuggestionAccepted payload: `suggestionId` (string), `occurredAt` (string ISO). SuggestionRejected payload: `suggestionId` (string), `reasonCode` (`z.enum(["hos","fuel","dock","infeasible"])`), `occurredAt` (string ISO). Export each `*Schema` AND add it to the `domainEventSchema` discriminated-union array. In domain-event.ts add the three `export type X = z.infer<typeof xSchema>` lines (with a Phase-25 doc comment mirroring the TrailerDiverted comment) and add the three members to the `DomainEvent` union. Re-export types + schemas through events/index.ts AND the main src/index.ts (the trailer-diverted lesson). Use the named stream convention: ActionSuggested → `coordinator-<centerId>`; Accepted/Rejected → the target's `trailer-<id>`/`hub-<id>` stream (documented in the type comment; actual emit is Plan 02/03). Strict TS: no `any`, no assertions.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/domain/src/events/suggestion-events.test.ts</automated>
  </verify>
  <done>The three schemas + types exist, are in the discriminated union + DomainEvent union, re-exported from both indexes; suggestion-events.test.ts (valid-accept + malformed-reject + closed-enum) passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the three events through EVERY exhaustive switch + scope-neutral classification + the discriminator count</name>
  <files>packages/domain/src/events/contract.assert.ts, packages/domain/test/events.unit.test.ts, packages/optimizer/src/rolling/scope.ts, packages/projections/src/reducers/*.ts (12 reducers), packages/projections/src/runner/inline.ts</files>
  <read_first>
    - packages/domain/src/events/contract.assert.ts:25-58 (assertExhaustive — the build-gating switch being modified)
    - packages/domain/test/events.unit.test.ts:197,295,319 (describeEvent switch + ALL_EVENTS list + the 26→29 discriminator-count assertion)
    - packages/optimizer/src/rolling/scope.ts:27-113 (hubsOf scope-neutral group + default:never; trailersOf default:return [] — being modified)
    - packages/projections/src/reducers/audit-timeline.ts:205-271 + exceptions.ts:193-end + the other 10 reducers' tail no-op case blocks (TrailerDiverted is the proxy — find every `case "TrailerDiverted"`)
    - packages/projections/src/runner/inline.ts:131-399 (the affected* helper switches with default:never)
  </read_first>
  <behavior>
    - Test: events.unit.test.ts "DomainEventType is the union of the N literal discriminators" updates 26→29 and passes (the count proof that no member was dropped).
    - Test: describeEvent over ALL_EVENTS includes the three new members (no assertNever throw).
    - Gate (RED→GREEN): pnpm typecheck FAILS before the switches are threaded (default:never receives the new members) and is CLEAN after — this is the real exhaustiveness proof, not vitest (esbuild strips types so a missing case passes vitest).
  </behavior>
  <action>
    Add the three new members as cases everywhere the closed union is switched exhaustively. In contract.assert.ts add three cases before `assertNever`. In scope.ts `hubsOf`, add `case "ActionSuggested": case "SuggestionAccepted": case "SuggestionRejected":` to the existing scope-neutral group (`return []`) with a Phase-25/Pitfall-11 comment ("suggestion events are SCOPE-NEUTRAL — they must NOT re-trigger the suggesting coordinator, mirroring PlanGenerated/Accepted/Superseded"); leave `trailersOf` alone (its `default: return []` already covers them — confirm by inspection, do NOT add a never-guard there). In each of the 12 projection reducers (audit-timeline, exceptions, delivery-kpi, driver-assignment, driver-status, geo-track, hub-inventory, package-location, tag-registry, trailer-fuel, trailer-state, zone-estimate) add the three events to the existing tail no-op case group (the same place `TrailerDiverted` sits) — NO surfacing logic this plan (COORD-03 surfacing is Plan 03). In inline.ts add the three events to each `affected*` helper that has a `default: never` (return null / [] — they touch no projection row this plan). Update the events.unit.test.ts discriminator-count assertion (26→29) and the ALL_EVENTS describeEvent list. Strict TS throughout.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm exec vitest run --project unit packages/domain/test/events.unit.test.ts</automated>
  </verify>
  <done>pnpm typecheck is CLEAN (every exhaustive switch names the three members; no default:never violation); the discriminator-count test asserts 29 and passes; all 12 reducers + inline.ts + scope.ts + contract.assert.ts build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: canonicalizeSuggestionPayload (fixed-key-order ActionSuggested canonicalizer) + flags-off golden re-assert</name>
  <files>packages/simulation/src/coordinator/canonical.ts, packages/simulation/src/coordinator/canonical.unit.test.ts</files>
  <read_first>
    - packages/simulation/src/ooda/canonical.ts (canonicalizeOodaPayload — the exact pattern to mirror, including the DET-03 purity doc-comment)
    - packages/simulation/src/coordinator/canonical.ts (the file being created — does not exist yet)
    - packages/simulation/test/determinism.unit.test.ts:125-132,236-242 (the LONG_RUN_GOLDEN_SHA256 3920accc… assertion that must stay green)
  </read_first>
  <behavior>
    - Test: canonicalizeSuggestionPayload returns the payload with keys in the fixed declaration order (suggestionId, coordinatorId, targetAgentId, kind, params, issuedAtSimMs, ttlSimMs), values untouched — two builds of the same logical payload JSON.stringify to identical bytes (Pitfall 7).
    - Test: the function is a pure mapping (no Date.now, no Math.random — it lives under coordinator/** which Plan 05's ESLint guard will protect).
    - Gate: the existing seed-42 10k determinism golden (3920accc…) is still byte-identical (this plan adds only no-op switch cases + a pure helper — zero behavior change).
  </behavior>
  <action>
    Create packages/simulation/src/coordinator/canonical.ts exporting `canonicalizeSuggestionPayload(payload: ActionSuggested["payload"]): ActionSuggested["payload"]` returning the object literal in the fixed key order matching the zod schema declaration order — values untouched, mirroring canonicalizeOodaPayload exactly (including the DET-03/Pitfall-7 doc-comment). Import `ActionSuggested` as a type from `@mm/domain`. (SuggestionAccepted/Rejected carry only ids + a reasonCode enum + occurredAt — a single canonicalizer for the ActionSuggested params object is sufficient this phase; if Plan 03 finds the reject payload also needs pinning, extend here.) Add the canonical.unit.test.ts. Re-assert the determinism golden as a smoke check (do NOT capture a coordinator-on golden — that is Plan 05; coordinators are not yet wired so the stream is unchanged).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/simulation/src/coordinator/canonical.unit.test.ts && pnpm exec vitest run --project unit packages/simulation/test/determinism.unit.test.ts -t "10k-tick determinism golden"</automated>
  </verify>
  <done>coordinator/canonical.ts exports canonicalizeSuggestionPayload; its key-order test passes; the seed-42 10k golden is still 3920accc… (zero behavior change).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new event type → closed union | a missed exhaustive switch case silently compiles under vitest (esbuild strips types) but breaks the contract — only `pnpm typecheck` catches it |
| new hashed payload → golden | a data-dependent key order in ActionSuggested drifts the scopeHash/golden (Pitfall 7) |
| suggestion event → optimizer scope | a wrongly-classified suggestion event re-triggers the optimizer/coordinator (Pitfall 11 feedback storm) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | the closed DomainEvent union | mitigate | `pnpm typecheck` gate (Task 2) + the 26→29 discriminator-count test proves no member dropped |
| T-25-02 | Tampering | ActionSuggested hashed payload | mitigate | route through `canonicalizeSuggestionPayload` (fixed key order, values untouched) — Task 3 |
| T-25-03 | Denial of Service | optimizer/coordinator scope feedback | mitigate | classify all three events scope-neutral in scope.ts hubsOf (Task 2) — they return [] so an unchanged scope re-emits nothing |
| T-25-04 | Repudiation | suggestion audit trail | accept | the rich ActionSuggested payload (suggestionId + coordinatorId + targetAgentId + kind + params + sim-time) is a replayable trail; surfacing is Plan 03 |
</threat_model>

<verification>
- `pnpm typecheck` clean (the exhaustiveness gate — the primary proof for this plan).
- `pnpm lint` clean on all touched files (no `any`, no assertions).
- Domain events.unit.test.ts (incl. the 29-discriminator count) + suggestion-events.test.ts + coordinator/canonical.unit.test.ts green.
- The seed-42 10k golden is byte-identical to `3920accc…` (zero behavior change; only no-op cases + a pure helper added).
- Run vitest lanes ONE AT A TIME (full concurrent gate OOMs — v2-gate-hygiene memory).
</verification>

<success_criteria>
- ActionSuggested / SuggestionAccepted / SuggestionRejected are members of the closed DomainEvent union, validate via zod, and are named in EVERY exhaustive switch (typecheck-proven).
- All three are classified scope-neutral in scope.ts (return [] in hubsOf; no re-scope).
- canonicalizeSuggestionPayload pins the ActionSuggested key order.
- The flags-off seed-42 10k golden is unchanged (`3920accc…`).
</success_criteria>

<output>
After completion, create `.planning/phases/25-coordination-centers/25-01-SUMMARY.md`
</output>
