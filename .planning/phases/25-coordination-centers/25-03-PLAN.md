---
phase: 25-coordination-centers
plan: 03
type: tdd
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/simulation/src/engine.ts
  - packages/simulation/src/coordinator/handshake.ts
  - packages/simulation/src/coordinator/handshake.unit.test.ts
  - packages/simulation/src/coordinator/index.ts
  - packages/simulation/test/coordinator-engine.unit.test.ts
  - packages/projections/src/reducers/exceptions.ts
  - packages/projections/src/reducers/exceptions.test.ts
  - packages/projections/src/reducers/audit-timeline.ts
  - packages/projections/src/reducers/audit-timeline.test.ts
autonomous: true
requirements: [COORD-02, COORD-03]

must_haves:
  truths:
    - "In the same tick a coordinator emits ActionSuggested, the target agent consumes it from pendingSuggestionsByTarget and either accepts (SuggestionAccepted + the binding event) or rejects (SuggestionRejected + reasonCode)"
    - "The agent's accept/reject decision is made on its own binding local feasibility (24-03) — a coordinator cannot force an infeasible action"
    - "A SuggestionRejected surfaces in the alert/exception feed AND the audit timeline carrying its reasonCode (e.g. won't divert: HOS/fuel) — the headline smart-and-honest demo moment"
    - "An accepted reroute/hold/consolidate/dispatch emits the SAME binding domain event the agent would have emitted on its own (no new binding path)"
  artifacts:
    - path: "packages/simulation/src/coordinator/handshake.ts"
      provides: "pure arbitrateSuggestion — maps a suggestion + the agent feasibility verdict to accept(+binding event kind) | reject(reasonCode)"
      min_lines: 30
    - path: "packages/simulation/src/engine.ts"
      provides: "stepAgents consumes pendingSuggestionsByTarget in the same tick, emits SuggestionAccepted+binding | SuggestionRejected"
      contains: "pendingSuggestionsByTarget"
    - path: "packages/projections/src/reducers/exceptions.ts"
      provides: "SuggestionRejected folds into the exception/alert feed with its reasonCode"
      contains: "SuggestionRejected"
    - path: "packages/projections/src/reducers/audit-timeline.ts"
      provides: "SuggestionRejected (and Accepted) produce audit-timeline rows"
      contains: "SuggestionRejected"
  key_links:
    - from: "packages/simulation/src/engine.ts (stepAgents)"
      to: "packages/simulation/src/coordinator/handshake.ts"
      via: "arbitrateSuggestion(suggestion, feasibilityVerdict) → accept|reject"
      pattern: "arbitrateSuggestion"
    - from: "SuggestionRejected event"
      to: "packages/projections/src/reducers/exceptions.ts"
      via: "fold into the open-exception/alert feed carrying reasonCode"
      pattern: "case \"SuggestionRejected\""
---

<objective>
Wire the accept/reject HANDSHAKE (COORD-02 consume half) and the visible REJECT-WITH-REASON surfacing (COORD-03). In the same tick the coordinator emitted `ActionSuggested` (Plan 02), the Phase-24 agent step drains `pendingSuggestionsByTarget` for each agent it processes, arbitrates each suggestion against the agent's OWN binding local feasibility verdict (24-03 — the un-overridable contract), and emits `SuggestionAccepted` + the existing binding event (TrailerDiverted / consolidation departure / TruckRested-as-hold / etc.) OR `SuggestionRejected` with a `reasonCode`. A rejected suggestion REUSES the existing alert/exception feed + audit timeline to surface "won't divert: HOS/fuel" — the headline smart-and-honest demo moment. No new UI panel (CONTEXT decision).

Purpose: this is the demo's emotional payload — a truck honestly declining a coordinator re-route because HOS/fuel won't allow it, visible to the operator. It also establishes the handshake whose anti-livelock guards Plan 04 hardens.
Output: a pure `arbitrateSuggestion`, the engine consumption in stepAgents, and the SuggestionRejected/Accepted surfacing in the exceptions + audit-timeline reducers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-coordination-centers/25-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/25-coordination-centers/25-02-SUMMARY.md
@.planning/phases/24-ooda-step-agents/24-03-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Mirror these EXACTLY. -->

The agent step + feasibility verdict (the accept/reject BASIS, 24-03):
- packages/simulation/src/ooda/feasibility.ts: `truckLegFeasibility(obs, hosConfig, fuelConfig, now) → { canDrive, mustRest, mustRefuel, remainingDriveMinutes, restReason: "rest-10h"|"break-30min"|null }`; `hubDockFeasibility(obs) → { canDispatch, canConsolidate }`. The reasonCode enum (hos|fuel|dock|infeasible, Plan 01) maps from this verdict: mustRest⇒hos, mustRefuel⇒fuel, !canDispatch/!canConsolidate⇒dock, otherwise⇒infeasible.
- engine.ts:1662-1755 (the stepAgents DECIDE+ACT loop). The handshake hooks HERE: after building the frozen obs for an agent (and BEFORE/AROUND its own decision), drain pendingSuggestionsByTarget.get(agent.stableId); for each suggestion, arbitrate against the SAME feasibility context already passed to decideTruck (engine.ts:1687-1698: hosLimits, fuel threshold, isoToEpochMinutes(clock.nowIso())). The agent's own autonomous decision remains the feasible no-op default (COORD-05, Plan 04) when no suggestion is accepted.
- emit + the existing binding events: TrailerDiverted (engine.ts:1711-1728, via canonicalizeOodaPayload), emitTruckRested/emitTruckRefueled (engine.ts:1702-1709), dispatchHubConsolidation (engine.ts:1751-1753). An ACCEPTED reroute emits TrailerDiverted; an accepted hold emits no binding event but DOES emit SuggestionAccepted (the agent holds — the feasible no-op); accepted consolidate/dispatch routes through dispatchHubConsolidation. Reuse — do NOT add a new binding event.

The COORD-03 surfacing targets (reuse, no new panel):
- packages/projections/src/reducers/exceptions.ts: `OpenException { kind, severity, ... }`, `open(state, row)` helper, `exceptionId(kind, ...)`, the WrongTrailerDetected/MissedUnloadDetected folds at :149-192, the tail no-op group at :193+ (where the 3 events were parked as no-ops in Plan 01). Add a `coordination-rejected` ExceptionKind (or equivalent) and fold SuggestionRejected into the feed carrying its reasonCode + targetAgentId + suggestionId; severity = info|warning (Claude's discretion — a reject is honest, not a fault).
- packages/projections/src/reducers/audit-timeline.ts: `AuditTimelineEntry`, the per-event row builders at :101-204, the tail no-op group at :205-222. Add SuggestionRejected (and optionally SuggestionAccepted) rows carrying the reasonCode/kind for the audit trail. occurredAt comes from the event payload (never the wall clock).

pendingSuggestionsByTarget (Plan 02): Map<string, ActionSuggested[]> keyed by targetAgentId, populated in the stepCoordinators pass earlier the same tick. The agent drains its own entry; clear it after consumption (within-tick lifecycle). Plan 05 decides what (if anything) of this survives a continuation boundary — for THIS plan it is a within-tick map (coordinators fire, agents consume, same tick).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure arbitrateSuggestion — map (suggestion, feasibility verdict) → accept(+binding kind) | reject(reasonCode)</name>
  <files>packages/simulation/src/coordinator/handshake.ts, packages/simulation/src/coordinator/index.ts, packages/simulation/src/coordinator/handshake.unit.test.ts</files>
  <read_first>
    - packages/simulation/src/ooda/feasibility.ts (the verdict shapes the arbitration reads — the basis)
    - packages/simulation/src/coordinator/coordinator.ts (the CoordinatorSuggestion union from Plan 02 — the input)
    - packages/simulation/src/coordinator/handshake.ts + handshake.unit.test.ts (files being created)
    - packages/domain/src/events/schemas.ts (the SuggestionRejected reasonCode enum — Plan 01)
  </read_first>
  <behavior>
    - Test: arbitrateSuggestion(reroute, verdict where !canDrive) → reject "hos" (a coordinator cannot force a drive when HOS forbids it — the un-overridable contract).
    - Test: arbitrateSuggestion(reroute, verdict where mustRefuel) → reject "fuel".
    - Test: arbitrateSuggestion(dispatch|consolidate, hub verdict where !canDispatch/!canConsolidate) → reject "dock".
    - Test: arbitrateSuggestion(reroute, feasible verdict) → accept with bindingKind "divert".
    - Test: arbitrateSuggestion(hold, any verdict) → accept with bindingKind "hold" (no binding event, but SuggestionAccepted; hold is always the feasible no-op so it never rejects — COORD-05 substrate).
    - Test: PURE + deterministic; the reasonCode is exactly the closed Plan-01 enum (hos|fuel|dock|infeasible).
  </behavior>
  <action>
    Create coordinator/handshake.ts exporting `arbitrateSuggestion(suggestion: CoordinatorSuggestion, verdict: TruckLegFeasibility | HubDockFeasibility): SuggestionArbitration` where `SuggestionArbitration = { accepted: true; bindingKind: "divert"|"hold"|"consolidate"|"dispatch"|"none" } | { accepted: false; reasonCode: "hos"|"fuel"|"dock"|"infeasible" }`. The mapping mirrors the 24-03 feasibility gate priority (mustRest/HOS outranks mustRefuel/fuel outranks dock): a truck suggestion (reroute) is accepted ONLY when canDrive && !mustRefuel, else rejected with the binding reasonCode; a hub suggestion (consolidate/dispatch) accepted ONLY when the dock verdict allows, else "dock"; a hold suggestion is ALWAYS accepted (the feasible no-op default). The function must NOT recompute feasibility — it READS the verdict the agent already produced (DRY; the un-overridable contract is that the agent's verdict is authoritative). Export from coordinator/index.ts. Strict TS, no `any`.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/simulation/src/coordinator/handshake.unit.test.ts</automated>
  </verify>
  <done>arbitrateSuggestion purely maps every (kind × verdict) to accept(+bindingKind) | reject(reasonCode) on the closed enum; HOS>fuel>dock priority preserved; hold always accepted; all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the same-tick handshake into stepAgents (consume pendingSuggestionsByTarget; emit Accepted+binding | Rejected)</name>
  <files>packages/simulation/src/engine.ts, packages/simulation/test/coordinator-engine.unit.test.ts</files>
  <read_first>
    - packages/simulation/src/engine.ts:1549-1762 (the FULL stepAgents body — where the handshake hooks)
    - packages/simulation/src/engine.ts:1687-1733 (the existing decideTruck feasibility-context call + the binding-event Act switch to reuse for an accepted suggestion)
    - packages/simulation/src/coordinator/handshake.ts (Task 1)
    - packages/simulation/test/coordinator-engine.unit.test.ts (Plan 02 — extend with handshake assertions)
  </read_first>
  <action>
    In the stepAgents DECIDE+ACT loop, for each agent (truck and hub), AFTER building the frozen observation and computing the agent's own feasibility verdict (reuse the truckLegFeasibility/hubDockFeasibility the Decide already uses), drain `pendingSuggestionsByTarget.get(agent.stableId)` (a stable-ordered list; iterate in order). For each pending ActionSuggested, call `arbitrateSuggestion(suggestion, verdict)`. If accepted: emit `SuggestionAccepted { suggestionId, occurredAt: clock.nowIso() }` to the agent's stream, THEN emit the binding event for `bindingKind` REUSING the existing Act paths (divert → the TrailerDiverted construction via canonicalizeOodaPayload; consolidate/dispatch → dispatchHubConsolidation; hold → no binding event). If rejected: emit `SuggestionRejected { suggestionId, reasonCode, occurredAt }`. Clear the agent's pending entry after consumption. Ensure an accepted suggestion does NOT double-emit with the agent's own autonomous decision (the suggestion-driven Act replaces the autonomous one for that agent this tick when accepted; document the precedence — Claude's discretion, but it MUST be deterministic and the agent's own feasible no-op default remains when nothing is accepted, the COORD-05 substrate). DET-03: occurredAt from clock.nowIso() (never Date.now); route any hashed payload through canonicalizeSuggestionPayload. Extend coordinator-engine.unit.test.ts: a coordinatorsEnabled-on run emits a non-trivial mix of SuggestionAccepted + SuggestionRejected, each Rejected carries a closed reasonCode, an infeasible suggestion (e.g. HOS-out truck) is ALWAYS rejected (the un-overridable contract), and same-seed byte-identical. Strict TS.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/simulation/test/coordinator-engine.unit.test.ts && pnpm typecheck</automated>
  </verify>
  <done>stepAgents consumes pendingSuggestionsByTarget in the same tick; accepted ⇒ SuggestionAccepted + the existing binding event; rejected ⇒ SuggestionRejected + reasonCode; an HOS/fuel-infeasible suggestion is always rejected; no double-emit; same-seed byte-identical; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface reject-with-reason in the exception/alert feed + audit timeline (COORD-03)</name>
  <files>packages/projections/src/reducers/exceptions.ts, packages/projections/src/reducers/exceptions.test.ts, packages/projections/src/reducers/audit-timeline.ts, packages/projections/src/reducers/audit-timeline.test.ts</files>
  <read_first>
    - packages/projections/src/reducers/exceptions.ts:38-192 (ExceptionKind, OpenException, open helper, exceptionId, the existing folds — the file being modified)
    - packages/projections/src/reducers/exceptions.test.ts (the test shape to extend)
    - packages/projections/src/reducers/audit-timeline.ts:52-222 (AuditTimelineEntry + row builders + the tail no-op group)
    - packages/projections/src/reducers/audit-timeline.test.ts
  </read_first>
  <behavior>
    - Test (exceptions): folding a SuggestionRejected { reasonCode: "hos" } opens an exception/alert row carrying the reasonCode, targetAgentId, suggestionId, and a human-readable label (e.g. "won't divert: HOS"); totalExceptions increments; severity is info/warning (a reject is honest, not a detection fault — keep the false-positive-rate math unaffected).
    - Test (audit-timeline): a SuggestionRejected (and SuggestionAccepted) produces a timeline entry with occurredAt from the payload, the kind, and the reasonCode where applicable.
    - Test: both reducers stay exhaustive (typecheck) and the OTHER no-op events (the remaining suggestion event in each reducer) are unchanged.
  </behavior>
  <action>
    In exceptions.ts: move SuggestionRejected OUT of the Plan-01 no-op group into a real fold — add a `coordination-rejected` ExceptionKind (extend the ExceptionKind union + exceptionId), build an OpenException carrying { reasonCode, targetAgentId, suggestionId, label } where label is a closed map from reasonCode ("hos"→"won't divert: HOS", "fuel"→"won't divert: fuel", "dock"→"won't dispatch: dock full", "infeasible"→"declined: infeasible"), severity info/warning, and route through the existing `open` helper. Keep SuggestionAccepted + ActionSuggested as no-ops in exceptions.ts (a reject is the alert; accepts/suggestions are not exceptions). In audit-timeline.ts: move SuggestionRejected (and SuggestionAccepted) out of the no-op group into real row builders carrying occurredAt (from payload) + kind + reasonCode. Keep ActionSuggested a no-op in audit-timeline this phase (the suggestion itself is in the coordinator stream; the audit trail records the agent's decision — Claude's discretion). Extend both reducers' .test.ts. Strict TS; preserve the falsePositiveRate ratio semantics (a coordination reject must not be counted as a low-confidence detection exception unless intended — document the choice).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/projections/src/reducers/exceptions.test.ts packages/projections/src/reducers/audit-timeline.test.ts && pnpm typecheck</automated>
  </verify>
  <done>A SuggestionRejected surfaces in the exception/alert feed with a reasonCode + human label AND an audit-timeline row; the false-positive-rate semantics are preserved; both reducers stay exhaustive; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator suggestion → agent action | a coordinator must NOT be able to force an infeasible binding action (the un-overridable contract, 24-03) |
| same-tick handshake → emit order | the consume must be deterministic + same-tick; a stale/cross-tick read breaks replay |
| reject event → operator feed | the reject reason must be honest + auditable (the demo's integrity claim) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Elevation of privilege | arbitrateSuggestion / stepAgents | mitigate | arbitration READS the agent's own feasibility verdict (24-03); an infeasible suggestion is ALWAYS rejected — Task 1 property test + Task 2 HOS-out always-reject test |
| T-25-10 | Tampering | same-tick handshake order | mitigate | coordinators fire before agents same-tick (Plan 02 ordering); pending list is stable-ordered + drained deterministically; same-seed byte-identical test (Task 2) |
| T-25-11 | Repudiation | reject audit trail | mitigate | SuggestionRejected carries suggestionId + reasonCode + occurredAt and folds into both the exception feed AND the audit timeline (Task 3) |
| T-25-12 | Tampering | double-emit (suggestion + autonomous) | mitigate | an accepted suggestion replaces the agent's autonomous Act that tick (documented deterministic precedence) — Task 2 no-double-emit assertion |
</threat_model>

<verification>
- `pnpm typecheck` clean (exhaustive switches in both reducers; the binding Act reuse).
- `pnpm lint` clean (no `any`, no assertions).
- handshake unit + coordinator-engine integration + exceptions + audit-timeline tests green.
- An HOS/fuel-infeasible suggestion is always rejected (the un-overridable contract witness).
- Same-seed coordinator-on run byte-identical (the golden is captured in Plan 05).
- Run vitest lanes ONE AT A TIME (OOM hygiene).
</verification>

<success_criteria>
- Same-tick accept/reject handshake: agent consumes pendingSuggestionsByTarget, emits Accepted+binding | Rejected+reasonCode on its own feasibility (COORD-02).
- A coordinator cannot force an infeasible action (always rejected).
- Reject-with-reason surfaces in the alert feed + audit timeline ("won't divert: HOS/fuel") — the demo moment (COORD-03).
</success_criteria>

<output>
After completion, create `.planning/phases/25-coordination-centers/25-03-SUMMARY.md`
</output>
