---
phase: 25-coordination-centers
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - packages/simulation/src/coordinator/coordinator.ts
  - packages/simulation/src/coordinator/coordinator.unit.test.ts
  - packages/simulation/src/coordinator/observe.ts
  - packages/simulation/src/coordinator/index.ts
  - packages/simulation/src/rng.ts
  - packages/simulation/src/continuation.ts
  - packages/simulation/src/engine.ts
  - packages/simulation/test/coordinator-engine.unit.test.ts
autonomous: true
requirements: [COORD-01, COORD-02]

must_haves:
  truths:
    - "With coordinatorsEnabled on, one coordinator per regional center runs in-fold as a self-rescheduling stepCoordinators SimTask, sorted by centerId"
    - "Each coordinator generates rule-based suggestions for all 4 kinds (reroute / hold / consolidate / dispatch) over a bounded per-center scope"
    - "Emitted ActionSuggested events land in an in-engine pendingSuggestionsByTarget map for same-tick consumption by the agent step"
    - "With coordinatorsEnabled absent/false the engine schedules NO stepCoordinators task, constructs ZERO coordinator substreams, and the seed-42 10k golden is byte-identical to 3920accc…"
  artifacts:
    - path: "packages/simulation/src/coordinator/coordinator.ts"
      provides: "pure rule-based suggestion generation per center+kind (decideCoordinatorSuggestions)"
      min_lines: 40
    - path: "packages/simulation/src/coordinator/observe.ts"
      provides: "frozen per-center coordinator observation built at pass entry (integer/string only)"
      min_lines: 20
    - path: "packages/simulation/src/engine.ts"
      provides: "stepCoordinators SimTask + dispatch case + bootstrap seed + self-reschedule + coordinatorsEnabled flag + pendingSuggestionsByTarget map"
      contains: "stepCoordinators"
    - path: "packages/simulation/src/rng.ts"
      provides: "COORDINATOR_RNG_SALT (ninth substream salt, pairwise-distinct) + deriveCoordinatorRng"
      exports: ["COORDINATOR_RNG_SALT", "deriveCoordinatorRng"]
  key_links:
    - from: "packages/simulation/src/engine.ts (stepCoordinators)"
      to: "pendingSuggestionsByTarget"
      via: "each emitted ActionSuggested is also recorded keyed by targetAgentId for the same-tick handshake"
      pattern: "pendingSuggestionsByTarget"
    - from: "packages/simulation/src/engine.ts (dispatch)"
      to: "stepCoordinators"
      via: "SimTask data variant case (self-rescheduling like stepAgents/inductPackage)"
      pattern: "case \"stepCoordinators\""
---

<objective>
Add the per-center coordination process-manager (COORD-01, COORD-02 generation half): a self-rescheduling `stepCoordinators` `SimTask` that runs IN-FOLD on the `(fireTick, seq)` queue, one coordinator per regional center, iterated in sorted-by-centerId order, each over a bounded per-center scope, generating RULE-BASED suggestions for all 4 kinds (reroute / hold / consolidate / dispatch) and emitting `ActionSuggested` into an in-engine `pendingSuggestionsByTarget` map for same-tick consumption by the Phase-24 agent step (handshake lands in Plan 03). Mirror the proven `stepAgents` shape exactly: strict `coordinatorsEnabled === true` flag (OFF by default), lazy coordinator substream construction, frozen observation at pass entry, sorted deterministic iteration, self-reschedule at a fixed interval.

Purpose: this is the headline differentiator's generation engine. The hybrid model (DESIGN-CONSULT Q1) puts coordinators as the micro-advisory layer; rule-based generation this phase (optimizer-backed is Phase 26). The flag must gate every draw, every emit, every ordering change so flags-off stays byte-identical to `3920accc…` (Pitfall 8).
Output: a `coordinator/` module (pure generation + frozen observation), the `stepCoordinators` engine wiring, the ninth RNG salt, and the `pendingSuggestionsByTarget` substrate (consumed in Plan 03, serialized in Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-coordination-centers/25-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/25-coordination-centers/25-01-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Mirror these EXACTLY. -->

stepAgents — the structural analog to MIRROR (packages/simulation/src/engine.ts):
- Flag: engine.ts:666 `const oodaAgentsEnabled = opts.oodaAgentsEnabled === true;` (STRICT === true, never ??/||). Add `const coordinatorsEnabled = opts.coordinatorsEnabled === true;` the same way; add `readonly coordinatorsEnabled?: boolean;` to SimulateOptions (engine.ts:176, doc-commented like oodaAgentsEnabled at :388-403).
- Cadence consts: engine.ts:443 `const OODA_INTERVAL_TICKS = 5;` :445 `const OODA_START_TICK = 1;`. Add COORDINATOR_INTERVAL_TICKS + COORDINATOR_START_TICK (Claude's discretion; coordinators may run on a coarser cadence than agents, but MUST fire in the SAME tick before stepAgents so the handshake is same-tick — see ordering note below).
- The SimTask data variant: continuation.ts:27-95 union; engine.ts:1549 stepAgents body, :1760 self-reschedule `scheduleNext(nextTick, { kind: "stepAgents", tick: nextTick })`, :2566 dispatch case, :2642 bootstrap `if (oodaAgentsEnabled) schedule(OODA_START_TICK, {...})`.
- Frozen observation at pass entry: engine.ts:1552-1660 (build plain-data snapshots ONCE; the Decide/Act loop never re-reads fold maps — the order-shuffle witness). The coordinator reads the SAME fold maps (pendingBySpoke, pendingAtSpoke, odometerByTrailer, clockByDriver, activeTripByTrailer, centerOf, the queue-depth proxies).
- Sorted iteration: `sortAgentsByStableId` (ooda) sorts by codepoint. Coordinators iterate sorted by centerId — reuse the same codepoint sort over center hub ids.
- centerOf + the center set: engine.ts:885 `const centerIds = new Set(continentalCenterOf.values());` :896 `const centerOf = (spokeHubId) => Hub`. The coordinator set is the centers; each center's bounded scope = the spokes whose centerOf === that center (+ the center's own hub).
- emit: engine.ts:809 `emit(streamId, event)` — append to the ordered stream + bump nextSequenceId. ActionSuggested stream id is `coordinator-<centerId>`.

The RNG salt discipline (packages/simulation/src/rng.ts + ooda/rng.ts):
- OODA_RNG_SALT = 0x7a9e3f1d is the EIGHTH salt (7 engine + OODA); the salt-collision test asserts Set size 8. The coordinator salt is the NINTH — pairwise-distinct from all 8 (Plan 05 extends the salt-collision test to Set size 9). `deriveAgentRng(seed, id) = makeRngFromState(mixSeed(mixSeed(seed) ^ OODA_RNG_SALT ^ stableAgentHash(id)))`. Mirror as `deriveCoordinatorRng(seed, centerId)` with COORDINATOR_RNG_SALT. Lazy: constructed ONLY when coordinatorsEnabled, ONLY for a center that actually suggests.

The agent feasibility verdict the coordinator's suggestions will be arbitrated against (Plan 03):
- ooda/feasibility.ts: truckLegFeasibility → { canDrive, mustRest, mustRefuel, remainingDriveMinutes, restReason }; hubDockFeasibility → { canDispatch, canConsolidate }. The coordinator generates a suggestion; the AGENT alone decides feasibility (the un-overridable contract from 24-03). The coordinator must NOT pre-check feasibility — it advises; the agent accepts/rejects (Plan 03).

canonicalizeSuggestionPayload (Plan 01): route every ActionSuggested payload through it (Pitfall 7).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coordinator RNG salt + frozen per-center observation + pure rule-based suggestion generation</name>
  <files>packages/simulation/src/rng.ts, packages/simulation/src/coordinator/observe.ts, packages/simulation/src/coordinator/coordinator.ts, packages/simulation/src/coordinator/index.ts, packages/simulation/src/coordinator/coordinator.unit.test.ts</files>
  <read_first>
    - packages/simulation/src/ooda/rng.ts (OODA_RNG_SALT + deriveAgentRng + stableAgentHash — the exact mirror)
    - packages/simulation/src/rng.ts (the engine salt exports + makeRngFromState/mixSeed — the file being modified)
    - packages/simulation/src/ooda/observe.ts (AgentObservation frozen integer/string surface — the analog for CoordinatorObservation)
    - packages/simulation/src/coordinator/coordinator.ts + observe.ts + index.ts (files being created)
    - .planning/research/ARCHITECTURE.md §4 (the process-manager + the 4 suggestion kinds + sorted-by-centerId)
  </read_first>
  <behavior>
    - Test: COORDINATOR_RNG_SALT is a uint32 pairwise-distinct from the 8 existing salts (a local Set-size-9 assertion; Plan 05 owns the canonical salt-collision test).
    - Test: deriveCoordinatorRng(seed, centerId) is keyed on the STABLE centerId (rename/reorder centers → unchanged stream); two centers never share their first-K draws (decorrelation, mirror the OODA N-agent test at small N).
    - Test: decideCoordinatorSuggestions(obs, rng) over a frozen CoordinatorObservation produces, per rule: a REROUTE suggestion when a downstream spoke's inbound queue exceeds a congestion threshold AND an alternate exists; a HOLD suggestion when a target's next hub is dock-busy; a CONSOLIDATE suggestion when a spoke's pending-consolidation manifest exceeds a fill threshold; a DISPATCH suggestion when an outbound-ready trailer + free dock exist. Each is a closed `{ kind, targetAgentId, params }` record; a no-suggestion center returns [] (the feasible no-op default substrate for COORD-05).
    - Test: the generation is PURE + deterministic (same frozen obs + same rng → byte-identical suggestion list); integer/string-only params (no float geometry, no Date.now).
  </behavior>
  <action>
    Add `COORDINATOR_RNG_SALT` (a fresh every-byte-distinct uint32, NINTH salt) + `deriveCoordinatorRng(seed, centerId)` to rng.ts (or a coordinator/rng.ts leaf mirroring ooda/rng.ts — Claude's discretion; keep it a pure leaf importing only ../rng.js + @mm/domain types, so Plan 05's ESLint guard covers coordinator/**). Create coordinator/observe.ts: `CoordinatorObservation` = a frozen integer/string snapshot per center — its centerId, the bounded set of its spokes with their inbound/outbound queue depths + pending-consolidation counts + dock-availability, and the in-region trucks' active-trip + next-hub + congestion fields (all from the existing fold maps, rounded to integers at this boundary). Create coordinator/coordinator.ts: `decideCoordinatorSuggestions(obs, rng): readonly CoordinatorSuggestion[]` — pure rule-based generation of the 4 kinds with NAMED thresholds (Claude's discretion following the OPT-06/observe.ts congestion proxies; e.g. reroute when a next-hub queue > a congestion threshold, consolidate when pending > a fill threshold). `CoordinatorSuggestion` is a closed union of the 4 kinds carrying `targetAgentId` + integer/string `params`. The coordinator does NOT check feasibility (the agent owns that — 24-03). Export the types + functions from coordinator/index.ts. Strict TS, no `any`.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/simulation/src/coordinator/coordinator.unit.test.ts</automated>
  </verify>
  <done>coordinator/{rng-or-salt,observe,coordinator,index}.ts exist; the salt is pairwise-distinct; decideCoordinatorSuggestions generates all 4 kinds rule-based, pure + deterministic over a frozen observation; all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: stepCoordinators SimTask skeleton — flag, cadence, dispatch case, bootstrap seed (off path inert)</name>
  <files>packages/simulation/src/continuation.ts, packages/simulation/src/engine.ts</files>
  <read_first>
    - packages/simulation/src/continuation.ts:27-95 (SimTask union — add the stepCoordinators data variant)
    - packages/simulation/src/engine.ts:443-445 (cadence consts), :666 (flag), :1760 (self-reschedule), :2558-2614 (dispatch switch), :2620-2648 (bootstrap seed block)
    - packages/simulation/src/engine.ts:176-403 (SimulateOptions — add coordinatorsEnabled)
    - packages/simulation/test/determinism.unit.test.ts:194-330 (the two-part flags-off gate pattern — Plan 05 adds the coordinatorsEnabled case; this task just keeps the existing golden green)
  </read_first>
  <action>
    Add `{ readonly kind: "stepCoordinators"; readonly tick: number }` to the SimTask union (continuation.ts). Add `readonly coordinatorsEnabled?: boolean;` to SimulateOptions with the strict-flag doc-comment (mirror oodaAgentsEnabled:388-403). Construct `const coordinatorsEnabled = opts.coordinatorsEnabled === true;` (STRICT). Add COORDINATOR_INTERVAL_TICKS + COORDINATOR_START_TICK consts. Add the `case "stepCoordinators": stepCoordinators(task.tick); return;` to the dispatch switch (a `stepCoordinators` body stub that returns immediately when off, self-rescheduling +COORDINATOR_INTERVAL_TICKS like stepAgents — full body in Task 3). In the `!resuming` bootstrap, add `if (coordinatorsEnabled) schedule(COORDINATOR_START_TICK, { kind: "stepCoordinators", tick: COORDINATOR_START_TICK });`. Construct the `pendingSuggestionsByTarget` map (empty; populated in Task 3) — `Map<string, ActionSuggested[]>` keyed by targetAgentId — lazily/only-when-on so the off path allocates nothing semantically (it may be an empty Map regardless; what MUST be off-path-inert is any draw/emit/ordering). CRITICAL off-path guarantee: with the flag off, NO stepCoordinators task is scheduled, NO coordinator substream constructed, NO ActionSuggested emitted — the seed-42 10k golden stays 3920accc…. Strict TS.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm typecheck && pnpm exec vitest run --project unit packages/simulation/test/determinism.unit.test.ts -t "10k-tick determinism golden"</automated>
  </verify>
  <done>The stepCoordinators SimTask variant + flag + cadence + dispatch case + bootstrap seed compile; the off path schedules nothing and the seed-42 10k golden is still 3920accc… (skeleton landed, golden byte-identical).</done>
</task>

<task type="auto">
  <name>Task 3: stepCoordinators pass body — sorted-by-centerId, bounded scope, frozen observe, emit ActionSuggested into pendingSuggestionsByTarget</name>
  <files>packages/simulation/src/engine.ts, packages/simulation/test/coordinator-engine.unit.test.ts</files>
  <read_first>
    - packages/simulation/src/engine.ts:1549-1762 (the FULL stepAgents body — frozen observe at entry, sorted iteration, lazy substream, emit, self-reschedule — the exact structure to mirror)
    - packages/simulation/src/engine.ts:885-901 (centerIds set + centerOf — the per-center scope source)
    - packages/simulation/src/coordinator/coordinator.ts + observe.ts (Task 1 — the generation + observation the body calls)
    - packages/simulation/src/coordinator/canonical.ts (canonicalizeSuggestionPayload — route every payload through it)
  </read_first>
  <action>
    Implement the `stepCoordinators(tick)` body mirroring stepAgents: return immediately if `!coordinatorsEnabled`. Build the FROZEN per-center observations ONCE at pass entry from the fold maps (the Decide loop never re-reads them — the order-independence witness). Iterate the centers in sorted-by-centerId (codepoint) order. For each center: build its bounded `CoordinatorObservation` (only that center's spokes/trucks — bounded scope is the COORD-01 scaling thesis; one center's pass never reads another center's trailers); skip a center with nothing to suggest (the anything-to-suggest guard, mirror the agent guard); derive the lazy `deriveCoordinatorRng(seed, centerId)` ONLY for a center that suggests; call `decideCoordinatorSuggestions(obs, rng)`; for each returned suggestion, build an `ActionSuggested` event with a deterministic `suggestionId` (Claude's discretion — e.g. `${centerId}-${tick}-${index}` or a monotonic coordinator counter; MUST be byte-stable + collision-free across the run), `issuedAtSimMs` from the virtual clock, `ttlSimMs` from a named constant (Plan 04 owns the TTL guard; here just stamp it), route the payload through `canonicalizeSuggestionPayload`, `emit("coordinator-"+centerId, suggestion)`, AND record it in `pendingSuggestionsByTarget` keyed by `targetAgentId` (append to a sorted/stable list) for the same-tick agent consumption (Plan 03). Self-reschedule `scheduleNext(tick + COORDINATOR_INTERVAL_TICKS, { kind: "stepCoordinators", tick: ... })`. ORDERING: ensure stepCoordinators fires BEFORE stepAgents within a tick so the handshake is same-tick — document the chosen mechanism (e.g. coordinators on the same cadence seeded one seq earlier, or a lower start offset; the (fireTick, claimSeq) queue makes a same-fireTick coordinator task that is scheduled first dispatch first — verify and document). Add coordinator-engine.unit.test.ts: coordinatorsEnabled-on produces a non-trivial count of ActionSuggested across all 4 kinds, same-seed byte-identical, and the per-center scope is bounded (a center's suggestions only name its own spokes). Strict TS, no `any`.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/simulation/test/coordinator-engine.unit.test.ts && pnpm typecheck</automated>
  </verify>
  <done>With coordinatorsEnabled on, one coordinator per center emits rule-based ActionSuggested (all 4 kinds observed) into pendingSuggestionsByTarget, sorted by centerId, bounded per center, same-seed byte-identical; flag-off still 3920accc…; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinatorsEnabled flag → default path | a non-lazy substream / unconditional schedule / reordered loop perturbs the flags-off golden (Pitfall 8) |
| per-tick observation → suggestion | a mid-tick fold re-read couples a suggestion to iteration order (Pitfall 4/11) |
| per-center scope → other centers | a coordinator reading another center's trailers re-opens the global-solve scaling stall + cross-center conflict (Pitfall 11/12) |
| ActionSuggested payload → golden | data-dependent key order or a non-deterministic suggestionId drifts the golden (Pitfall 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Tampering | flags-off golden | mitigate | strict `coordinatorsEnabled === true`, lazy substream, no schedule/emit when off — Task 2 keeps 3920accc… green |
| T-25-06 | Tampering | suggestionId / payload | mitigate | deterministic byte-stable suggestionId + canonicalizeSuggestionPayload (Task 3) |
| T-25-07 | DoS / Information disclosure | per-center scope bound | mitigate | each coordinator observes ONLY its own center's spokes/trucks (Task 3 bounded scope; the test asserts no cross-center naming) |
| T-25-08 | Elevation of privilege | coordinator vs agent feasibility | accept | coordinator only ADVISES (generates suggestions); it does NOT check/override feasibility — the agent owns the un-overridable verdict (24-03), enforced by the Plan 03 handshake |
</threat_model>

<verification>
- `pnpm typecheck` clean (the stepCoordinators SimTask variant threads the union).
- `pnpm lint` clean on coordinator/** + engine.ts (no `any`).
- coordinator unit + coordinator-engine integration tests green (4 kinds generated, bounded scope, same-seed identity).
- Flag-off seed-42 10k golden byte-identical to `3920accc…` (Task 2 + Task 3 off path inert).
- Run vitest lanes ONE AT A TIME (OOM hygiene).
</verification>

<success_criteria>
- One coordinator per center runs in-fold as a self-rescheduling stepCoordinators SimTask, sorted by centerId, bounded per-center scope (COORD-01).
- Rule-based ActionSuggested generation for all 4 kinds, landing in pendingSuggestionsByTarget for same-tick handshake (COORD-02 generation half).
- coordinatorsEnabled off ⇒ no task scheduled, no substream, golden byte-identical to `3920accc…`.
</success_criteria>

<output>
After completion, create `.planning/phases/25-coordination-centers/25-02-SUMMARY.md`
</output>
