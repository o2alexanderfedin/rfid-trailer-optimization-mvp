---
phase: 25-coordination-centers
plan: 05
type: tdd
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - packages/simulation/src/continuation.ts
  - packages/simulation/src/engine.ts
  - packages/simulation/src/coordinator/canonical.ts
  - packages/simulation/test/determinism.unit.test.ts
  - packages/simulation/test/coordinator-continuation.unit.test.ts
  - packages/simulation/test/coordinator-determinism.unit.test.ts
  - eslint.config.ts
autonomous: true
requirements: [COORD-04]

must_haves:
  truths:
    - "The two-part flags-off gate holds for coordinatorsEnabled: explicit false === absent AND absent ⇒ the seed-42 10k golden is byte-identical to 3920accc…"
    - "COORDINATOR_RNG_SALT is pairwise-distinct from all prior salts (the salt-collision test is green at Set size 9)"
    - "The coordinator lease + reject-pruning + backoff + hysteresis state serializes into SerializedWorldState so a chunked coordinator-on run is byte-identical to all-at-once (continuation-equivalence)"
    - "A new coordinator-on golden is captured reproducibility-first (same-seed in-process AND across-process before baking) and is != 3920accc…"
    - "The DET-03 ESLint guard scoped to coordinator/** fails CI on Date.now/Math.random/new Date()/async-queue/kysely (proven by a planted violation)"
  artifacts:
    - path: "packages/simulation/src/continuation.ts"
      provides: "the coordinator guard state (lease/prune/backoff/hysteresis) + pendingSuggestionsByTarget serialized into SerializedWorldState (present-only-when-on, fixed key order)"
      contains: "coordinator"
    - path: "packages/simulation/test/coordinator-determinism.unit.test.ts"
      provides: "the COORDINATOR_ON_GOLDEN_SHA256 + salt-collision (Set 9) + reproducibility-first capture assertions"
      contains: "COORDINATOR_ON_GOLDEN_SHA256"
    - path: "packages/simulation/test/coordinator-continuation.unit.test.ts"
      provides: "chunked == all-at-once coordinator-on continuation-equivalence at multiple chunk sizes"
      min_lines: 30
    - path: "eslint.config.ts"
      provides: "the DET-03 static guard scoped to packages/simulation/src/coordinator/**"
      contains: "coordinator"
  key_links:
    - from: "packages/simulation/src/engine.ts (captureContinuation + bootstrap restore)"
      to: "SerializedWorldState coordinator state"
      via: "capture lease/prune/backoff/hysteresis maps; restore them on resume"
      pattern: "leaseByAgent|rejectCountByOption"
    - from: "eslint.config.ts DET-03 block"
      to: "packages/simulation/src/coordinator/**"
      via: "no-restricted-syntax/imports (Date.now, Math.random, async-queue, kysely)"
      pattern: "coordinator/\\*\\*"
---

<objective>
Close the determinism + continuation keystone for the coordinator model (COORD-04 determinism facet). (1) The two-part flags-off gate: `coordinatorsEnabled: false === absent` AND `absent ⇒ 3920accc…`. (2) Serialize the coordinator guard state (lease, reject-pruning, backoff, hysteresis) + any cross-tick pending-suggestion state into `SerializedWorldState` so a chunked coordinator-on run is byte-identical to all-at-once (continuation-equivalence). (3) Capture a NEW coordinator-on golden reproducibility-first (same-seed in-process AND across-process BEFORE baking the literal — never a flaky golden). (4) Extend the salt-collision test to Set size 9 (COORDINATOR_RNG_SALT pairwise-distinct). (5) Land the DET-03 ESLint static guard scoped to `coordinator/**` banning Date.now/Math.random/new Date()/async-queue/kysely, proven by a planted violation.

Purpose: determinism is the milestone keystone. Every prior plan re-asserted the gate; this plan makes the coordinator-on model a first-class, reproducible, continuation-equivalent, statically-guarded golden — the basis Phase 28's consolidated audit inherits.
Output: the serialized coordinator state, the two-part gate + salt-collision + reproducibility-first golden + continuation-equivalence tests, and the coordinator/** ESLint guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-coordination-centers/25-CONTEXT.md
@.planning/phases/24-ooda-step-agents/24-04-SUMMARY.md
@.planning/phases/25-coordination-centers/25-02-SUMMARY.md
@.planning/phases/25-coordination-centers/25-04-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Mirror these EXACTLY — this is the proven Phase-24 keystone pattern. -->

The two-part flags-off gate (packages/simulation/test/determinism.unit.test.ts):
- LONG_RUN_GOLDEN_SHA256 = "3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861" (lines 125-126, re-used at 236-242).
- The oodaAgentsEnabled gate at 295-330 is the exact template: (a) `explicit false === absent` over a short run; (b) `absent ⇒ 3920accc…` over the 10k golden; (c) `false === absent` over the 10k golden run. Add the coordinatorsEnabled triple mirroring it.

Continuation-equivalence (packages/simulation/test/ooda-continuation.unit.test.ts + continuation.ts):
- SerializedWorldState (continuation.ts:119-182) — add the coordinator guard state as present-only-when-on, fixed-key-order tuple arrays (mirror activeTripByTrailer:178-181 EXACTLY: empty [] on the off path ⇒ byte-identical serialized form). Fields: leaseByAgent ([agentId,{coordinatorId,expiresAtSimMs}]), rejectCountByOption, backoffUntilByOption, metricAboveSinceByAgent, and any cross-tick pending suggestions (if the Plan-03 handshake is strictly within-tick, pendingSuggestionsByTarget need NOT be serialized — confirm and document; if a suggestion can survive a tick boundary via TTL, serialize it).
- engine.ts captureContinuation (the world capture) + the !resuming bootstrap restore (engine.ts:778-790 restores activeTripByTrailer) — capture + restore the coordinator state the SAME way. Off path: all maps empty ⇒ captured arrays are [] ⇒ serialized form byte-identical to pre-Phase-25.
- The per-coordinator RNG is a STATELESS re-derive (deriveCoordinatorRng rebuilt each pass from seed+centerId, NO stored position — mirror the OODA decision at 24-04) ⇒ NO new SerializedRngStates field. Confirm the backoff jitter draw does not require a persisted stream position (if it draws per-pass from the freshly-derived rng keyed on a sim-time/count input, it is stateless — design for this).
- Chunk sizes proven for OODA: 1, 7, 23, 500. Mirror.

Reproducibility-first golden capture (24-04 OODA_ON_GOLDEN pattern):
- Before baking the literal, run the coordinator-on config twice in-process (identical) AND across two separate test-process invocations (identical) — PITFALLS "never commit a non-reproducible golden." Then assert `!= 3920accc…`. Config: simulate({ seed: 42, durationTicks: 10000, coordinatorsEnabled: true, continentalTopology: true, oodaAgentsEnabled: true, + hos/fuel/induction/consolidation as the OODA-on golden used }). NOTE: coordinators require centers, so the coordinator-on golden likely runs continentalTopology + oodaAgentsEnabled on — pick the config the model genuinely exercises (one coordinator per center advising real agents), document it next to the literal with the capture environment (arch/os, event count).

The salt-collision test (ooda-determinism.unit.test.ts / determinism.unit.test.ts):
- OODA_RNG_SALT made Set size 8 (7 engine + OODA). Add COORDINATOR_RNG_SALT ⇒ Set size 9, pairwise-distinct.

The DET-03 ESLint guard (eslint.config.ts, the 24-04 block scoped to ooda/**):
- A flat-config block `files: ["packages/simulation/src/coordinator/**/*.ts"]`, `ignores: ["…/*.test.ts"]` with no-restricted-syntax (Date.now CallExpression, Math.random CallExpression, new Date() NewExpression arguments.length=0) + no-restricted-imports (kysely + @alexanderfedin/async-queue + patterns). Mirror the ooda/** block exactly, scoped to coordinator/**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialize the coordinator guard state into SerializedWorldState (continuation-equivalence)</name>
  <files>packages/simulation/src/continuation.ts, packages/simulation/src/engine.ts, packages/simulation/test/coordinator-continuation.unit.test.ts</files>
  <read_first>
    - packages/simulation/src/continuation.ts:119-182 (SerializedWorldState + the activeTripByTrailer present-only-when-on tuple-array pattern — the exact mirror)
    - packages/simulation/src/engine.ts:769-790 (activeTripByTrailer construct + restore), the captureContinuation world-capture site, the !resuming bootstrap
    - packages/simulation/src/coordinator/guards.ts (the lease/prune/backoff/hysteresis state shapes from Plan 04)
    - packages/simulation/test/coordinator-continuation.unit.test.ts (file being created)
  </read_first>
  <behavior>
    - Test: a coordinator-on run driven in CHUNKS via runToHorizon is byte-identical (SHA-256 over the ordered stream) to all-at-once at chunk sizes 1, 7, 23, 500 (mirror the OODA continuation test).
    - Test: the captured continuation carries the coordinator guard state as plain JSON-round-trippable data; resuming from the JSON-revived continuation matches the in-memory one.
    - Test: with coordinatorsEnabled OFF, every coordinator-state array captures as [] (off path byte-identical — the keystone).
  </behavior>
  <action>
    Add the coordinator guard state to SerializedWorldState as present-only-when-on, fixed-key-order tuple arrays mirroring activeTripByTrailer EXACTLY: `leaseByAgent` ([agentId,{coordinatorId,expiresAtSimMs}]), `rejectCountByOption`, `backoffUntilByOption`, `metricAboveSinceByAgent` (sorted by key for stable order). If the Plan-03 handshake proves strictly within-tick (a suggestion emitted in stepCoordinators is consumed in the SAME tick's stepAgents and never survives), pendingSuggestionsByTarget need NOT be serialized — document this; otherwise serialize any cross-tick pending suggestions too. In engine.ts: capture these maps in captureContinuation (sorted, pointer-free) and restore them in the !resuming bootstrap, exactly as activeTripByTrailer is. Confirm + document that the per-coordinator RNG is a stateless re-derive (no new SerializedRngStates field). Off-path guarantee: when coordinatorsEnabled is off, all maps are empty ⇒ captured arrays are [] ⇒ the serialized form is byte-identical to pre-Phase-25 (the existing continuation goldens stay green). Create coordinator-continuation.unit.test.ts. Strict TS, no `any`.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/simulation/test/coordinator-continuation.unit.test.ts && pnpm typecheck</automated>
  </verify>
  <done>The coordinator guard state serializes present-only-when-on with fixed key order; a chunked coordinator-on run is byte-identical to all-at-once at chunk sizes 1/7/23/500; the off path captures [] (byte-identical); the per-coordinator RNG is a documented stateless re-derive (no new rng field); typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-part flags-off gate + salt-collision (Set 9) + reproducibility-first coordinator-on golden</name>
  <files>packages/simulation/test/determinism.unit.test.ts, packages/simulation/test/coordinator-determinism.unit.test.ts</files>
  <read_first>
    - packages/simulation/test/determinism.unit.test.ts:125-132,194-330 (the LONG_RUN_GOLDEN + the oodaAgentsEnabled two-part gate template to mirror for coordinatorsEnabled)
    - packages/simulation/test/ooda-determinism.unit.test.ts (the OODA_ON_GOLDEN reproducibility-first capture + salt-collision Set-8 pattern — the exact mirror)
    - packages/simulation/src/coordinator/rng (COORDINATOR_RNG_SALT — the ninth salt)
    - packages/simulation/test/coordinator-determinism.unit.test.ts (file being created)
  </read_first>
  <behavior>
    - Test (two-part gate): `coordinatorsEnabled: false` === absent over a short run; `coordinatorsEnabled` absent ⇒ seed-42 10k golden === 3920accc…; `false === absent` over the 10k golden run.
    - Test (salt-collision): the Set of all salts (8 prior + COORDINATOR_RNG_SALT) has size 9 — pairwise-distinct.
    - Test (coordinator-on golden): a coordinator-on config hashes identically twice in-process AND across two test-process invocations (reproducibility-first), is committed as COORDINATOR_ON_GOLDEN_SHA256, is != 3920accc…, every emitted event passes validateEvent, and the stream carries a non-trivial count of ActionSuggested + SuggestionAccepted + SuggestionRejected (the model genuinely changed).
  </behavior>
  <action>
    Add the coordinatorsEnabled two-part flags-off gate to determinism.unit.test.ts mirroring the oodaAgentsEnabled block (a/b/c triple). Extend the salt-collision test to Set size 9 (COORDINATOR_RNG_SALT pairwise-distinct from the 8 prior salts). Create coordinator-determinism.unit.test.ts: capture the coordinator-on golden REPRODUCIBILITY-FIRST — run the chosen config (seed 42, 10k, coordinatorsEnabled + continentalTopology + oodaAgentsEnabled + the same hos/fuel/induction/consolidation flags the OODA-on golden uses) twice in-process and across two separate process invocations, confirm byte-identity, THEN bake the literal `COORDINATOR_ON_GOLDEN_SHA256`. Assert: in-process reproducible, != 3920accc…, validateEvent boundary, and the three suggestion event types are present with non-trivial counts. Document the capture environment (arch/os, event count) next to the literal (the integer-LUT cross-arch contingency note, per the roadmap). NOTE: if coordinators meaningfully require the continental topology to have multiple centers, the golden config MUST enable continentalTopology — pick + document the config that exercises one-coordinator-per-center advising real agents. Strict TS.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm exec vitest run --project unit packages/simulation/test/coordinator-determinism.unit.test.ts && pnpm exec vitest run --project unit packages/simulation/test/determinism.unit.test.ts</automated>
  </verify>
  <done>The coordinatorsEnabled two-part gate (false===absent + absent⇒3920accc… + false===absent over 10k) is green; the salt-collision Set is size 9; COORDINATOR_ON_GOLDEN_SHA256 is captured reproducibility-first (in-process + across-process), committed, != 3920accc…, with non-trivial suggestion-event counts.</done>
</task>

<task type="auto">
  <name>Task 3: DET-03 ESLint static guard scoped to coordinator/** (proven by a planted violation)</name>
  <files>eslint.config.ts</files>
  <read_first>
    - eslint.config.ts (the existing DET-03 block scoped to packages/simulation/src/ooda/** — the exact mirror to copy for coordinator/**)
    - packages/simulation/src/coordinator/*.ts (confirm the leaf is already pure: imports only ../rng + @mm/domain types + virtual-clock-passed values — no Date.now/Math.random/async-queue/kysely)
    - .planning/phases/24-ooda-step-agents/24-04-SUMMARY.md (the planted-violation proof method)
  </read_first>
  <action>
    Add a flat-config block to eslint.config.ts scoped to `files: ["packages/simulation/src/coordinator/**/*.ts"]`, `ignores: ["packages/simulation/src/coordinator/**/*.test.ts"]`, mirroring the ooda/** DET-03 block: `no-restricted-syntax` for Date.now (`CallExpression[callee.object.name='Date'][callee.property.name='now']`), Math.random (`CallExpression[callee.object.name='Math'][callee.property.name='random']`), wall-clock new Date (`NewExpression[callee.name='Date'][arguments.length=0]`); `no-restricted-imports` for kysely (+ patterns) and @alexanderfedin/async-queue (+ *async-queue* pattern). PROVE it fires: temporarily plant Date.now()/Math.random()/new Date() in a throwaway coordinator/__guard_probe.ts and kysely/async-queue imports in a __guard_probe2.ts, run pnpm lint, confirm every one errors with its DET-03 message, then REMOVE both probes and confirm the clean tree passes `pnpm lint` (exit 0). Confirm the existing ooda/** guard + all other lint rules stay green. No production-code change expected (coordinator/** is already pure from Plans 02/04) — if lint flags a real violation, that is a genuine determinism bug to fix.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm lint</automated>
  </verify>
  <done>The DET-03 ESLint guard is scoped to coordinator/**, proven to fire on Date.now/Math.random/new Date()/async-queue/kysely (planted-violation check), both probes removed, and the clean tree passes pnpm lint (exit 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinatorsEnabled flag → golden | a flags-off regression shifts the seed-42 golden off 3920accc… (Pitfall 8) |
| coordinator state → continuation boundary | unserialized guard state ⇒ chunked diverges from all-at-once (the OODA odometer-clobber class of bug) |
| coordinator-on golden → reproducibility | a non-reproducible golden bakes a flake in forever (Pitfall 2) |
| coordinator/** core → wall-clock/random | a Date.now/Math.random in the decision core breaks replay (DET-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-18 | Tampering | flags-off seed-42 golden | mitigate | the two-part gate (Task 2) — false===absent AND absent⇒3920accc… over short + 10k |
| T-25-19 | Tampering | guard state across a continuation boundary | mitigate | serialize lease/prune/backoff/hysteresis present-only-when-on; chunked==all-at-once test (Task 1) |
| T-25-20 | Repudiation | non-reproducible coordinator golden | mitigate | reproducibility-first capture (in-process + across-process before baking) — Task 2 |
| T-25-21 | Tampering | salt collision | mitigate | COORDINATOR_RNG_SALT in the size-9 salt-collision Set (Task 2) |
| T-25-22 | Tampering | wall-clock/random in coordinator core | mitigate | the coordinator/** DET-03 ESLint guard, proven by a planted violation (Task 3) |
</threat_model>

<verification>
- `pnpm typecheck` clean (the serialized coordinator state threads SerializedWorldState).
- `pnpm lint` clean WITH the coordinator/** DET-03 guard active.
- coordinator-continuation + coordinator-determinism + determinism suites green.
- The flags-off seed-42 10k golden is byte-identical to `3920accc…`; the coordinator-on golden is captured reproducibility-first, committed, != 3920accc…, continuation-equivalent.
- ALL prior goldens (DET-02, fuel, consolidation, continental, OODA-on 94689f99…, continuation-equivalence) stay green.
- Run vitest lanes ONE AT A TIME with pkill -f vitest between runs (the v2-gate-OOM memory — exit 137 risk).
</verification>

<success_criteria>
- Two-part flags-off gate holds for coordinatorsEnabled (false===absent AND absent⇒3920accc…) (COORD-04 determinism facet).
- COORDINATOR_RNG_SALT pairwise-distinct (salt-collision Set size 9).
- Coordinator guard state serialized ⇒ chunked coordinator-on run byte-identical to all-at-once.
- A new coordinator-on golden captured reproducibility-first, != 3920accc….
- The DET-03 ESLint guard scoped to coordinator/** fails CI on Date.now/Math.random/async-queue/kysely.
</success_criteria>

<output>
After completion, create `.planning/phases/25-coordination-centers/25-05-SUMMARY.md`
</output>
