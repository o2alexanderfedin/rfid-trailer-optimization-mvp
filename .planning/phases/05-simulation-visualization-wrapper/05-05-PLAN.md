---
phase: 05-simulation-visualization-wrapper
plan: 05
type: tdd
wave: 3
depends_on: [05-01, 05-02, 05-04]
files_modified:
  - packages/simulation/src/scenario.ts
  - packages/simulation/src/scenario.test.ts
  - packages/simulation/src/engine.ts
  - packages/simulation/src/index.ts
  - packages/api/src/sim/driver.ts
  - packages/api/src/sim/driver.test.ts
  - packages/api/src/routes/scenario.ts
  - packages/api/src/routes/scenario.test.ts
  - packages/api/src/server.ts
  - packages/api/test/scenario-reopt.e2e.test.ts
autonomous: true
requirements: [SIM-04]
must_haves:
  truths:
    - "`POST /api/scenario` accepts the four operator knobs (hub congestion, trip delay, demand spike, sensor-noise level) and injects them deterministically into the running sim."
    - "A knob change triggers a SCOPED re-optimization (via the Phase-5 live loop) within bounded ticks."
    - "The re-optimization is visible to clients: a changed plan + a pushed state diff (tick envelope with planChanges / changed buckets / new exceptions)."
    - "Given the same seed + same knob sequence, the injected stream and resulting plans are byte-identical (deterministic)."
  artifacts:
    - path: "packages/simulation/src/scenario.ts"
      provides: "Deterministic scenario-injection model: the four knobs → seeded modifications to the event stream"
      exports: ["applyScenario", "type ScenarioKnobs"]
    - path: "packages/api/src/routes/scenario.ts"
      provides: "POST /scenario — validates knobs, injects into the sim driver, triggers a re-opt tick"
      exports: ["registerScenarioRoutes"]
    - path: "packages/api/test/scenario-reopt.e2e.test.ts"
      provides: "KEYSTONE (c): scenario-knob → visible re-optimization e2e"
  key_links:
    - from: "packages/api/src/routes/scenario.ts"
      to: "packages/api/src/sim/driver.ts"
      via: "POST applies knobs to the running deterministic sim"
      pattern: "applyScenario|ScenarioKnobs"
    - from: "packages/api/src/sim/driver.ts"
      to: "packages/api/src/optimizer/live-loop.ts"
      via: "an injected scenario change triggers a scoped re-optimization tick"
      pattern: "RollingLoop|tick"
    - from: "packages/api/src/sim/driver.ts"
      to: "packages/api/src/ws/snapshots.ts"
      via: "the re-opt broadcasts a tick envelope (planChanges / buckets / exceptions)"
      pattern: "broadcast"
---

<objective>
Deliver SIM-04 — the operator scenario knobs that drive VISIBLE re-optimization, the demo's interactive heart. This is KEYSTONE (c): a scenario-knob → visible re-optimization e2e (knob change ⇒ changed plan + pushed state diff).

Per 05-CONTEXT.md (Claude's-discretion product defaults, flagged): four operator controls — hub congestion, trip delay, demand spike, sensor-noise level — via `POST /api/scenario`; deterministic given seed; a knob change triggers a SCOPED re-optimization (Plan 05-02's live loop) visible on the map/feed/KPIs within bounded ticks. Per 05-RESEARCH.md cross-cutting: knob changes are COMMANDS to the server, not new map plumbing — they alter the sim, which flows back through the same `tick` stream (Plan 05-01).

Depends on the ws envelope (05-01, so the re-opt is pushable as a tick delta) and the live loop (05-02, so the re-opt actually runs). The scenario injection lives in `@mm/simulation` as a deterministic, seeded modification (no Date.now, no unseeded Math.random — SIM determinism is non-negotiable).

Purpose: this is the moment the demo audience sees the optimizer react live — the payload that makes Phase 4's work legible.
Output: the scenario model, the sim-driver injection hook, the `POST /scenario` route, and the keystone e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-simulation-visualization-wrapper/05-CONTEXT.md
@.planning/phases/05-simulation-visualization-wrapper/05-RESEARCH.md
@packages/api/src/sim/driver.ts
@packages/simulation/src/engine.ts
@.planning/phases/05-simulation-visualization-wrapper/05-01-SUMMARY.md
@.planning/phases/05-simulation-visualization-wrapper/05-02-SUMMARY.md

<interfaces>
<!-- Sim determinism primitives to USE (DO NOT add Date.now/unseeded RNG) — @mm/simulation -->
makeRng(seed): Rng           // ALL randomness flows through this
VirtualClock                 // ALL time comes from this
simulate({ seed, durationTicks, rfid? }): SimulatedEvent[]   // deterministic generator
SimulatedEvent = { streamId, event: DomainEvent, occurredAt }

<!-- The sim driver to EXTEND (packages/api/src/sim/driver.ts) -->
driveSimulation({ db, seed, durationTicks, rfid?, detection?, broadcast }): Promise<{ ticks }>
// per-tick loop: append → inline projections → detector → catchup → broadcast()

<!-- The live loop to TRIGGER (Plan 05-02) -->
RollingLoop.tick({ events, simMs }): EpochResult    // scoped re-optimization

<!-- The ws envelope to PUSH through (Plan 05-01) -->
broadcast(simMs): WsEnvelope    // tick delta carries planChanges / changed buckets / exceptionsNew

<!-- The four knobs (05-CONTEXT product defaults) -->
ScenarioKnobs = {
  hubCongestion?: { hubId, level },   // extra dwell/dock contention
  tripDelay?: { routeId, delayMin },  // added travel time on a leg
  demandSpike?: { hubId, factor },    // more packages created
  sensorNoise?: { missRate, rssiNoise } // RFID noise level (feeds rfid config)
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic scenario-injection model in @mm/simulation (RED→GREEN)</name>
  <files>packages/simulation/src/scenario.ts, packages/simulation/src/scenario.test.ts, packages/simulation/src/engine.ts, packages/simulation/src/index.ts</files>
  <behavior>
    - Test: `applyScenario(stream, knobs, rng)` returns a modified deterministic stream — hubCongestion adds dwell, tripDelay adds travel min, demandSpike adds seeded PackageCreated events, sensorNoise adjusts RFID emission.
    - Test (SIM determinism): same seed + same knobs ⇒ byte-identical modified stream; ALL randomness via `makeRng`, ALL time via the virtual clock (NO Date.now, NO unseeded Math.random).
    - Test: knobs are additive/composable (two knobs applied in a fixed order give a stable result).
    - Test: an empty/absent knob is a no-op (the unmodified stream).
  </behavior>
  <action>Create `scenario.ts` exporting `ScenarioKnobs` (the four knobs) and `applyScenario` — a pure, seeded transformation over the deterministic event stream / engine schedule. Wire the knobs into the engine's existing seeded generation (extend `engine.ts` where injection points live: dwell ticks, transit ticks, package-creation batches, RFID config). Keep the engine's determinism contract intact (T-01-15). Export from `index.ts`. Write the failing determinism + per-knob tests FIRST, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- scenario</automated>
  </verify>
  <done>`applyScenario` deterministically injects each of the four knobs; same seed + knobs ⇒ identical stream; no Date.now/unseeded RNG; `tsc -b` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sim-driver injection hook → scoped re-opt → broadcast</name>
  <files>packages/api/src/sim/driver.ts, packages/api/src/sim/driver.test.ts</files>
  <behavior>
    - Test: an injected scenario change mid-run modifies subsequent ticks (the new events appear in the stream).
    - Test: after a scenario injection, the driver runs a SCOPED re-optimization tick (RollingLoop.tick with the affected events) within bounded ticks.
    - Test: the re-opt produces a changed plan AND a broadcast tick envelope carrying the change (planChanges / changed buckets / new exceptions).
    - Test (backward-compat): with NO scenario injected, the driver behaves exactly as before (existing FND/ws/Phase-3 sim tests stay green).
  </behavior>
  <action>Extend `driveSimulation` (or add a controllable run mode) so a `ScenarioKnobs` change can be injected into the running deterministic sim and the per-tick loop: (1) applies the knobs (Task 1), (2) after appending the affected events, calls the Plan 05-02 `RollingLoop.tick({ events, simMs })` to re-optimize the affected scope, (3) broadcasts the resulting tick envelope (Plan 05-01). Keep the existing per-tick discipline (one broadcast per tick, OCC-safe appends). Preserve backward-compatibility: no knob ⇒ identical to today. Write failing tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/api test -- sim/driver</automated>
  </verify>
  <done>A scenario injection drives a scoped re-opt that produces a changed plan + a pushed tick diff within bounded ticks; the no-knob path is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POST /api/scenario route + KEYSTONE (c) e2e (knob → visible re-opt)</name>
  <files>packages/api/src/routes/scenario.ts, packages/api/src/routes/scenario.test.ts, packages/api/src/server.ts, packages/api/test/scenario-reopt.e2e.test.ts</files>
  <behavior>
    - Test: `POST /scenario` validates the four-knob body (Fastify schema; rejects unknown/invalid knobs with 400) and applies it to the running sim.
    - Test: `POST /scenario` is the ONLY mutation surface added (it commands the sim; it does not write events directly — the sim/loop do).
    - KEYSTONE e2e: start a seeded run, connect a ws client, capture the baseline plan/state, `POST /scenario` a congestion/demand spike, then assert within bounded ticks the client receives a `tick` envelope whose `planChanges` (or changed buckets/new exceptions) reflect a re-optimized plan — proving knob ⇒ visible re-optimization. Run it twice on the same seed+knobs and assert the re-opt is deterministic.
  </behavior>
  <action>Implement `registerScenarioRoutes(app, sim)` with a schema-validated `POST /scenario` that hands the knobs to the running sim driver (Task 2). Register in `server.ts`. Write the KEYSTONE e2e in `packages/api/test/` using a Testcontainer + `app.inject()`/ws client: seed → snapshot → POST knobs → assert a tick envelope shows the re-opt within bounded ticks, and assert determinism across two identical runs. Write the failing route test + keystone first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/api test -- scenario</automated>
  </verify>
  <done>`POST /api/scenario` validates + injects the four knobs; the KEYSTONE e2e proves knob ⇒ changed plan + pushed state diff within bounded ticks, deterministically; `tsc -b` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→POST /scenario | The ONE untrusted-input mutation surface — knobs command the sim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | POST /scenario body | mitigate | Fastify JSON schema validates the closed four-knob shape + numeric bounds; unknown/out-of-range knobs rejected 400 before reaching the sim. |
| T-05-11 | Denial of service | demandSpike factor | mitigate | The schema bounds the spike factor + injected package count; the scoped re-opt (OPT-05) bounds optimizer work per tick. |
| T-05-12 | Repudiation | injected scenario | mitigate | The injection flows through the event store / sim stream (auditable); the determinism contract makes a knob sequence replayable. |
</threat_model>

<verification>
- `pnpm --filter @mm/simulation test` + `pnpm --filter @mm/api test` green (scenario, sim/driver, routes/scenario, keystone e2e).
- `pnpm build` (turbo) clean.
- Determinism gate: the keystone e2e runs twice on one seed+knob sequence and asserts identical re-opt output.
- Grep gate: `grep -rn "Date.now\|Math.random" packages/simulation/src/scenario.ts | grep -v '^#'` returns zero (seeded determinism).
</verification>

<success_criteria>
An operator `POST /api/scenario` with any of the four knobs triggers a scoped, deterministic re-optimization that is visible to clients as a changed plan + a pushed tick state-diff within bounded ticks — the SIM-04 keystone.
</success_criteria>

<output>
After completion, create `.planning/phases/05-simulation-visualization-wrapper/05-05-SUMMARY.md`
</output>
