---
phase: 05-simulation-visualization-wrapper
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/api/src/kpis/compute-kpis.ts
  - packages/api/src/kpis/compute-kpis.test.ts
  - packages/api/src/kpis/comparison.ts
  - packages/api/src/kpis/comparison.test.ts
  - packages/api/src/routes/kpis.ts
  - packages/api/src/routes/kpis.test.ts
  - packages/api/src/server.ts
autonomous: true
requirements: [UI-03, UI-04]
must_haves:
  truths:
    - "`GET /api/kpis` returns the live operational KPIs: utilization, rehandle count/minutes, wrong-trailer count, missed-unload count, SLA-violation rate, on-time departure/arrival."
    - "`GET /api/kpis/comparison` returns baseline-planner vs optimizer KPIs on the SAME seeded stream, with per-metric deltas — the money slide."
    - "Both endpoints are seed-deterministic: re-running the same seed yields identical KPI values."
    - "The optimizer demonstrably beats the baseline on rehandle + SLA (not theater) for the calibrated demo seed."
  artifacts:
    - path: "packages/api/src/kpis/compute-kpis.ts"
      provides: "Pure KPI computation from projection rows + plan scores (reuses Phase-2 scoring + Phase-3 exception counts)"
      exports: ["computeKpis", "type KpiSnapshot"]
    - path: "packages/api/src/kpis/comparison.ts"
      provides: "baseline-vs-optimizer KPI comparison on one seeded stream + deltas"
      exports: ["computeComparison", "type KpiComparison"]
    - path: "packages/api/src/routes/kpis.ts"
      provides: "GET /kpis + GET /kpis/comparison read routes"
      exports: ["registerKpiRoutes"]
  key_links:
    - from: "packages/api/src/kpis/compute-kpis.ts"
      to: "@mm/load-planner scorePlan/utilizationScore/rehandleScore"
      via: "reuses the Phase-2 shared scoring plumbing (P8 — money slide is wiring)"
      pattern: "scorePlan|utilizationScore|rehandle"
    - from: "packages/api/src/kpis/comparison.ts"
      to: "@mm/load-planner baselinePlan + planLoad"
      via: "runs baseline + optimizer on the SAME inputs"
      pattern: "baselinePlan|planLoad"
    - from: "packages/api/src/server.ts"
      to: "packages/api/src/routes/kpis.ts"
      via: "composition root registers the KPI routes"
      pattern: "registerKpiRoutes"
---

<objective>
Deliver the KPI dashboard data (UI-03) and the before/after "money slide" comparison (UI-04) — the highest-leverage differentiator. Per 05-CONTEXT.md + ROADMAP Phase 5 notes, this is mostly WIRING given the Phase-2 baseline planner + shared KPI plumbing (P8) and Phase-3 exception counts; the work is computing a `KpiSnapshot` and a seed-deterministic baseline-vs-optimizer comparison.

`GET /api/kpis`: utilization, rehandle count/minutes, wrong-trailer count, missed-unload count, SLA-violation rate, on-time depart/arrive — from the live projections + plan scores. `GET /api/kpis/comparison`: the SAME seeded stream scored under the FIFO baseline vs the optimizer, with deltas, calibrated so the optimizer demonstrably wins on rehandle + SLA (not theater). The `KpiSnapshot` shape must match the ws envelope's `KpiSnapshot` (Plan 05-01) so the dashboard reads one shape from both REST and ws.

Purpose: the money slide proves the optimizer beats the baseline; it must be reproducible (seed-frozen) and honest (same inputs, same scoring plumbing).
Output: pure KPI computation, the comparison, and the two read routes registered in the composition root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-simulation-visualization-wrapper/05-CONTEXT.md
@packages/api/src/routes/exceptions.ts
@packages/api/src/routes/plan.ts

<interfaces>
<!-- Phase-2 scoring to REUSE (P8 — DO NOT re-implement) — from @mm/load-planner -->
scorePlan(plan, blocks, route, config): ScoreResult       // rehandle + utilization
utilizationScore(...), rehandleScore(...), rehandleBreakdown(...)
planLoad(blocks, route, config): LoadPlan                 // the optimizer's plan
baselinePlan(blocks, route, config): LoadPlan             // FIFO baseline on the SAME inputs (LOAD-09)
validatePlan(...), isFeasible(...)

<!-- Phase-3 exception counts to REUSE — from @mm/projections -->
readExceptionKpi(db): { totalExceptions, lowConfidenceExceptions, falsePositiveRate }
readOpenExceptions(db): ExceptionRow[]  // kind:"wrong-trailer"|"missed-unload" → counts

<!-- The KpiSnapshot shape (MUST match Plan 05-01's ws envelope KpiSnapshot) -->
KpiSnapshot = {
  utilization, rehandleCount, rehandleMinutes, wrongTrailerCount, missedUnloadCount,
  slaViolationRate, onTimeDeparture, onTimeArrival,
  baseline: Omit<KpiSnapshot, "baseline">
}

<!-- Seeded sim driver to reproduce a stream (packages/api/src/sim/driver.ts) -->
driveSimulation({ db, seed, durationTicks, rfid?, broadcast? }): Promise<{ ticks }>  // deterministic per seed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure computeKpis from projection rows + plan scores (RED→GREEN)</name>
  <files>packages/api/src/kpis/compute-kpis.ts, packages/api/src/kpis/compute-kpis.test.ts</files>
  <behavior>
    - Test: given trailer plans (scored via Phase-2 scoring) + exception rows, `computeKpis` returns utilization, rehandleCount, rehandleMinutes, wrongTrailerCount, missedUnloadCount, slaViolationRate, onTimeDeparture, onTimeArrival.
    - Test: wrongTrailerCount / missedUnloadCount are derived from `readOpenExceptions` kinds (reuse Phase-3 counts).
    - Test: utilization + rehandle reuse `scorePlan` outputs (P8 — not re-implemented).
    - Test (P3): same inputs ⇒ identical KpiSnapshot (no Date.now, no float-key nondeterminism; rates rounded by a fixed rule).
    - Test: the returned shape is structurally assignable to the ws envelope `KpiSnapshot` (minus `baseline`).
  </behavior>
  <action>Implement `computeKpis(args): Omit<KpiSnapshot,"baseline">` as a pure function over plan scores + exception counts + departure/arrival on-time tallies from the operational twin. REUSE `scorePlan`/`utilizationScore`/`rehandleScore` and `readExceptionKpi`/`readOpenExceptions` — never re-derive scoring. Define `KpiSnapshot` here and ensure it matches Plan 05-01's envelope shape (single source — import the envelope type if 05-01 has landed, else define the identical shape and note the alignment). Deterministic: fixed rounding, sorted iteration. Write failing tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/api test -- compute-kpis</automated>
  </verify>
  <done>`computeKpis` returns the full operational KPI set from reused scoring + exception counts; deterministic; shape matches the envelope KpiSnapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed-deterministic baseline-vs-optimizer comparison + deltas</name>
  <files>packages/api/src/kpis/comparison.ts, packages/api/src/kpis/comparison.test.ts</files>
  <behavior>
    - Test (KEYSTONE-adjacent): on the calibrated demo seed, the optimizer KPIs beat the baseline on rehandle AND SLA (rehandleCount lower, slaViolationRate lower) — proving the win is real, not theater.
    - Test (seed-determinism): two runs with the same seed produce byte-identical `KpiComparison` (baseline, optimizer, deltas).
    - Test: deltas are computed per metric as `optimizer - baseline` (and a sign/labeling that the UI renders).
    - Test: baseline and optimizer run on the SAME inputs through the SAME scoring gate (honest comparison, P8/P2).
  </behavior>
  <action>Implement `computeComparison(args): KpiComparison` that runs `baselinePlan` and `planLoad` over the SAME aggregated blocks/route/config, scores both through the shared gate, computes a `KpiSnapshot` for each, and returns `{ baseline, optimizer, deltas }`. Source the inputs from a seeded stream slice (deterministic). Note: the demo seed must be calibrated so scenarios are hard enough that LIFO sometimes can't win without over-carry/hold/reassign (ROADMAP P8 note) — if the default seed does not yet show a win, document the calibrated seed in the SUMMARY and use it. Write the failing keystone + determinism tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/api test -- comparison</automated>
  </verify>
  <done>The comparison is seed-deterministic and the optimizer demonstrably beats the baseline on rehandle + SLA on the calibrated seed; deltas computed per metric.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register GET /kpis + GET /kpis/comparison in the composition root</name>
  <files>packages/api/src/routes/kpis.ts, packages/api/src/routes/kpis.test.ts, packages/api/src/server.ts</files>
  <behavior>
    - Test: `GET /kpis` returns a 200 with the full KpiSnapshot (incl. `baseline`).
    - Test: `GET /kpis/comparison` returns a 200 with `{ baseline, optimizer, deltas }`.
    - Test: both routes are read-only (no event-store writes — verified via inject).
  </behavior>
  <action>Implement `registerKpiRoutes(app, db)` with two thin read handlers calling `computeKpis`/`computeComparison`. Register it in `server.ts` alongside the other routes. Keep handlers thin (validate, compute, map to DTO). Write failing inject tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/api test -- routes/kpis</automated>
  </verify>
  <done>Both KPI routes are registered and return correct shapes; read-only; `tsc -b` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client→KPI routes | Untrusted read; must not write or leak beyond demo metrics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | KPI comparison | mitigate | Baseline + optimizer run on the SAME inputs through the SAME scoring gate (P8/P2) — the comparison cannot be gamed by scoring one plan differently. |
| T-05-06 | Information disclosure | KPI routes | accept | KPIs are aggregate operational metrics for the demo audience; no PII. |
</threat_model>

<verification>
- `pnpm --filter @mm/api test` green (compute-kpis, comparison, routes/kpis).
- `pnpm build` (turbo) clean.
- Determinism gate: the comparison test runs twice on one seed and asserts byte-identical output.
</verification>

<success_criteria>
`GET /api/kpis` returns the live operational KPI set and `GET /api/kpis/comparison` returns a seed-deterministic baseline-vs-optimizer comparison with deltas, on the same seeded stream, with the optimizer demonstrably winning on rehandle + SLA. The KpiSnapshot shape matches the ws envelope.
</success_criteria>

<output>
After completion, create `.planning/phases/05-simulation-visualization-wrapper/05-03-SUMMARY.md`
</output>
