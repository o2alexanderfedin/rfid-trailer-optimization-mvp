---
phase: 05-simulation-visualization-wrapper
plan: 06
type: tdd
wave: 3
depends_on: [05-01]
files_modified:
  - packages/web/src/map/wsClient.ts
  - packages/web/src/map/wsClient.test.ts
  - packages/web/src/map/simClock.ts
  - packages/web/src/map/simClock.test.ts
  - packages/web/src/map/animate.ts
  - packages/web/src/map/animate.test.ts
  - packages/web/src/map/coloring.ts
  - packages/web/src/map/coloring.test.ts
  - packages/web/src/map/layers.ts
  - packages/web/src/map/MapView.tsx
  - packages/web/src/map/Legend.tsx
  - packages/web/test/leak.e2e.ts
  - packages/web/test/soak.e2e.ts
  - packages/web/playwright.config.ts
autonomous: true
requirements: [VIZ-02, VIZ-03]
must_haves:
  truths:
    - "Trailers animate smoothly along their route LineString, interpolated client-side from server keyframes, driven by the OL postrender loop + a sim clock (not a bare rAF)."
    - "A new snapshot mid-tween re-anchors keyframes without snapping/teleporting (resync-safe)."
    - "Hubs and routes are colored by metric bucket via a pre-allocated STYLE_CACHE (zero per-frame allocation) with a matching legend."
    - "Heap stays flat (returns to ~baseline after forced GC) over a multi-minute animated run — KEYSTONE (a)."
  artifacts:
    - path: "packages/web/src/map/animate.ts"
      provides: "postrender tween: fraction = clamp((simNow-departMs)/(etaMs-departMs),0,1) → LineString.getCoordinateAt → in-place setCoordinates"
      exports: ["attachTrailerAnimation", "fractionFor"]
    - path: "packages/web/src/map/coloring.ts"
      provides: "STYLE_CACHE keyed by integer bucket + zero-alloc StyleFunctions + COLORS/LABELS single source"
      exports: ["hubStyle", "routeStyle", "HUB_COLORS", "HUB_BUCKET_LABELS"]
    - path: "packages/web/src/map/wsClient.ts"
      provides: "ws client parsing the versioned snapshot/tick envelope, applying upsert/delete deltas off the React render path"
      exports: ["useWsEnvelope"]
    - path: "packages/web/test/soak.e2e.ts"
      provides: "KEYSTONE (a): multi-minute headed flat-memory soak"
  key_links:
    - from: "packages/web/src/map/animate.ts"
      to: "ol postrender + LineString.getCoordinateAt"
      via: "one postrender listener tweens all trailers in place along the route"
      pattern: "postrender|getCoordinateAt"
    - from: "packages/web/src/map/coloring.ts"
      to: "feature.set(bucket) + STYLE_CACHE"
      via: "StyleFunction returns cached refs; updates via feature.set, never source rebuild"
      pattern: "STYLE_CACHE|feature.set"
    - from: "packages/web/src/map/wsClient.ts"
      to: "@mm/api envelope types"
      via: "parses the versioned WsEnvelope union"
      pattern: "snapshot|tick|WsEnvelope"
---

<objective>
Deliver VIZ-02 (smooth client-side trailer animation) and VIZ-03 (state-driven coloring) on the existing Phase-1 map, consuming the Plan 05-01 ws envelope — and prove the leak/perf discipline with KEYSTONE (a): a multi-minute headed flat-memory soak.

The current map TELEPORTS trailers to the latest keyframe each snapshot. This plan adds: (a) client-side tweening along the route LineString driven by the OL `postrender` loop + a sim clock (per 05-RESEARCH.md Q2 — NOT a bare rAF), resync-safe (re-anchor keyframes, never snap to a vertex mid-leg); (b) data-driven coloring via a pre-allocated `STYLE_CACHE` keyed by integer bucket with a zero-allocation `StyleFunction` + a legend from the same COLORS/LABELS arrays (Q4); (c) a new ws client that parses the versioned `snapshot`/`tick` envelope and applies upsert/delete deltas off the React render path. ALL Phase-1 leak invariants are preserved (single source, in-place mutation, map-once-in-ref, strict disposal incl. new overlays), verified by extending the leak guard AND a new multi-minute soak (Q5).

Per 05-RESEARCH.md Tactic A: keep real `ol/Feature`s, mutate geometry in place per frame (smallest delta from shipped code; free hit-testing for VIZ-05/UI-02 in Plan 05-07). Apply the `frontend-design` skill for a clean, legible legend/controls.

Purpose: this is the animated centerpiece — smooth motion + meaningful color over a multi-minute run without leaks is the demo's first impression.
Output: the ws client, sim clock, animation loop, coloring + legend, extended MapView, and the soak keystone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-simulation-visualization-wrapper/05-RESEARCH.md
@packages/web/src/map/MapView.tsx
@packages/web/src/map/layers.ts
@packages/web/src/map/useTrailerSnapshots.ts
@packages/web/test/leak.e2e.ts
@.planning/phases/05-simulation-visualization-wrapper/05-01-SUMMARY.md

<interfaces>
<!-- The ws envelope to consume (Plan 05-01, exported from @mm/api) -->
WsEnvelope = { v:1; type:"snapshot"; seq; simMs; payload: SnapshotPayload }
           | { v:1; type:"tick";     seq; simMs; payload: TickPayload }
TrailerKeyframe = { id, routeId, departMs, etaMs, state:"onTime"|"slaRisk"|"late"|"idle", util? }
HubState = { id, volumeBucket, slaRiskBucket, congestionBucket }   // integer buckets
RouteState = { id, loadBucket, slaRiskBucket }
TickPayload = { trailers?, trailersGone?, hubs?, routes?, ... }

<!-- Existing leak-guarded layers to EXTEND (packages/web/src/map/layers.ts) -->
createTrailerLayer(): { layer, source }   // single source, ONE shared Style
updateTrailerFeatures(source, trailers)   // in-place setCoordinates upsert by id
// Phase-1 invariants: single source, in-place mutation, map-once-in-ref, dispose on teardown

<!-- OL animation API (05-RESEARCH.md Q2 — verified) -->
trailerLayer.on("postrender", (event) => { const simNow = simClock.fromFrameTime(event.frameState.time); ... })
LineString.getCoordinateAt(fraction): coordinate   // 0=start, 1=end
LineString.getLength()   // cache per route; recompute only on route change
map.render()             // schedule next frame

<!-- Leak/soak setup (05-RESEARCH.md Q5) -->
Chromium launch args: --enable-precise-memory-info, --js-flags=--expose-gc
performance.memory.usedJSHeapSize   // assert flat after globalThis.gc()
</interfaces>

Apply the `frontend-design` skill (spacing, alignment, contrast) for the legend + any metric toggle.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ws envelope client + sim clock (RED→GREEN)</name>
  <files>packages/web/src/map/wsClient.ts, packages/web/src/map/wsClient.test.ts, packages/web/src/map/simClock.ts, packages/web/src/map/simClock.test.ts</files>
  <behavior>
    - Test: `useWsEnvelope` parses a `snapshot` envelope into full local state and a `tick` envelope into upsert/delete deltas (trailers/hubs/routes/exceptions), keyed by id.
    - Test: a `seq` gap is detected (drop detection) and surfaces a resync signal.
    - Test (clock): `simClock.fromFrameTime(frameTime)` maps the OL frame timestamp to sim-time ms; resync to `simMs` nudges (clamped correction), never lurches.
    - Test: the handler is held in a ref so a changing closure never reopens the socket (carried Phase-1 discipline).
  </behavior>
  <action>Create `wsClient.ts` (replacing/extending `useTrailerSnapshots`) that parses the versioned `WsEnvelope` union (import the wire types from `@mm/api`), maintains local entity maps, and applies upsert/delete deltas — all OFF the React render path (no React state for per-frame data). Create `simClock.ts`: a monotonic wall→sim mapping resynced on each envelope's `simMs` with a clamped smoothing nudge (Q2). Keep the single-socket + handler-in-ref discipline. Write failing tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/web test -- wsClient simClock</automated>
  </verify>
  <done>The ws client parses snapshot+tick envelopes with seq-gap detection; the sim clock resyncs smoothly; socket discipline preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: postrender tween + STYLE_CACHE coloring + legend</name>
  <files>packages/web/src/map/animate.ts, packages/web/src/map/animate.test.ts, packages/web/src/map/coloring.ts, packages/web/src/map/coloring.test.ts, packages/web/src/map/layers.ts, packages/web/src/map/Legend.tsx, packages/web/src/map/MapView.tsx</files>
  <behavior>
    - Test (animate): `fractionFor(t, simNow)` = clamp((simNow-departMs)/(etaMs-departMs), 0, 1); 0 before depart, 1 after eta, no extrapolation past the leg.
    - Test (animate): one postrender listener positions ALL trailers via `getCoordinateAt(fraction)` along the route — in-place `setCoordinates`, never new Feature/Point/Style in the loop.
    - Test (resync): a snapshot mid-tween re-anchors departMs/etaMs/routeGeom in place; position stays continuous (no snap to vertex); a same-leg confirming snapshot produces zero visual change.
    - Test (coloring): `hubStyle(feature)` returns the SAME cached Style ref for a given bucket every call (zero per-frame allocation); an out-of-range/missing bucket returns the default style.
    - Test (coloring): `feature.set("volumeBucket", b)` re-invokes the StyleFunction; the source is never rebuilt.
    - Test (legend): the legend rows derive from the same COLORS/LABELS arrays as the cache (single source of truth).
  </behavior>
  <action>Create `animate.ts` with `fractionFor` + `attachTrailerAnimation(layer, map, simClock, trailers)` implementing the Q2 canonical pattern: ONE `postrender` listener, sim-clock fraction clamped [0,1], `LineString.getCoordinateAt` along the shared route geom (cache `getLength()` per route), in-place `setCoordinates`, `map.render()` to schedule the next frame; resync re-anchors keyframes in place (Q2 — bridge a leg change via `getClosestPoint`). Create `coloring.ts` with `HUB_COLORS`/`HUB_BUCKET_LABELS` + pre-allocated `STYLE_CACHE: Style[]` + zero-alloc `hubStyle`/`routeStyle` StyleFunctions (Q4). Wire hub/route layers to the StyleFunctions in `layers.ts`; apply bucket deltas via `feature.set` (never source rebuild). Add `Legend.tsx` from the shared arrays (frontend-design: spacing/alignment/contrast). Extend `MapView.tsx` to consume the ws client (Task 1), attach the animation, apply coloring deltas, and add overlays' disposal to teardown. Write failing tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/web test -- animate coloring</automated>
  </verify>
  <done>Trailers tween along the route resync-safe; coloring uses a zero-alloc STYLE_CACHE with a matching legend; updates are in-place; no per-frame allocation in the render path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The animated, colored live map: trailers tween smoothly along routes from the ws envelope, hubs/routes colored by metric bucket with a legend, all on the Phase-1 leak-guarded map.</what-built>
  <how-to-verify>
    1. Run the API (`pnpm --filter @mm/api dev`) and web (`pnpm --filter @mm/web dev`); open the printed localhost URL.
    2. Confirm trailers move SMOOTHLY along the route LineStrings (no teleport/jitter), and motion stays locked to the basemap while you pan/zoom.
    3. Confirm hubs/routes are colored by state and the legend matches the colors.
    4. Leave it running ~2 minutes; motion stays smooth, no visible stutter/leak.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (teleport, jitter, wrong colors, stutter).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: KEYSTONE (a) — multi-minute flat-memory soak + extended leak guard</name>
  <files>packages/web/test/soak.e2e.ts, packages/web/test/leak.e2e.ts, packages/web/playwright.config.ts</files>
  <behavior>
    - KEYSTONE soak: stream `tick` envelopes (~every 250ms) with moving trailers + churning hub/route buckets + occasional exceptions for ~2.5 min; force GC; assert `usedJSHeapSize` after ≈ baseline (bounded growth, NO monotonic climb).
    - Soak: structural invariants still hold after the run — `data-map-instances==1`, `data-trailer-source-instances==1`, `data-map-net-live==1`, `data-trailer-count==fleet size`.
    - Leak guard: extend the existing leak.e2e to the new envelope shape (snapshot+tick) and assert one postrender listener + overlay disposal on teardown.
  </behavior>
  <action>Add `soak.e2e.ts` implementing the Q5 harness: launch Chromium with `--enable-precise-memory-info --js-flags=--expose-gc` (in `playwright.config.ts`), stub `/api/hubs`/`/api/routes`, push envelope `tick`s via `routeWebSocket`, warm up, GC, baseline, run ~150s, GC, assert `after - before < before * 0.25` and the structural invariants. Update `leak.e2e.ts` to the new envelope. Keep the soak a separate (slow) suite — a short ~30s flat-heap smoke can run per-PR, the full soak on-demand/nightly (05-RESEARCH.md open Q6). Write the failing soak first (it will fail if any per-frame allocation leaks), then ensure Tasks 1–2 keep it green.</action>
  <verify>
    <automated>pnpm --filter @mm/web test:e2e -- soak leak</automated>
  </verify>
  <done>The multi-minute soak proves flat heap after GC + bounded feature count + single map/source; the leak guard passes on the new envelope. KEYSTONE (a) green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ws server→browser | The browser renders untrusted-but-read-only server state; the render path must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-24 | Denial of service | OL render path | mitigate | In-place geometry mutation + single source + zero per-frame allocation + one postrender listener + strict disposal (incl. overlays); proven by the flat-memory soak (P10). |
| T-05-13 | Tampering | envelope parsing | mitigate | The client narrows the versioned union defensively (read-side guard); a malformed/unknown `v`/type is ignored, not applied. |
| T-05-14 | Denial of service | seq-gap resync | mitigate | A `seq` gap requests a fresh snapshot (bounded recovery) rather than dead-reckoning off-route. |
</threat_model>

<verification>
- `pnpm --filter @mm/web test` green (wsClient, simClock, animate, coloring).
- `pnpm --filter @mm/web test:e2e` green (soak + leak).
- Grep gate (P10): `grep -nE "new (Feature|Point|Style|LineString)" packages/web/src/map/animate.ts packages/web/src/map/coloring.ts | grep -v '^#'` returns ZERO inside any postrender/render handler.
- `pnpm build` (turbo) clean.
</verification>

<success_criteria>
Trailers animate smoothly along their routes from the ws envelope, resync-safe; hubs/routes are colored by metric bucket with a matching legend; the render path allocates nothing per frame; and a multi-minute headed soak proves flat memory after forced GC with all Phase-1 leak invariants intact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-simulation-visualization-wrapper/05-06-SUMMARY.md`
</output>
