---
phase: 05-simulation-visualization-wrapper
plan: 08
type: tdd
wave: 5
depends_on: [05-01, 05-03, 05-06, 05-07]
files_modified:
  - packages/web/src/api/client.ts
  - packages/web/src/panels/KpiDashboard.tsx
  - packages/web/src/panels/KpiDashboard.test.tsx
  - packages/web/src/panels/MoneySlide.tsx
  - packages/web/src/panels/MoneySlide.test.tsx
  - packages/web/src/panels/RightRail.tsx
  - packages/web/src/App.tsx
  - packages/web/src/index.css
  - packages/web/test/money-slide.e2e.ts
autonomous: false
requirements: [UI-03, UI-04]
must_haves:
  truths:
    - "A KPI dashboard displays operational KPIs: utilization, rehandle count/minutes, wrong-trailer count, missed-unload count, SLA-violation rate, on-time departure/arrival — updating from KPI deltas in the tick envelope."
    - "A before/after money-slide view shows baseline-vs-optimizer KPI deltas on the SAME seeded stream."
    - "The KPI numbers visibly move on change (animated deltas), not static labels."
    - "The money slide is seed-deterministic and shows the optimizer beating the baseline on rehandle + SLA — KEYSTONE (b)."
  artifacts:
    - path: "packages/web/src/panels/KpiDashboard.tsx"
      provides: "UI-03 live KPI dashboard from GET /api/kpis + tick kpis partials, with animated deltas"
      exports: ["KpiDashboard"]
    - path: "packages/web/src/panels/MoneySlide.tsx"
      provides: "UI-04 before/after baseline-vs-optimizer view from GET /api/kpis/comparison"
      exports: ["MoneySlide"]
    - path: "packages/web/test/money-slide.e2e.ts"
      provides: "KEYSTONE (b): seed-deterministic KPI comparison proving optimizer beats baseline, rendered"
  key_links:
    - from: "packages/web/src/panels/KpiDashboard.tsx"
      to: "GET /api/kpis + tick.kpis partials"
      via: "initial fetch + live KPI delta application (animated)"
      pattern: "kpis"
    - from: "packages/web/src/panels/MoneySlide.tsx"
      to: "GET /api/kpis/comparison"
      via: "renders baseline vs optimizer + deltas"
      pattern: "comparison|baseline|optimizer|delta"
---

<objective>
Deliver UI-03 (KPI dashboard) and UI-04 (the before/after "money slide") — the highest-leverage differentiator, the slide that proves the optimizer's value. These consume the Plan 05-03 endpoints (`GET /api/kpis`, `GET /api/kpis/comparison`) and the Plan 05-01 tick `kpis` partials. This plan includes KEYSTONE (b): a seed-deterministic KPI comparison proving the optimizer beats the baseline, rendered.

Per 05-RESEARCH.md cross-cutting + ROADMAP P8: the baseline planner already shares KPI plumbing (Plan 05-03), so this is WIRING — render `KpiSnapshot` fields as React components, animate KPI deltas on change ("numbers must visibly move"), and render the baseline-vs-optimizer comparison + per-metric deltas. A toggle switches the right-rail / a dedicated view between the live dashboard and the money slide (05-CONTEXT layout).

This plan has a human-verify checkpoint (the money slide is the demo's visual payoff), so `autonomous: false`. Apply the `frontend-design` skill (clean comparison layout, clear win indicators, readable deltas).

Purpose: the money slide is the demo's closing argument — it must be reproducible (seed-frozen), honest (same inputs/scoring), and legible (deltas that move and clearly show the optimizer winning).
Output: the KPI dashboard, the money slide, the right-rail toggle, and the keystone comparison e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-simulation-visualization-wrapper/05-RESEARCH.md
@packages/web/src/api/client.ts
@.planning/phases/05-simulation-visualization-wrapper/05-01-SUMMARY.md
@.planning/phases/05-simulation-visualization-wrapper/05-03-SUMMARY.md
@.planning/phases/05-simulation-visualization-wrapper/05-06-SUMMARY.md

<interfaces>
<!-- KPI endpoints (Plan 05-03) -->
GET /api/kpis            -> KpiSnapshot
GET /api/kpis/comparison -> { baseline: KpiSnapshot', optimizer: KpiSnapshot', deltas: {...} }
KpiSnapshot = { utilization, rehandleCount, rehandleMinutes, wrongTrailerCount, missedUnloadCount,
                slaViolationRate, onTimeDeparture, onTimeArrival, baseline: Omit<KpiSnapshot,"baseline"> }

<!-- tick KPI partials (Plan 05-01) -->
TickPayload.kpis?: Partial<KpiSnapshot>   // only changed KPI fields

<!-- ws client (Plan 05-06) -->
useWsEnvelope(...)   // exposes tick.kpis partials to React (off the map render path)
</interfaces>

Apply the `frontend-design` skill: KPI cards, animated delta numbers, a clear before/after comparison with win indicators, spacing/alignment/contrast.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KpiDashboard (UI-03) with live animated deltas</name>
  <files>packages/web/src/panels/KpiDashboard.tsx, packages/web/src/panels/KpiDashboard.test.tsx, packages/web/src/api/client.ts</files>
  <behavior>
    - Test: KpiDashboard renders all operational KPIs (utilization, rehandleCount, rehandleMinutes, wrongTrailerCount, missedUnloadCount, slaViolationRate, onTimeDeparture, onTimeArrival) from an initial `GET /api/kpis`.
    - Test: a tick `kpis` partial updates only the changed fields (live).
    - Test: a changed number triggers a visible delta animation (assert the animation state/class toggles, not a static render).
    - Test: values are formatted (counts as integers, rates as percentages) deterministically.
  </behavior>
  <action>Create `KpiDashboard.tsx` that fetches `GET /api/kpis` on mount (add `fetchKpis` to `client.ts`) and applies tick `kpis` partials from the ws client (off the map render path). Render KPI cards (frontend-design) and animate value changes ("numbers must visibly move" — a small count-up/flash on change). Write failing tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/web test -- KpiDashboard</automated>
  </verify>
  <done>The KPI dashboard renders all operational KPIs, updates live from tick partials, and visibly animates changed numbers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MoneySlide (UI-04) before/after + right-rail toggle</name>
  <files>packages/web/src/panels/MoneySlide.tsx, packages/web/src/panels/MoneySlide.test.tsx, packages/web/src/panels/RightRail.tsx, packages/web/src/App.tsx, packages/web/src/index.css, packages/web/src/api/client.ts</files>
  <behavior>
    - Test: MoneySlide fetches `GET /api/kpis/comparison` and renders baseline vs optimizer side-by-side with per-metric deltas.
    - Test: the optimizer's wins (lower rehandle, lower SLA-violation) are clearly indicated (a win class/marker), losses too — honest.
    - Test: a toggle switches between the live KPI dashboard and the money-slide view.
    - Test: deltas are formatted + signed consistently.
  </behavior>
  <action>Create `MoneySlide.tsx` (fetch `GET /api/kpis/comparison` via a new `fetchKpiComparison` helper) rendering a clean before/after comparison with explicit win indicators (frontend-design). Add a toggle in `RightRail.tsx`/`App.tsx` switching the dashboard ↔ money slide. Style in `index.css`. Write failing tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/web test -- MoneySlide</automated>
  </verify>
  <done>The money slide renders baseline-vs-optimizer with clear deltas + win indicators; a toggle switches dashboard/money-slide views.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: KEYSTONE (b) — seed-deterministic comparison proves optimizer beats baseline (rendered)</name>
  <files>packages/web/test/money-slide.e2e.ts</files>
  <behavior>
    - KEYSTONE e2e: with `GET /api/kpis/comparison` served from the real seeded backend (or a fixture captured from it on the calibrated seed), the rendered money slide shows the optimizer beating the baseline on rehandleCount AND slaViolationRate.
    - Test: loading the money slide twice on the same seed renders identical numbers (seed-deterministic — the win is reproducible, not theater).
    - Test: the win indicators are present for the metrics the optimizer wins.
  </behavior>
  <action>Add `money-slide.e2e.ts` (Playwright) that drives the rendered money slide against the seeded comparison endpoint (real backend on the calibrated seed from Plan 05-03, or a captured deterministic fixture). Assert the optimizer beats the baseline on rehandle + SLA and that two loads on the same seed render identical numbers. This is the visible end of KEYSTONE (b) (the deterministic backend half lives in Plan 05-03's comparison test). Write the failing keystone first, then ensure Tasks 1–2 + Plan 05-03 keep it green.</action>
  <verify>
    <automated>pnpm --filter @mm/web test:e2e -- money-slide</automated>
  </verify>
  <done>The rendered money slide demonstrably shows the optimizer beating the baseline on rehandle + SLA, seed-deterministically. KEYSTONE (b) green end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The KPI dashboard (live operational KPIs with animated deltas) and the before/after money slide (baseline vs optimizer on the same seeded stream), with a toggle between them.</what-built>
  <how-to-verify>
    1. Run API + web (dev) on the calibrated demo seed; open the localhost URL.
    2. Confirm the KPI dashboard shows all operational KPIs and the numbers visibly MOVE as the sim advances (not static).
    3. Toggle to the money slide; confirm baseline vs optimizer side-by-side with clear deltas and that the optimizer demonstrably wins on rehandle + SLA.
    4. Reload; confirm the money-slide numbers are identical (seed-deterministic).
    5. Confirm the comparison layout reads clearly (win indicators, spacing/alignment/contrast).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (static numbers, unclear deltas, non-deterministic, layout).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→KPI/money-slide panels | Panels render untrusted-but-read-only KPI responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | money-slide comparison | mitigate | Comparison numbers come from the backend's same-inputs/same-scoring computation (Plan 05-03, P8/P2); the UI only renders them — it cannot reweight a plan to fake a win. |
| T-05-19 | Information disclosure | KPI panels | accept | KPIs are aggregate demo metrics; no PII. |
</threat_model>

<verification>
- `pnpm --filter @mm/web test` green (KpiDashboard, MoneySlide).
- `pnpm --filter @mm/web test:e2e -- money-slide` green (seed-deterministic optimizer-beats-baseline).
- `pnpm build` (turbo) clean.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
The KPI dashboard shows live operational KPIs with visibly moving numbers, and the money slide shows a seed-deterministic baseline-vs-optimizer comparison in which the optimizer demonstrably beats the baseline on rehandle + SLA — the persuasive close of the demo.
</success_criteria>

<output>
After completion, create `.planning/phases/05-simulation-visualization-wrapper/05-08-SUMMARY.md`
</output>
