---
phase: 05-simulation-visualization-wrapper
plan: 07
type: tdd
wave: 4
depends_on: [05-01, 05-04, 05-06]
files_modified:
  - packages/web/src/api/client.ts
  - packages/web/src/panels/AlertFeed.tsx
  - packages/web/src/panels/AlertFeed.test.tsx
  - packages/web/src/panels/AuditTimeline.tsx
  - packages/web/src/panels/AuditTimeline.test.tsx
  - packages/web/src/panels/TrailerDetail.tsx
  - packages/web/src/panels/TrailerDetail.test.tsx
  - packages/web/src/panels/RightRail.tsx
  - packages/web/src/map/MapView.tsx
  - packages/web/src/App.tsx
  - packages/web/src/index.css
  - packages/web/test/panels.e2e.ts
autonomous: false
requirements: [UI-01, UI-02, VIZ-05]
must_haves:
  truths:
    - "An alert feed surfaces every exception (wrong-trailer, missed-unload, blocked-freight, low-utilization) with severity, human-readable reason, and recommended action, updating in realtime from the tick envelope."
    - "Clicking a trailer on the map opens a detail panel showing its rear→nose load order, loading instructions, and the plan's plain-English explanation."
    - "A read-only audit timeline shows a package's OR trailer's full event history including the captured system recommendation at each decision."
  artifacts:
    - path: "packages/web/src/panels/AlertFeed.tsx"
      provides: "Realtime exception feed (severity/reason/recommended-action) from exceptionsNew/exceptionsResolved deltas"
      exports: ["AlertFeed"]
    - path: "packages/web/src/panels/TrailerDetail.tsx"
      provides: "VIZ-05 selected-trailer plan detail (rear→nose + instructions + explanation)"
      exports: ["TrailerDetail"]
    - path: "packages/web/src/panels/AuditTimeline.tsx"
      provides: "UI-02 read-only audit timeline for package OR trailer incl. captured recommendation"
      exports: ["AuditTimeline"]
  key_links:
    - from: "packages/web/src/map/MapView.tsx"
      to: "packages/web/src/panels/TrailerDetail.tsx"
      via: "forEachFeatureAtPixel hit-test → selected trailerId → fetch GET /trailers/:id/plan"
      pattern: "forEachFeatureAtPixel|trailers/.*plan"
    - from: "packages/web/src/panels/AlertFeed.tsx"
      to: "tick envelope exceptionsNew/exceptionsResolved"
      via: "appends/removes feed entries off the map render path (normal React state)"
      pattern: "exceptionsNew|exceptionsResolved"
    - from: "packages/web/src/panels/AuditTimeline.tsx"
      to: "GET /trailers/:id/history + GET /packages/:id/history"
      via: "fetches the ordered timeline incl. recommendation"
      pattern: "history"
---

<objective>
Deliver the operator right-rail panels: UI-01 (realtime alert feed), UI-02 (read-only audit timeline for package OR trailer with the captured recommendation), and VIZ-05 (click a trailer → plan detail panel). These read the Plan 05-01 tick envelope (exceptions) and the Plan 05-04 endpoints (`GET /trailers/:id/plan`, `GET /trailers/:id/history`, `GET /packages/:id/history`).

Per 05-RESEARCH.md cross-cutting: the exception feed is driven by `exceptionsNew`/`exceptionsResolved` in the `tick` — appended to a React list OFF the map render path (normal React state, no leak risk). VIZ-05 uses Tactic A's real features for `forEachFeatureAtPixel` hit-testing; on click, fetch the trailer's plan and show it in a side panel (and/or an `ol/Overlay` popup — dispose any overlay on teardown, Q5 item 6). UI-02's timeline includes the captured system recommendation (persisted by Plan 05-04).

Layout (05-CONTEXT): map centerpiece + right-rail panels (alert feed, selected-trailer detail) with a clean, legible operator aesthetic. Apply the `frontend-design` skill (spacing, alignment, contrast, severity color coding).

This plan has a human-verify checkpoint (interactive click-through + realtime feed are visual), so `autonomous: false`.

Purpose: these panels turn raw exceptions and plans into an operator-legible feed + click-through detail + auditable history.
Output: the three panels, the right-rail container, the map click wiring, and a panels e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-simulation-visualization-wrapper/05-RESEARCH.md
@packages/web/src/api/client.ts
@packages/web/src/App.tsx
@.planning/phases/05-simulation-visualization-wrapper/05-01-SUMMARY.md
@.planning/phases/05-simulation-visualization-wrapper/05-04-SUMMARY.md
@.planning/phases/05-simulation-visualization-wrapper/05-06-SUMMARY.md

<interfaces>
<!-- tick envelope exceptions (Plan 05-01) -->
ExceptionItem = { id, kind:"wrongTrailer"|"missedUnload"|"blockedFreight"|"lowUtilization", severity:"low"|"med"|"high", entityId, reason, recommendedAction, simMs }
TickPayload.exceptionsNew?: ExceptionItem[]
TickPayload.exceptionsResolved?: string[]   // ids cleared

<!-- Plan-detail + history endpoints (Plan 05-04) -->
GET /api/trailers/:id/plan    -> { rearToNose: [...], instructions: LoadingInstructions, explanation: string }
GET /api/trailers/:id/history -> AuditEntry[]  (incl. captured recommendation field)
GET /api/packages/:id/history -> AuditEntry[]

<!-- Map hit-testing (Tactic A — real features exist) -->
map.forEachFeatureAtPixel(pixel, fn)   // returns the trailer feature under the click
feature.get("trailerId")               // set in layers.ts updateTrailerFeatures

<!-- ws client to subscribe to (Plan 05-06) -->
useWsEnvelope(...)   // exposes the latest tick deltas to React panels (off the map render path)
</interfaces>

Apply the `frontend-design` skill: right-rail layout, severity color coding, readable timeline, spacing/alignment/contrast.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AlertFeed (UI-01) + typed fetch helpers</name>
  <files>packages/web/src/panels/AlertFeed.tsx, packages/web/src/panels/AlertFeed.test.tsx, packages/web/src/api/client.ts</files>
  <behavior>
    - Test: AlertFeed renders each exception with kind, severity, human-readable reason, and recommended action.
    - Test: an `exceptionsNew` delta appends entries; an `exceptionsResolved` id removes its entry (realtime).
    - Test: severity is visually coded (a class/attribute per low/med/high) and the feed is ordered (newest first, stable tie-break by id).
    - Test: an empty feed renders a clear empty state (not a crash).
  </behavior>
  <action>Create `AlertFeed.tsx` consuming exception deltas from the ws client as normal React state (OFF the map render path). Render severity-coded rows (frontend-design: color/contrast). Add typed fetch helpers to `client.ts` for the new endpoints (`fetchTrailerPlan`, `fetchTrailerHistory`, `fetchPackageHistory`). Write failing component tests (Vitest + React Testing Library, or the project's component test setup) first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/web test -- AlertFeed</automated>
  </verify>
  <done>The alert feed renders + updates from exception deltas with severity coding, ordering, and an empty state; fetch helpers typed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TrailerDetail (VIZ-05) + AuditTimeline (UI-02) + map click wiring</name>
  <files>packages/web/src/panels/TrailerDetail.tsx, packages/web/src/panels/TrailerDetail.test.tsx, packages/web/src/panels/AuditTimeline.tsx, packages/web/src/panels/AuditTimeline.test.tsx, packages/web/src/panels/RightRail.tsx, packages/web/src/map/MapView.tsx, packages/web/src/App.tsx, packages/web/src/index.css</files>
  <behavior>
    - Test (TrailerDetail): given a fetched plan, renders rear→nose load order, instructions by zone, and the plain-English explanation.
    - Test (TrailerDetail): an unselected/unknown trailer renders a clear prompt/empty state.
    - Test (AuditTimeline): renders an ordered timeline for a package OR trailer, including the captured recommendation on decision entries.
    - Test (click wiring): a map click hit-tests a trailer feature → sets the selected trailerId → the detail panel fetches `GET /trailers/:id/plan` (mocked).
    - Test (teardown): any `ol/Overlay` added for selection is removed on unmount (Q5 item 6).
  </behavior>
  <action>Create `TrailerDetail.tsx` (fetches + renders plan detail), `AuditTimeline.tsx` (fetches + renders package/trailer history incl. recommendation), and `RightRail.tsx` composing AlertFeed + TrailerDetail (+ a timeline view). Wire `MapView.tsx`: on map click use `forEachFeatureAtPixel` to get the trailer feature, set selected trailerId (React state), and let the detail panel fetch on change — keep this off the per-frame render path. If a selection overlay is used, dispose it on teardown. Update `App.tsx` to the map-centerpiece + right-rail layout; style in `index.css` (frontend-design). Write failing tests first, then green.</action>
  <verify>
    <automated>pnpm --filter @mm/web test -- TrailerDetail AuditTimeline</automated>
  </verify>
  <done>Clicking a trailer opens the plan-detail panel (rear→nose + instructions + explanation); the audit timeline renders package/trailer history with captured recommendations; overlays disposed on teardown.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The operator right-rail: a realtime alert feed (severity/reason/action), a click-a-trailer plan detail panel (rear→nose + instructions + why), and a read-only audit timeline (package/trailer incl. captured recommendation), beside the animated map.</what-built>
  <how-to-verify>
    1. Run API + web (dev); open the localhost URL.
    2. Watch the alert feed populate in realtime as exceptions fire (wrong-trailer / missed-unload); confirm each shows severity + a readable reason + a recommended action.
    3. Click a moving trailer on the map; confirm the detail panel shows its rear→nose load order, loading instructions, and a plain-English explanation.
    4. Open the audit timeline for a package and a trailer; confirm ordered history and that decision entries show the captured system recommendation.
    5. Confirm the layout is clean and legible (spacing/alignment/contrast).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (feed not updating, click not selecting, missing recommendation, layout).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→browser panels | Panels render untrusted-but-read-only API responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Denial of service | alert feed growth | mitigate | The feed is bounded (cap retained entries / window) and lives in React state OFF the map render path, so it cannot regress the P10 map-leak invariants. |
| T-05-16 | Cross-site scripting | reason/recommendedAction/explanation text | mitigate | Render server text via React's default escaping (no `dangerouslySetInnerHTML`); the text is plain-English from the server, never HTML. |
| T-05-17 | Denial of service | selection overlay | mitigate | Any `ol/Overlay` is removed on teardown (Q5 item 6) so click-to-inspect does not leak overlays. |
</threat_model>

<verification>
- `pnpm --filter @mm/web test` green (AlertFeed, TrailerDetail, AuditTimeline).
- `pnpm --filter @mm/web test:e2e -- panels` green (click-through + realtime feed).
- `pnpm build` (turbo) clean.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
The right-rail surfaces every exception in realtime with severity/reason/action, clicking a trailer opens its rear→nose plan detail with instructions + explanation, and the audit timeline shows package/trailer history including the captured system recommendation — in a clean, legible operator layout.
</success_criteria>

<output>
After completion, create `.planning/phases/05-simulation-visualization-wrapper/05-07-SUMMARY.md`
</output>
