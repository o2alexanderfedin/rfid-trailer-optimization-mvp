---
phase: 05-simulation-visualization-wrapper
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/api/src/optimizer/rolling-service.ts
  - packages/api/src/optimizer/rolling-service.test.ts
  - packages/api/src/optimizer/live-loop.ts
  - packages/api/src/optimizer/live-loop.test.ts
  - packages/api/src/optimizer/twin-snapshot.ts
  - packages/api/src/optimizer/twin-snapshot.test.ts
  - packages/api/src/routes/optimizer.ts
  - packages/api/src/routes/optimizer.test.ts
autonomous: true
requirements: [OPT-02, OPT-05, OPT-06, OPT-07]
must_haves:
  truths:
    - "A live rolling loop runs `runEpoch` periodically AND on event triggers, reusing the Phase-4 scope/twin/freeze core, writing PlanGenerated/PlanAccepted OCC-safely on accept."
    - "The live freight-assignment path runs min-cost-flow (`assignFreight`) over the time-expanded graph built from the live twin snapshot."
    - "`GET /api/optimizer/recommendations` surfaces split/reassign/hold/over-carry repairs, each with a rationale, from the latest live epoch."
    - "Identical epoch input yields an identical plan (freeze honored, idempotent per epoch/scope) — no thrash on the live loop."
  artifacts:
    - path: "packages/api/src/optimizer/twin-snapshot.ts"
      provides: "Builds a deterministic `TwinSnapshot` (hubs/routes/trailers+blocks) from the live operational projections, for runEpoch"
      exports: ["buildTwinSnapshot"]
    - path: "packages/api/src/optimizer/live-loop.ts"
      provides: "`RollingLoop` — periodic + event-triggered driver that calls RollingOptimizerService.runOnce with epoch clock from sim/event time"
      exports: ["RollingLoop"]
    - path: "packages/api/src/routes/optimizer.ts"
      provides: "recommendations DTO extended with repair recs (kind + rationale)"
  key_links:
    - from: "packages/api/src/optimizer/rolling-service.ts"
      to: "packages/optimizer (assignFreight + runEpoch)"
      via: "min-cost-flow on the live freight-assignment path inside the epoch"
      pattern: "assignFreight|runEpoch"
    - from: "packages/api/src/optimizer/live-loop.ts"
      to: "packages/api/src/optimizer/rolling-service.ts"
      via: "the loop invokes runOnce per epoch with a sim-time clock"
      pattern: "runOnce"
    - from: "packages/api/src/routes/optimizer.ts"
      to: "packages/optimizer repair recommendations"
      via: "exposes split/reassign/hold/overCarry recs with rationale"
      pattern: "rationale|changeKind|kind"
---

<objective>
Close the OPT-02/05/06/07 live-wiring gap carried from Phase 4. The algorithms are delivered + verified (SSP min-cost-flow matched a 1153-instance glpk LP fuzz; rolling shell, freeze/idempotency, local-repair are unit-tested) — the remaining gap is LIVE WIRING: a periodic + event-triggered rolling loop on the API, min-cost-flow on the live freight-assignment path, and a repair-surfacing endpoint. SIM-04's "visible re-optimization" (Plan 05-05) depends on this loop existing.

Per 05-CONTEXT.md: add a `RollingLoop` live driver reusing Phase-4 `runEpoch`/`scope`/`twin`/`freeze`; build a `TwinSnapshot` from the live projections; wire `assignFreight` (min-cost flow) on the live path; surface repair recommendations (split/reassign/hold/over-carry with rationale) via `GET /api/optimizer/recommendations`. The loop writes plan events OCC-safe (Phase-1 contract, threat T-04-14) and the rolling-service already memoizes per `(epochId, scopeHash)` for idempotency.

Purpose: turns the proven-correct-but-dormant optimizer into a live, continuously-repairing loop that the scenario knobs and the money slide make visible.
Output: the twin-snapshot builder, the live loop, min-cost-flow on the live path, and the repair-bearing recommendations endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-simulation-visualization-wrapper/05-CONTEXT.md
@packages/api/src/optimizer/rolling-service.ts
@packages/api/src/routes/optimizer.ts
@packages/optimizer/src/rolling/types.ts
@packages/optimizer/src/repair/local-repair.ts

<interfaces>
<!-- Phase-4 pure core to REUSE (DO NOT re-implement) — from @mm/optimizer -->
runEpoch(epoch: Epoch, input: EpochInput, weights: ObjectiveWeights): EpochResult
assignFreight(...): FreightAssignment        // min-cost-flow freight→leg assignment (OPT-02)
localRepair(scope: RepairScope): Recommendation[]  // ranked split/reassign/hold/overCarry, each with rationale
DEFAULT_OBJECTIVE_WEIGHTS: ObjectiveWeights

Epoch = { epochId, nowMin, freezeWindowMin }   // nowMin from sim/event time, NEVER Date.now
EpochInput = { events: DomainEvent[], twinSnapshot: TwinSnapshot }
TwinSnapshot = { hubs: string[], routes: TwinRoute[], trailers: TwinTrailer[] }
TwinRoute = { routeId, fromHubId, toHubId, travelMin, capacity }
TwinTrailer = { trailerId, currentHubId, departureMin, capacity, route: TwinStop[], blocks: TwinBlock[] }
EpochResult = { epochId, scopeHash, generated, accepted, recommendations: EpochRecommendation[] }
Recommendation = { kind:"split"|"reassign"|"hold"|"overCarry", rationale, resultingMetrics, feasibility }

<!-- The existing stateful shell to EXTEND (packages/api/src/optimizer/rolling-service.ts) -->
class RollingOptimizerService {
  runOnce(epoch: Epoch, input: EpochInput): Promise<{ result: EpochResult; committed: boolean }>
  latestResult(): EpochResult | null   // memoized per `${epochId}:${scopeHash}` (OPT-06 idempotency)
}
// appendPlan already uses appendWithRetry (OCC-safe, T-04-14)

<!-- Live projections to read the twin from (packages/api/src/routes/queries.ts + @mm/projections) -->
trailer_state rows: { trailer_id, status, current_hub_id, trip_id, assigned_package_ids }
RouteRegistered events: { routeId, fromHubId, toHubId, geometry }   // travelMin derivable from geometry/transit model
hub_inventory rows: { hub_id, inbound, outbound, staged }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a deterministic TwinSnapshot from the live projections (RED→GREEN)</name>
  <files>packages/api/src/optimizer/twin-snapshot.ts, packages/api/src/optimizer/twin-snapshot.test.ts</files>
  <behavior>
    - Test: given trailer_state + RouteRegistered + hub_inventory rows, `buildTwinSnapshot` returns a `TwinSnapshot` with hubs (sorted ids), routes (routeId/from/to/travelMin/capacity), and trailers each with currentHubId/departureMin/capacity/route(TwinStop[])/blocks(TwinBlock[]).
    - Test (P3): same input rows ⇒ byte-identical snapshot (collections sorted by id; no Date.now, no Math.random; departureMin derived from sim/event time only).
    - Test: blocks reference only hubs that appear in the trailer's route (nextUnloadHubId ∈ stops).
    - Test: capacities/volumes are integers (P12 — costs scaled to ints).
  </behavior>
  <action>Implement `buildTwinSnapshot(db): Promise<TwinSnapshot>` reading trailer_state, RouteRegistered events, and the staged/outbound inventory to assemble each `TwinTrailer`'s assigned blocks. Derive `travelMin` from the route's transit model (the sim uses TRANSIT_TICKS = 30 min; reuse a constant, do not invent floats). Derive `departureMin` from sim/event time on the trailer's pending departure (NEVER Date.now). Sort every collection by id for determinism. Keep it a pure read mapping — no event-store writes. Write failing tests first against fixture rows, then implement to green.</action>
  <verify>
    <automated>pnpm --filter @mm/api test -- twin-snapshot</automated>
  </verify>
  <done>`buildTwinSnapshot` produces a deterministic TwinSnapshot from live projection rows; all tests pass; `tsc -b` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live RollingLoop (periodic + event-triggered) wiring min-cost-flow on the live path</name>
  <files>packages/api/src/optimizer/rolling-service.ts, packages/api/src/optimizer/rolling-service.test.ts, packages/api/src/optimizer/live-loop.ts, packages/api/src/optimizer/live-loop.test.ts</files>
  <behavior>
    - Test: `RollingLoop.tick({ events, simMs })` builds the twin (Task 1), constructs an `Epoch` with `nowMin` from `simMs` (NEVER Date.now) + the configured freeze window, calls `service.runOnce`, and returns the EpochResult.
    - Test (OPT-02): the epoch input drives `assignFreight` (min-cost-flow) on the live freight-assignment path — assert the assignment is computed (spy/observe the min-cost-flow term in the result breakdown).
    - Test (OPT-06 idempotency / freeze): feeding the SAME `(epoch, input)` twice appends a PlanAccepted AT MOST once (memoized); a trailer departing within the freeze window is left untouched.
    - Test (OPT-05): an event-triggered tick (a new exception/plan event in `events`) re-scopes to only the affected trailers; an empty-scope tick appends nothing.
    - Test (T-04-14): two concurrent loop ticks against the same stream converge via appendWithRetry (no lost write, no duplicate accept).
  </behavior>
  <action>Add a `RollingLoop` class in `live-loop.ts` that the composition root starts: it accepts the `RollingOptimizerService`, a `buildTwinSnapshot` port (Task 1), a freeze-window config, and an epoch-clock source from sim/event time. On each `tick({ events, simMs })` it builds the twin, forms the Epoch (`nowMin = simMs / 60000`, deterministic), and calls `runOnce`. Confirm `runEpoch` already routes freight via `assignFreight` (min-cost-flow); if the live path needs the assignment wired explicitly, do so inside the epoch input assembly — DO NOT re-implement the SSP solver (reuse `@mm/optimizer`). Extend `RollingOptimizerService` only if needed to expose the loop's clock injection; keep its single OCC-safe write path. Write the failing loop tests first, then implement to green.</action>
  <verify>
    <automated>pnpm --filter @mm/api test -- live-loop rolling-service</automated>
  </verify>
  <done>The live loop runs runEpoch periodically + on events with a sim-time clock, min-cost-flow is on the live assignment path, freeze + idempotency hold, OCC-safe writes converge under concurrency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface split/reassign/hold/over-carry repairs on GET /api/optimizer/recommendations</name>
  <files>packages/api/src/routes/optimizer.ts, packages/api/src/routes/optimizer.test.ts</files>
  <behavior>
    - Test: after a live epoch produced repairs, `GET /optimizer/recommendations` returns each repair with its `kind` ("split"|"reassign"|"hold"|"overCarry") and a non-empty `rationale`.
    - Test: feasibility verdict is reported SEPARATELY from objective cost (anti-P2) per recommendation.
    - Test: before the first epoch the endpoint returns 204 (unchanged).
    - Test (T-04-12): the GET never triggers an epoch or appends an event (read-only).
  </behavior>
  <action>Extend `OptimizerRecommendationsDto` / `RecommendationDto` to carry the local-repair output: each trailer's ranked recovery recommendations (`kind` + `rationale` + the separate `feasible` flag). Map the latest `EpochResult.recommendations` plus the `localRepair` output the epoch surfaces. Keep the route strictly read-only (the loop owns the write path). Write failing route tests via `app.inject()` first, then implement to green.</action>
  <verify>
    <automated>pnpm --filter @mm/api test -- routes/optimizer</automated>
  </verify>
  <done>The recommendations endpoint returns repair recs with kind + rationale + separate feasibility; remains read-only and 204-before-first-epoch; `tsc -b` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optimizer→event store | The loop is a concurrent writer alongside the sim/detector. |
| HTTP client→recommendations route | Untrusted read; must not trigger side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | concurrent plan appends | mitigate | All plan writes go through `appendWithRetry` (OCC: reload+retry on ConcurrencyError) — carried from Phase 1/4. |
| T-04-12 | Elevation of privilege | GET /recommendations | mitigate | Endpoint is strictly read-only off the memoized latest result; never triggers an epoch or append (asserted by test). |
| T-05-04 | Denial of service | rolling loop frequency | mitigate | Loop is scoped (OPT-05 — only affected hubs/trailers) + memoized per epoch/scope (OPT-06); an empty-scope tick is a no-op, bounding work. |
</threat_model>

<verification>
- `pnpm --filter @mm/api test` green (twin-snapshot, live-loop, rolling-service, routes/optimizer).
- `pnpm build` (turbo) clean.
- Grep gate: the live epoch path references `assignFreight` (min-cost-flow on the live path) and the loop's epoch clock derives `nowMin` from `simMs`, never `Date.now` — `grep -rn "Date.now" packages/api/src/optimizer/ | grep -v '^#'` returns zero in the optimization path.
</verification>

<success_criteria>
A live rolling loop continuously re-optimizes (periodic + event-triggered) reusing the Phase-4 core, runs min-cost-flow on the live freight-assignment path, honors freeze + idempotency, writes plan events OCC-safe, and surfaces split/reassign/hold/over-carry repairs with rationale via the recommendations endpoint. OPT-02/05/06/07 are live.
</success_criteria>

<output>
After completion, create `.planning/phases/05-simulation-visualization-wrapper/05-02-SUMMARY.md`
</output>
