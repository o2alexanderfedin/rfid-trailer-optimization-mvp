# v3.0 Demo Go-Live Checklist

**Do this as a dedicated step at milestone end, right before restarting the live demo** (user decision,
Phase 27 review). The v3.0 CODE is complete + verified; this task flips the LIVE demo server (`main.ts` /
the paced driver) from the v2.0 model to the full continental OODA stack and smoke-tests it. **No golden
impact** — the live driver is the presentation path, not the golden path (goldens call `simulate()` directly).

## Why deferred
Phase 27 is code-complete (verifier 12/12 must-haves). But `packages/api/src/main.ts` `driveOpts` only
enables the v2.0 lifecycle flags; the four v3.0 flags are NOT wired into the live driver, and the driver
(`driveSimulationPaced`/`driveSimulationOpenEnded`) only threads `coordinatorUsesOptimizer` so far. Wiring
the rest is an integration step best done against the fully-hardened post-Phase-28 tree with a live
smoke-test the operator can eyeball. Deferred per user (over "do it now in Phase 27").

## Tasks
1. **Thread 3 flags through the live driver** (`packages/api/src/sim/driver.ts`): add
   `continentalTopology`, `oodaAgentsEnabled`, `coordinatorsEnabled` to `DriveSimulationOptions` /
   `SimRunnerOptions` and pass them into `makeSimRunner` / `simulate()` at the 3 call sites
   (~524/732/1080), mirroring how `coordinatorUsesOptimizer` is already threaded.
2. **Env-gate them in `main.ts` `driveOpts`** (mirror the existing `!== "0"` demo-default pattern):
   `CONTINENTAL_TOPOLOGY`, `OODA_AGENTS`, `COORDINATORS`, `COORDINATOR_USES_OPTIMIZER` — default **ON**
   for the v3.0 demo. (Keep them toggleable so the v2.0 star demo is still reachable via env=0.)
3. **WR-02 (from 27-REVIEW):** gate the P27-B `refuelThresholdMiles: 250` override to **continental
   topology only** (`main.ts:73-78`) — otherwise short default-star spoke legs trigger spurious mid-trip
   refuels. Apply the 250 override only when `continentalTopology` is enabled; else keep
   `DEFAULT_FUEL_CONFIG.refuelThresholdMiles`.
4. **Live smoke-test** (the 2 human-UAT items from 27-VERIFICATION):
   - Boot the demo (Postgres up + API + web), run a few minutes, confirm: continental ~80–130 hubs render
     clustered/decluttered with distinct center/spoke/backbone tiers; **≥1 "won't divert: HOS/fuel"
     `SuggestionRejected` appears in the Alert Feed** (P27-B live); the accept-green/reject-red suggestion
     overlay flashes (~2500ms) and reads clearly.
   - Watch for perf/stall at 92 hubs × `FLEET_PER_SPOKE` × all lifecycle flags (PERF-04 validated the
     continental sim path, but NOT the full live driver with every v2.0 flag also on — verify here).
   - Kill the demo by PORT when done: `lsof -t -iTCP:3001 -sTCP:LISTEN | xargs -r kill -9` (+ :5173).

## Source review dispositions (27-REVIEW.md)
- CR-01 (rebuild TRUNCATE geo tables) — **FIXED** (ca321a5).
- CR-02 (worker-client close double-reject race) — **FIXED** (ca321a5).
- WR-01 (applyTrailerFuel key-scoping) — **FIXED** (ca321a5).
- WR-02 (refuel threshold continental-gating) — **deferred here** (task 3).
- WR-03 (v3.0 flags absent from live driver) — **deferred here** (tasks 1–2).
- IN-01 (vendor AsyncQueue LIFO-vs-FIFO doc mismatch) — cosmetic; vendor submodule doc only; no action.
