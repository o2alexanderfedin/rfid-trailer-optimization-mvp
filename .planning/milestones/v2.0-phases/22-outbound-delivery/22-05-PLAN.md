---
phase: 22-outbound-delivery
plan: "05"
type: execute
wave: 3
depends_on:
  - "22-03"
  - "22-04"
files_modified:
  - packages/api/src/ws/envelope.ts
  - packages/api/src/ws/snapshots.ts
  - packages/api/src/routes/queries.ts
  - packages/api/src/routes/delivery-kpi.ts
  - packages/api/test/ws-delivery.unit.test.ts
autonomous: true
requirements:
  - VIZ-14
  - OUT-05
must_haves:
  truths:
    - "DeliveryEvent interface exists in envelope.ts; TickPayload has deliveryEvents? readonly field; SnapshotPayload does NOT have deliveryEvents"
    - "Broadcast type accepts optional deliveryEvents parameter; diffTick wires deliveryEvents to TickPayload"
    - "ws-delivery.unit.test.ts: Pitfall-7 test GREEN (deliveryEvents absent from snapshot envelope)"
    - "GET /api/delivery-kpi returns { deliveredCount: number, onTimeCount: number } from the delivery_kpi projection"
  artifacts:
    - path: packages/api/src/ws/envelope.ts
      provides: DeliveryEvent interface + TickPayload.deliveryEvents? field
      contains: "DeliveryEvent"
    - path: packages/api/src/ws/snapshots.ts
      provides: Broadcast type extension with deliveryEvents parameter
      contains: "deliveryEvents"
    - path: packages/api/src/routes/delivery-kpi.ts
      provides: GET /api/delivery-kpi Fastify route
      contains: "delivery-kpi"
    - path: packages/api/test/ws-delivery.unit.test.ts
      provides: 3 tests GREEN including Pitfall-7 guard
      contains: "deliveryEvents"
  key_links:
    - from: packages/api/src/ws/envelope.ts
      to: packages/api/src/ws/snapshots.ts
      via: DeliveryEvent imported into Broadcast type signature
      pattern: "DeliveryEvent"
    - from: packages/api/src/ws/snapshots.ts (diffTick)
      to: TickPayload.deliveryEvents
      via: optional field spread (same as inductionEvents)
      pattern: "deliveryEvents"
---

<objective>
Add the `DeliveryEvent` WS interface + `TickPayload.deliveryEvents` tick-only field
(VIZ-14), extend the `Broadcast` function type + `diffTick` to wire delivery events
through to the live WS stream, and create the `GET /api/delivery-kpi` REST endpoint
for OUT-05 (P2). Turn `ws-delivery.unit.test.ts` GREEN.

Purpose: Close the transport layer — `PackageDelivered` events flow from the engine to
the web client as transient tick events (never replay on reconnect — Pitfall-7) and
the KPI counters are queryable by the operator panel widget.
Output: WS tick carries deliveryEvents; snapshot never does; KPI endpoint returns
live counters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-outbound-delivery/22-CONTEXT.md
@.planning/phases/22-outbound-delivery/22-RESEARCH.md
@.planning/phases/22-outbound-delivery/22-PATTERNS.md
@.planning/phases/22-outbound-delivery/22-VALIDATION.md
@.planning/phases/22-outbound-delivery/22-01-SUMMARY.md
@.planning/phases/22-outbound-delivery/22-03-SUMMARY.md
@.planning/phases/22-outbound-delivery/22-04-SUMMARY.md

<interfaces>
<!-- Verified code sections from 22-RESEARCH.md and 22-PATTERNS.md. -->

packages/api/src/ws/envelope.ts — Read full file. Key: `InductionEvent` at lines 135-142 (exact template for DeliveryEvent); `TickPayload` definition (find where `inductionEvents?` is declared, around line 227-231); `SnapshotPayload` definition (lines 181-200, MUST NOT gain deliveryEvents field).

packages/api/src/ws/snapshots.ts — Read full file. Key: `Broadcast` type at lines 83-86 (add `deliveryEvents?` parameter after `inductionEvents?`); `diffTick` function (find where inductionEvents is spread into TickPayload — add same pattern for deliveryEvents). Also: `attachSnapshotSocket` — understand how Broadcast is called from the sim loop.

packages/api/src/routes/queries.ts — Read to understand existing API route patterns and how hub-detail / delivery-kpi endpoint would be structured. Also check for HubBalance/hub-detail endpoint as the KPI endpoint template.

packages/api/test/ws-induction.unit.test.ts — Read full file (template for ws-delivery.unit.test.ts). Critical: FakeSocket interface, makeFakeApp helper, how `broadcast` function is constructed and called, Pitfall-7 test structure at lines 123-142.

packages/api/test/ws-delivery.unit.test.ts — Read the stub created in Plan 02 (the failing RED tests to turn GREEN).

ANALOG: ws-induction.unit.test.ts (exact copy-and-adapt per 22-PATTERNS.md § ws-delivery section); snapshots.ts `Broadcast` type with inductionEvents as the template for deliveryEvents addition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeliveryEvent WS interface + TickPayload field + Broadcast + diffTick wiring</name>
  <files>
    packages/api/src/ws/envelope.ts,
    packages/api/src/ws/snapshots.ts
  </files>
  <read_first>
    packages/api/src/ws/envelope.ts (full file — locate InductionEvent, TickPayload, SnapshotPayload);
    packages/api/src/ws/snapshots.ts (full file — locate Broadcast type, diffTick function, where inductionEvents is used).
    ANALOG: envelope.ts InductionEvent + TickPayload.inductionEvents (lines 135-142, 227-231 per 22-PATTERNS.md § envelope.ts section).
    ANALOG: snapshots.ts Broadcast type (lines 83-86 per 22-PATTERNS.md § snapshots.ts section).
  </read_first>
  <behavior>
    ws-delivery.unit.test.ts tests that must GREEN after this task:
    - "includes deliveryEvents in TickPayload when delivery events present" — broadcast call with deliveryEvents array produces tick with deliveryEvents field
    - "omits deliveryEvents from TickPayload when none" — broadcast without deliveryEvents produces tick without the field
    - "never places deliveryEvents on the initial snapshot payload (Pitfall-7)" — snapshot envelope.payload has no deliveryEvents key
  </behavior>
  <action>
    envelope.ts:
    (1) Add `DeliveryEvent` interface immediately after `InductionEvent` (per 22-PATTERNS.md § envelope.ts + 22-RESEARCH.md § WS TickPayload):
    `{ readonly packageId: string; readonly hubId: string; readonly deliveredAt: string; readonly onTime: boolean; }` — with jsdoc: "VIZ-14 — a package delivered at its destination hub this tick. TRANSIENT: present only on a TickPayload (never SnapshotPayload), so a reconnect/resync does NOT re-animate all historical deliveries (Pitfall 7 — same rule as InductionEvent)."
    (2) Add `readonly deliveryEvents?: readonly DeliveryEvent[];` to `TickPayload` immediately after `inductionEvents?`. Add the same Pitfall-7 jsdoc comment.
    (3) Verify `SnapshotPayload` does NOT gain `deliveryEvents`. If you see any risk of accidental addition, add a comment in `SnapshotPayload` "// deliveryEvents MUST NOT be added here — Pitfall-7: reconnect re-flash".
    (4) Export `DeliveryEvent` from the module (check if re-exported via index.ts or barrel — add if needed).

    snapshots.ts:
    (1) Import `DeliveryEvent` from `./envelope.js`.
    (2) Extend `Broadcast` type signature: add `deliveryEvents?: readonly DeliveryEvent[]` as optional third parameter (per 22-PATTERNS.md § snapshots.ts section).
    (3) In `diffTick()` (or wherever `TickPayload` is constructed and `inductionEvents` is spread): add `...(deliveryEvents !== undefined && { deliveryEvents })` following the exact same optional-spread pattern used for `inductionEvents`.
    (4) Update ALL callers of `Broadcast` / `diffTick` in the API to pass the new optional parameter (can pass `undefined` where delivery events are not yet available — the parameter is optional).

    CRITICAL: `SnapshotPayload` MUST NOT gain `deliveryEvents`. The Pitfall-7 guard is the non-negotiable invariant (D-22-4 binding_invariants item 8 in planning_context). Any snapshot payload construction must NOT spread deliveryEvents into it.
  </action>
  <verify>
    <automated>pnpm --filter @mm/api build && pnpm --filter @mm/api test -- --run packages/api/test/ws-delivery.unit.test.ts</automated>
  </verify>
  <done>
    API builds clean. ws-delivery.unit.test.ts: all 3 tests GREEN including Pitfall-7 (snapshot has no deliveryEvents key). SnapshotPayload type does NOT contain deliveryEvents.
  </done>
</task>

<task type="auto">
  <name>Task 2: GET /api/delivery-kpi endpoint + KPI query wiring</name>
  <files>
    packages/api/src/routes/delivery-kpi.ts,
    packages/api/src/routes/queries.ts
  </files>
  <read_first>
    packages/api/src/routes/queries.ts (full file — understand query pattern, DB access, how hub-detail is structured; this is the template for the delivery-kpi query);
    packages/api/src/routes/delivery-kpi.ts (does not exist — creating new file);
    packages/projections/src/reducers/index.ts (confirm deliveryKpiReducer + DeliveryKpiState are exported after Plan 04);
    packages/api/src/routes (check index.ts or app.ts for how routes are registered — find where hub-detail or other routes are plugged in to understand route registration pattern).
    ANALOG: Hub-detail route or HubBalance fetch endpoint as the template for REST + Fastify route structure.
  </read_first>
  <action>
    delivery-kpi.ts (NEW ROUTE FILE):
    Create a Fastify plugin (or inline route) for `GET /api/delivery-kpi`. Response schema: `{ deliveredCount: number, onTimeCount: number }`. The handler reads from the `delivery_kpi` projection in Postgres (or from the in-memory reducer fold). Per D-22-3, this endpoint reads the event-derived counters — NOT `COUNT(*)` over `packageLocation` or `hubInventory`.

    Implementation approach: If other projections use a reducer fold over the event store (pattern from prior phases), follow that same pattern — fold all events through `deliveryKpiReducer` starting from `emptyDeliveryKpiState` and return the result. If projections are stored in a Postgres table, query the `delivery_kpi` projection table (check which pattern the existing hub-detail endpoint uses and replicate exactly).

    queries.ts: Add any required query function `queryDeliveryKpi(db): Promise<DeliveryKpiState>` following the style of existing queries. Import `deliveryKpiReducer`, `emptyDeliveryKpiState` from `@mm/projections`.

    Register the new route in the app registration point (check app.ts / routes/index.ts / plugin structure). Use TypeBox or Zod schema for the response (whichever the project uses — check existing routes).

    The endpoint is used by the `DeliveryKpi.tsx` widget (Plan 06). No auth required (matches existing API pattern — the API is internal sim-only).
  </action>
  <verify>
    <automated>pnpm --filter @mm/api build && pnpm --filter @mm/api test -- --run packages/api/test/queries.int.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    API builds clean. GET /api/delivery-kpi route is registered (visible in app plugin list). queries.int.test.ts still green (no regressions). The endpoint returns `{ deliveredCount, onTimeCount }` on a GET request.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS server → Web client | deliveryEvents pushed over authenticated WebSocket; TRANSIENT tick-only |
| HTTP client → GET /api/delivery-kpi | Internal API; no auth change from existing endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Information Disclosure | deliveryEvents WS field | accept | No new threats: deliveryEvents contains simulation package IDs (non-PII); same trust boundary as inductionEvents. No auth change — internal demo API. |
| T-22-08 | Tampering | GET /api/delivery-kpi | accept | Response is read-only aggregate from event-derived reducer; no input validation beyond existing Fastify schema; no secrets exposed. Same pattern as hub-detail endpoint. |
</threat_model>

<verification>
After both tasks:
1. `pnpm --filter @mm/api build` — zero TS errors
2. `pnpm --filter @mm/api test -- --run packages/api/test/ws-delivery.unit.test.ts` — 3/3 green including Pitfall-7
3. `pnpm --filter @mm/api test -- --run packages/api/test/ws-envelope.unit.test.ts` — still green (no regressions)
4. `pnpm --filter @mm/api test -- --run packages/api/test/snapshots.unit.test.ts` — still green
5. The GET /api/delivery-kpi route is registered and returns `{ deliveredCount: number, onTimeCount: number }`
</verification>

<success_criteria>
- DeliveryEvent interface in envelope.ts; TickPayload.deliveryEvents?: readonly DeliveryEvent[] present
- SnapshotPayload has NO deliveryEvents field (Pitfall-7 guard)
- Broadcast type accepts optional deliveryEvents parameter; diffTick wires it to TickPayload
- ws-delivery.unit.test.ts: 3 tests GREEN
- GET /api/delivery-kpi returns 200 with { deliveredCount, onTimeCount } from delivery_kpi projection
</success_criteria>

<output>
After completion, create `.planning/phases/22-outbound-delivery/22-05-SUMMARY.md`
</output>
