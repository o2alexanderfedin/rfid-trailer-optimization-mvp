---
phase: 22-outbound-delivery
plan: "03"
type: execute
wave: 2
depends_on:
  - "22-01"
  - "22-02"
files_modified:
  - packages/simulation/src/engine.ts
  - packages/simulation/test/outbound-determinism.unit.test.ts
  - packages/simulation/test/determinism.unit.test.ts
  - packages/simulation/test/continuation-equivalence.unit.test.ts
autonomous: true
requirements:
  - OUT-02
  - OUT-03
  - D-22-4
must_haves:
  truths:
    - "simulate() with outboundDeliveryEnabled absent or false produces ZERO PackageDelivered events and is byte-identical to the seed-42 golden SHA 3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861"
    - "explicit-false is byte-identical to absent"
    - "with outboundDeliveryEnabled: true, every package that receives PackageArrivedAtHub at its destination hub eventually emits PackageDelivered at a strictly later tick"
    - "PackageDelivered.payload.onTime = (deliveredAt <= slaDeadlineIso) for inducted packages; true for center-origin packages with no deadline"
    - "deliveredAt is whole-minute ISO (epochMinutesToIso(isoToEpochMinutes(clock.nowIso())))"
    - "Continuation is byte-identical when chunked across a mid-dwell boundary"
    - "outboundRng is built lazily (only when outboundDeliveryEnabled === true)"
  artifacts:
    - path: packages/simulation/src/engine.ts
      provides: outboundOn guard, outboundRng lazy construction, deliverPackage() one-shot task, arriveTrailer hook, slaDeadlineByPackage world state, captureContinuation outbound additions, dispatch switch case
      contains: "outboundOn"
    - path: packages/simulation/test/outbound-determinism.unit.test.ts
      provides: all 7 tests GREEN
      contains: "PackageDelivered"
    - path: packages/simulation/test/determinism.unit.test.ts
      provides: extended DET-01 section with outboundDeliveryEnabled: false byte-identity assertion
      contains: "outboundDeliveryEnabled"
    - path: packages/simulation/test/continuation-equivalence.unit.test.ts
      provides: "outbound" FEATURE_CASE active (not todo) and GREEN
      contains: '"outbound"'
  key_links:
    - from: packages/simulation/src/engine.ts
      to: packages/simulation/src/continuation.ts
      via: SimTask.deliverPackage DATA variant (D-22-4 Pitfall-6 serializable)
      pattern: "deliverPackage"
    - from: arriveTrailer() in engine.ts
      to: deliverPackage() in engine.ts
      via: scheduleNext(fireTick, { kind: "deliverPackage", ... })
      pattern: "scheduleNext.*deliverPackage"
    - from: deliverPackage() in engine.ts
      to: slaDeadlineByPackage Map in world state
      via: slaDeadlineByPackage.get(packageId) at schedule time
      pattern: "slaDeadlineByPackage"
---

<objective>
Implement `deliverPackage` in the simulation engine: the `outboundRng` lazy substream,
the `slaDeadlineByPackage` world-state map, the `arriveTrailer()` delivery-scheduling
hook, the `deliverPackage()` one-shot task function, the `dispatch` switch case, and
the `captureContinuation()` outbound capture. Turn `outbound-determinism.unit.test.ts`
GREEN (all 7 tests). Extend `determinism.unit.test.ts` DET-01 section. Activate the
`"outbound"` FEATURE_CASE in `continuation-equivalence.unit.test.ts`.

Purpose: Core engine behavior — `PackageArrivedAtHub` is no longer terminal; every
package at its destination hub schedules a seeded one-shot delivery task.
Output: Engine emits `PackageDelivered` events when flag is on; golden byte-identity
preserved when flag is off; continuation-equivalence proven across mid-dwell boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-outbound-delivery/22-CONTEXT.md
@.planning/phases/22-outbound-delivery/22-RESEARCH.md
@.planning/phases/22-outbound-delivery/22-PATTERNS.md
@.planning/phases/22-outbound-delivery/22-VALIDATION.md
@.planning/phases/22-outbound-delivery/22-01-SUMMARY.md
@.planning/phases/22-outbound-delivery/22-02-SUMMARY.md

<interfaces>
<!-- Key code sections the executor must read before implementing. All line numbers verified in 22-RESEARCH.md. -->

packages/simulation/src/engine.ts — Read these sections:
  - Lines 85-120: RNG salt constants block (INDUCTION_RNG_SALT is at line 116; OUTBOUND_RNG_SALT was added in Plan 01)
  - Lines 147-290: SimulateOptions definition (outboundDeliveryEnabled field added in Plan 01)
  - Lines 505-515: inductionOn flag guard (const inductionOn = opts.inductionEnabled === true — exact template for outboundOn)
  - Lines 580-600: inductionRng lazy construction (exact template for outboundRng)
  - Lines 1103-1176: inductPackage() function (ONE-SHOT pattern reference — note NO scheduleNext at end)
  - Lines 1495-1560: arriveTrailer() + the carried-packages unload loop ending with PackageArrivedAtHub emit (hook site)
  - Lines 1829-1863: dispatch() switch (add deliverPackage case here)
  - Lines 1919-1973: captureContinuation() (induction capture at lines 1950/1966 = template)

packages/simulation/src/continuation.ts — SimTask union (deliverPackage variant added in Plan 01); SerializedWorldState (deliveredCounter + slaDeadlineByPackage added in Plan 01); SerializedRngStates.outbound (added in Plan 01).

packages/simulation/src/rng.ts — `makeRng(seed: number): Rng`, `makeRngFromState(rawState: number): Rng`, `Rng.getState(): number`. Already imported in engine.ts.

packages/domain/src/hos.ts lines 141-152 — `epochMinutesToIso` and `isoToEpochMinutes` (already imported in engine.ts at lines 32-38).

packages/simulation/test/induction-determinism.unit.test.ts — template for flag-off/on tests, terminal-completeness structure.

packages/simulation/test/continuation-equivalence.unit.test.ts — FEATURE_CASES structure; how "consolidation" case was activated in Phase 21 (remove any `.todo` wrapper).

packages/simulation/test/determinism.unit.test.ts lines 190-220 — DET-01 section: existing flag-off byte-identity tests for inductionEnabled/consolidationEnabled (exact copy target for outboundDeliveryEnabled: false test).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine — outboundOn, outboundRng, slaDeadlineByPackage, deliverPackage(), arriveTrailer hook, dispatch, captureContinuation</name>
  <files>packages/simulation/src/engine.ts</files>
  <read_first>
    packages/simulation/src/engine.ts (full key sections — lines 85-120, 505-515, 580-600, 1103-1176, 1495-1560, 1829-1863, 1919-1973 — read ALL sections before writing any change);
    packages/simulation/src/continuation.ts (SimTask.deliverPackage fields; SerializedWorldState new fields — to know what to capture);
    packages/simulation/src/rng.ts (Rng interface — .int(), .getState(), makeRngFromState());
    packages/domain/src/hos.ts lines 141-152 (epochMinutesToIso, isoToEpochMinutes — confirm imports present).
    ANALOG: engine.ts inductPackage (lines 1103-1176) + inductionRng construction (lines 580-595) + arriveTrailer (lines 1495-1560) — all per 22-PATTERNS.md § engine.ts sections.
  </read_first>
  <behavior>
    Engine behaviors that outbound-determinism.unit.test.ts must assert:
    - outboundDeliveryEnabled absent => ZERO PackageDelivered events in stream
    - outboundDeliveryEnabled: false => byte-identical to absent
    - outboundDeliveryEnabled: true + inductionEnabled: true => at least one PackageDelivered fires within 500 ticks
    - Every PackageDelivered occurs at a strictly later tick than PackageArrivedAtHub for same packageId (D-22-2)
    - deliveredAt matches /T\d{2}:\d{2}:00\.000Z$/ (whole-minute ISO, D-22-5)
    - onTime is boolean (true for center-origin packages without deadline, per D-22-5)
  </behavior>
  <action>
    Make the following additions to engine.ts in ORDER (to avoid forward-reference issues):

    1. OUTBOUND FLAG GUARD (near line 511, after inductionOn): `const outboundOn = opts.outboundDeliveryEnabled === true;` — strict `=== true` (never `??` or `||`; per D-22-4 Pitfall-4 in RESEARCH.md).

    2. WORLD STATE MAP (declare alongside consolidationDestByPackage): `const slaDeadlineByPackage = new Map<string, string>();` — stores packageId → slaDeadlineIso for inducted packages. If resuming from continuation, restore from `continuation.world.slaDeadlineByPackage`.

    3. COUNTER (alongside inductionCounter): `let deliveredCounter = 0;` — restored from `continuation.world.deliveredCounter` when resuming.

    4. OUTBOUND RNG CONSTRUCTION (after inductionRng, around line 595, per 22-PATTERNS.md § outboundRng):
       ```
       outboundRng = outboundOn ? (restoredRng?.outbound !== undefined ? makeRngFromState(restoredRng.outbound) : makeRng((seed ^ OUTBOUND_RNG_SALT) >>> 0)) : undefined
       ```
       Type: `let outboundRng: Rng | undefined`. Built ONLY when outboundOn (lazy).

    5. SLA DEADLINE CAPTURE AT INDUCTION (inside inductPackage, after the emit of PackageInducted): `if (outboundOn) slaDeadlineByPackage.set(packageId, slaDeadlineIso);` — populates the world-state map for later delivery.

    6. DELIVER PACKAGE FUNCTION (one-shot, after inductPackage, per 22-PATTERNS.md § deliverPackage):
       - Guard: `if (!outboundOn || outboundRng === undefined) return;`
       - `deliveredCounter += 1`
       - `const deliveredAt = epochMinutesToIso(isoToEpochMinutes(clock.nowIso()))` — whole-minute canonical (D-22-5)
       - `const deadline = slaDeadlineByPackage.get(packageId)` (may be undefined for center-origin packages)
       - `const onTime = deadline !== undefined ? deliveredAt <= deadline : true` — string lexicographic comparison of ISO dates is correct for whole-minute ISO (both are YYYY-MM-DDTHH:MM:00.000Z format) (D-22-5)
       - Emit `PackageDelivered { packageId, hubId, deliveredAt, onTime, occurredAt: deliveredAt }`
       - `slaDeadlineByPackage.delete(packageId)` — clean up world state
       - NO scheduleNext() — this is ONE-SHOT (not self-rescheduling; this is the key difference from inductPackage)

    7. ARRIVE TRAILER HOOK (inside arriveTrailer, immediately after the `emit(..., atHub)` call at ~line 1558, per 22-RESEARCH.md Code Anchor 2 + 22-PATTERNS.md § arriveTrailer hook):
       ```
       if (outboundOn) {
         const dwell = 1 + outboundRng!.int(OUTBOUND_DWELL_TICKS_MAX);  // dwell >= 1 (D-22-2)
         const fireTick = arriveTick + dwell;
         scheduleNext(fireTick, {
           kind: "deliverPackage",
           packageId,
           hubId: spoke.hubId,
           slaDeadlineIso: slaDeadlineByPackage.get(packageId),  // undefined for center-origin packages
           fireTick,
         });
       }
       ```
       Add `OUTBOUND_DWELL_TICKS_MAX = 20` constant (Claude's discretion, per RESEARCH.md Open Question 3: 1 + int(20) = 1..20 ticks).

    8. DISPATCH SWITCH (add to dispatch() at line 1829-1863):
       `case "deliverPackage": deliverPackage(task.packageId, task.hubId, task.slaDeadlineIso, task.fireTick); return;`

    9. CAPTURE CONTINUATION (extend captureContinuation() at lines 1919-1973):
       - In world: add `deliveredCounter` and `slaDeadlineByPackage: [...slaDeadlineByPackage.entries()].map(([k, v]) => [k, v] as const)`
       - In rng: add `outbound: outboundRng?.getState()`
       
    10. CONTINUATION RESTORE (at start of simulate, when continuation provided): restore `deliveredCounter` from `continuation.world.deliveredCounter` and populate `slaDeadlineByPackage` from `continuation.world.slaDeadlineByPackage`. Mirror the inductionCounter restore pattern.

    CRITICAL INVARIANTS:
    - `outboundOn = opts.outboundDeliveryEnabled === true` — strict, no fallback
    - outboundRng constructed ONLY when outboundOn
    - dwell >= 1 always (D-22-2)
    - deliveredAt uses epochMinutesToIso(isoToEpochMinutes(...)) round-trip (D-22-5)
    - deliverPackage is ONE-SHOT (no scheduleNext inside it)
    - slaDeadlineByPackage.get() may return undefined for center-origin packages — treat as onTime: true (D-22-5)
  </action>
  <verify>
    <automated>pnpm --filter @mm/simulation build && pnpm --filter @mm/simulation test -- --run packages/simulation/test/outbound-determinism.unit.test.ts</automated>
  </verify>
  <done>
    pnpm build passes. outbound-determinism.unit.test.ts: all 7 tests GREEN. Flag-off produces zero PackageDelivered events. Flag-on produces at least one PackageDelivered. Every PackageDelivered is at a strictly later tick than PackageArrivedAtHub. deliveredAt matches whole-minute ISO regex. onTime is always boolean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Golden determinism gate + continuation-equivalence "outbound" case activation</name>
  <files>
    packages/simulation/test/determinism.unit.test.ts,
    packages/simulation/test/continuation-equivalence.unit.test.ts
  </files>
  <read_first>
    packages/simulation/test/determinism.unit.test.ts (lines 125-149 for LONG_RUN_GOLDEN_SHA256 constant; lines 190-220 for DET-01 section with flag-off tests for inductionEnabled and consolidationEnabled — exact copy targets);
    packages/simulation/test/continuation-equivalence.unit.test.ts (full file — understand FEATURE_CASES shape, how "consolidation" was added in Phase 21, and where the "outbound" case + any .todo wrapper are from Plan 01).
    ANALOG for DET-01 extension: determinism.unit.test.ts lines 190-220 (inductionEnabled: false + consolidationEnabled: false pattern).
    ANALOG for continuation case: continuation-equivalence.unit.test.ts "consolidation" case (lines 176-179).
  </read_first>
  <action>
    determinism.unit.test.ts: In the DET-01 section (lines 190-220), add two new `it()` tests following the exact style of the existing inductionEnabled/consolidationEnabled flag-off tests:
    - Test A: `"outboundDeliveryEnabled ABSENT is byte-identical to seed-42 golden (DET-01)"` — run `simulate({ seed: 42, durationTicks: 10000 })` (no outboundDeliveryEnabled), hash the result, assert equals `LONG_RUN_GOLDEN_SHA256` (`"3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861"`). This is the non-negotiable acceptance gate.
    - Test B: `"outboundDeliveryEnabled: false is byte-identical to absent (DET-01)"` — run with `outboundDeliveryEnabled: false` and without; assert JSON.stringify of both are identical.
    CRITICAL: The golden hash `3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861` MUST appear literally in the test assertion (not a variable derived at runtime). Both tests use `durationTicks: 10000` — this is the ONE permitted 10k-tick run per gate-hygiene; it re-uses the existing golden test infrastructure.

    continuation-equivalence.unit.test.ts: Remove any `.todo` or `.skip` wrapper from the `"outbound"` FEATURE_CASE added in Plan 01. The "outbound" case uses `{ timing: SHORT_TIMING, inductionEnabled: true, outboundDeliveryEnabled: true }` with `durationTicks: 800` (gate-hygiene bound). The test asserts chunked(7) === all-at-once (byte-identical JSON.stringify of event streams). Update `"all-on"` case to include `outboundDeliveryEnabled: true` (proving that the full-feature combination is also continuation-safe). Gate-hygiene: add `--max-workers 1 --no-file-parallelism` to the continuation test invocation in comments/docs if not already present.
  </action>
  <verify>
    <automated>pnpm --filter @mm/simulation test -- --run packages/simulation/test/determinism.unit.test.ts && pnpm --filter @mm/simulation test -- --run --max-workers 1 packages/simulation/test/continuation-equivalence.unit.test.ts</automated>
  </verify>
  <done>
    determinism.unit.test.ts: ALL existing tests pass (LONG_RUN_GOLDEN_SHA256 unchanged); new DET-01 outboundDeliveryEnabled tests pass. continuation-equivalence.unit.test.ts: ALL cases GREEN including "outbound" and updated "all-on". The literal hash `3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861` appears in the test and the assertion passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine → Event stream | PackageDelivered emitted from internal engine state; no external input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | engine.ts deliverPackage | accept | No new threats: simulation event extension; PackageDelivered payload derived entirely from validated internal world state (slaDeadlineByPackage populated by inductPackage which validated PackageInducted at ingest); no user-facing auth, no external input, no secrets. |
| T-22-04 | Denial of Service | outboundRng lazy construction | accept | outboundRng only constructed when outboundDeliveryEnabled === true; default is undefined; no risk of unintended RNG draws when flag is off. |
</threat_model>

<verification>
After both tasks:
1. `pnpm --filter @mm/simulation build` — zero errors
2. `pnpm --filter @mm/simulation test -- --run packages/simulation/test/outbound-determinism.unit.test.ts` — 7/7 green
3. `pnpm --filter @mm/simulation test -- --run packages/simulation/test/determinism.unit.test.ts` — ALL green including 2 new DET-01 outboundDeliveryEnabled tests; golden SHA `3920accc…` unchanged
4. `pnpm --filter @mm/simulation test -- --run --max-workers 1 packages/simulation/test/continuation-equivalence.unit.test.ts` — ALL green including "outbound" case
5. `pnpm --filter @mm/simulation test -- --run packages/simulation/test/fuel-determinism.unit.test.ts` — still green (salt test from Plan 01)
</verification>

<success_criteria>
- Flag-off golden: `simulate({ seed: 42, durationTicks: 10000 })` hashes to `3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861` (byte-identical to pre-Phase-22 golden)
- Explicit false ≡ absent: `simulate({ ..., outboundDeliveryEnabled: false })` byte-identical to absent-flag run
- Flag-on: lifecycle ordering holds (PackageDelivered always strictly after PackageArrivedAtHub for same packageId)
- Flag-on: terminal completeness (all packages reach PackageDelivered within 500 ticks)
- deliveredAt is whole-minute ISO format on every event
- Continuation-equivalence: chunked(7) ≡ all-at-once for "outbound" case with durationTicks: 800
</success_criteria>

<output>
After completion, create `.planning/phases/22-outbound-delivery/22-03-SUMMARY.md`
</output>
