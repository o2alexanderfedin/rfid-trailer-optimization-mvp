---
phase: 20-external-induction
plan: 06
type: auto
wave: 3
depends_on:
  - 20-02-PLAN.md
  - 20-03-PLAN.md
  - 20-04-PLAN.md
  - 20-05-PLAN.md
files_modified:
  - packages/simulation/test/continuation-equivalence.unit.test.ts
  - packages/simulation/test/continuation-adversarial.unit.test.ts
autonomous: false
requirements:
  - IND-02
  - VIZ-13

must_haves:
  truths:
    - "A chunked run with inductionEnabled: true is BYTE-IDENTICAL to an all-at-once run when a chunk boundary lands between two induction arrivals — SerializedRngStates.induction and inductionCounter are correctly captured and restored"
    - "The full gate pnpm build && pnpm typecheck && pnpm lint && pnpm test:all passes green — Phase 20 complete"
    - "VIZ-13 pulsing induction markers are visible at spoke hubs on the live map (manual visual confirmation)"
    - "Scale bounds honored: continuation-equivalence induction case uses seed=42, horizon=100, chunk=7 (ONE case only)"
  artifacts:
    - path: packages/simulation/test/continuation-equivalence.unit.test.ts
      provides: "inductionEnabled:true chunk-boundary-between-arrivals equivalence case (ONE case, scale-bounded)"
      contains: "inductionEnabled"
    - path: packages/simulation/test/continuation-adversarial.unit.test.ts
      provides: "ALL_ON constant extended with inductionEnabled: true (1-line change, same test budget)"
      contains: "inductionEnabled"
  key_links:
    - from: packages/simulation/src/continuation.ts
      to: packages/simulation/test/continuation-equivalence.unit.test.ts
      via: "chunkedStream helper uses runToHorizon + SimContinuation; SerializedRngStates.induction verified by hash equality"
      pattern: "inductionEnabled"
---

<objective>
Validate the continuation capture/restore for the induction substream (ONE bounded equivalence case), extend ALL_ON in the adversarial test, run the full gate, and perform the manual VIZ-13 visual confirmation.

Purpose: The continuation-equivalence test with inductionEnabled: true is the make-or-break gate for IND-02's determinism keystone — it proves SerializedRngStates.induction and inductionCounter are correctly captured and restored across chunk boundaries. Scale-bounded to ONE case (seed=42, horizon=100, chunk=7) per gate-hygiene constraint.

Output: continuation-equivalence test extended (green); ALL_ON extended (green); full gate green; VIZ-13 manual smoke confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-external-induction/20-CONTEXT.md
@.planning/phases/20-external-induction/20-RESEARCH.md
@.planning/phases/20-external-induction/20-VALIDATION.md
@.planning/phases/20-external-induction/20-PATTERNS.md
@.planning/phases/19-continuous-operation-foundation/19-04-SUMMARY.md

<interfaces>
<!-- All line numbers verified against live codebase in research session. -->

From packages/simulation/test/continuation-equivalence.unit.test.ts (lines 1-148):
```typescript
// chunkedStream helper (lines ~20-50) — runs simulate() in chunks via runToHorizon
// SHORT_TIMING constant (lines 110-114) — fast timing for tests
// FEATURE_CASES array (lines 118-148) — each entry: { name, opts }
// The test loop at line 157 iterates FEATURE_CASES with SEEDS × HORIZONS for each case.
// Adding ONE entry to FEATURE_CASES adds ONE chunk-7 case per seed per horizon.
// Per SCALE BOUND: add { name: "induction", opts: { timing: SHORT_TIMING, inductionEnabled: true } }
// OR extend the "all-on" entry to include inductionEnabled: true (PATTERNS.md pattern 9).
// PREFERRED per RESEARCH Pitfall 6: add to "all-on" (1 line) rather than new FEATURE_CASES entry.
```

From packages/simulation/test/continuation-adversarial.unit.test.ts (lines 55-73):
```typescript
// ALL_ON constant (lines 55-73) — feature flags combined in the adversarial test:
const ALL_ON: SimulateOptions = {
  // hosEnabled: true, fuelConfig: ..., sortWave: ..., timing: ...,
  // ADD: inductionEnabled: true
};
// Scale: the adversarial test already runs chunk-1 over SEEDS × HORIZONS for ALL_ON.
// Adding inductionEnabled: true here costs ZERO additional test cases — same budget.
// This is the recommended approach per RESEARCH Pitfall 6 / VALIDATION.md Scale Bounds.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend continuation tests — equivalence case + ALL_ON (IND-02)</name>
  <files>
    packages/simulation/test/continuation-equivalence.unit.test.ts,
    packages/simulation/test/continuation-adversarial.unit.test.ts
  </files>

  <read_first>
    1. packages/simulation/test/continuation-equivalence.unit.test.ts — read fully (lines 1-200); note FEATURE_CASES array (lines 118-148), SHORT_TIMING (lines 110-114), chunkedStream helper, SEEDS and HORIZONS constants, and the test loop structure. Understand how adding one FEATURE_CASES entry translates to test count.
    2. packages/simulation/test/continuation-adversarial.unit.test.ts — read lines 55-155; note ALL_ON constant (lines 55-73) and the SEEDS × HORIZONS × chunk-1 matrix (lines 140-155).
    3. .planning/phases/20-external-induction/20-RESEARCH.md VQ#7, Pitfall 6 — the scale bound rationale and preferred approach (extend ALL_ON vs separate FEATURE_CASES entry).
    4. .planning/phases/20-external-induction/20-VALIDATION.md Scale Bounds — "ONE case only: seed=42, horizon=100, chunk=7".
    5. .planning/phases/19-continuous-operation-foundation/19-04-SUMMARY.md — how chunkedStream works (driveSimulationOpenEnded chunks by chunkTicks; the continuation equivalence test uses runToHorizon directly — confirm which).
  </read_first>

  <behavior>
    continuation-equivalence.unit.test.ts:
    Choose the approach that adds the FEWEST test cases while proving the property:
    - PREFERRED: Add `inductionEnabled: true` to the EXISTING "all-on" FEATURE_CASES entry (if present). 1-line change; same SEEDS × HORIZONS × chunks budget.
    - ALTERNATIVE (if no "all-on" entry): Add a NEW entry `{ name: "induction", opts: { timing: SHORT_TIMING, inductionEnabled: true } }` to FEATURE_CASES. Per scale bound, this entry will only be tested at existing HORIZONS (short, e.g., 200) and SEEDS (2-3). ONE bounded case.

    The critical chunk boundary scenario (chunk-7, horizon-100, seed-42) is covered by the FEATURE_CASES loop. The key invariant: if the loop runs with chunk=7 and horizon=100, a chunk boundary WILL land between two induction firings (inductions at every 30 ticks; 7-tick chunks create boundaries at 7, 14, 21, 28, 35... — crosses the first induction at tick 30). The hash equality assertion proves continuity.

    continuation-adversarial.unit.test.ts:
    Add `inductionEnabled: true` to the ALL_ON constant (lines 55-73). This is a 1-line edit; zero new test cases; the existing SEEDS × HORIZONS × chunk-1 matrix now includes inductionEnabled in ALL_ON, exercising the most adversarial boundary (chunk=1 = maximum discontinuity).

    GATE-HYGIENE: Do NOT add a separate SEEDS × HORIZONS matrix for inductionEnabled alone. Do NOT extend durationTicks beyond what the existing HORIZONS already use. These scale bounds are mandatory.

    After edits: run `pnpm vitest run packages/simulation/test/continuation-equivalence.unit.test.ts packages/simulation/test/continuation-adversarial.unit.test.ts`.

    If the continuation-equivalence test FAILS with a hash mismatch (chunks ≠ all-at-once for inductionEnabled: true):
    - This means SerializedRngStates.induction or SerializedWorldState.inductionCounter was NOT correctly captured/restored in Plan 02.
    - Fix in engine.ts captureContinuation() and the restore path in runToHorizon.
    - Re-run until GREEN.

    Commit: `test(20-06): IND-02 continuation-equivalence inductionEnabled case + ALL_ON extension`.
  </behavior>

  <action>
    Read both test files fully. Apply the minimal edit to continuation-equivalence.unit.test.ts (prefer adding inductionEnabled to the "all-on" entry if it exists; otherwise add a new FEATURE_CASES entry). Apply the 1-line ALL_ON extension to continuation-adversarial.unit.test.ts.

    Run: `pnpm build && pnpm typecheck && pnpm vitest run packages/simulation/test/continuation-equivalence.unit.test.ts packages/simulation/test/continuation-adversarial.unit.test.ts`

    If hash mismatch on inductionEnabled case: diagnose by inspecting captureContinuation() in engine.ts (rng.induction capture) and the restore block (restoredRng?.induction). Fix the capture/restore and re-run.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm build && pnpm typecheck && pnpm vitest run packages/simulation/test/continuation-equivalence.unit.test.ts packages/simulation/test/continuation-adversarial.unit.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm build && pnpm typecheck` exits 0.
    - `vitest run packages/simulation/test/continuation-equivalence.unit.test.ts` exits 0 — all cases green including inductionEnabled:true (chunk boundary between arrivals → hash identical to all-at-once).
    - `vitest run packages/simulation/test/continuation-adversarial.unit.test.ts` exits 0 — ALL_ON now includes inductionEnabled:true; all chunk-1 adversarial cases green.
    - `grep -c "inductionEnabled" packages/simulation/test/continuation-equivalence.unit.test.ts` returns ≥ 1.
    - `grep -c "inductionEnabled" packages/simulation/test/continuation-adversarial.unit.test.ts` returns ≥ 1 (in ALL_ON).
    - Scale bound honored: the new induction continuation test runs in < 5 seconds (horizon ≤ 200, chunk ≥ 7). Confirm with `time vitest run packages/simulation/test/continuation-equivalence.unit.test.ts`.
  </acceptance_criteria>

  <done>
    Continuation-equivalence with inductionEnabled: true proves the PRNG state and inductionCounter are correctly captured and restored across chunk boundaries. ALL_ON exercises adversarial chunk-1 coverage. IND-02 fully validated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full phase gate — pnpm test:all + DET regression confirmation</name>
  <files></files>

  <read_first>
    1. .planning/phases/20-external-induction/20-VALIDATION.md — full gate checklist and expected test count.
    2. packages/simulation/test/determinism.unit.test.ts — note the golden SHA256 hash for seed-42/durationTicks:10000; this MUST be unchanged after Phase 20.
  </read_first>

  <behavior>
    Run the full phase gate:
    1. `pnpm build` — turbo build; expect 0 errors.
    2. `pnpm typecheck` — separate TS gate; expect 0 errors (tests + source).
    3. `pnpm lint` — ESLint; expect 0 errors.
    4. `pnpm test:all` — unit + integration + ui; all green.

    During test:all, confirm:
    - `determinism.unit.test.ts` golden hash (`3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861`) is BYTE-IDENTICAL (DET-01 keystone — inductionEnabled absent = flag off = zero induction draws).
    - `induction-determinism.unit.test.ts` all 4 cases green.
    - `fuel-determinism.unit.test.ts` INDUCTION_RNG_SALT pairwise-distinct case green.
    - `hub-inventory.unit.test.ts` PackageInducted → inbound case green.
    - `scope.unit.test.ts` PackageInducted scope classification case green.
    - `continuation-equivalence.unit.test.ts` inductionEnabled case green.

    If any test fails: diagnose and fix before marking done. The full gate must be clean before the manual VIZ-13 checkpoint.
  </behavior>

  <action>
    Run: `pnpm build && pnpm typecheck && pnpm lint && pnpm test:all`

    Record the test count output. Verify the DET-01 golden hash is unchanged in determinism.unit.test.ts output.

    If failures exist: fix the root cause. Do not skip or mark as known-failing.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/jobs/intelliswift && pnpm build && pnpm typecheck && pnpm lint && pnpm test:all</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm build` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm lint` exits 0 (0 errors, 0 warnings or only pre-existing warnings).
    - `pnpm test:all` exits 0 — all tests green; total test count ≥ 1737 (pre-Phase-20 count) + new Phase-20 tests.
    - Seed-42/10000 golden hash unchanged: `3920accc05220b45f79736cc98c9773fa7ffd8df08eb607bdbed2b8c054d6861` (DET-01 regression confirmed).
    - `grep -c "FAIL" <(pnpm test:all 2>&1)` returns 0 (no failing test suites).
  </acceptance_criteria>

  <done>
    Full gate green. All Phase-20 tests green. DET-01 golden unchanged. Ready for VIZ-13 manual confirmation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>VIZ-13 Visual Confirmation: pulsing induction markers on live map</name>

  <what-built>
    inductionLayer with pulsing-circle style (purple "+", radius 14px, white border) added to the OpenLayers map; flashInduction() triggers a timed-feature add/remove on PackageInducted ws messages; snapshots.ts populates TickPayload.inductionEvents from the simulation engine.
  </what-built>

  <how-to-verify>
    1. Start the full app stack: `pnpm dev` (or the equivalent dev command for this project).
    2. Open the operator UI map in a browser.
    3. Confirm the simulation starts and trailers begin moving on the map (existing behavior).
    4. Enable induction: restart the simulation with `inductionEnabled: true` in the config, OR confirm it is already enabled in the dev config. (Check the API's simulation start call — if inductionEnabled is wired to a config option, set it.)
    5. Watch spoke hubs (non-center hubs) on the map for approximately 30–60 simulated minutes (30 ticks at 1 tick/sec wall-clock in fast mode).
    6. Confirm: pulsing circles (purple/violet, "+") appear briefly at spoke hub locations and then disappear after ~2 seconds.
    7. Confirm: markers appear at spokes, NOT at the center hub.
    8. Confirm: markers appear on a repeating schedule (approximately every 30 sim-ticks).
    9. If the marker appears but doesn't disappear: check flashInduction setTimeout wiring.
    10. If no markers appear: check WsProvider/ws consumer is calling flashInduction() on inductionEvents; check snapshots.ts is populating inductionEvents; check that inductionEnabled: true is set.
  </how-to-verify>

  <resume-signal>
    Type "approved" if pulsing markers appear correctly at spoke hubs and disappear after ~2s.
    Or describe any issues observed (e.g., "markers appear at center hub", "markers don't disappear", "no markers visible") and the plan will diagnose and fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → simulation engine | Tests run in-process with simulation engine; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Information Disclosure | Full gate output | accept | Test output is developer-internal; no credentials or secrets in test output. |

Phase 20 Plan 06 makes no changes to production code beyond test files. No high-severity security threats.
</threat_model>

<verification>
After this plan — Phase 20 complete:
- Continuation equivalence with inductionEnabled: true → BYTE-IDENTICAL chunked vs all-at-once
- ALL_ON includes inductionEnabled: true (adversarial coverage)
- Full gate: `pnpm build && pnpm typecheck && pnpm lint && pnpm test:all` all green
- DET-01 golden hash unchanged
- VIZ-13 manual: pulsing induction markers at spoke hubs confirmed
</verification>

<success_criteria>
1. continuation-equivalence inductionEnabled case green (chunk boundary between arrivals → hash identical)
2. ALL_ON in adversarial test includes inductionEnabled: true (1-line change)
3. Full gate `pnpm build && pnpm typecheck && pnpm lint && pnpm test:all` exits 0
4. Seed-42 determinism golden UNCHANGED (DET-01 keystone preserved)
5. VIZ-13 visual checkpoint: pulsing markers at spoke hubs approved
</success_criteria>

<output>
After completion, create `.planning/phases/20-external-induction/20-06-SUMMARY.md`
</output>
