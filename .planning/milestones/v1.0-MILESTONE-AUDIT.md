---
milestone: v1.0
audited: 2026-06-19
method: adversarial code-grounded (10-agent workflow — per-phase extraction + integration + live-wiring verification + dark-code sweep)
status: passed
scores:
  requirements: 42/48 satisfied (2 unsatisfied, 4 partial)
  phases: 5/5 review-passed (with live-wiring caveats now exposed)
  integration: 10/13 flows complete (1 broken, 2 partial)
  flows: core spine end-to-end WORKS; KPI/exception-feed/min-cost-flow surfaces have dark spots
gaps:
  requirements:
    - id: "OPT-02"
      status: "unsatisfied"
      phase: "Phase 4 (live-wired claim: Phase 5)"
      verification_status: "passed (per review) — but code-grounded DARK"
      evidence: "min-cost-flow (assignFreight/minCostFlow, packages/optimizer/src/flow/assign-freight.ts:40) is implemented + glpk.js-oracle-validated but NEVER called on the live path. runEpoch (packages/optimizer/src/rolling/epoch.ts:364) assigns freight via routeTrailers (VRPTW) + scorePlan only. The optimizer barrel does not export assignFreight; live-loop.ts:23 + live-loop.test.ts:371 comments claiming runEpoch 'calls assignFreight internally' are FALSE. Requirement text is 'assigns freight blocks to route legs via min-cost flow' — the live demo does not."
    - id: "SNS-05"
      status: "unsatisfied (live) / detection-complete (unit)"
      phase: "Phase 3 (deferral to Phase 5 never closed)"
      verification_status: "passed (per review, asterisked) — code-grounded DARK_OR_STUB"
      evidence: "detectMissedUnload predicate is correct + wired (packages/sensor-fusion/src/detection.ts:199-226; detector.ts:106-110,254-272), but the sim engine is structurally incapable of producing a missed-unload: every TrailerDeparted.fromHubId=center (engine.ts:282), every package.destHubId=spoke (engine.ts:227/237), so the gate dest!==departed is ALWAYS true → zero candidates. On spoke arrival every package is unconditionally unloaded (engine.ts:330-344) — no over-carry path. No SIM-04 scenario knob injects a spoke-origin departure with a still-aboard package. NO live/scenario test asserts a MissedUnloadDetected event fires (live-demo.int.test.ts:254 only checks missedUnloadCount is typeof number, never >0). In-code comment detection-config.ts:89-90 still documents the gap honestly."
  integration:
    - flow: "Optimizer min-cost-flow freight->route-leg assignment (OPT-02)"
      status: "broken"
      breaks_at: "runEpoch never calls assignFreight/minCostFlow; live epoch uses VRPTW only. Solver orphaned from any caller outside flow/*.ts + tests."
      reqs: ["OPT-02"]
    - flow: "ws snapshot/tick -> KpiDashboard live KPI updates (UI-03)"
      status: "broken"
      breaks_at: "buildSnapshotPayload hardcodes kpis: ZEROED_KPIS on every snapshot AND tick (packages/api/src/ws/snapshots.ts:85-96,308,461); diffTick never emits a kpis delta; the first zeroed snapshot clobbers the REST-fetched live KPIs via applyKpiPartial (0 is a defined value, 0 ?? prev = 0). Net: live KPI panel shows zeros after the first snapshot."
      reqs: ["UI-03", "VIZ-04"]
    - flow: "ws exception feed -> AlertFeed rendering (UI-01)"
      status: "broken (rendering)"
      breaks_at: "snapshots.ts:292-301 blind-casts projection kind (wrong-trailer/missed-unload) + severity (info/warning/critical) onto the wire enum (wrongTrailer/missedUnload, low/med/high) with NO value mapping. The live sim fires real wrong-trailer exceptions → AlertFeed kindLabel returns undefined (blank label) + severityClass yields a non-existent 'info' CSS class."
      reqs: ["UI-01"]
    - flow: "GET /kpis on-time departure/arrival (UI-03)"
      status: "partial"
      breaks_at: "onTimeDeparture/Arrival counts hardcoded 0 (packages/api/src/routes/kpis.ts:114-117); computeKpis returns default 1.0 when denominator is 0, so on-time KPIs ALWAYS show 100%. Keystone gate (c) passes precisely because of this default — masking missing wiring."
      reqs: ["UI-03"]
    - flow: "Event-triggered re-opt via POST /scenario (OPT-05)"
      status: "partial"
      breaks_at: "Periodic per-tick re-opt is live + confirmed. Event-triggered scenario path returned HTTP 500 (TypeError: applyScenario is not a function, driver.ts:518) due to a STALE @mm/simulation dist that omits the applyScenario export. tsc -b rebuild fixes it (all 5 keystone subtests pass). dist/ is gitignored (clean clone + turbo build is correct), BUT root pnpm test/test:all run `vitest run` with NO build prerequisite (turbo's test task has dependsOn ^build; the root scripts bypass it), so the documented test command can run against stale dist and the keystone gate is not reliably green."
      reqs: ["OPT-05"]
    - flow: "GET /api/packages/:id/history (UI-02 package timeline)"
      status: "broken"
      breaks_at: "Route never registered server-side (only /trailers/:id/history exists). packages/web/src/api/client.ts fetchPackageHistory targets it and returns a network error → UI-02 package-history view non-functional (trailer history works)."
      reqs: ["UI-02"]
    - flow: "Web<->server end-to-end (Playwright)"
      status: "partial"
      breaks_at: "All Playwright specs stub /api/* + ws with fixtures (page.route + routeWebSocket). The real web->Fastify->projection contract is never exercised E2E; only the API-internal live-demo.int.test.ts drives the server chain (it does not load the web bundle)."
      reqs: ["VIZ-01", "UI-01", "UI-03"]
tech_debt:
  - phase: "01-foundation"
    items:
      - "L-1..L-10 (01-REVIEW.md): unsound data-trailer-uid probe; O(N) per-event projection rewrite; transcendental route geometry (byte-identical caveat); concurrent-append global_seq nondeterminism (safe today); batched-append domain-time collapse; geo-track keyframe drop; no global error handler; wildcard CORS; ws hubs array ignored by client; FND-04 purity proven behaviorally not by static scan"
      - "VIZ-01 manual animation-smoothness visual check NOT YET PERFORMED (manual-by-design)"
  - phase: "02-load-planning"
    items:
      - "L4 keyId separator-collision not enforced (safe debt); L5 plan-load.ts docstring describes an unreachable SOFT/partial-LIFO branch (dead-doc); per-slice capacityWeight placeholder (maxBlockVolume*100); off-route-hub edge semantics untested; POST /plan envelope not .strict(); POST /plan rationale prose not human-eyeballed"
  - phase: "03-rfid"
    items:
      - "SNS-05 live-firing (see gaps); asDistribution zero-fill; golden-replay keystone doesn't assert exceptions/KPI; engine<->detector calibration not contract-tested; readDepartedHubs MVP inference overridden by live driver"
  - phase: "04-optimizer"
    items:
      - "(epoch,scopeHash) idempotency in-memory only (no DB durability across restart); scope completeness — package events scope hub but not trailers loaded there (under-scoping); frozen trailers surfaced feasible:false/cost:0 (cosmetic, frozen flag authoritative)"
  - phase: "05-sim-viz"
    items:
      - "UI-04 money slide = calibrated 2-metric seed-42 before/after (ACCEPTED MVP; full live 8-metric A/B = v2); package-history route stub (see gaps); ws seq is drop-detector only; route slaRiskBucket hardcoded 0; trailer keyframe state hardcoded onTime; utilization = package-count proxy not fill ratio; panel .tsx tests excluded by root vitest glob (run only via per-package script)"
nyquist:
  compliant_phases: 0
  partial_phases: 4   # 01,02,03,04 have VALIDATION.md but frontmatter nyquist_compliant:false (stale/never finalized; underlying keystone tests exist + pass)
  missing_phases: 1   # 05 has no VALIDATION.md
  overall: "bookkeeping-incomplete — all VALIDATION.md flags read non-compliant despite the keystone tests they gate existing + passing; 05 has no VALIDATION.md at all. This is doc reconciliation, not a coverage gap (per 01/02-VERIFICATION.md)."
---

# Milestone v1.0 — Adversarial Code-Grounded Audit

**Method:** 10-agent workflow. Per-phase artifact extraction (SUMMARY + REVIEW + VERIFICATION + VALIDATION) ×5, cross-phase integration check (gsd-integration-checker over the `live-demo.int.test.ts` keystone), targeted live-wiring verification of the at-risk requirements (SNS-05, OPT-02/05/06/07, VIZ-02/03/04 + UI-03/04), and a source-tree dark-code/stub sweep.

**Headline:** the milestone's **core value is real and live** — sim → event store → projections → RFID detection → rolling optimizer (VRPTW + LIFO rehandle + selectPlan + localRepair) → PlanGenerated/PlanAccepted → audit-timeline → read APIs, with a real Postgres-backed end-to-end keystone test. **But the "all 48 complete / clean ship" claim does not survive code-grounding.** Six requirements are dark or partial on the live path, all behind a green 827-test suite — the classic happy-path/default-value masking the project's own handoff warned about.

## Requirements Coverage (3-source cross-reference)

| Source | Result |
|---|---|
| REQUIREMENTS.md traceability | 48/48 marked Complete (SNS-05*, UI-04† asterisked) |
| SUMMARY frontmatter (`requirements-completed`) | 48/48 listed across 34 plan SUMMARYs |
| VERIFICATION.md | only Phase 1 (11/11) + Phase 2 (14/14) have one; 03/04/05 rely on REVIEW.md |
| **Code-grounded verification** | **42 satisfied · 2 unsatisfied (OPT-02, SNS-05) · 4 partial (OPT-05, UI-01, UI-02, UI-03)** |

### What's genuinely live-confirmed (strongest)
- **VIZ-02/03/04** — real postrender route tween, state-driven STYLE_CACHE coloring from live projection buckets, ws snapshot+per-tick-delta over a single shared socket. Confirmed against code + the integration keystone.
- **UI-04 money slide** — runs the REAL FIFO baseline + LIFO optimizer on the same seed-42 blocks through the same scorePlan gate; deltas are COMPUTED (not hardcoded). Calibrated seed-frozen, honestly self-labeled — accepted MVP scope.
- **OPT-06 / OPT-07** — freeze window (15min) + scopeHash idempotency wired into the live loop; `GET /optimizer/recommendations` serves real `localRepair` output from the live infeasible-trailer path.

### Unsatisfied / partial on the live path (the gaps)
See the `gaps` block in the frontmatter for file:line evidence. Summary:

| REQ | Verdict | One-line |
|---|---|---|
| **OPT-02** | unsatisfied | min-cost-flow built + oracle-validated but **never called** by `runEpoch` (live uses VRPTW); barrel doesn't even export it; in-code comments claiming otherwise are false |
| **SNS-05** | unsatisfied (live) | predicate correct + wired, but sim **cannot produce** a missed-unload and no scenario knob/test fires one — deferral was never closed |
| **UI-01** | partial | ws exception enum **blind-cast** → real wrong-trailer alerts render blank label + broken severity class |
| **UI-03** | partial | on-time KPIs **hardcoded 0 → always 100%**; ws KPIs permanently ZEROED + **clobber** the live REST values |
| **UI-02** | partial | package-history route **never registered** → `fetchPackageHistory` errors (trailer history works) |
| **OPT-05** | partial | periodic re-opt live ✓; event-triggered `POST /scenario` 500s on a **stale dist** because root test scripts skip the build |

## Phase Verifications

| Phase | Review verdict | VERIFICATION.md | VALIDATION.md (Nyquist) | Notes |
|---|---|---|---|---|
| 1 | passed (0 HIGH, 6 MED resolved, 10 LOW) | ✅ 11/11 | ⚠️ non-compliant flag (stale) | VIZ-01 manual visual check pending |
| 2 | passed (M1+L1 resolved, 14/14) | ✅ 14/14 | ⚠️ non-compliant flag (stale) | rationale prose not human-eyeballed |
| 3 | passed (HIGH A + MED D fixed; MED-1 deferred) | ❌ none | ⚠️ non-compliant flag | SNS-05 live-firing deferred + never closed |
| 4 | passed (oracle keystone exact, 447 tests) | ❌ none | ⚠️ non-compliant flag | OPT-02 live-wiring deferred to P5; **not delivered** |
| 5 | passed (review-fixed, live-demo keystone) | ❌ none | ❌ no VALIDATION.md | KPI/alert/package-history dark spots |

## Cross-Phase Integration

10/13 flows complete. The core operational spine is genuinely wired end-to-end (verified against `packages/api/test/live-demo.int.test.ts`, which drives a real Postgres testcontainer over 120 ticks). **Broken:** OPT-02 min-cost-flow (orphaned), ws→KpiDashboard live updates (zeroed+clobber), ws→AlertFeed rendering (enum blind-cast). **Partial:** on-time KPIs (stubbed), POST /scenario event-trigger (stale-dist 500), web↔server E2E (all Playwright boundaries stubbed).

> **Why the green suite missed all this:** the keystone gate (c) asserts non-zero KPIs but is satisfied by the `computeKpis` default-1.0 branch; gate for missed-unload only checks `typeof number`; e2e stubs every server boundary. Happy-path + default-value assertions masked missing wiring — the exact adversarial failure mode the project handoff flagged.

## Verdict & Routing

**`gaps_found`.** The milestone is **released** (`main` @ `ba65143`, tag `v1.0.0`) and its core value works, but it is **not** the clean 48/48 the traceability claims. Recommended: fix the well-scoped live-path bugs on `develop` (toward a v1.0.1 / v1.1), and treat OPT-02 live-wiring + SNS-05 sim modeling + real web↔server E2E as scoped closure work. Do **not** archive/complete the milestone as "clean" until at least the demo-honesty bugs (UI-01 alert rendering, UI-03 KPI zeroing, package-history) are closed.

*Generated 2026-06-19 by the code-grounded milestone audit workflow (`wf_68dda576-507`).*

---

## ✅ Resolution — 2026-06-19 (audit-fix campaign)

All gaps fixed on `feature/v1.0-audit-fixes` (TDD; rival-judged where warranted), verified by a full gate: `pnpm lint` **0** · `pnpm typecheck` **0** · `turbo build` **10/10** · **872 unit+integration tests / 98 files green**.

| Gap | Resolution | Commit |
|---|---|---|
| UI-01 ws enum blind-cast | exhaustive projection→wire mappers | `ca97190` |
| UI-03 ws KPI zeroed clobber | REST source-of-truth + refetch-on-tick | `9dd1860` |
| UI-03 on-time fake 100% / utilization proxy | honest `null` on-time + real volume fill | `20700ed` |
| OPT-02 min-cost-flow not live | freight stage in `runEpoch` (rival-judged; loser had a capacity-corruption bug) | `d5b2ad1` |
| F-05 stale-dist test trap | root test scripts build-first | `566448f` |
| UI-02 package-history | route already existed (queries.ts); removed F-04 duplicate, enhanced canonical route with recommendation | `9f3f7cf` |
| F-08 real web↔server e2e | new `chromium-real` Playwright keystone (the F-01 catcher) | `de37623` |
| SNS-05 missed-unload dark | seeded over-carry → fires live (rate 0.15) | `8235650` |
| VIZ-03 route SLA-risk + trailer coloring | driven from the real exception signal | `ee48d83` |

**Quality follow-ups (user-directed):** `pnpm lint` 176→0 strict (test files included, no suppressions); `eslint.config.js`→`.ts` (TypeScript everywhere); 24 hidden tsc fixture errors fixed; added `typecheck` + `check` gates so test files always get lint **and** type-checking.

**Corrections to this audit's own findings (verified in code during fixes):** UI-02 was NOT unregistered — `queries.ts` has owned it since v1.0 base (the audit trusted a stale 05-07 "Known Stub" note). The OPT-02 barrel DID already export `assignFreight`.

**Status: `gaps_found` → resolved.** Two regressions introduced mid-campaign (F-04 duplicate route, F-03 int-test on-time contract) were caught by the full integration gate and fixed before merge.
