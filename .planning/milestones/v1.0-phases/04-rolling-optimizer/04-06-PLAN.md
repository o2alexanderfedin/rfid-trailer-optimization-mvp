---
phase: 04-rolling-optimizer
plan: 06
type: tdd
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - packages/optimizer/src/rolling/index.ts
  - packages/optimizer/src/rolling/types.ts
  - packages/optimizer/src/rolling/scope.ts
  - packages/optimizer/src/rolling/scope.test.ts
  - packages/optimizer/src/rolling/twin.ts
  - packages/optimizer/src/rolling/twin.test.ts
  - packages/optimizer/src/rolling/freeze-idempotency.ts
  - packages/optimizer/src/rolling/freeze-idempotency.test.ts
  - packages/optimizer/src/rolling/epoch.ts
  - packages/optimizer/src/rolling/epoch.test.ts
  - packages/api/src/optimizer/rolling-service.ts
  - packages/api/src/routes/optimizer.ts
  - packages/api/test/optimizer-rolling.int.test.ts
autonomous: true
requirements: [OPT-04, OPT-05, OPT-06]
must_haves:
  truths:
    - "Each rolling epoch is SCOPED to only the affected hubs/trailers/blocks via detectAffectedScope(events) — not the whole network"
    - "The optimizer evaluates candidates on a structuredClone planning-twin sandbox: running it emits NO events and mutates NO projection until accept"
    - "On accept, exactly ONE PlanAccepted event is appended (the only operational side effect); a PlanGenerated is recorded for the candidate"
    - "KEYSTONE freeze+idempotency: identical (epochId, scopeHash) input ⇒ byte-identical plan; trailers departing within the freeze window are untouched across epochs"
    - "The epoch clock comes from sim/event time, never Date.now(); no Math.random in the loop"
  artifacts:
    - path: "packages/optimizer/src/rolling/epoch.ts"
      provides: "runEpoch: readNew→updateTwin→scope→buildInput→optimize→validate/repair→select→(maybe)accept"
      exports: ["runEpoch"]
    - path: "packages/optimizer/src/rolling/twin.ts"
      provides: "structuredClone sandbox of the affected scope; no side effects until accept"
      exports: ["buildTwin"]
    - path: "packages/optimizer/src/rolling/freeze-idempotency.ts"
      provides: "scopeHash + (epochId,scopeHash) memoization + freeze-window filter"
      exports: ["scopeHash", "isFrozen"]
    - path: "packages/api/src/optimizer/rolling-service.ts"
      provides: "stateful shell running the epoch loop + emitting PlanGenerated/PlanAccepted"
      exports: ["RollingOptimizerService"]
    - path: "packages/api/src/routes/optimizer.ts"
      provides: "endpoint exposing recommendations + plan + objective breakdown"
  key_links:
    - from: "packages/optimizer/src/rolling/epoch.ts"
      to: "packages/optimizer/src/rolling/twin.ts"
      via: "optimizes over the cloned twin, no projection writes during evaluation"
      pattern: "buildTwin"
    - from: "packages/api/src/optimizer/rolling-service.ts"
      to: "@mm/event-store append"
      via: "appends exactly one PlanAccepted on accept (optimistic-concurrency writer)"
      pattern: "PlanAccepted"
    - from: "packages/optimizer/src/rolling/epoch.ts"
      to: "packages/optimizer/src/rolling/freeze-idempotency.ts"
      via: "memoizes per (epochId, scopeHash); skips freeze-window trailers"
      pattern: "scopeHash"
---

<objective>
Build the **stateful rolling shell** that ties the pure algorithms together (OPT-04/05/06): a scoped rolling-horizon epoch loop running over a **structuredClone planning-twin sandbox** with NO operational side effects until accept (then ONE `PlanAccepted`), honoring **freeze windows** and **(epochId, scopeHash) idempotency** (the anti-thrash keystone). A thin `@mm/api` rolling service runs the loop on a demo stream and exposes recommendations + plan + objective breakdown; an integration test on the shared Postgres proves the twin sandbox and idempotency.

Purpose: This is the only stateful, non-pure part. It must keep the Phase-1 determinism + optimistic-concurrency invariants (the optimizer is a concurrent event-store writer), defend P7 (thrash) via freeze + idempotency, and prove OPT-04's zero-side-effect guarantee against a real store.
Output: `runEpoch` + twin + freeze/idempotency in `@mm/optimizer`, a `RollingOptimizerService` + endpoint in `@mm/api`, and the twin-sandbox + idempotency integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rolling-optimizer/04-CONTEXT.md
@.planning/phases/04-rolling-optimizer/04-RESEARCH.md
@.planning/phases/04-rolling-optimizer/04-VALIDATION.md

<read_first>
- packages/optimizer/src/index.ts (Plans 02-05) — `buildTimeExpandedGraph`, `assignFreight`, `routeTrailers`, `objective`, `selectPlan`, `localRepair` + their types. runEpoch composes these. Do NOT reimplement any of them.
- packages/domain/src/events/index.ts (Plan 01) — `PlanGenerated`, `PlanAccepted`, `validateEvent`. The service appends these.
- packages/event-store/src/index.ts — `createDb`, `migrate`, the append API + `ConcurrencyError` (the optimizer is a concurrent writer; retry-on-conflict per Phase 1).
- packages/projections — the twin input (load blocks, trailers, routes, hub state). The twin clones a read of these; evaluation must NOT write them.
- packages/api/test/pg-fixture.ts — the `MM_PG_URL` shared-Postgres per-run-db fixture (precedence: MM_PG_URL → DATABASE_URL → testcontainer). COPY this pattern for optimizer-rolling.int.test.ts (`*.int.test.ts` runs only under `pnpm test:all`).
- packages/api/src/routes/plan.ts + app.ts — the Fastify route + app registration pattern to mirror for routes/optimizer.ts.
</read_first>

<interfaces>
src/rolling/types.ts:
```
export interface Epoch { readonly epochId: string; readonly nowMin: number; readonly freezeWindowMin: number; } // nowMin from sim/event time, NEVER Date.now
export interface EpochInput { readonly events: readonly DomainEvent[]; readonly twinSnapshot: TwinSnapshot; }
export interface EpochResult { readonly epochId: string; readonly scopeHash: string; readonly generated: PlanGenerated["payload"] | null; readonly accepted: PlanAccepted["payload"] | null; readonly recommendations: readonly Recommendation[]; }
export function detectAffectedScope(events: readonly DomainEvent[]): OptimizerScope;
export function runEpoch(epoch: Epoch, input: EpochInput, weights: ObjectiveWeights): EpochResult; // PURE given inputs
```
src/rolling/freeze-idempotency.ts:
```
export function scopeHash(scope: OptimizerScope, twinSnapshot: TwinSnapshot): string; // stable hash of canonical-serialized input
export function isFrozen(trailerDepartureMin: number, epoch: Epoch): boolean; // departure within freezeWindow ⇒ frozen
```
RollingOptimizerService (api, the ONLY stateful part): reads new events, calls runEpoch, and on `accepted != null` appends ONE PlanAccepted (+ records PlanGenerated) via the event store with ConcurrencyError retry. Memoize EpochResult by `${epochId}:${scopeHash}` so identical input ⇒ identical plan with no recompute.
Endpoint: `GET /optimizer/recommendations` → latest EpochResult (plan + objective breakdown + recommendations).
</interfaces>
</context>

<feature>
  <name>Scoped rolling epoch + twin sandbox + freeze/idempotency + service (OPT-04/05/06)</name>
  <files>packages/optimizer/src/rolling/scope.ts, packages/optimizer/src/rolling/twin.ts, packages/optimizer/src/rolling/freeze-idempotency.ts, packages/optimizer/src/rolling/epoch.ts, packages/api/src/optimizer/rolling-service.ts</files>
  <behavior>
    scope (OPT-05):
    - detectAffectedScope(events) returns only the hubs/trailers/blocks referenced by the events — a network with 10 hubs but events touching 2 yields a scope of those 2.
    twin sandbox (OPT-04):
    - buildTwin(scope, snapshot) returns a structuredClone; mutating the twin does NOT mutate the source snapshot.
    - runEpoch over the twin returns generated/accepted payloads but performs NO IO (pure) — proven by it taking data in and returning data out, no store handle.
    freeze + idempotency (OPT-06 KEYSTONE):
    - Two runEpoch calls with identical (epoch, input) return byte-identical EpochResult (deep-equal incl. scopeHash) — no Date.now/Math.random anywhere in the path.
    - isFrozen is true for a trailer departing within freezeWindowMin; runEpoch never changes a frozen trailer's plan across epochs.
    integration (OPT-04 on shared PG):
    - Running the RollingOptimizerService evaluation phase over a seeded twin appends NO events and changes NO projection rows until accept; on accept exactly ONE PlanAccepted (+ the PlanGenerated record) is appended; event count delta == expected.
    - Idempotent service: feeding the same epoch/scope twice appends the plan once (memoized), no duplicate PlanAccepted.
  </behavior>
  <implementation>
    GREEN: `detectAffectedScope` collects referenced ids from events. `buildTwin` structuredClones the scoped snapshot. `runEpoch` = updateTwin → detectAffectedScope → buildInput (graph from Plan 02, freight via Plan 03, routes via Plan 04) → validate/repair (Plan 05 localRepair) → selectPlan (Plan 05) → emit PlanGenerated payload, and if the selected plan is feasible + better than previous (objective incl. churn), emit a PlanAccepted payload; otherwise accepted=null. `scopeHash` canonical-serializes the scope+snapshot to a stable string hash (sorted keys; integer values). `isFrozen` compares departure vs freeze window. The PURE core takes nowMin/epochId as inputs (never Date.now). RollingOptimizerService (api) is the ONLY stateful shell: it reads new events from the store, calls runEpoch, memoizes by `${epochId}:${scopeHash}`, and on accept appends ONE PlanAccepted via the event store with ConcurrencyError retry (Phase-1 optimistic concurrency). routes/optimizer.ts exposes the latest EpochResult.
    REFACTOR: extract buildInput composition into a named helper; keep runEpoch a readable pipeline.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD scoped detection + structuredClone twin sandbox (OPT-04/05)</name>
  <files>packages/optimizer/src/rolling/types.ts, packages/optimizer/src/rolling/scope.ts, packages/optimizer/src/rolling/scope.test.ts, packages/optimizer/src/rolling/twin.ts, packages/optimizer/src/rolling/twin.test.ts</files>
  <action>Define rolling/types.ts contracts. RED: scope.test.ts (detectAffectedScope returns only referenced hubs/trailers/blocks, not the whole network) + twin.test.ts (buildTwin structuredClone; mutating twin leaves source unchanged) — commit `test(04-06): failing scope + twin tests`. GREEN: implement detectAffectedScope + buildTwin (commit `feat(04-06): scoped twin sandbox`). REFACTOR.</action>
  <verify>
    <automated>pnpm --filter @mm/optimizer build && pnpm vitest run packages/optimizer/src/rolling/scope.test.ts packages/optimizer/src/rolling/twin.test.ts</automated>
  </verify>
  <done>Scope is affected-only; twin is an isolated structuredClone; source snapshot never mutated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KEYSTONE — TDD freeze window + (epochId,scopeHash) idempotency + runEpoch (OPT-06/05)</name>
  <files>packages/optimizer/src/rolling/freeze-idempotency.ts, packages/optimizer/src/rolling/freeze-idempotency.test.ts, packages/optimizer/src/rolling/epoch.ts, packages/optimizer/src/rolling/epoch.test.ts, packages/optimizer/src/rolling/index.ts</files>
  <action>RED: freeze-idempotency.test.ts (THE keystone: identical (epoch,input) ⇒ byte-identical EpochResult incl. scopeHash; frozen trailer untouched across epochs) + epoch.test.ts (runEpoch composes Plans 02-05, pure, deterministic) — commit `test(04-06): failing freeze+idempotency+epoch tests`. GREEN: implement scopeHash + isFrozen + runEpoch composing the Wave-2 algorithms (commit `feat(04-06): rolling epoch + freeze + idempotency`). Assert no `Date.now`/`Math.random` in src (grep gate). Export runEpoch + detectAffectedScope + scopeHash + isFrozen from src/rolling/index.ts (the rolling barrel; the root re-exports it). REFACTOR.</action>
  <verify>
    <automated>pnpm --filter @mm/optimizer build && pnpm vitest run packages/optimizer/src/rolling/freeze-idempotency.test.ts packages/optimizer/src/rolling/epoch.test.ts && ! grep -rnE "Date\.now|Math\.random" packages/optimizer/src --include=*.ts | grep -v "\.test\.ts"</automated>
  </verify>
  <done>KEYSTONE: identical input ⇒ byte-identical plan; frozen trailers untouched; runEpoch pure/deterministic; zero Date.now/Math.random in optimizer src (grep clean).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RollingOptimizerService + endpoint + twin-sandbox/idempotency integration test (OPT-04 on shared PG)</name>
  <files>packages/api/src/optimizer/rolling-service.ts, packages/api/src/routes/optimizer.ts, packages/api/test/optimizer-rolling.int.test.ts</files>
  <action>Implement RollingOptimizerService (stateful shell): read new events, call runEpoch, memoize by `${epochId}:${scopeHash}`, and on accept append ONE PlanAccepted (+ PlanGenerated record) via @mm/event-store with ConcurrencyError retry. Register routes/optimizer.ts (`GET /optimizer/recommendations` → latest EpochResult with objective breakdown + recommendations) in app.ts. RED→GREEN: write optimizer-rolling.int.test.ts using the MM_PG_URL pg-fixture pattern proving (a) evaluating candidates over the twin appends NO events / mutates NO projections until accept, (b) on accept exactly ONE PlanAccepted is appended, (c) feeding identical (epoch,scope) twice appends the plan once (memoized, no duplicate). Add `@mm/optimizer` to @mm/api dependencies.</action>
  <verify>
    <automated>MM_PG_URL=postgres://mm:mm@localhost:5432/postgres pnpm install && pnpm -r build && MM_PG_URL=postgres://mm:mm@localhost:5432/postgres pnpm vitest run packages/api/test/optimizer-rolling.int.test.ts</automated>
  </verify>
  <done>Evaluation has zero side effects until accept; accept appends exactly one PlanAccepted; service is idempotent per (epoch,scope); endpoint returns the latest plan + objective breakdown + recommendations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rolling service→event store | The optimizer is a concurrent writer appending PlanAccepted across the optimistic-concurrency guard |
| event stream→epoch loop | New events drive scope detection + twin updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | premature side effects | mitigate | OPT-04: evaluation runs only on a structuredClone twin; NO store/projection write until accept (proven by integration test on shared PG) |
| T-04-13 | Denial of service | plan thrashing (P7) | mitigate | freeze window + (epochId,scopeHash) idempotency + churn penalty + deterministic id tie-breaks; identical input ⇒ identical plan (keystone) |
| T-04-14 | Tampering | concurrent-write race | mitigate | reuse Phase-1 UNIQUE(stream_id,version) optimistic concurrency + ConcurrencyError retry; optimizer re-verified as a concurrent writer |
| T-04-15 | Tampering | non-determinism via clock/RNG | mitigate | epoch clock from sim/event time; grep-gate asserts no Date.now/Math.random in optimizer src |
</threat_model>

<verification>
- `pnpm vitest run packages/optimizer/src/rolling` green (scope, twin, freeze/idempotency, epoch).
- KEYSTONE: identical (epoch,scope) ⇒ byte-identical plan; frozen trailers untouched.
- No Date.now/Math.random in optimizer src (grep clean).
- `MM_PG_URL=... pnpm vitest run packages/api/test/optimizer-rolling.int.test.ts` green: zero side effects until accept; exactly one PlanAccepted on accept; idempotent service.
- `pnpm -r build` green.
</verification>

<success_criteria>
A scoped rolling-horizon loop evaluates candidates on a structuredClone twin with zero side effects until accept (then one PlanAccepted), honors freeze windows, and is idempotent per (epochId, scopeHash) — proven against the shared Postgres — exposed via an API endpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rolling-optimizer/04-06-SUMMARY.md`
</output>
