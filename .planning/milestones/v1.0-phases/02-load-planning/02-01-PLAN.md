---
phase: 02-load-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/entities/index.ts
  - packages/domain/src/planning/index.ts
  - packages/domain/src/index.ts
autonomous: true
requirements: [AGG-01, AGG-02, LOAD-01]
must_haves:
  truths:
    - "Downstream packages can import a LoadBlock type that carries the 7-part block key, aggregate volume/weight/count, and a priority"
    - "Downstream packages can import a TrailerSlice type modelling depth (0=rear) with used volume/weight and placed block ids"
    - "The pure modules can consume a plain PlanningPackage input carrying sla/deadline/handling/size/volume without reading the event store"
    - "The closed Phase-1 DomainEvent union and its contract test still pass unchanged"
  artifacts:
    - path: "packages/domain/src/planning/index.ts"
      provides: "Phase-2 planning value types: PlanningPackage, SlaClass, HandlingClass, DeadlineBucket, SizeWeightClass, RouteStop, PlannerConfig + zod schemas"
      contains: "planningPackageSchema"
    - path: "packages/domain/src/entities/index.ts"
      provides: "Fleshed-out LoadBlock + TrailerSlice schemas (key, aggregates, priority; depth/used/blocks)"
      contains: "blockKeySchema"
  key_links:
    - from: "packages/domain/src/index.ts"
      to: "packages/domain/src/planning/index.ts"
      via: "re-export of planning types + schemas"
      pattern: "export.*planning"
---

<objective>
Flesh out the Phase-2 domain vocabulary that both pure modules build against: enrich the
`LoadBlock` and `TrailerSlice` stubs into full Phase-2 shapes, and add the Phase-2 **planning
value types** (`PlanningPackage`, `SlaClass`, `HandlingClass`, `DeadlineBucket`,
`SizeWeightClass`, `RouteStop`, and the `PlannerConfig` defaults object). These are the contracts
`@mm/aggregation` and `@mm/load-planner` consume — defined first so executors build against them,
not against a moving target.

Purpose: `@mm/domain` is the zero-(workspace-)dependency leaf every package imports. Defining the
Phase-2 types here (interface-first) removes the "scavenger hunt" for the two new pure packages and
keeps the LIFO depth/unload-order vocabulary single-sourced. The current `Package` entity has no
SLA/deadline/handling/volume fields, so aggregation needs a richer planning-input view; the
`LoadBlock`/`TrailerSlice` stubs are intentionally minimal and get filled in here.

Output: extended `entities/index.ts`, a new `planning/index.ts`, and updated `index.ts` re-exports
— all pure zod schemas + inferred types, only `zod` as a runtime dep. The Phase-1 closed event union
and its contract assertion remain untouched and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-load-planning/02-CONTEXT.md
@.planning/phases/02-load-planning/02-RESEARCH.md

<read_first>
@packages/domain/src/entities/index.ts
@packages/domain/src/index.ts
@packages/domain/src/events/domain-event.ts
</read_first>

<interfaces>
<!-- Established @mm/domain conventions the executor MUST match. Extracted from the codebase. -->
<!-- Entities are zod schemas; the inferred TS type is the single source of truth (DRY). -->

From packages/domain/src/entities/index.ts (existing helpers to REUSE, do not redefine):
- `const id = z.string().min(1)` — non-empty identifier
- `sizeClassSchema = z.enum(["small","medium","large"])`, `SizeClass`
- `packageSchema` (packageId, originHubId, destHubId, sizeClass, weight) — leave intact; add the
  planning view separately rather than mutating this Phase-1 shape
- Current STUB to REPLACE:
  `loadBlockSchema = z.object({ loadBlockId: id, packageIds: z.array(id) })`
  `trailerSliceSchema = z.object({ index: z.number().int().nonnegative(), loadBlockIds: z.array(id) })`

From packages/domain/src/index.ts:
- Existing barrel re-exports every entity type + schema. The closed `DomainEvent` union and
  `assertNever`/`PHASE1_SCHEMA_VERSION` exports MUST stay exactly as-is.

tsconfig (packages/domain/tsconfig.json): `rootDir: src`, NodeNext, `verbatimModuleSyntax`,
`exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`. Use `.js` extensions on relative imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase-2 planning value types (planning/index.ts)</name>
  <files>packages/domain/src/planning/index.ts, packages/domain/src/planning/planning.test.ts</files>
  <behavior>
    - `slaClassSchema` parses the closed enum (e.g. `["express","priority","standard","economy"]`); each class carries a stable integer `slaClassWeight` (higher = more urgent) exposed via a `SLA_CLASS_WEIGHT` record so AGG-04 priority is deterministic and single-sourced.
    - `handlingClassSchema` parses a closed enum incl. at least `["standard","fragile","heavy"]` (drives the AGG-03 fragile⊄heavy incompatibility rule).
    - `sizeWeightClassSchema` parses a coarse closed enum.
    - `deadlineBucketSchema` is a non-negative integer bucket (coarse time bucket); `PlanningPackage` carries a numeric `deadline` (ms since a fixed epoch, from event/payload timestamps — NO wall clock) plus `slaClass`, `handlingClass`, `sizeWeightClass`, `volume` (>0), `weight` (>0), `currentHubId`, `nextUnloadHubId`, `finalDestHubId`, `packageId`.
    - `routeStopSchema`: `{ hubId, stopIndex }` with `stopIndex` a non-negative integer (stop 0 = earliest unload).
    - `plannerConfigSchema` parses the full config with spec-derived DEFAULTS exported as `DEFAULT_PLANNER_CONFIG`: `{ maxAllowedBlockers: 2, maxBlockVolume, unloadReloadMin, volCost, fragilePenalty, dockDelayPenalty, slaImpactPenalty, targetUtil: 0.80, utilLow: 0.75, utilHigh: 0.90, wLow, wHigh }`.
    - Invalid inputs (negative volume, unknown enum member, non-integer bucket) fail `.parse`.
    - Same input parses to the same object (determinism); no `Date.now()` / `Math.random()` anywhere.
  </behavior>
  <action>Create `packages/domain/src/planning/` with `index.ts` defining the Phase-2 planning value types as zod schemas plus inferred types, mirroring the entities-as-schemas style (reuse the `id` non-empty pattern; re-declare locally since `id` is module-private in entities). Export `SLA_CLASS_WEIGHT` (a `Record<SlaClass, number>`) and `DEFAULT_PLANNER_CONFIG` (typed `PlannerConfig`) as the single source of SLA weighting and config defaults — AGG-04 and all LOAD scoring import these, never re-stating weights. Keep this file pure: only `zod`, no I/O, no clock, no RNG. Write `planning.test.ts` asserting the behaviors above (valid parse round-trips, invalid rejects, weight ordering express>...>economy, config defaults match spec values).</action>
  <verify>
    <automated>pnpm --filter @mm/domain build && pnpm --filter @mm/domain test planning</automated>
  </verify>
  <done>`planning/index.ts` exports PlanningPackage/SlaClass/HandlingClass/DeadlineBucket/SizeWeightClass/RouteStop/PlannerConfig types + schemas + `SLA_CLASS_WEIGHT` + `DEFAULT_PLANNER_CONFIG`; tests green; `tsc -b` clean under strict flags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flesh out LoadBlock + TrailerSlice and wire the barrel</name>
  <files>packages/domain/src/entities/index.ts, packages/domain/src/index.ts, packages/domain/src/entities/entities.test.ts</files>
  <behavior>
    - `blockKeySchema` parses the 7-part key `{ currentHubId, nextUnloadHubId, finalDestHubId, slaClass, deadlineBucket, handlingClass, sizeWeightClass }` (AGG-01) using the planning enums.
    - `loadBlockSchema` (AGG-02) parses `{ loadBlockId, key: BlockKey, packageIds: string[] (min 1), totalVolume>0, totalWeight>0, packageCount = packageIds.length (refined), priority: number }`; a mismatched `packageCount` fails parse.
    - `trailerSliceSchema` (LOAD-01) parses `{ depth: int >=0 (0 = rear/easiest access), capacityVolume>0, capacityWeight>0, usedVolume>=0, usedWeight>=0, loadBlockIds: string[] }`; `usedVolume <= capacityVolume` and `usedWeight <= capacityWeight` enforced by refinement.
    - The Phase-1 `DomainEvent` union, `domainEventSchema`, and `contract.assert.ts` continue to type-check and the existing domain test suite stays green (no event-union changes).
  </behavior>
  <action>Replace the two STUB schemas in `entities/index.ts` with the full Phase-2 shapes above, importing the planning enums from `../planning/index.js` (entities may depend on planning — both are leaf modules within domain; avoid a cycle by having planning NOT import entities). Use depth 0 = rear per the locked LOAD-01 decision; derive nothing from wall clock. Update `packages/domain/src/index.ts` to (a) re-export the new planning types/schemas/constants and (b) keep all existing entity + event exports. Do NOT touch the events/ files. Add `entities.test.ts` covering block-key parse, aggregate refinements (packageCount match, used<=capacity), and TrailerSlice depth semantics.</action>
  <verify>
    <automated>pnpm --filter @mm/domain build && pnpm --filter @mm/domain test && pnpm --filter @mm/domain lint</automated>
  </verify>
  <done>`LoadBlock`/`TrailerSlice` carry full Phase-2 fields; `BlockKey` exported; barrel re-exports planning + entities; entire `@mm/domain` suite green incl. the existing event-contract test; lint clean (no `any`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plain input → planning schemas | Phase-2 inputs (packages, config) cross into the pure modules; zod `.parse` is the validation seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | planning/config inputs | mitigate | zod schemas reject negative volume/weight, unknown enum members, non-integer buckets; `DEFAULT_PLANNER_CONFIG` is the trusted default |
| T-02-02 | Information disclosure | domain leaf | accept | pure value types, no I/O, no secrets; demo scope |
| T-02-03 | Tampering | Phase-1 event union | mitigate | this plan does not modify events/; the existing contract.assert test guards the closed union from drift |
</threat_model>

<verification>
- `pnpm --filter @mm/domain build` clean under strict TS (no `any`, noUncheckedIndexedAccess, exactOptionalPropertyTypes).
- `pnpm --filter @mm/domain test` green incl. the pre-existing event-contract type-equality test.
- `grep -rn "Date.now\|Math.random" packages/domain/src/planning packages/domain/src/entities` returns nothing (filter comments): determinism guard.
</verification>

<success_criteria>
- `@mm/domain` exports `PlanningPackage`, `BlockKey`, fully-fleshed `LoadBlock`/`TrailerSlice`, `RouteStop`, `PlannerConfig`, `SLA_CLASS_WEIGHT`, `DEFAULT_PLANNER_CONFIG` — the single contract the two pure packages import.
- The closed Phase-1 `DomainEvent` union and its contract test are unchanged and green.
- All new types are zod-schema-derived, deterministic, and IO-free.
</success_criteria>

<output>
After completion, create `.planning/phases/02-load-planning/02-01-SUMMARY.md`.
</output>
